name: CI/CD
on:
  push: { branches: [main], tags: ["v*"] }
  pull_request:
  # ONE cloud release, every projection. hanzoai/ci's client: lane
  # (hanzo.yml) is what this fires.
  repository_dispatch: { types: [spec-update] }
  workflow_dispatch:
jobs:
  cicd:
    uses: hanzoai/ci/.github/workflows/build.yml@v1
    secrets: inherit
