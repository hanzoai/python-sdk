# coding: utf-8

"""
    Hanzo Cloud API

    The Hanzo Cloud API as a customer calls it: every operation under /v1/ except the operator's admin product, relay routes, legacy spellings and capabilities still reached by flag. Tagged by product: the first path segment after /v1/.

    The version of the OpenAPI document: v1
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictBool, StrictFloat, StrictInt, StrictStr
from typing import Any, ClassVar, Dict, List, Optional, Union
from typing import Optional, Set
from typing_extensions import Self

class WireFact(BaseModel):
    """
    WireFact
    """ # noqa: E501
    at: Optional[StrictStr] = Field(default=None, description="At is when the thing was so, RFC 3339, as the asserter gave it.")
    by: Optional[StrictStr] = Field(default=None, description="By is the identity that filed it — `owner` or `owner/user` — stamped from the validated principal at the write, never from the body.")
    confidence: Optional[Union[StrictFloat, StrictInt]] = Field(default=None, description="Confidence in [0,1] as the asserter gave it; absent is 0. It breaks a tie between two assertions equally knowable and decides nothing else.")
    entity: Optional[StrictStr] = Field(default=None, description="Entity is the thing described, in the organization's own namespace.")
    evidence: Optional[StrictStr] = Field(default=None, description="Evidence points at the record the claim came from. Absent when the asserter gave none.")
    id: Optional[StrictStr] = Field(default=None, description="ID is the assertion's content address, minted by the server from what was asserted. Two callers who assert the identical thing land on one ID and one row; changing any asserted field makes a different ID and a second row.")
    knowable: Optional[StrictStr] = Field(default=None, description="Knowable is the first instant this plane could have answered with the assertion, RFC 3339: the later of Seen and the server's clock at the write. Derived and never supplied, which is what stops history filed today from being backdated into a past read.")
    names: Optional[StrictBool] = Field(default=None, description="Names true means the assertion is an edge and Value is an entity. A walk reads only these.")
    relation: Optional[StrictStr] = Field(default=None, description="Relation is what was asserted of it.")
    seen: Optional[StrictStr] = Field(default=None, description="Seen is when the asserter says it became knowable, RFC 3339. Provenance only — Knowable is what an as-of read is bounded by.")
    source: Optional[StrictStr] = Field(default=None, description="Source names who asserted, as the caller gave it. This plane ranks no source above another, so it never outweighs a later Knowable.")
    value: Optional[StrictStr] = Field(default=None, description="Value is what the relation points at: another entity's key when Names is true, otherwise a scalar.")
    __properties: ClassVar[List[str]] = ["at", "by", "confidence", "entity", "evidence", "id", "knowable", "names", "relation", "seen", "source", "value"]

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of WireFact from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of WireFact from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "at": obj.get("at"),
            "by": obj.get("by"),
            "confidence": obj.get("confidence"),
            "entity": obj.get("entity"),
            "evidence": obj.get("evidence"),
            "id": obj.get("id"),
            "knowable": obj.get("knowable"),
            "names": obj.get("names"),
            "relation": obj.get("relation"),
            "seen": obj.get("seen"),
            "source": obj.get("source"),
            "value": obj.get("value")
        })
        return _obj


