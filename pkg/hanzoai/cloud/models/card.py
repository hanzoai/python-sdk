# coding: utf-8

"""
    Hanzo Cloud API

    The Hanzo Cloud API as a customer calls it: every operation under /v1/ except the operator's admin product, relay routes, legacy spellings and capabilities still reached by flag. Tagged by product: the first path segment after /v1/.

    The version of the OpenAPI document: v1
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictStr
from typing import Any, ClassVar, Dict, List, Optional
from hanzoai.cloud.models.component import Component
from hanzoai.cloud.models.rate import Rate
from hanzoai.cloud.models.speed import Speed
from typing import Optional, Set
from typing_extensions import Self

class Card(BaseModel):
    """
    Card
    """ # noqa: E501
    components: Optional[List[Component]] = Field(default=None, description="Components are the four things a bill is made of.")
    rates: Optional[List[Rate]] = Field(default=None, description="Rates are the priced lines, each in micro-USD per its own unit.")
    unit: Optional[StrictStr] = Field(default=None, description="Unit is the unit every amount on this card is stated in.")
    windows: Optional[List[Speed]] = Field(default=None, description="Speeds are the completion speeds, dearest first.")
    __properties: ClassVar[List[str]] = ["components", "rates", "unit", "windows"]

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of Card from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # override the default output from pydantic by calling `to_dict()` of each item in components (list)
        _items = []
        if self.components:
            for _item_components in self.components:
                if _item_components:
                    _items.append(_item_components.to_dict())
            _dict['components'] = _items
        # override the default output from pydantic by calling `to_dict()` of each item in rates (list)
        _items = []
        if self.rates:
            for _item_rates in self.rates:
                if _item_rates:
                    _items.append(_item_rates.to_dict())
            _dict['rates'] = _items
        # override the default output from pydantic by calling `to_dict()` of each item in windows (list)
        _items = []
        if self.windows:
            for _item_windows in self.windows:
                if _item_windows:
                    _items.append(_item_windows.to_dict())
            _dict['windows'] = _items
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of Card from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "components": [Component.from_dict(_item) for _item in obj["components"]] if obj.get("components") is not None else None,
            "rates": [Rate.from_dict(_item) for _item in obj["rates"]] if obj.get("rates") is not None else None,
            "unit": obj.get("unit"),
            "windows": [Speed.from_dict(_item) for _item in obj["windows"]] if obj.get("windows") is not None else None
        })
        return _obj


