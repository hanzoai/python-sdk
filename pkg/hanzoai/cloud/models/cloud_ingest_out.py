# coding: utf-8

"""
    Hanzo Cloud — Unified API (V8 · Open Edition)

    The single unified OpenAPI surface for ALL Hanzo services, aggregated from the per-service specs. Every route is https://api.hanzo.ai/v1/<service>/*. Grouped by the canonical domains in capabilities.yaml (the ONE registry). cloud/openapi.yaml is hanzoai/cloud's own woven document, copied verbatim by sync.py, and it wins wherever it and a hand-written spec describe the same route. Regenerated by merge.py — edit the per-service <svc>/openapi.yaml + capabilities.yaml, not this file.

    The version of the OpenAPI document: 8.0.0
    Contact: support@hanzo.ai
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictBool, StrictInt, StrictStr
from typing import Any, ClassVar, Dict, List, Optional
from typing import Optional, Set
from typing_extensions import Self

class CloudIngestOut(BaseModel):
    """
    CloudIngestOut
    """ # noqa: E501
    attempts_ingested: Optional[StrictInt] = Field(default=None, description="AttemptsIngested is how many attempt versions this call appended.")
    attempts_retained: Optional[StrictInt] = Field(default=None, description="AttemptsRetained is the full versioned attempt history the store now holds.")
    canonical_attempts: Optional[StrictInt] = Field(default=None, description="CanonicalAttempts is the deduped attempt count the store now holds.")
    canonical_experiments: Optional[StrictInt] = Field(default=None, description="CanonicalExperiments is the deduped experiment count the store now holds.")
    experiments_ingested: Optional[StrictInt] = Field(default=None, description="ExperimentsIngested is how many experiment versions this call appended.")
    experiments_retained: Optional[StrictInt] = Field(default=None, description="ExperimentsRetained is the full versioned experiment history the store now holds.")
    project: Optional[StrictStr] = Field(default=None, description="Project is the project the batch was filed under — the SERVER's value, never the body's.")
    rolled_up: Optional[StrictBool] = Field(default=None, description="RolledUp is false when the OLAP roll-up was skipped; the SQLite write still stands.")
    __properties: ClassVar[List[str]] = ["attempts_ingested", "attempts_retained", "canonical_attempts", "canonical_experiments", "experiments_ingested", "experiments_retained", "project", "rolled_up"]

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of CloudIngestOut from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of CloudIngestOut from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "attempts_ingested": obj.get("attempts_ingested"),
            "attempts_retained": obj.get("attempts_retained"),
            "canonical_attempts": obj.get("canonical_attempts"),
            "canonical_experiments": obj.get("canonical_experiments"),
            "experiments_ingested": obj.get("experiments_ingested"),
            "experiments_retained": obj.get("experiments_retained"),
            "project": obj.get("project"),
            "rolled_up": obj.get("rolled_up")
        })
        return _obj


