# coding: utf-8

"""
    Hanzo Cloud API

    The Hanzo Cloud API as a customer calls it: every operation under /v1/ except the operator's admin product, relay routes, legacy spellings and capabilities still reached by flag. Tagged by product: the first path segment after /v1/.

    The version of the OpenAPI document: v1
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictBool, StrictFloat, StrictInt, StrictStr
from typing import Any, ClassVar, Dict, List, Optional, Union
from typing import Optional, Set
from typing_extensions import Self

class GraphFact(BaseModel):
    """
    GraphFact
    """ # noqa: E501
    at: Optional[StrictStr] = Field(default=None, description="At is when the thing was so, RFC 3339. Required, and refused when it sits more than five minutes ahead of the server clock — an assertion dated further out would never mature and would skew every read until it did.")
    confidence: Optional[Union[StrictFloat, StrictInt]] = Field(default=None, description="Confidence in [0,1]. A tie-breaker within the order, never a substitute for it. Absent is 0, the weakest an assertion can be.")
    entity: Optional[StrictStr] = Field(default=None, description="Entity is the thing being described, in the organization's own namespace. It is not created: an entity exists because something was asserted about it. Required, 512 bytes at most.")
    evidence: Optional[StrictStr] = Field(default=None, description="Evidence points at the record this claim came from, 512 bytes at most. An assertion without one is admitted and carries no defence.")
    names: Optional[StrictBool] = Field(default=None, description="Names says the value is an entity. A walk reads only the edges, so this is a declaration and never a guess about the value's shape.")
    relation: Optional[StrictStr] = Field(default=None, description="Relation is what is being asserted — `depends`, `owner`, `same`, `title`. It is open: this plane holds no vocabulary of its own. Required, 128 bytes at most.")
    seen: Optional[StrictStr] = Field(default=None, description="Seen is when this assertion became knowable, RFC 3339. Defaults to At and may not precede it. It is provenance and it decides nothing: the instant every read uses is derived as the later of Seen and the server's own clock.")
    source: Optional[StrictStr] = Field(default=None, description="Source names who asserted. Required, because an assertion nobody is named for cannot be weighed against one that is. Open text: this plane ranks no source above another.")
    value: Optional[StrictStr] = Field(default=None, description="Value is what the relation points at. When Names is true it is another entity's key and the assertion is an EDGE; otherwise it is a scalar and the assertion is a property. 2048 bytes at most, or 512 when it names an entity.")
    __properties: ClassVar[List[str]] = ["at", "confidence", "entity", "evidence", "names", "relation", "seen", "source", "value"]

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of GraphFact from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of GraphFact from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "at": obj.get("at"),
            "confidence": obj.get("confidence"),
            "entity": obj.get("entity"),
            "evidence": obj.get("evidence"),
            "names": obj.get("names"),
            "relation": obj.get("relation"),
            "seen": obj.get("seen"),
            "source": obj.get("source"),
            "value": obj.get("value")
        })
        return _obj


