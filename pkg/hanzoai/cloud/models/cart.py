# coding: utf-8

"""
    Hanzo Cloud API

    Composed from each subsystem's own projection of its router, in the fleet's mount order — every operation below is a route the subsystem that publishes it registered. Tagged by product: the first path segment after /v1/.

    The version of the OpenAPI document: v1
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictInt, StrictStr
from typing import Any, ClassVar, Dict, List, Optional
from hanzoai.cloud.models.cart_item import CartItem
from typing import Optional, Set
from typing_extensions import Self

class Cart(BaseModel):
    """
    Cart
    """ # noqa: E501
    created_at: Optional[StrictStr] = Field(default=None, description="CreatedAt is when the cart was opened, RFC3339.", alias="createdAt")
    currency: Optional[StrictStr] = Field(default=None, description="Currency is the ISO 4217 code every amount below is denominated in.")
    discount_cents: Optional[StrictInt] = Field(default=None, description="DiscountCents is what coupons and promotions took off, in whole cents.", alias="discountCents")
    email: Optional[StrictStr] = Field(default=None, description="Email is the shopper's address, when the cart carries one.")
    id: Optional[StrictStr] = Field(default=None, description="ID is the cart's id — what every other cart op addresses it by, and what a storefront persists against the browser session.")
    items: Optional[List[CartItem]] = Field(default=None, description="Items are the cart's lines, in the order they were added.")
    line_total_cents: Optional[StrictInt] = Field(default=None, description="LineTotalCents is the sum of the lines before any discount, in whole cents.", alias="lineTotalCents")
    order: Optional[StrictStr] = Field(default=None, description="Order is the order this cart became, once checkout completed it. Empty until then, and its presence is what makes a cart final.")
    shipping_cents: Optional[StrictInt] = Field(default=None, description="ShippingCents is the shipping charge, in whole cents. It stays zero until a shipping option is priced at checkout.", alias="shippingCents")
    status: Optional[StrictStr] = Field(default=None, description="Status is \"active\" for a cart still being filled, \"ordered\" once checkout turned it into an order, and \"discarded\" when the shopper abandoned it.")
    store: Optional[StrictStr] = Field(default=None, description="Store is the storefront the cart is being filled on.")
    subtotal_cents: Optional[StrictInt] = Field(default=None, description="SubtotalCents is LineTotalCents less DiscountCents, in whole cents.", alias="subtotalCents")
    tax_cents: Optional[StrictInt] = Field(default=None, description="TaxCents is the sales tax, in whole cents. It stays zero until checkout resolves the shopper's tax region.", alias="taxCents")
    total_cents: Optional[StrictInt] = Field(default=None, description="TotalCents is what the shopper pays: subtotal plus shipping plus tax, in whole cents.", alias="totalCents")
    updated_at: Optional[StrictStr] = Field(default=None, description="UpdatedAt is when the cart was last amended, RFC3339.", alias="updatedAt")
    user: Optional[StrictStr] = Field(default=None, description="User is the signed-in shopper this cart belongs to, empty for a guest cart.")
    __properties: ClassVar[List[str]] = ["createdAt", "currency", "discountCents", "email", "id", "items", "lineTotalCents", "order", "shippingCents", "status", "store", "subtotalCents", "taxCents", "totalCents", "updatedAt", "user"]

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of Cart from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # override the default output from pydantic by calling `to_dict()` of each item in items (list)
        _items = []
        if self.items:
            for _item_items in self.items:
                if _item_items:
                    _items.append(_item_items.to_dict())
            _dict['items'] = _items
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of Cart from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "createdAt": obj.get("createdAt"),
            "currency": obj.get("currency"),
            "discountCents": obj.get("discountCents"),
            "email": obj.get("email"),
            "id": obj.get("id"),
            "items": [CartItem.from_dict(_item) for _item in obj["items"]] if obj.get("items") is not None else None,
            "lineTotalCents": obj.get("lineTotalCents"),
            "order": obj.get("order"),
            "shippingCents": obj.get("shippingCents"),
            "status": obj.get("status"),
            "store": obj.get("store"),
            "subtotalCents": obj.get("subtotalCents"),
            "taxCents": obj.get("taxCents"),
            "totalCents": obj.get("totalCents"),
            "updatedAt": obj.get("updatedAt"),
            "user": obj.get("user")
        })
        return _obj


