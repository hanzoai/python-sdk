# coding: utf-8

"""
    Hanzo Cloud API

    The Hanzo Cloud API as a customer calls it: every operation under /v1/ except the operator's admin product, relay routes, legacy spellings and capabilities still reached by flag. Tagged by product: the first path segment after /v1/.

    The version of the OpenAPI document: v1
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictStr
from typing import Any, ClassVar, Dict, List, Optional
from hanzoai.cloud.models.my_referral_view import MyReferralView
from hanzoai.cloud.models.status_counts import StatusCounts
from typing import Optional, Set
from typing_extensions import Self

class MyReferrals(BaseModel):
    """
    MyReferrals
    """ # noqa: E501
    code: Optional[StrictStr] = Field(default=None, description="Code is the org's STABLE referral code — a deterministic function of the org id, so it never changes and never has to be stored to be reproduced.")
    counts: Optional[StatusCounts] = Field(default=None, description="Counts tallies this org's referrals by status.")
    link: Optional[StrictStr] = Field(default=None, description="Link is the shareable signup link carrying the code, on the brand's own host.")
    referrals: Optional[List[MyReferralView]] = Field(default=None, description="Referrals is one row per org that signed up with this code.")
    __properties: ClassVar[List[str]] = ["code", "counts", "link", "referrals"]

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of MyReferrals from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # override the default output from pydantic by calling `to_dict()` of counts
        if self.counts:
            _dict['counts'] = self.counts.to_dict()
        # override the default output from pydantic by calling `to_dict()` of each item in referrals (list)
        _items = []
        if self.referrals:
            for _item_referrals in self.referrals:
                if _item_referrals:
                    _items.append(_item_referrals.to_dict())
            _dict['referrals'] = _items
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of MyReferrals from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "code": obj.get("code"),
            "counts": StatusCounts.from_dict(obj["counts"]) if obj.get("counts") is not None else None,
            "link": obj.get("link"),
            "referrals": [MyReferralView.from_dict(_item) for _item in obj["referrals"]] if obj.get("referrals") is not None else None
        })
        return _obj


