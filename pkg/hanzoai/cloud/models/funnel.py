# coding: utf-8

"""
    Hanzo Cloud API

    The Hanzo Cloud API as a customer calls it: every operation under /v1/ except the operator's admin product, relay doors, legacy spellings and capabilities still reached by flag. Tagged by product: the first path segment after /v1/.

    The version of the OpenAPI document: v1
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictBool, StrictFloat, StrictInt
from typing import Any, ClassVar, Dict, List, Optional, Union
from typing import Optional, Set
from typing_extensions import Self

class Funnel(BaseModel):
    """
    Funnel
    """ # noqa: E501
    available: Optional[StrictBool] = Field(default=None, description="Available separates \"this org has no traffic\" from \"we could not ask\". False means the warehouse was unreachable or the org has emitted nothing at all, and every count below is then a placeholder zero rather than a measurement — a caller must read this before reading any of them.")
    orders: Optional[StrictInt] = Field(default=None, description="Orders counts completed orders in the window — purchases, not carts started.")
    pageviews: Optional[StrictInt] = Field(default=None, description="Pageviews counts page events in the window, one per view rather than per person, so a single visitor reading ten pages counts ten.")
    revenue: Optional[Union[StrictFloat, StrictInt]] = Field(default=None, description="Revenue is the sum of the amounts those orders reported, in whatever currency the beacon stamped on them (major units, e.g. 49.5 for $49.50) — NOT cents, and not converted to a single currency. Contrast revenueCents on the profile, which is the money of record.")
    signups: Optional[StrictInt] = Field(default=None, description="Signups counts completed signups in the window, the step where an anonymous visitor becomes somebody with an account.")
    visitors: Optional[StrictInt] = Field(default=None, description="Visitors is the number of DISTINCT people seen in the window, counted by the beacon's distinct id — so it is unique visitors, not sessions and not views.")
    window_days: Optional[StrictInt] = Field(default=None, description="WindowDays is the length of the trailing window every count covers, so a reader knows whether 40 signups is a month or a day.", alias="windowDays")
    __properties: ClassVar[List[str]] = ["available", "orders", "pageviews", "revenue", "signups", "visitors", "windowDays"]

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of Funnel from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of Funnel from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "available": obj.get("available"),
            "orders": obj.get("orders"),
            "pageviews": obj.get("pageviews"),
            "revenue": obj.get("revenue"),
            "signups": obj.get("signups"),
            "visitors": obj.get("visitors"),
            "windowDays": obj.get("windowDays")
        })
        return _obj


