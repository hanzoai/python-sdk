# coding: utf-8

"""
    Hanzo Cloud API

    The Hanzo Cloud API as a customer calls it: every operation under /v1/ except the operator's admin product, relay doors, legacy spellings and capabilities still reached by flag. Tagged by product: the first path segment after /v1/.

    The version of the OpenAPI document: v1
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictBool, StrictInt, StrictStr
from typing import Any, ClassVar, Dict, List, Optional
from hanzoai.cloud.models.obligation import Obligation
from typing import Optional, Set
from typing_extensions import Self

class Upkeep(BaseModel):
    """
    Upkeep
    """ # noqa: E501
    at_least: Optional[StrictBool] = Field(default=None, description="AtLeast reports that some obligation is a minimum, so YearlyCents is a floor rather than a final figure.", alias="atLeast")
    currency: Optional[StrictStr] = Field(default=None, description="Currency is the ISO code every amount here is denominated in.")
    jurisdiction: Optional[StrictStr] = Field(default=None, description="Jurisdiction is the state whose obligations these are.")
    obligations: Optional[List[Obligation]] = Field(default=None, description="Obligations are the recurring charges, in the order a reader should see them.")
    structure: Optional[StrictStr] = Field(default=None, description="Structure is the entity this prices.")
    yearly_cents: Optional[StrictInt] = Field(default=None, description="YearlyCents is what the entity owes every year, all obligations summed.", alias="yearlyCents")
    __properties: ClassVar[List[str]] = ["atLeast", "currency", "jurisdiction", "obligations", "structure", "yearlyCents"]

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of Upkeep from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # override the default output from pydantic by calling `to_dict()` of each item in obligations (list)
        _items = []
        if self.obligations:
            for _item_obligations in self.obligations:
                if _item_obligations:
                    _items.append(_item_obligations.to_dict())
            _dict['obligations'] = _items
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of Upkeep from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "atLeast": obj.get("atLeast"),
            "currency": obj.get("currency"),
            "jurisdiction": obj.get("jurisdiction"),
            "obligations": [Obligation.from_dict(_item) for _item in obj["obligations"]] if obj.get("obligations") is not None else None,
            "structure": obj.get("structure"),
            "yearlyCents": obj.get("yearlyCents")
        })
        return _obj


