# coding: utf-8

"""
    Hanzo Cloud API

    The Hanzo Cloud API as a customer calls it: every operation under /v1/ except the operator's admin product, relay doors, legacy spellings and capabilities still reached by flag. Tagged by product: the first path segment after /v1/.

    The version of the OpenAPI document: v1
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictStr
from typing import Any, ClassVar, Dict, List, Optional
from typing import Optional, Set
from typing_extensions import Self

class Blog(BaseModel):
    """
    Blog
    """ # noqa: E501
    case_study: Optional[StrictStr] = Field(default=None, description="CaseStudy is one worked instance — somebody who ran it, and what happened.", alias="caseStudy")
    how: Optional[StrictStr] = Field(default=None, description="How is the run book: the steps to execute the tactic.")
    slug: Optional[StrictStr] = Field(default=None, description="Slug is the post's address — the last path segment it is published at.")
    title: Optional[StrictStr] = Field(default=None, description="Title is the post's headline.")
    why: Optional[StrictStr] = Field(default=None, description="Why is the mechanism: the reason the tactic works, stated as a principle rather than as instructions.")
    __properties: ClassVar[List[str]] = ["caseStudy", "how", "slug", "title", "why"]

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of Blog from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of Blog from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "caseStudy": obj.get("caseStudy"),
            "how": obj.get("how"),
            "slug": obj.get("slug"),
            "title": obj.get("title"),
            "why": obj.get("why")
        })
        return _obj


