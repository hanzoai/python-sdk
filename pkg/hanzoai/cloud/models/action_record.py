# coding: utf-8

"""
    Hanzo Cloud API

    The Hanzo Cloud API as a customer calls it: every operation under /v1/ except the operator's admin product, relay routes, legacy spellings and capabilities still reached by flag. Tagged by product: the first path segment after /v1/.

    The version of the OpenAPI document: v1
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictBool, StrictInt, StrictStr
from typing import Any, ClassVar, Dict, List, Optional
from typing import Optional, Set
from typing_extensions import Self

class ActionRecord(BaseModel):
    """
    ActionRecord
    """ # noqa: E501
    args: Optional[StrictStr] = Field(default=None, description="Args is the JSON the tool was called with, recorded as TEXT exactly as sent — including whatever the AI drafted into it — so a run can be read back and reproduced. It is a string, not an object.")
    created_at: Optional[StrictInt] = Field(default=None, description="CreatedAt is when the run was recorded, as Unix seconds. The ledger is read newest-first on this column.", alias="createdAt")
    err: Optional[StrictStr] = Field(default=None, description="Err is why the run failed, when it did. Empty on a successful run.")
    id: Optional[StrictStr] = Field(default=None, description="ID identifies this one execution. The ledger is append-only, so an id is never reused and never updated.")
    ok: Optional[StrictBool] = Field(default=None, description="OK is whether the tool ran to completion. It is the ledger's own verdict, not the tool's opinion of the outcome — a tool that succeeded at reporting bad news is ok.")
    result: Optional[StrictStr] = Field(default=None, description="Result is the tool's own answer, likewise recorded as JSON text. Present on a failed run too, where the tool answered but the answer was a refusal.")
    step_id: Optional[StrictStr] = Field(default=None, description="StepID is the checklist step the Business AI was acting on.", alias="stepId")
    tool: Optional[StrictStr] = Field(default=None, description="Tool is the MCP tool that was dispatched, by name.")
    __properties: ClassVar[List[str]] = ["args", "createdAt", "err", "id", "ok", "result", "stepId", "tool"]

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of ActionRecord from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of ActionRecord from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "args": obj.get("args"),
            "createdAt": obj.get("createdAt"),
            "err": obj.get("err"),
            "id": obj.get("id"),
            "ok": obj.get("ok"),
            "result": obj.get("result"),
            "stepId": obj.get("stepId"),
            "tool": obj.get("tool")
        })
        return _obj


