# coding: utf-8

"""
    Hanzo Cloud API

    The Hanzo Cloud API as a customer calls it: every operation under /v1/ except the operator's admin product, relay doors, legacy spellings and capabilities still reached by flag. Tagged by product: the first path segment after /v1/.

    The version of the OpenAPI document: v1
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from datetime import datetime
from pydantic import BaseModel, ConfigDict, Field, StrictBool, StrictStr
from typing import Any, ClassVar, Dict, List, Optional
from typing import Optional, Set
from typing_extensions import Self

class IamKey(BaseModel):
    """
    IamKey
    """ # noqa: E501
    access_key: Optional[StrictStr] = Field(default=None, description="AccessKey (pk-*) is the publishable identifier and lookup index; AccessSecret (sk-*) is the confidential secret. AccessSecret IS NOT PERSISTED for a key minted at or after the digest change: it carries the secret out to its holder once, in the mint response, and the row keeps only AccessSecretDigest. It stays on the struct because that one-time reveal is the whole point of minting, and it stays in the schema because rows written before the change still hold a plaintext secret that the resolver drains on first use.", alias="accessKey")
    access_secret: Optional[StrictStr] = Field(default=None, alias="accessSecret")
    access_secret_digest: Optional[StrictStr] = Field(default=None, description="AccessSecretDigest is how a presented secret finds its key: the resolver digests what the caller sent and looks THAT up. It is what lets the row hold no plaintext and still be found in one indexed read — a salted hash cannot be looked up by value, which is the reason the plaintext was here.", alias="accessSecretDigest")
    act: Optional[StrictBool] = Field(default=None, description="Act is the durable, opt-in grant that lets this key act FOR a user in its own org — the credential behind as(): presenting it authorizes minting a short-lived, user-bound token for a member of the key's tenant. Default false, so a server key mints nothing on anyone's behalf until the grant is set deliberately — the capability is never inherited by every key. It is confined at mint time to the key's OWN Owner, and a reserved-org or SuperAdmin target is refused, so the grant reaches only ordinary members of the one tenant that holds the key.")
    application: Optional[StrictStr] = None
    created_at: Optional[datetime] = Field(default=None, alias="createdAt")
    created_time: Optional[StrictStr] = Field(default=None, description="CreatedTime and UpdatedTime are RFC3339 audit stamps carried as strings for byte-parity with the v1 row (orm.Model separately tracks CreatedAt / UpdatedAt as time.Time for the store's own lifecycle).", alias="createdTime")
    deleted: Optional[StrictBool] = None
    display_name: Optional[StrictStr] = Field(default=None, description="DisplayName is the human-facing label.", alias="displayName")
    expire_time: Optional[StrictStr] = Field(default=None, description="ExpireTime is when the key stops being honored (empty = never). State is the lifecycle flag (\"Active\", \"test\", …); \"test\" mints test-env credentials instead of live ones.", alias="expireTime")
    id: Optional[StrictStr] = None
    name: Optional[StrictStr] = None
    organization: Optional[StrictStr] = None
    owner: Optional[StrictStr] = Field(default=None, description="Owner is the tenant that holds the key; Name is unique within Owner.")
    scope: Optional[StrictStr] = Field(default=None, description="Scope is the key's ACCESS CLASS, orthogonal to Type (which names the bound principal). Empty (the default, \"secret\") is a full key: a pk- publishable half AND a confidential sk- half, the sk- authenticating a server-side reader. KeyScopePublish is a WRITE-ONLY publishable key — a pk- half only, no secret — that resolves to just an ORG (never a principal) at the ingest door and is safe to ship in client JS. A missing value on an existing row reads as the default, so every pre-Scope key is a secret key unchanged.")
    state: Optional[StrictStr] = None
    type: Optional[StrictStr] = Field(default=None, description="Type is the scope the key is bound to — \"Organization\", \"Application\", \"User\", or \"General\" — and Organization / Application / User name the concrete principal for whichever scope Type selects.")
    updated_at: Optional[datetime] = Field(default=None, alias="updatedAt")
    updated_time: Optional[StrictStr] = Field(default=None, alias="updatedTime")
    user: Optional[StrictStr] = None
    __properties: ClassVar[List[str]] = ["accessKey", "accessSecret", "accessSecretDigest", "act", "application", "createdAt", "createdTime", "deleted", "displayName", "expireTime", "id", "name", "organization", "owner", "scope", "state", "type", "updatedAt", "updatedTime", "user"]

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of IamKey from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of IamKey from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "accessKey": obj.get("accessKey"),
            "accessSecret": obj.get("accessSecret"),
            "accessSecretDigest": obj.get("accessSecretDigest"),
            "act": obj.get("act"),
            "application": obj.get("application"),
            "createdAt": obj.get("createdAt"),
            "createdTime": obj.get("createdTime"),
            "deleted": obj.get("deleted"),
            "displayName": obj.get("displayName"),
            "expireTime": obj.get("expireTime"),
            "id": obj.get("id"),
            "name": obj.get("name"),
            "organization": obj.get("organization"),
            "owner": obj.get("owner"),
            "scope": obj.get("scope"),
            "state": obj.get("state"),
            "type": obj.get("type"),
            "updatedAt": obj.get("updatedAt"),
            "updatedTime": obj.get("updatedTime"),
            "user": obj.get("user")
        })
        return _obj


