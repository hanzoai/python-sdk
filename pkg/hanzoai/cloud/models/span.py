# coding: utf-8

"""
    Hanzo Cloud API

    The Hanzo Cloud API as a customer calls it: every operation under /v1/ except the operator's admin product, relay doors, legacy spellings and capabilities still reached by flag. Tagged by product: the first path segment after /v1/.

    The version of the OpenAPI document: v1
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictFloat, StrictInt, StrictStr
from typing import Any, ClassVar, Dict, List, Optional, Union
from typing import Optional, Set
from typing_extensions import Self

class Span(BaseModel):
    """
    Span
    """ # noqa: E501
    end_line: Optional[StrictInt] = Field(default=None, description="EndLine is the last line of the span, inclusive. It equals Line for a one-line span rather than being zero or absent.", alias="endLine")
    file: Optional[StrictStr] = Field(default=None, description="File is the path inside the repo, relative to its root and never absolute.")
    kind: Optional[StrictStr] = Field(default=None, description="Kind is what the indexer decided this chunk IS — \"func\", \"method\", \"type\", \"struct\", \"interface\", \"var\", \"const\", or \"block\" for a run of code that declares nothing. Absent when the chunker could not classify it.")
    line: Optional[StrictInt] = Field(default=None, description="Line is where the span starts, 1-based, as an editor counts.")
    repo: Optional[StrictStr] = Field(default=None, description="Repo is the indexed repository the span was found in, as it was indexed (\"owner/name\"). A search may be scoped to one repo or run across all of them, so this is how a caller tells the results apart.")
    role: Optional[StrictStr] = Field(default=None, description="context: match | definition | caller")
    score: Optional[Union[StrictFloat, StrictInt]] = Field(default=None, description="Score ranks this span against the OTHERS IN THE SAME RESPONSE and means nothing across responses or between tiers: the hybrid tier's number is a reciprocal-rank fusion sum (Σ 1/(60+rank), so tenths at best), the symbol tier's is a descending position count, and the text and semantic tiers pass through bm25 and cosine. Compare within a list; never threshold on it.")
    snippet: Optional[StrictStr] = Field(default=None, description="Snippet is the code itself: a bounded excerpt on /search, the whole chunk on /context — which is why the same type serves both and why a /context span is the one an agent pastes into its window.")
    symbol: Optional[StrictStr] = Field(default=None, description="Symbol is the declared name, when the span declares one. Absent on a block.")
    tier: Optional[StrictStr] = Field(default=None, description="Tier is which retrieval produced the span: \"hybrid\" (the default — all three fused), \"text\" (trigram/FTS), \"regex\", \"semantic\" (vector), or \"symbol\". It is what explains a Score, so the two travel together.")
    __properties: ClassVar[List[str]] = ["endLine", "file", "kind", "line", "repo", "role", "score", "snippet", "symbol", "tier"]

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of Span from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of Span from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "endLine": obj.get("endLine"),
            "file": obj.get("file"),
            "kind": obj.get("kind"),
            "line": obj.get("line"),
            "repo": obj.get("repo"),
            "role": obj.get("role"),
            "score": obj.get("score"),
            "snippet": obj.get("snippet"),
            "symbol": obj.get("symbol"),
            "tier": obj.get("tier")
        })
        return _obj


