# coding: utf-8

"""
    Hanzo Cloud API

    The Hanzo Cloud API as a customer calls it: every operation under /v1/ except the operator's admin product, relay doors, legacy spellings and capabilities still reached by flag. Tagged by product: the first path segment after /v1/.

    The version of the OpenAPI document: v1
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictBool, StrictFloat, StrictInt, StrictStr
from typing import Any, ClassVar, Dict, List, Optional, Union
from typing import Optional, Set
from typing_extensions import Self

class LLMOverview(BaseModel):
    """
    LLMOverview
    """ # noqa: E501
    available: Optional[StrictBool] = Field(default=None, description="Available is true whenever the ledger answered — including with no usage in the window, which is honest zeros rather than a missing lens.")
    completion_tokens: Optional[StrictInt] = Field(default=None, description="CompletionTokens is the output half of Tokens.", alias="completionTokens")
    error_rate: Optional[Union[StrictFloat, StrictInt]] = Field(default=None, description="ErrorRate is Errors/Requests, 0..1, rounded to three places. Zero when there were no requests.", alias="errorRate")
    errors: Optional[StrictInt] = Field(default=None, description="Errors is how many of Requests failed.")
    models: Optional[StrictInt] = Field(default=None, description="Models is how many distinct models the org called.")
    prompt_tokens: Optional[StrictInt] = Field(default=None, description="PromptTokens is the input half of Tokens.", alias="promptTokens")
    providers: Optional[StrictInt] = Field(default=None, description="Providers is how many distinct providers served them.")
    requests: Optional[StrictInt] = Field(default=None, description="Requests is how many LLM calls the org made in the window.")
    source: Optional[StrictStr] = Field(default=None, description="Source is the warehouse table the lens read.")
    spend_cents: Optional[StrictInt] = Field(default=None, description="SpendCents is what those calls cost, in cents.", alias="spendCents")
    tokens: Optional[StrictInt] = Field(default=None, description="Tokens is prompt plus completion tokens over those calls.")
    __properties: ClassVar[List[str]] = ["available", "completionTokens", "errorRate", "errors", "models", "promptTokens", "providers", "requests", "source", "spendCents", "tokens"]

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of LLMOverview from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of LLMOverview from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "available": obj.get("available"),
            "completionTokens": obj.get("completionTokens"),
            "errorRate": obj.get("errorRate"),
            "errors": obj.get("errors"),
            "models": obj.get("models"),
            "promptTokens": obj.get("promptTokens"),
            "providers": obj.get("providers"),
            "requests": obj.get("requests"),
            "source": obj.get("source"),
            "spendCents": obj.get("spendCents"),
            "tokens": obj.get("tokens")
        })
        return _obj


