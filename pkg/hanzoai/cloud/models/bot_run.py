# coding: utf-8

"""
    Hanzo Cloud API

    Composed from each subsystem's own projection of its router, in the fleet's mount order — every operation below is a route the subsystem that publishes it registered. Tagged by product: the first path segment after /v1/.

    The version of the OpenAPI document: v1
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictStr
from typing import Any, ClassVar, Dict, List, Optional
from typing import Optional, Set
from typing_extensions import Self

class BotRun(BaseModel):
    """
    BotRun
    """ # noqa: E501
    run_id: Optional[StrictStr] = Field(default=None, description="RunID is the run's id in the bot runtime, and the node id its live VNC session is registered under.", alias="runId")
    session_url: Optional[StrictStr] = Field(default=None, description="SessionURL is the live session the hanzo.app /vnc panel embeds to watch or attach to this run. Derived here from the run id, never sent by the runtime.", alias="sessionUrl")
    started_at: Optional[StrictStr] = Field(default=None, description="StartedAt is when the run began, RFC 3339, as the runtime stamped it.", alias="startedAt")
    status: Optional[StrictStr] = Field(default=None, description="Status is the run's state as the runtime reports it; \"running\" when the runtime names none of its own.")
    surface: Optional[StrictStr] = Field(default=None, description="Surface is what the bot drives: the desktop or terminal sandbox it runs in.")
    task: Optional[StrictStr] = Field(default=None, description="Task is the instruction the bot is executing.")
    __properties: ClassVar[List[str]] = ["runId", "sessionUrl", "startedAt", "status", "surface", "task"]

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of BotRun from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of BotRun from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "runId": obj.get("runId"),
            "sessionUrl": obj.get("sessionUrl"),
            "startedAt": obj.get("startedAt"),
            "status": obj.get("status"),
            "surface": obj.get("surface"),
            "task": obj.get("task")
        })
        return _obj


