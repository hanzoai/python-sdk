# coding: utf-8

"""
    Hanzo Cloud API

    Composed from each subsystem's own projection of its router, in the fleet's mount order — every operation below is a route the subsystem that publishes it registered. Tagged by product: the first path segment after /v1/.

    The version of the OpenAPI document: v1
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictBool, StrictFloat, StrictInt, StrictStr
from typing import Any, ClassVar, Dict, List, Optional, Union
from typing import Optional, Set
from typing_extensions import Self

class RiskOrgFeature(BaseModel):
    """
    RiskOrgFeature
    """ # noqa: E501
    blind: Optional[StrictBool] = Field(default=None, description="Blind is true when the dimension is present in no bucket at all: this organisation's surface does not carry it, and saying so is the difference between no risk and no data.")
    buckets: Optional[StrictInt] = Field(default=None, description="Buckets is how many five-minute buckets of this organisation's surface were measured.")
    max: Optional[Union[StrictFloat, StrictInt]] = Field(default=None, description="Max is the largest value it reached in the window.")
    mean: Optional[Union[StrictFloat, StrictInt]] = Field(default=None, description="Mean is the dimension's average where it was present.")
    name: Optional[StrictStr] = Field(default=None, description="Name is the dimension as this API publishes it.")
    present: Optional[StrictInt] = Field(default=None, description="Present is in how many of them the dimension carried a value at all.")
    source: Optional[StrictStr] = Field(default=None, description="Source names the plane it is rolled up from, so a dimension that reads zero everywhere traces to a plane the organisation does not use rather than to a defect.")
    unit: Optional[StrictStr] = Field(default=None, description="Unit is how to read the numbers below.")
    __properties: ClassVar[List[str]] = ["blind", "buckets", "max", "mean", "name", "present", "source", "unit"]

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of RiskOrgFeature from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of RiskOrgFeature from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "blind": obj.get("blind"),
            "buckets": obj.get("buckets"),
            "max": obj.get("max"),
            "mean": obj.get("mean"),
            "name": obj.get("name"),
            "present": obj.get("present"),
            "source": obj.get("source"),
            "unit": obj.get("unit")
        })
        return _obj


