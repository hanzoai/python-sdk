# coding: utf-8

"""
    Hanzo Cloud API

    The Hanzo Cloud API as a customer calls it: every operation under /v1/ except the operator's admin product, relay routes, legacy spellings and capabilities still reached by flag. Tagged by product: the first path segment after /v1/.

    The version of the OpenAPI document: v1
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictInt
from typing import Any, ClassVar, Dict, List, Optional
from typing import Optional, Set
from typing_extensions import Self

class RiskSplitCounts(BaseModel):
    """
    RiskSplitCounts
    """ # noqa: E501
    judged: Optional[StrictInt] = Field(default=None, description="Judged is how many rows carry a disposition. It is zero until a label plane writes one, and reporting it plainly is what lets a model plane refuse to rank rather than name a winner it cannot justify.")
    productive: Optional[StrictInt] = Field(default=None, description="Productive is how many judged rows carry the one disposition.")
    rows: Optional[StrictInt] = Field(default=None, description="Rows is how many rows the version holds across every split. It is the size of the version, not of the source window — the horizon, the cuts and the row cap all bind before this number.")
    subjects: Optional[StrictInt] = Field(default=None, description="Subjects is how many distinct subjects the rows belong to. Every row of one subject is in ONE split, so this is the real sample size — the row count flatters it whenever a subject is active.")
    test: Optional[StrictInt] = Field(default=None, description="Test is how many fall after the second cut — the LATEST slice, and the only one a score is honest about, since the split is temporal.")
    train: Optional[StrictInt] = Field(default=None, description="Train is how many rows fall before the first cut — the EARLIEST slice of the window, which is what a model is fitted on.")
    unproductive: Optional[StrictInt] = Field(default=None, description="Unproductive is how many carry the other. With Productive it accounts for Judged, so the class imbalance is visible before anyone trains on it; both stay 0 while Judged is 0.")
    val: Optional[StrictInt] = Field(default=None, description="Val is how many fall between the two cuts, held out for tuning.")
    __properties: ClassVar[List[str]] = ["judged", "productive", "rows", "subjects", "test", "train", "unproductive", "val"]

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of RiskSplitCounts from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of RiskSplitCounts from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "judged": obj.get("judged"),
            "productive": obj.get("productive"),
            "rows": obj.get("rows"),
            "subjects": obj.get("subjects"),
            "test": obj.get("test"),
            "train": obj.get("train"),
            "unproductive": obj.get("unproductive"),
            "val": obj.get("val")
        })
        return _obj


