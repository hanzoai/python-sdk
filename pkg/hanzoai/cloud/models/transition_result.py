# coding: utf-8

"""
    Hanzo Cloud API

    The Hanzo Cloud API as a customer calls it: every operation under /v1/ except the operator's admin product, relay doors, legacy spellings and capabilities still reached by flag. Tagged by product: the first path segment after /v1/.

    The version of the OpenAPI document: v1
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictStr
from typing import Any, ClassVar, Dict, List, Optional
from hanzoai.cloud.models.publish_result import PublishResult
from hanzoai.cloud.models.storefront_result import StorefrontResult
from typing import Optional, Set
from typing_extensions import Self

class TransitionResult(BaseModel):
    """
    TransitionResult
    """ # noqa: E501
    distribution: Optional[PublishResult] = Field(default=None, description="Distribution is the channel fan-out this move triggered. Present ONLY on the move to published, the single edge that distributes — so its absence means no fan-out was attempted, never that one failed quietly. A fan-out that DID fail is present carrying its own honest status, because distribution never rolls the status change back.")
    doctype: Optional[StrictStr] = Field(default=None, description="DocType is the content type that moved — Campaign, SocialPost or Asset — echoed from the path.")
    var_from: Optional[StrictStr] = Field(default=None, description="From is the state the item held when it was read. A document carrying no status yet reads as \"draft\".", alias="from")
    name: Optional[StrictStr] = Field(default=None, description="Name is the document that moved, echoed from the path.")
    storefront: Optional[StorefrontResult] = Field(default=None, description="Storefront is the catalog side effect, present only when a published Asset was product imagery — it carries a design and a kind of ecom, product or lifestyle. Absent for everything else, so absence reads as \"not catalog imagery\" rather than \"the catalog failed\".")
    to: Optional[StrictStr] = Field(default=None, description="To is the state it holds now. From == To on an idempotent re-transition, which is legal and is where a caller that lost a publish race lands.")
    __properties: ClassVar[List[str]] = ["distribution", "doctype", "from", "name", "storefront", "to"]

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of TransitionResult from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # override the default output from pydantic by calling `to_dict()` of distribution
        if self.distribution:
            _dict['distribution'] = self.distribution.to_dict()
        # override the default output from pydantic by calling `to_dict()` of storefront
        if self.storefront:
            _dict['storefront'] = self.storefront.to_dict()
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of TransitionResult from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "distribution": PublishResult.from_dict(obj["distribution"]) if obj.get("distribution") is not None else None,
            "doctype": obj.get("doctype"),
            "from": obj.get("from"),
            "name": obj.get("name"),
            "storefront": StorefrontResult.from_dict(obj["storefront"]) if obj.get("storefront") is not None else None,
            "to": obj.get("to")
        })
        return _obj


