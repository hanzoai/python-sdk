# coding: utf-8

"""
    Hanzo Cloud API

    The Hanzo Cloud API as a customer calls it: every operation under /v1/ except the operator's admin product, relay doors, legacy spellings and capabilities still reached by flag. Tagged by product: the first path segment after /v1/.

    The version of the OpenAPI document: v1
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictBool, StrictStr
from typing import Any, ClassVar, Dict, List, Optional
from typing import Optional, Set
from typing_extensions import Self

class TrustAsk(BaseModel):
    """
    TrustAsk
    """ # noqa: E501
    accept: Optional[StrictBool] = Field(default=None, description="Accept must be true when the centre states an NDA. The text accepted is recorded verbatim on the request, so a later edit to the NDA cannot rewrite what this party agreed to.")
    email: Optional[StrictStr] = Field(default=None, description="Email is where the grant will be sent, and the ONLY address the resulting link admits. Required.")
    item: Optional[StrictStr] = Field(default=None, description="Item names one published item to ask for. Optional; omitting it asks for everything released on request.")
    party: Optional[StrictStr] = Field(default=None, description="Party is the company the asker is from. Optional, and recorded as stated.")
    reason: Optional[StrictStr] = Field(default=None, description="Reason is why they want it. Optional, and recorded as stated — it is what the person deciding reads.")
    slug: Optional[StrictStr] = Field(default=None, description="Slug is the centre's public address, taken from the path.")
    __properties: ClassVar[List[str]] = ["accept", "email", "item", "party", "reason", "slug"]

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of TrustAsk from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of TrustAsk from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "accept": obj.get("accept"),
            "email": obj.get("email"),
            "item": obj.get("item"),
            "party": obj.get("party"),
            "reason": obj.get("reason"),
            "slug": obj.get("slug")
        })
        return _obj


