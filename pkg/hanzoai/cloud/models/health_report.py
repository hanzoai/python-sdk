# coding: utf-8

"""
    Hanzo Cloud API

    Composed from each subsystem's own projection of its router, in the fleet's mount order — every operation below is a route the subsystem that publishes it registered. Tagged by product: the first path segment after /v1/.

    The version of the OpenAPI document: v1
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictBool, StrictStr
from typing import Any, ClassVar, Dict, List, Optional
from hanzoai.cloud.models.health_lenses import HealthLenses
from hanzoai.cloud.models.health_plane import HealthPlane
from hanzoai.cloud.models.loss import Loss
from typing import Optional, Set
from typing_extensions import Self

class HealthReport(BaseModel):
    """
    HealthReport
    """ # noqa: E501
    datastore: Optional[StrictBool] = Field(default=None, description="Datastore reports whether the shared warehouse client has a live connection. It is load-bearing for the READ path: false is one of the two ways this answers 503.")
    lenses: Optional[HealthLenses] = Field(default=None, description="Lenses is per-lens table availability, probed only when connected — so it is absent from a degraded report, which has nothing to say about tables it could not reach.")
    lost: Optional[Loss] = Field(default=None, description="Lost is the count of facts the sink irrecoverably dropped since boot (warehouse.go). It is reported on the DEGRADED report too, and deliberately: a warehouse that is unreachable is exactly when facts start failing their deliveries, so suppressing the number here would hide it precisely when it moves. ANY NON-ZERO VALUE IS AN ALARM — it counts data the door already answered 200 for.")
    plane: Optional[HealthPlane] = Field(default=None, description="Plane reports the event plane — the bus and the stream every accepted event is published to BEFORE any of it reaches the warehouse. It is load-bearing for the WRITE path, and it is here because its absence was a real outage: this endpoint answered 200/ok on warehouse connectivity alone while every POST /v1/event 503'd on a stream that could not bind, so 100% ingest loss was invisible to monitoring. A probe that cannot see the write path cannot report the write path.")
    reason: Optional[StrictStr] = Field(default=None, description="Reason is the human-readable cause, present only on a degraded report.")
    service: Optional[StrictStr] = Field(default=None, description="Service names the subsystem answering, so a probe aggregating several health endpoints can attribute a degraded one.")
    status: Optional[StrictStr] = Field(default=None, description="Status is ok or degraded. Degraded is the 503 and means EITHER load-bearing dependency is down — the warehouse this subsystem reads, or the event plane it writes. It is not moved by a missing lens table, which is honest-empty.")
    warehouse: Optional[StrictStr] = Field(default=None, description="Warehouse names the datastore database every lens reads.")
    __properties: ClassVar[List[str]] = ["datastore", "lenses", "lost", "plane", "reason", "service", "status", "warehouse"]

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of HealthReport from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # override the default output from pydantic by calling `to_dict()` of lenses
        if self.lenses:
            _dict['lenses'] = self.lenses.to_dict()
        # override the default output from pydantic by calling `to_dict()` of lost
        if self.lost:
            _dict['lost'] = self.lost.to_dict()
        # override the default output from pydantic by calling `to_dict()` of plane
        if self.plane:
            _dict['plane'] = self.plane.to_dict()
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of HealthReport from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "datastore": obj.get("datastore"),
            "lenses": HealthLenses.from_dict(obj["lenses"]) if obj.get("lenses") is not None else None,
            "lost": Loss.from_dict(obj["lost"]) if obj.get("lost") is not None else None,
            "plane": HealthPlane.from_dict(obj["plane"]) if obj.get("plane") is not None else None,
            "reason": obj.get("reason"),
            "service": obj.get("service"),
            "status": obj.get("status"),
            "warehouse": obj.get("warehouse")
        })
        return _obj


