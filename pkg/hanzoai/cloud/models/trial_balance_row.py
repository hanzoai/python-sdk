# coding: utf-8

"""
    Hanzo Cloud API

    The Hanzo Cloud API as a customer calls it: every operation under /v1/ except the operator's admin product, relay routes, legacy spellings and capabilities still reached by flag. Tagged by product: the first path segment after /v1/.

    The version of the OpenAPI document: v1
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictInt, StrictStr
from typing import Any, ClassVar, Dict, List, Optional
from typing import Optional, Set
from typing_extensions import Self

class TrialBalanceRow(BaseModel):
    """
    TrialBalanceRow
    """ # noqa: E501
    account: Optional[StrictStr] = Field(default=None, description="Account is the chart-of-accounts NUMBER this line reports on (\"1000\", \"4000\") — the stable posting key, not a display label.")
    closing_credit: Optional[StrictInt] = Field(default=None, description="ClosingCredit is that closing balance in cents when it is a credit balance.", alias="closingCredit")
    closing_debit: Optional[StrictInt] = Field(default=None, description="ClosingDebit is the balance at the end of the window, in cents, when it is a debit balance. This is the column the report's totals are summed from.", alias="closingDebit")
    credit: Optional[StrictInt] = Field(default=None, description="Credit is the same window movement in cents when it was net credit.")
    debit: Optional[StrictInt] = Field(default=None, description="Debit is the account's MOVEMENT within the window — closing minus opening, not the closing balance — in cents, when that movement was net debit. Zero when the account moved net credit.")
    name: Optional[StrictStr] = Field(default=None, description="Name is that account's human name from the fixed chart.")
    opening_credit: Optional[StrictInt] = Field(default=None, description="OpeningCredit is the same opening balance in cents when it fell on the credit side. Zero when the balance was a debit one.", alias="openingCredit")
    opening_debit: Optional[StrictInt] = Field(default=None, description="OpeningDebit is the account's balance before the window began, in whole cents, when that balance was on the debit side. Zero when the balance was a credit one — the pair is exclusive, never two halves of one number.", alias="openingDebit")
    type: Optional[StrictStr] = Field(default=None, description="Type is the account's fundamental class — asset, liability, income, expense or equity — which is also its normal balance side. It is carried for presentation and does NOT decide which column an amount lands in: placement follows the sign of the real net, so a contra balance shows up as one.")
    __properties: ClassVar[List[str]] = ["account", "closingCredit", "closingDebit", "credit", "debit", "name", "openingCredit", "openingDebit", "type"]

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of TrialBalanceRow from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of TrialBalanceRow from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "account": obj.get("account"),
            "closingCredit": obj.get("closingCredit"),
            "closingDebit": obj.get("closingDebit"),
            "credit": obj.get("credit"),
            "debit": obj.get("debit"),
            "name": obj.get("name"),
            "openingCredit": obj.get("openingCredit"),
            "openingDebit": obj.get("openingDebit"),
            "type": obj.get("type")
        })
        return _obj


