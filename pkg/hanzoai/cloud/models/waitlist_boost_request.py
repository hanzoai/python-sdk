# coding: utf-8

"""
    Hanzo Cloud API

    Composed from each subsystem's own projection of its router, in the fleet's mount order — every operation below is a route the subsystem that publishes it registered. Tagged by product: the first path segment after /v1/.

    The version of the OpenAPI document: v1
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictInt, StrictStr
from typing import Any, ClassVar, Dict, List, Optional
from typing import Optional, Set
from typing_extensions import Self

class WaitlistBoostRequest(BaseModel):
    """
    WaitlistBoostRequest
    """ # noqa: E501
    email: Optional[StrictStr] = Field(default=None, description="Email identifies the entry to boost. Either this or RefCode is required.")
    points: Optional[StrictInt] = Field(default=None, description="Points is how many points to award. Must be positive — this seam exists to move someone UP toward the cutoff.")
    reason: Optional[StrictStr] = Field(default=None, description="Reason is the operator's justification. Not sent to the engine; it is recorded on the audit row, which is the point of asking for it.")
    ref_code: Optional[StrictStr] = Field(default=None, description="RefCode identifies the entry by its referral code, when the email is unknown.", alias="refCode")
    waitlist: Optional[StrictStr] = Field(default=None, description="Waitlist is the waitlist slug the grant lands on. Required.")
    __properties: ClassVar[List[str]] = ["email", "points", "reason", "refCode", "waitlist"]

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of WaitlistBoostRequest from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of WaitlistBoostRequest from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "email": obj.get("email"),
            "points": obj.get("points"),
            "reason": obj.get("reason"),
            "refCode": obj.get("refCode"),
            "waitlist": obj.get("waitlist")
        })
        return _obj


