# coding: utf-8

"""
    Hanzo Cloud API

    The Hanzo Cloud API as a customer calls it: every operation under /v1/ except the operator's admin product, relay routes, legacy spellings and capabilities still reached by flag. Tagged by product: the first path segment after /v1/.

    The version of the OpenAPI document: v1
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictBool, StrictInt, StrictStr
from typing import Any, ClassVar, Dict, List, Optional
from typing import Optional, Set
from typing_extensions import Self

class RunnerIdentity(BaseModel):
    """
    RunnerIdentity
    """ # noqa: E501
    ephemeral: Optional[StrictBool] = Field(default=None, description="Ephemeral means the runner takes ONE job and exits, so the forge must not expect it back.")
    id: Optional[StrictInt] = Field(default=None, description="ID is the forge's own row id for this runner.")
    labels: Optional[List[StrictStr]] = Field(default=None, description="Labels are what a workflow's `runs-on:` selects this runner by.")
    name: Optional[StrictStr] = Field(default=None, description="Name is what a person calls this runner in the forge's UI.")
    token: Optional[StrictStr] = Field(default=None, description="Token is the secret that authenticates that handle. It is in cleartext here and nowhere else; the forge stores only a digest of it.")
    uuid: Optional[StrictStr] = Field(default=None, description="UUID is the handle the runner presents on every later op.")
    version: Optional[StrictStr] = Field(default=None, description="Version is the runner build the forge last heard from.")
    __properties: ClassVar[List[str]] = ["ephemeral", "id", "labels", "name", "token", "uuid", "version"]

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of RunnerIdentity from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of RunnerIdentity from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "ephemeral": obj.get("ephemeral"),
            "id": obj.get("id"),
            "labels": obj.get("labels"),
            "name": obj.get("name"),
            "token": obj.get("token"),
            "uuid": obj.get("uuid"),
            "version": obj.get("version")
        })
        return _obj


