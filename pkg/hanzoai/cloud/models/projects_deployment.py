# coding: utf-8

"""
    Hanzo Cloud API

    The Hanzo Cloud API as a customer calls it: every operation under /v1/ except the operator's admin product, relay routes, legacy spellings and capabilities still reached by flag. Tagged by product: the first path segment after /v1/.

    The version of the OpenAPI document: v1
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictInt, StrictStr
from typing import Any, ClassVar, Dict, List, Optional
from hanzoai.cloud.models.projects_upload_grant import ProjectsUploadGrant
from typing import Optional, Set
from typing_extensions import Self

class ProjectsDeployment(BaseModel):
    """
    ProjectsDeployment
    """ # noqa: E501
    bucket: Optional[StrictStr] = Field(default=None, description="Bucket is the object-store bucket its files were written to.")
    bytes: Optional[StrictInt] = Field(default=None, description="Bytes is their total size in bytes.")
    commit: Optional[StrictStr] = Field(default=None, description="Commit is the revision that was built, for a deployment that came from a repository. Absent for an uploaded artifact, which has no revision.")
    created_at: Optional[StrictInt] = Field(default=None, description="CreatedAt is when the deployment was queued, as Unix seconds.", alias="createdAt")
    files: Optional[StrictInt] = Field(default=None, description="Files is how many objects the deployment published.")
    id: Optional[StrictStr] = Field(default=None, description="ID identifies this one deployment attempt, and is what CI quotes back to complete it.")
    live_url: Optional[StrictStr] = Field(default=None, description="LiveURL is where this deployment serves, once it is live.", alias="liveUrl")
    message: Optional[StrictStr] = Field(default=None, description="Message is what happened, in words — the build's own note, or on a failure why it failed.")
    prefix: Optional[StrictStr] = Field(default=None, description="Prefix is the key prefix within that bucket holding EXACTLY this deployment's objects — the unit an upload grant is scoped to, so a grant for one deployment cannot write over another.")
    project_id: Optional[StrictStr] = Field(default=None, description="ProjectID is the project this deployment belongs to.", alias="projectId")
    source: Optional[StrictStr] = Field(default=None, description="Source is what caused the deployment — a git push, an uploaded artifact, a generated site.")
    status: Optional[StrictStr] = Field(default=None, description="Status is where the attempt got to — queued, live, or failed. A deployment that is live is not necessarily the one SERVING: the project's own currentDeploymentId says which is.")
    updated_at: Optional[StrictInt] = Field(default=None, description="UpdatedAt is when it last changed state, as Unix seconds — so the gap between the two is how long the build took.", alias="updatedAt")
    upload: Optional[ProjectsUploadGrant] = Field(default=None, description="Upload is the prefix-scoped, short-lived S3 write grant handed to CI with a queued git deployment, so it needs no bucket credential (grant.go). Present ONLY on the 202 that creates the deployment — it is never stored and never replayed on a later read, so a grant cannot outlive the build it was minted for by being fetched again.")
    version: Optional[StrictInt] = Field(default=None, description="Version counts deployments of this project from 1, so the history reads as an ordered sequence rather than by timestamp. It is per project, not global.")
    __properties: ClassVar[List[str]] = ["bucket", "bytes", "commit", "createdAt", "files", "id", "liveUrl", "message", "prefix", "projectId", "source", "status", "updatedAt", "upload", "version"]

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of ProjectsDeployment from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # override the default output from pydantic by calling `to_dict()` of upload
        if self.upload:
            _dict['upload'] = self.upload.to_dict()
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of ProjectsDeployment from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "bucket": obj.get("bucket"),
            "bytes": obj.get("bytes"),
            "commit": obj.get("commit"),
            "createdAt": obj.get("createdAt"),
            "files": obj.get("files"),
            "id": obj.get("id"),
            "liveUrl": obj.get("liveUrl"),
            "message": obj.get("message"),
            "prefix": obj.get("prefix"),
            "projectId": obj.get("projectId"),
            "source": obj.get("source"),
            "status": obj.get("status"),
            "updatedAt": obj.get("updatedAt"),
            "upload": ProjectsUploadGrant.from_dict(obj["upload"]) if obj.get("upload") is not None else None,
            "version": obj.get("version")
        })
        return _obj


