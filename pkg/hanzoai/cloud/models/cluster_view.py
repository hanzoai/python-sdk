# coding: utf-8

"""
    Hanzo Cloud API

    The Hanzo Cloud API as a customer calls it: every operation under /v1/ except the operator's admin product, relay routes, legacy spellings and capabilities still reached by flag. Tagged by product: the first path segment after /v1/.

    The version of the OpenAPI document: v1
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictInt, StrictStr
from typing import Any, ClassVar, Dict, List, Optional
from hanzoai.cloud.models.node_pool_view import NodePoolView
from typing import Optional, Set
from typing_extensions import Self

class ClusterView(BaseModel):
    """
    ClusterView
    """ # noqa: E501
    amd_gpu: Optional[StrictInt] = Field(default=None, description="AmdGPU is the same count for `amd.com/gpu`: AMD accelerators across the BYO cluster's nodes, as of the attach.", alias="amdGpu")
    created_at: Optional[StrictStr] = Field(default=None, description="CreatedAt is when the cluster started existing: the earliest creation time among its pools for a managed cluster, and for a BYO one the RFC 3339 moment it was attached. Empty when the source states none.", alias="createdAt")
    do_cluster_id: Optional[StrictStr] = Field(default=None, description="DoClusterID carries the SAME id as DoksClusterID. Both names exist because the console's Cluster type reads either one; neither is a second identifier.", alias="doClusterId")
    doks_cluster_id: Optional[StrictStr] = Field(default=None, description="DoksClusterID is the provider's own id for the cluster, and the value the /v1/visor/k8s/clusters/:id routes take. Empty for a BYO cluster: an attached kubeconfig was never provisioned, so there is no provider id to state.", alias="doksClusterId")
    kind: Optional[StrictStr] = Field(default=None, description="Kind says which of the two kinds of cluster this row is, and there are only two: \"managed\" — Visor provisioned it and Hanzo's account pays the provider — or \"byo\", an existing cluster the org attached by kubeconfig.")
    name: Optional[StrictStr] = Field(default=None, description="Name is the cluster's name: the provider's for a managed cluster, and for a BYO one the lower-cased fleet name it was attached under — which is also how the detach route addresses it.")
    node_count: Optional[StrictInt] = Field(default=None, description="NodeCount is how many worker nodes the cluster has — the sum over its pools for a managed cluster, and for a BYO one the node count read off the cluster when it was attached.", alias="nodeCount")
    node_pools: Optional[List[NodePoolView]] = Field(default=None, description="NodePools is the authoritative node inventory — every pool, each with its own size and count. It is empty in two cases that are not \"no pools\": a row from the /v1/visor/k8s/clusters LIST, which is deliberately lightweight and whose :id detail carries them, and a BYO cluster, whose pools were never read.", alias="nodePools")
    node_size: Optional[StrictStr] = Field(default=None, description="NodeSize is a display convenience: the size slug of the FIRST pool. A cluster mixing sizes has more than one, and NodePools is where they all are.", alias="nodeSize")
    nvidia_gpu: Optional[StrictInt] = Field(default=None, description="NvidiaGPU is how many NVIDIA accelerators the cluster's nodes advertise, the sum of `nvidia.com/gpu` allocatable across them. BYO only, and counted ONCE when the cluster was attached — it is an inventory, not live capacity.", alias="nvidiaGpu")
    region: Optional[StrictStr] = Field(default=None, description="Region is the provider region slug for a managed cluster. A BYO cluster has no region we can read, so it carries the free-form `provider` label the attach named it with (\"gke\", \"on-prem\") instead.")
    status: Optional[StrictStr] = Field(default=None, description="Status is the cluster's state: the provider's own word for a managed cluster (\"running\", \"provisioning\"), \"unknown\" when the provider stated none, and always \"attached\" for a BYO cluster — that one says the kubeconfig is on file, not that the cluster is reachable this second.")
    __properties: ClassVar[List[str]] = ["amdGpu", "createdAt", "doClusterId", "doksClusterId", "kind", "name", "nodeCount", "nodePools", "nodeSize", "nvidiaGpu", "region", "status"]

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of ClusterView from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # override the default output from pydantic by calling `to_dict()` of each item in node_pools (list)
        _items = []
        if self.node_pools:
            for _item_node_pools in self.node_pools:
                if _item_node_pools:
                    _items.append(_item_node_pools.to_dict())
            _dict['nodePools'] = _items
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of ClusterView from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "amdGpu": obj.get("amdGpu"),
            "createdAt": obj.get("createdAt"),
            "doClusterId": obj.get("doClusterId"),
            "doksClusterId": obj.get("doksClusterId"),
            "kind": obj.get("kind"),
            "name": obj.get("name"),
            "nodeCount": obj.get("nodeCount"),
            "nodePools": [NodePoolView.from_dict(_item) for _item in obj["nodePools"]] if obj.get("nodePools") is not None else None,
            "nodeSize": obj.get("nodeSize"),
            "nvidiaGpu": obj.get("nvidiaGpu"),
            "region": obj.get("region"),
            "status": obj.get("status")
        })
        return _obj


