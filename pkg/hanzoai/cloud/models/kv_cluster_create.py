# coding: utf-8

"""
    Hanzo Cloud — Unified API (V8 · Open Edition)

    The single unified OpenAPI surface for ALL Hanzo services, aggregated from the per-service specs. Every route is https://api.hanzo.ai/v1/<service>/*. Grouped by the canonical domains in capabilities.yaml (the ONE registry). cloud/openapi.yaml is hanzoai/cloud's own woven document, copied verbatim by sync.py, and it wins wherever it and a hand-written spec describe the same route. Regenerated by merge.py — edit the per-service <svc>/openapi.yaml + capabilities.yaml, not this file.

    The version of the OpenAPI document: 8.0.0
    Contact: support@hanzo.ai
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, StrictBool, StrictInt, StrictStr, field_validator
from typing import Any, ClassVar, Dict, List, Optional
from typing import Optional, Set
from typing_extensions import Self

class KvClusterCreate(BaseModel):
    """
    KvClusterCreate
    """ # noqa: E501
    name: StrictStr
    mode: Optional[StrictStr] = 'standalone'
    version: Optional[StrictStr] = '8.0'
    max_memory_mb: Optional[StrictInt] = 256
    replicas: Optional[StrictInt] = 0
    tls: Optional[StrictBool] = True
    eviction_policy: Optional[StrictStr] = 'allkeys-lru'
    __properties: ClassVar[List[str]] = ["name", "mode", "version", "max_memory_mb", "replicas", "tls", "eviction_policy"]

    @field_validator('mode')
    def mode_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['standalone', 'sentinel', 'cluster']):
            raise ValueError("must be one of enum values ('standalone', 'sentinel', 'cluster')")
        return value

    @field_validator('eviction_policy')
    def eviction_policy_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['noeviction', 'allkeys-lru', 'allkeys-lfu', 'volatile-lru', 'volatile-lfu', 'allkeys-random']):
            raise ValueError("must be one of enum values ('noeviction', 'allkeys-lru', 'allkeys-lfu', 'volatile-lru', 'volatile-lfu', 'allkeys-random')")
        return value

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of KvClusterCreate from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of KvClusterCreate from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "name": obj.get("name"),
            "mode": obj.get("mode") if obj.get("mode") is not None else 'standalone',
            "version": obj.get("version") if obj.get("version") is not None else '8.0',
            "max_memory_mb": obj.get("max_memory_mb") if obj.get("max_memory_mb") is not None else 256,
            "replicas": obj.get("replicas") if obj.get("replicas") is not None else 0,
            "tls": obj.get("tls") if obj.get("tls") is not None else True,
            "eviction_policy": obj.get("eviction_policy") if obj.get("eviction_policy") is not None else 'allkeys-lru'
        })
        return _obj


