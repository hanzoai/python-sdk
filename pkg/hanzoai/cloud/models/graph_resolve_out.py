# coding: utf-8

"""
    Hanzo Cloud API

    The Hanzo Cloud API as a customer calls it: every operation under /v1/ except the operator's admin product, relay routes, legacy spellings and capabilities still reached by flag. Tagged by product: the first path segment after /v1/.

    The version of the OpenAPI document: v1
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictBool, StrictStr
from typing import Any, ClassVar, Dict, List, Optional
from hanzoai.cloud.models.wire_fact import WireFact
from typing import Optional, Set
from typing_extensions import Self

class GraphResolveOut(BaseModel):
    """
    GraphResolveOut
    """ # noqa: E501
    as_of: Optional[StrictStr] = Field(default=None, description="AsOf is the instant this answer was taken at, RFC 3339: the one asked for, or the server's clock when none was.")
    conflicts: Optional[List[WireFact]] = Field(default=None, description="Conflicts is every OTHER assertion knowable at AsOf, strongest first. They are not all disagreements: one that repeats the winner's value ranks below it and is listed here too.")
    contested: Optional[StrictBool] = Field(default=None, description="Contested is true when at least one conflict claims a value different from the winner's. Any number of conflicts that all agree leaves it false.")
    entity: Optional[StrictStr] = Field(default=None, description="Entity is the entity the question named, echoed so a stored answer still says what it is about.")
    known: Optional[StrictBool] = Field(default=None, description="Known is false when this plane held nothing knowable at AsOf. That is an answer, not an error.")
    relation: Optional[StrictStr] = Field(default=None, description="Relation is the relation the question named, echoed for the same reason.")
    truncated: Optional[StrictBool] = Field(default=None, description="Truncated says this pair holds more assertions than one read returns, so the winner was decided from the most recent ceiling-full of them. It is reported because a provenance plane that trims silently is a plane that answers confidently and wrongly; narrow the question with as_of to see what it dropped.")
    winner: Optional[WireFact] = Field(default=None, description="Winner is the assertion in force — the strongest of those knowable at AsOf under the order `rule` names. Absent exactly when Known is false.")
    __properties: ClassVar[List[str]] = ["as_of", "conflicts", "contested", "entity", "known", "relation", "truncated", "winner"]

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of GraphResolveOut from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # override the default output from pydantic by calling `to_dict()` of each item in conflicts (list)
        _items = []
        if self.conflicts:
            for _item_conflicts in self.conflicts:
                if _item_conflicts:
                    _items.append(_item_conflicts.to_dict())
            _dict['conflicts'] = _items
        # override the default output from pydantic by calling `to_dict()` of winner
        if self.winner:
            _dict['winner'] = self.winner.to_dict()
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of GraphResolveOut from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "as_of": obj.get("as_of"),
            "conflicts": [WireFact.from_dict(_item) for _item in obj["conflicts"]] if obj.get("conflicts") is not None else None,
            "contested": obj.get("contested"),
            "entity": obj.get("entity"),
            "known": obj.get("known"),
            "relation": obj.get("relation"),
            "truncated": obj.get("truncated"),
            "winner": WireFact.from_dict(obj["winner"]) if obj.get("winner") is not None else None
        })
        return _obj


