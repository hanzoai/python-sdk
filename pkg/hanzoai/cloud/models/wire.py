# coding: utf-8

"""
    Hanzo Cloud API

    The Hanzo Cloud API as a customer calls it: every operation under /v1/ except the operator's admin product, relay routes, legacy spellings and capabilities still reached by flag. Tagged by product: the first path segment after /v1/.

    The version of the OpenAPI document: v1
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictBool, StrictInt, StrictStr
from typing import Any, ClassVar, Dict, List, Optional
from typing import Optional, Set
from typing_extensions import Self

class Wire(BaseModel):
    """
    Wire
    """ # noqa: E501
    action: Optional[StrictStr] = Field(default=None, description="Action is the verb that was performed. It is the event's name, not the HTTP method — a request-sourced record carries both, and the pair is what makes a row readable (\"grant.create\" at POST /v1/admin/grants).")
    auth_method: Optional[StrictStr] = Field(default=None, description="Auth is the credential the actor presented: \"jwt\", \"api-key\", or \"none\".", alias="authMethod")
    email: Optional[StrictStr] = Field(default=None, description="Email is the actor's validated address, absent when the credential carried none. It comes from the verified token, never from a client header.")
    hash: Optional[StrictStr] = Field(default=None, description="Hash is this record's SHA-256 over its own canonical JSON with both hash fields cleared, folded with prevHash. Recomputing it from the row's other fields is what proves the row has not been edited.")
    home: Optional[StrictStr] = Field(default=None, description="Home is present ONLY on a cross-org action: the org the actor came FROM, while Org is the org they acted IN. A console row carrying `home` is a platform-admin impersonation and should be rendered as one.")
    is_admin: Optional[StrictBool] = Field(default=None, description="IsAdmin is the VALIDATED platform-SuperAdmin bit at decision time (membership of the reserved admin org), never the client's own claim to be one.", alias="isAdmin")
    method: Optional[StrictStr] = Field(default=None, description="Method is the HTTP verb, on a record a request produced. Absent on an event emitted from inside the binary with no request behind it.")
    org: Optional[StrictStr] = Field(default=None, description="Org is the tenant the action was taken IN — the effective org, which for everyone but an impersonating SuperAdmin is also the actor's own. Empty on an unauthenticated request.")
    path: Optional[StrictStr] = Field(default=None, description="Path is the request's route. Any segment shaped like a credential is replaced before the record is written, so a key that rides in a path is not preserved here by the very control meant to watch it.")
    prev_hash: Optional[StrictStr] = Field(default=None, description="PrevHash is the hash of record seq-1, which is what links the rows into a chain: a deleted or reordered record breaks the recomputation at that point. The first record of a chain carries 64 zeros rather than an empty string, so \"start of chain\" and \"field missing\" cannot look alike.", alias="prevHash")
    reason: Optional[StrictStr] = Field(default=None, description="Reason is a short explanation for a deny or an error (\"SuperAdmin required\", \"insufficient_balance\"). It is never a secret and never a raw upstream error body; absent on a success.")
    request_id: Optional[StrictStr] = Field(default=None, description="RequestID ties this row to the request-line log and any downstream trace — the X-Request-Id the pipeline minted for that request.", alias="requestId")
    resource: Optional[StrictStr] = Field(default=None, description="Resource is the KIND of thing acted upon (\"org\", \"role\", \"secret\", \"provider-config\", \"credit\"). Where a mutation has no finer semantics than its route, this is the route family and resourceId is empty — the action and the path already pin the object.")
    resource_id: Optional[StrictStr] = Field(default=None, description="ResourceID is the specific instance, absent when the kind alone identifies it.", alias="resourceId")
    result: Optional[StrictStr] = Field(default=None, description="Result is how the action ended: \"success\", \"deny\" or \"error\". A deny is a decision this binary made and is as much evidence as a success.")
    seq: Optional[StrictInt] = Field(default=None, description="Seq is the record's position in the chain, 0-based and gapless. The Recorder assigns it under its own lock, so it is a true total order: seq n+1 was written after seq n, and a missing number is a missing record.")
    source_ip: Optional[StrictStr] = Field(default=None, description="SourceIP is the client address the edge resolved for the request, after the proxy chain — the address a responder would act on, not the socket peer.", alias="sourceIp")
    status: Optional[StrictInt] = Field(default=None, description="Status is the HTTP status the caller received. It is the outcome as the client saw it, so a 200 carrying a domain refusal still reads 200 here.")
    sub: Optional[StrictStr] = Field(default=None, description="Sub is the acting user (the IAM subject). Empty for a machine principal or an anonymous request, which is how a service action is told from a person's.")
    time: Optional[StrictStr] = Field(default=None, description="Time is when the action happened, RFC3339Nano in UTC. The stored column has the same precision and sorts the same way, so a client can range and order on this string verbatim.")
    user_agent: Optional[StrictStr] = Field(default=None, description="UserAgent is the client the request announced itself as. Client-supplied, so it is evidence about what claimed to act, not proof of it.", alias="userAgent")
    __properties: ClassVar[List[str]] = ["action", "authMethod", "email", "hash", "home", "isAdmin", "method", "org", "path", "prevHash", "reason", "requestId", "resource", "resourceId", "result", "seq", "sourceIp", "status", "sub", "time", "userAgent"]

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of Wire from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of Wire from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "action": obj.get("action"),
            "authMethod": obj.get("authMethod"),
            "email": obj.get("email"),
            "hash": obj.get("hash"),
            "home": obj.get("home"),
            "isAdmin": obj.get("isAdmin"),
            "method": obj.get("method"),
            "org": obj.get("org"),
            "path": obj.get("path"),
            "prevHash": obj.get("prevHash"),
            "reason": obj.get("reason"),
            "requestId": obj.get("requestId"),
            "resource": obj.get("resource"),
            "resourceId": obj.get("resourceId"),
            "result": obj.get("result"),
            "seq": obj.get("seq"),
            "sourceIp": obj.get("sourceIp"),
            "status": obj.get("status"),
            "sub": obj.get("sub"),
            "time": obj.get("time"),
            "userAgent": obj.get("userAgent")
        })
        return _obj


