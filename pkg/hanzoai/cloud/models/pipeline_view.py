# coding: utf-8

"""
    Hanzo Cloud API

    Composed from each subsystem's own projection of its router, in the fleet's mount order — every operation below is a route the subsystem that publishes it registered. Tagged by product: the first path segment after /v1/.

    The version of the OpenAPI document: v1
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictBool, StrictStr
from typing import Any, ClassVar, Dict, List, Optional
from hanzoai.cloud.models.filters import Filters
from typing import Optional, Set
from typing_extensions import Self

class PipelineView(BaseModel):
    """
    PipelineView
    """ # noqa: E501
    created_at: Optional[StrictStr] = Field(default=None, description="CreatedAt is when the pipeline was first stored, RFC3339 UTC. Absent on the default.", alias="createdAt")
    default: Optional[StrictBool] = Field(default=None, description="Default is true when no pipeline is stored for this project and these are the built-in world feeds. Writing one turns it false.")
    feeds: Optional[List[StrictStr]] = Field(default=None, description="Feeds is the RSS/Atom feed URLs the pipeline reads. Every host is on the server's allowlist — a URL that is not cannot be stored.")
    filters: Optional[Filters] = Field(default=None, description="Filters narrows the merged feed.")
    org: Optional[StrictStr] = Field(default=None, description="Org is the tenant the pipeline belongs to, resolved server-side from the validated principal.")
    project: Optional[StrictStr] = Field(default=None, description="Project is the org sub-scope the pipeline belongs to.")
    updated_at: Optional[StrictStr] = Field(default=None, description="UpdatedAt is when it was last written, RFC3339 UTC. Absent on the default.", alias="updatedAt")
    __properties: ClassVar[List[str]] = ["createdAt", "default", "feeds", "filters", "org", "project", "updatedAt"]

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of PipelineView from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # override the default output from pydantic by calling `to_dict()` of filters
        if self.filters:
            _dict['filters'] = self.filters.to_dict()
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of PipelineView from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "createdAt": obj.get("createdAt"),
            "default": obj.get("default"),
            "feeds": obj.get("feeds"),
            "filters": Filters.from_dict(obj["filters"]) if obj.get("filters") is not None else None,
            "org": obj.get("org"),
            "project": obj.get("project"),
            "updatedAt": obj.get("updatedAt")
        })
        return _obj


