# coding: utf-8

"""
    Hanzo Cloud API

    The Hanzo Cloud API as a customer calls it: every operation under /v1/ except the operator's admin product, relay doors, legacy spellings and capabilities still reached by flag. Tagged by product: the first path segment after /v1/.

    The version of the OpenAPI document: v1
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictBool, StrictInt, StrictStr
from typing import Any, ClassVar, Dict, List, Optional
from hanzoai.cloud.models.reference_source import ReferenceSource
from typing import Optional, Set
from typing_extensions import Self

class ReferenceSet(BaseModel):
    """
    ReferenceSet
    """ # noqa: E501
    age: Optional[StrictStr] = Field(default=None, description="Age is how long ago that was.")
    as_of: Optional[StrictStr] = Field(default=None, description="AsOf is when the OLDEST contributing publisher was current, RFC 3339. The oldest and not the newest: a set is exactly as fresh as its weakest source.", alias="asOf")
    keys: Optional[StrictInt] = Field(default=None, description="Keys is how many members the baseline carries.")
    kind: Optional[StrictStr] = Field(default=None, description="Kind is how the baseline comes to exist: fetch (downloaded from a publisher), local (computed here), attest (held by the component that screens against it, freshness reported), or seam (declared and NOT held, because the source needs a licence we do not have).")
    match: Optional[StrictStr] = Field(default=None, description="Match is how a key is tested: exact, domain, net, digits, pattern or range.")
    max_age: Optional[StrictStr] = Field(default=None, description="MaxAge is how old this set may be before it is stale.", alias="maxAge")
    overrides: Optional[StrictInt] = Field(default=None, description="Overrides is how many entries YOUR org has laid over this baseline.")
    refusal: Optional[StrictStr] = Field(default=None, description="Refusal names why the set cannot be relied on, when it cannot: never loaded, held elsewhere, or a licence we do not hold. Non-empty means a lookup against this set will not answer, rather than answering clean.")
    set: Optional[StrictStr] = Field(default=None, description="Set is the name this set is addressed by.")
    sources: Optional[List[ReferenceSource]] = Field(default=None, description="Sources is each contributing publisher, its licence and its own freshness.")
    stale: Optional[StrictBool] = Field(default=None, description="Stale is whether it is past that bound. A stale set still answers and says so, because yesterday's list beats none.")
    version: Optional[StrictStr] = Field(default=None, description="Version is the exact baseline consulted — every contributing publisher and its content digest. A decision records this and an auditor resolves it back.")
    what: Optional[StrictStr] = Field(default=None, description="What the set holds, in one sentence.")
    __properties: ClassVar[List[str]] = ["age", "asOf", "keys", "kind", "match", "maxAge", "overrides", "refusal", "set", "sources", "stale", "version", "what"]

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of ReferenceSet from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # override the default output from pydantic by calling `to_dict()` of each item in sources (list)
        _items = []
        if self.sources:
            for _item_sources in self.sources:
                if _item_sources:
                    _items.append(_item_sources.to_dict())
            _dict['sources'] = _items
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of ReferenceSet from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "age": obj.get("age"),
            "asOf": obj.get("asOf"),
            "keys": obj.get("keys"),
            "kind": obj.get("kind"),
            "match": obj.get("match"),
            "maxAge": obj.get("maxAge"),
            "overrides": obj.get("overrides"),
            "refusal": obj.get("refusal"),
            "set": obj.get("set"),
            "sources": [ReferenceSource.from_dict(_item) for _item in obj["sources"]] if obj.get("sources") is not None else None,
            "stale": obj.get("stale"),
            "version": obj.get("version"),
            "what": obj.get("what")
        })
        return _obj


