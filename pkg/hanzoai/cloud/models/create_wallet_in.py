# coding: utf-8

"""
    Hanzo Cloud API

    Composed from each subsystem's own projection of its router, in the fleet's mount order — every operation below is a route the subsystem that publishes it registered. Tagged by product: the first path segment after /v1/.

    The version of the OpenAPI document: v1
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictStr
from typing import Any, ClassVar, Dict, List, Optional
from typing import Optional, Set
from typing_extensions import Self

class CreateWalletIn(BaseModel):
    """
    CreateWalletIn
    """ # noqa: E501
    account_id: Optional[StrictStr] = Field(default=None, description="AccountID is the account this wallet belongs to. Required, and it must be an account of the caller's own org — an unknown one is a 404.", alias="accountId")
    agent: Optional[StrictStr] = Field(default=None, description="Agent optionally narrows the wallet to one agent within the org. It becomes a segment of the key ref, so it must be a url-safe segment with no slash.")
    chain: Optional[StrictStr] = Field(default=None, description="Chain is the EVM chain this wallet is for, as \"eip155:<n>\" or a bare decimal chain id. Optional; a Safe defaults to the Hanzo L1 (36963).")
    custody: Optional[StrictStr] = Field(default=None, description="Custody selects the signing backend: \"kms\" (in-process, always available), \"mpc\" or \"treasury\" (the deployed MPC ring), or \"safe\" (a Safe smart wallet owned by an MPC key). Empty uses the deployment's default. A backend that is not configured fails CLOSED with 503 rather than fabricating a signature.")
    name: Optional[StrictStr] = Field(default=None, description="Name is the wallet's display label. Optional.")
    tier: Optional[StrictStr] = Field(default=None, description="Tier is the MPC wallet tier: hot, warm, cold, gas, bridge, contract_admin, validator, quarantine or disaster_recovery. Empty defaults to hot.")
    __properties: ClassVar[List[str]] = ["accountId", "agent", "chain", "custody", "name", "tier"]

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of CreateWalletIn from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of CreateWalletIn from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "accountId": obj.get("accountId"),
            "agent": obj.get("agent"),
            "chain": obj.get("chain"),
            "custody": obj.get("custody"),
            "name": obj.get("name"),
            "tier": obj.get("tier")
        })
        return _obj


