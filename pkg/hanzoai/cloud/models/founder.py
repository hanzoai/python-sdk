# coding: utf-8

"""
    Hanzo Cloud API

    Composed from each subsystem's own projection of its router, in the fleet's mount order — every operation below is a route the subsystem that publishes it registered. Tagged by product: the first path segment after /v1/.

    The version of the OpenAPI document: v1
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictInt, StrictStr
from typing import Any, ClassVar, Dict, List, Optional
from typing import Optional, Set
from typing_extensions import Self

class Founder(BaseModel):
    """
    Founder
    """ # noqa: E501
    decided_by: Optional[StrictStr] = Field(default=None, description="DecidedBy is who settled a terminal KYC status: the provider name, or a reviewer's user id.", alias="decidedBy")
    email: Optional[StrictStr] = Field(default=None, description="Email is the founder's email, and the key a KYC decision addresses a founder by — POST /v1/company/kyc/decision matches on it.")
    equity_bps: Optional[StrictInt] = Field(default=None, description="EquityBps is the founder's ownership in basis points, 0–10000 (1% == 100 bps, so 10000 is the whole company). The founders' shares seed the cap-table genesis.", alias="equityBps")
    kyc_ref: Optional[StrictStr] = Field(default=None, description="KYCRef is the idv provider's session reference for this founder.", alias="kycRef")
    kyc_status: Optional[StrictStr] = Field(default=None, description="KYCStatus is the founder's identity-verification state: pending, verified (a real idv provider reported a pass), reviewer_confirmed (a privileged reviewer confirmed out-of-band) or failed. The payment stage is unreachable until every founder passes.", alias="kycStatus")
    name: Optional[StrictStr] = Field(default=None, description="Name is the founder's full legal name, as it appears on the formation documents.")
    __properties: ClassVar[List[str]] = ["decidedBy", "email", "equityBps", "kycRef", "kycStatus", "name"]

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of Founder from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of Founder from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "decidedBy": obj.get("decidedBy"),
            "email": obj.get("email"),
            "equityBps": obj.get("equityBps"),
            "kycRef": obj.get("kycRef"),
            "kycStatus": obj.get("kycStatus"),
            "name": obj.get("name")
        })
        return _obj


