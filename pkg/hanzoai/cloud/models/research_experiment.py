# coding: utf-8

"""
    Hanzo Cloud — Unified API (V8 · Open Edition)

    The single unified OpenAPI surface for ALL Hanzo services, aggregated from the per-service specs. Every route is https://api.hanzo.ai/v1/<service>/*. Grouped by the canonical domains in capabilities.yaml (the ONE registry). Regenerated by merge.py — edit the per-service <svc>/openapi.yaml + capabilities.yaml, not this file.

    The version of the OpenAPI document: 8.0.0
    Contact: support@hanzo.ai
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictBool, StrictFloat, StrictInt, StrictStr, field_validator
from typing import Any, ClassVar, Dict, List, Optional, Union
from typing import Optional, Set
from typing_extensions import Self

class ResearchExperiment(BaseModel):
    """
    One versioned run record. On read, the canonical (latest answered) version.
    """ # noqa: E501
    project: Optional[StrictStr] = Field(default=None, description="Server-stamped on ingest; a payload cannot forge it.")
    id: StrictStr = Field(description="Stable id — \"<kind>:<subject>:<task>\". Keys supersession.")
    revision: Optional[StrictStr] = 'original'
    status: Optional[StrictStr] = 'complete'
    canonical: Optional[StrictBool] = Field(default=None, description="True when this is the current authoritative version.")
    visibility: Optional[StrictStr] = Field(default='private', description="Read-only on ingest (forced private); set via a grant.")
    trainable: Optional[StrictBool] = Field(default=False, description="Read-only on ingest; set via a grant.")
    publishable: Optional[StrictBool] = Field(default=False, description="Read-only on ingest; set via a grant.")
    kind: StrictStr
    subject: StrictStr = Field(description="model / kernel / dataset under test")
    task: Optional[StrictStr] = Field(default=None, description="benchmark id")
    metric: Optional[StrictStr] = Field(default=None, description="accuracy | tok/s | loss | net-lift")
    value: Optional[Union[StrictFloat, StrictInt]] = Field(default=None, description="the headline number")
    n: Optional[StrictInt] = Field(default=None, description="answered sample size / coverage")
    n_total: Optional[StrictInt] = Field(default=None, description="item target (done vs remaining = n_total - n)")
    cost_usd: Optional[Union[StrictFloat, StrictInt]] = None
    meta: Optional[Dict[str, Any]] = Field(default=None, description="nonpersonal provenance (permanent retention class)")
    git_sha: Optional[StrictStr] = Field(default=None, description="producing repo commit at run time (indexed)")
    git_branch: Optional[StrictStr] = Field(default=None, description="producing repo branch at run time (indexed)")
    git_dirty: Optional[StrictBool] = Field(default=None, description="tree was dirty at run time")
    lib_versions: Optional[Dict[str, StrictStr]] = Field(default=None, description="{lib: version} at run time")
    ts: Optional[StrictInt] = Field(default=None, description="run/observation clock (unix seconds); orders versions")
    endpoint: Optional[StrictStr] = Field(default=None, description="OPTIONAL BYO arm URL; SSRF-gated at ingest (https")
    __properties: ClassVar[List[str]] = ["project", "id", "revision", "status", "canonical", "visibility", "trainable", "publishable", "kind", "subject", "task", "metric", "value", "n", "n_total", "cost_usd", "meta", "git_sha", "git_branch", "git_dirty", "lib_versions", "ts", "endpoint"]

    @field_validator('revision')
    def revision_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['original', 'corrected', 'retracted']):
            raise ValueError("must be one of enum values ('original', 'corrected', 'retracted')")
        return value

    @field_validator('status')
    def status_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['queued', 'planning', 'running', 'scoring', 'complete', 'partial', 'failed', 'faulted', 'cancelled']):
            raise ValueError("must be one of enum values ('queued', 'planning', 'running', 'scoring', 'complete', 'partial', 'failed', 'faulted', 'cancelled')")
        return value

    @field_validator('visibility')
    def visibility_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['private', 'org', 'public']):
            raise ValueError("must be one of enum values ('private', 'org', 'public')")
        return value

    @field_validator('kind')
    def kind_validate_enum(cls, value):
        """Validates the enum"""
        if value not in set(['benchmark', 'kernel-perf', 'training', 'ablation', 'policy-eval']):
            raise ValueError("must be one of enum values ('benchmark', 'kernel-perf', 'training', 'ablation', 'policy-eval')")
        return value

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of ResearchExperiment from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        * OpenAPI `readOnly` fields are excluded.
        """
        excluded_fields: Set[str] = set([
            "canonical",
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of ResearchExperiment from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "project": obj.get("project"),
            "id": obj.get("id"),
            "revision": obj.get("revision") if obj.get("revision") is not None else 'original',
            "status": obj.get("status") if obj.get("status") is not None else 'complete',
            "canonical": obj.get("canonical"),
            "visibility": obj.get("visibility") if obj.get("visibility") is not None else 'private',
            "trainable": obj.get("trainable") if obj.get("trainable") is not None else False,
            "publishable": obj.get("publishable") if obj.get("publishable") is not None else False,
            "kind": obj.get("kind"),
            "subject": obj.get("subject"),
            "task": obj.get("task"),
            "metric": obj.get("metric"),
            "value": obj.get("value"),
            "n": obj.get("n"),
            "n_total": obj.get("n_total"),
            "cost_usd": obj.get("cost_usd"),
            "meta": obj.get("meta"),
            "git_sha": obj.get("git_sha"),
            "git_branch": obj.get("git_branch"),
            "git_dirty": obj.get("git_dirty"),
            "lib_versions": obj.get("lib_versions"),
            "ts": obj.get("ts"),
            "endpoint": obj.get("endpoint")
        })
        return _obj


