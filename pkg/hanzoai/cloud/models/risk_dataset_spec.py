# coding: utf-8

"""
    Hanzo Cloud API

    Composed from each subsystem's own projection of its router, in the fleet's mount order — every operation below is a route the subsystem that publishes it registered. Tagged by product: the first path segment after /v1/.

    The version of the OpenAPI document: v1
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictInt, StrictStr
from typing import Any, ClassVar, Dict, List, Optional
from typing import Optional, Set
from typing_extensions import Self

class RiskDatasetSpec(BaseModel):
    """
    RiskDatasetSpec
    """ # noqa: E501
    cuts: Optional[List[StrictStr]] = Field(default=None, description="Cuts are the two RFC 3339 instants dividing train | val | test. Omit them to take 70% and 85% of the window by time. Splitting is TEMPORAL and then grouped by subject — a random split puts one device on both sides of the line and the model memorises the entity instead of the behaviour.")
    dims: Optional[List[StrictStr]] = Field(default=None, description="Dims are the coordinates to carry, by published name. Empty takes the whole surface. They are stored in the plane's own order, never the order given, so two requests naming the same dims produce identical rows.")
    var_from: Optional[StrictStr] = Field(default=None, description="From and To bound the event window, half-open, RFC 3339. The window may not be longer than the source's own retention: past that, its older half is already gone and the dataset would silently be shorter than it says.", alias="from")
    horizon: Optional[StrictInt] = Field(default=None, description="Horizon is how many days a row must have aged before it may be admitted. It is what keeps a fact that was not yet knowable at scoring time out of a training set: a chargeback lands 30 to 120 days after the transaction it condemns, so 120 for the payment lane and 14 for signup abuse. Zero admits the whole window and is honest only where the outcome is immediate.")
    kind: Optional[StrictStr] = Field(default=None, description="Kind narrows to one subject kind — person, session or account. Empty takes every kind.")
    name: Optional[StrictStr] = Field(default=None, description="Name identifies the dataset across its versions: lower-case letters, digits and hyphens, starting with a letter.")
    rows: Optional[StrictInt] = Field(default=None, description="Rows caps the materialisation. Zero takes the plane's own bound.")
    seed: Optional[StrictStr] = Field(default=None, description="Seed decides WHICH subjects are admitted when the window holds more rows than the cap allows. It is recorded on the version, so a capped dataset is reproducible rather than being whichever rows the store returned first. Omit it to seed from the dataset's name.")
    to: Optional[StrictStr] = None
    __properties: ClassVar[List[str]] = ["cuts", "dims", "from", "horizon", "kind", "name", "rows", "seed", "to"]

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of RiskDatasetSpec from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of RiskDatasetSpec from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "cuts": obj.get("cuts"),
            "dims": obj.get("dims"),
            "from": obj.get("from"),
            "horizon": obj.get("horizon"),
            "kind": obj.get("kind"),
            "name": obj.get("name"),
            "rows": obj.get("rows"),
            "seed": obj.get("seed"),
            "to": obj.get("to")
        })
        return _obj


