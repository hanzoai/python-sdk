# coding: utf-8

"""
    Hanzo Cloud API

    Composed from each subsystem's own projection of its router, in the fleet's mount order — every operation below is a route the subsystem that publishes it registered. Tagged by product: the first path segment after /v1/.

    The version of the OpenAPI document: v1
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictInt, StrictStr
from typing import Any, ClassVar, Dict, List, Optional
from typing import Optional, Set
from typing_extensions import Self

class ReferenceSource(BaseModel):
    """
    ReferenceSource
    """ # noqa: E501
    as_of: Optional[StrictStr] = Field(default=None, description="AsOf is when this publisher was current, RFC 3339.", alias="asOf")
    basis: Optional[StrictStr] = Field(default=None, description="Basis is the KIND of permission this publisher's data reaches you under: licence (an explicit grant), registry (the registry of record publishing for anyone to consult), operator (an operator's own machine-readable statement about its own network, published for third parties to filter by — not a licence, and not claimed as one), own (computed here), or none (nothing reaches you: the membership is held by the component that screens against it). It is on the wire so the licence position is an audit you can run.")
    keys: Optional[StrictInt] = Field(default=None, description="Keys is how many members this publisher contributed.")
    origin: Optional[StrictStr] = Field(default=None, description="Origin is exactly where it was taken from, so it can be taken again.")
    refusal: Optional[StrictStr] = Field(default=None, description="Refusal is why this publisher's last take failed, if it did. The set keeps its previous version of this source and ages out visibly rather than silently shrinking.")
    source: Optional[StrictStr] = Field(default=None, description="Source is the publisher.")
    terms: Optional[StrictStr] = Field(default=None, description="Terms is the CITATION that basis points at — the licence identifier, the registry, or the operator publication. A source with no stated terms is not in the catalog.")
    version: Optional[StrictStr] = Field(default=None, description="Version is the content digest of what this publisher last supplied. Two refreshes that agree on it took the same data.")
    __properties: ClassVar[List[str]] = ["asOf", "basis", "keys", "origin", "refusal", "source", "terms", "version"]

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of ReferenceSource from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of ReferenceSource from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "asOf": obj.get("asOf"),
            "basis": obj.get("basis"),
            "keys": obj.get("keys"),
            "origin": obj.get("origin"),
            "refusal": obj.get("refusal"),
            "source": obj.get("source"),
            "terms": obj.get("terms"),
            "version": obj.get("version")
        })
        return _obj


