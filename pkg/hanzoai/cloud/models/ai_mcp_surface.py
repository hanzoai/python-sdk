# coding: utf-8

"""
    Hanzo Cloud API

    Composed from each subsystem's own projection of its router, in the fleet's mount order — every operation below is a route the subsystem that publishes it registered. Tagged by product: the first path segment after /v1/.

    The version of the OpenAPI document: v1
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictInt, StrictStr
from typing import Any, ClassVar, Dict, List, Optional
from hanzoai.cloud.models.ai_mcp_app import AiMCPApp
from typing import Optional, Set
from typing_extensions import Self

class AiMCPSurface(BaseModel):
    """
    AiMCPSurface
    """ # noqa: E501
    apps: Optional[List[AiMCPApp]] = Field(default=None, description="Apps is one row per subsystem this deployment composes, in manifest order.")
    names: Optional[List[StrictStr]] = Field(default=None, description="Names are this process's own tool names, present only when the query asked for them.")
    tools: Optional[StrictInt] = Field(default=None, description="Tools is how many tools THIS PROCESS's door carries: its own typed-op registry, projected. It is the only number a subsystem can state honestly — what the FLEET's door carries is a question only the host can ask, and it asks it by asking every subsystem (POST /v1/mcp, tools/list).")
    __properties: ClassVar[List[str]] = ["apps", "names", "tools"]

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of AiMCPSurface from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # override the default output from pydantic by calling `to_dict()` of each item in apps (list)
        _items = []
        if self.apps:
            for _item_apps in self.apps:
                if _item_apps:
                    _items.append(_item_apps.to_dict())
            _dict['apps'] = _items
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of AiMCPSurface from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "apps": [AiMCPApp.from_dict(_item) for _item in obj["apps"]] if obj.get("apps") is not None else None,
            "names": obj.get("names"),
            "tools": obj.get("tools")
        })
        return _obj


