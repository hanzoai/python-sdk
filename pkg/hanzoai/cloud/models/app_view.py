# coding: utf-8

"""
    Hanzo Cloud API

    The Hanzo Cloud API as a customer calls it: every operation under /v1/ except the operator's admin product, relay routes, legacy spellings and capabilities still reached by flag. Tagged by product: the first path segment after /v1/.

    The version of the OpenAPI document: v1
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictInt, StrictStr
from typing import Any, ClassVar, Dict, List, Optional
from hanzoai.cloud.models.env_var_json import EnvVarJSON
from hanzoai.cloud.models.git_source import GitSource
from hanzoai.cloud.models.image_view import ImageView
from typing import Optional, Set
from typing_extensions import Self

class AppView(BaseModel):
    """
    AppView
    """ # noqa: E501
    build_type: Optional[StrictStr] = Field(default=None, description="BuildType is how a git app builds: `pack`, the zero-config default that detects the project, or `dockerfile`. An image app carries `image`, which means it never builds.", alias="buildType")
    created_at: Optional[StrictInt] = Field(default=None, description="CreatedAt is when the app was created, unix seconds.", alias="createdAt")
    current_deployment_id: Optional[StrictStr] = Field(default=None, description="CurrentDeploymentID is the deployment that is live — the pointer a deploy advances monotonically by version, so it never regresses to an older one. Empty until the first deploy reaches the cluster.", alias="currentDeploymentId")
    description: Optional[StrictStr] = Field(default=None, description="Description is free text about what the app is. Nothing derives from it.")
    dockerfile: Optional[StrictStr] = Field(default=None, description="Dockerfile is the path inside the repo to build from, for buildType `dockerfile`. The build path keys off its presence, and it is validated at create against the same allowlist the privileged build enforces.")
    domains: Optional[List[StrictStr]] = Field(default=None, description="Domains are the ingress hosts rendered into the app's CR, its own `<slug>.<org>.<sites host>` first. That one is seeded at create and cannot be removed; a custom host joins only after add-domain and DNS verification.")
    env: Optional[List[EnvVarJSON]] = Field(default=None, description="Env is the app's environment variables, with every SECRET value masked to \"\" — the plaintext is in KMS and this surface never echoes it. That masking is why an empty secret value means \"keep what is sealed\" when posted back.")
    environment: Optional[StrictStr] = Field(default=None, description="Environment is the deploy target this app names, `production` when none was given. It is a LABEL: /v1/platform/environments derives the environment list from the apps that name one, so an environment exists as long as an app points at it and no route creates or deletes one.")
    health: Optional[StrictStr] = Field(default=None, description="Health rolls ready-vs-desired replicas up to a colour: green (all ready), yellow (some ready, or deliberately scaled to zero), red (none), or \"\" when the cluster reports no replica counts at all — unknown, never a guessed green.")
    id: Optional[StrictStr] = Field(default=None, description="ID is the server-minted application id (`app_…`). Routes address an app by project and slug; this is the key its deployments and builds carry.")
    image: Optional[ImageView] = Field(default=None, description="Image is the image a source `image` app runs. For a git app only the tag is filled, stamped by the deploy that went live; the built ref is on the deployment.")
    name: Optional[StrictStr] = Field(default=None, description="Name is the display name. It is not an address — the slug is.")
    namespace: Optional[StrictStr] = Field(default=None, description="Namespace is where the app's cluster objects live, `tenant-<org>`. It is derived from the validated org and is never accepted from a request.")
    org: Optional[StrictStr] = Field(default=None, description="Org is the tenant that owns the app. It comes from the validated identity, never from the request, and it is the boundary every route is scoped to.")
    phase: Optional[StrictStr] = Field(default=None, description="Phase is the operator's own `status.phase` for the app's Service CR, read from the cluster on this request. Empty when there is no CR yet or the cluster could not be read.")
    port: Optional[StrictInt] = Field(default=None, description="Port is the container port traffic is sent to. 8080 when the create asked for none, or for one outside 1–65535.")
    project_id: Optional[StrictStr] = Field(default=None, description="ProjectID is the IAM project the app lives under, and it is that project's NAME — the (org,name) key IAM identifies it by, which is also what the `:project` path segment carries. There is no platform-minted project id.", alias="projectId")
    replicas: Optional[StrictInt] = Field(default=None, description="Replicas is how many copies the CR declares. It is CLAMPED to the deployment's ceiling rather than refused, so it can be below what was asked.")
    repo: Optional[GitSource] = Field(default=None, description="Repo is the git origin a source `git` app builds from, and the repo+branch a landed push has to match to build it.")
    secret_sync: Optional[StrictStr] = Field(default=None, description="SecretSync is how far the app's secret env has got into the cluster: \"\"|pending|syncing|ready|failed (secrets.go). It is best-effort and never fails a deploy, so `pending` is ordinary right after one.", alias="secretSync")
    secret_sync_detail: Optional[StrictStr] = Field(default=None, description="SecretSyncDetail is the honest reason when the sync is not ready — a missing CRD, an RBAC grant, a per-tenant credential. Empty when it is.", alias="secretSyncDetail")
    slug: Optional[StrictStr] = Field(default=None, description="Slug is the app's identity in the cluster: the operator CR's name, the first label of its default host, and the `:app` path segment. Unique per project.")
    source: Optional[StrictStr] = Field(default=None, description="Source is what the app deploys FROM: `git`, which builds Repo, or `image`, which runs Image as it is. It decides whether a deploy builds at all.")
    status: Optional[StrictStr] = Field(default=None, description="Status is the lifecycle THIS store records: draft (created, nothing in the cluster yet), building, deploying, live, stopped or error. What the cluster itself says is Phase and Health.")
    storage_gb: Optional[StrictInt] = Field(default=None, description="StorageGB is the persistent volume size in GiB. Absent means stateless — no volume at all — and it is clamped like Replicas.", alias="storageGb")
    updated_at: Optional[StrictInt] = Field(default=None, description="UpdatedAt is when it last changed, unix seconds. Every lifecycle transition moves it, so it tracks deploys as well as edits.", alias="updatedAt")
    __properties: ClassVar[List[str]] = ["buildType", "createdAt", "currentDeploymentId", "description", "dockerfile", "domains", "env", "environment", "health", "id", "image", "name", "namespace", "org", "phase", "port", "projectId", "replicas", "repo", "secretSync", "secretSyncDetail", "slug", "source", "status", "storageGb", "updatedAt"]

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of AppView from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # override the default output from pydantic by calling `to_dict()` of each item in env (list)
        _items = []
        if self.env:
            for _item_env in self.env:
                if _item_env:
                    _items.append(_item_env.to_dict())
            _dict['env'] = _items
        # override the default output from pydantic by calling `to_dict()` of image
        if self.image:
            _dict['image'] = self.image.to_dict()
        # override the default output from pydantic by calling `to_dict()` of repo
        if self.repo:
            _dict['repo'] = self.repo.to_dict()
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of AppView from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "buildType": obj.get("buildType"),
            "createdAt": obj.get("createdAt"),
            "currentDeploymentId": obj.get("currentDeploymentId"),
            "description": obj.get("description"),
            "dockerfile": obj.get("dockerfile"),
            "domains": obj.get("domains"),
            "env": [EnvVarJSON.from_dict(_item) for _item in obj["env"]] if obj.get("env") is not None else None,
            "environment": obj.get("environment"),
            "health": obj.get("health"),
            "id": obj.get("id"),
            "image": ImageView.from_dict(obj["image"]) if obj.get("image") is not None else None,
            "name": obj.get("name"),
            "namespace": obj.get("namespace"),
            "org": obj.get("org"),
            "phase": obj.get("phase"),
            "port": obj.get("port"),
            "projectId": obj.get("projectId"),
            "replicas": obj.get("replicas"),
            "repo": GitSource.from_dict(obj["repo"]) if obj.get("repo") is not None else None,
            "secretSync": obj.get("secretSync"),
            "secretSyncDetail": obj.get("secretSyncDetail"),
            "slug": obj.get("slug"),
            "source": obj.get("source"),
            "status": obj.get("status"),
            "storageGb": obj.get("storageGb"),
            "updatedAt": obj.get("updatedAt")
        })
        return _obj


