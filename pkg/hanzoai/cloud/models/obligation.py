# coding: utf-8

"""
    Hanzo Cloud API

    The Hanzo Cloud API as a customer calls it: every operation under /v1/ except the operator's admin product, relay doors, legacy spellings and capabilities still reached by flag. Tagged by product: the first path segment after /v1/.

    The version of the OpenAPI document: v1
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictBool, StrictInt, StrictStr
from typing import Any, ClassVar, Dict, List, Optional
from typing import Optional, Set
from typing_extensions import Self

class Obligation(BaseModel):
    """
    Obligation
    """ # noqa: E501
    amount_cents: Optional[StrictInt] = Field(default=None, description="AmountCents is what it costs each period.", alias="amountCents")
    as_of: Optional[StrictStr] = Field(default=None, description="AsOf is when that amount was last checked against its source, RFC 3339 date.", alias="asOf")
    code: Optional[StrictStr] = Field(default=None, description="Code names the obligation so a caller can branch without reading prose.")
    every: Optional[StrictStr] = Field(default=None, description="Every is how often it falls due — \"yearly\" for every obligation known here.")
    label: Optional[StrictStr] = Field(default=None, description="Label is what the payer sees.")
    minimum: Optional[StrictBool] = Field(default=None, description="Minimum marks a floor rather than a fixed price — a franchise tax that scales with shares or assets is quoted at its minimum, and an entity past the threshold owes more. Saying so is the difference between a quote and a number someone later disputes.")
    pass_through: Optional[StrictBool] = Field(default=None, description="PassThrough marks money we collect and remit to the state rather than keep.", alias="passThrough")
    source: Optional[StrictStr] = Field(default=None, description="Source names the authority that publishes a pass-through amount, so the figure can be checked without first working out who would know.")
    stale: Optional[StrictBool] = Field(default=None, description="Stale reports that AsOf is older than the review window. It tells; it does not block.")
    __properties: ClassVar[List[str]] = ["amountCents", "asOf", "code", "every", "label", "minimum", "passThrough", "source", "stale"]

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of Obligation from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of Obligation from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "amountCents": obj.get("amountCents"),
            "asOf": obj.get("asOf"),
            "code": obj.get("code"),
            "every": obj.get("every"),
            "label": obj.get("label"),
            "minimum": obj.get("minimum"),
            "passThrough": obj.get("passThrough"),
            "source": obj.get("source"),
            "stale": obj.get("stale")
        })
        return _obj


