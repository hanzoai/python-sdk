# coding: utf-8

"""
    Hanzo Cloud — Unified API (V8 · Open Edition)

    The single unified OpenAPI surface for ALL Hanzo services, aggregated from the per-service specs. Every route is https://api.hanzo.ai/v1/<service>/*. Grouped by the canonical domains in capabilities.yaml (the ONE registry). cloud/openapi.yaml is hanzoai/cloud's own woven document, copied verbatim by sync.py, and it wins wherever it and a hand-written spec describe the same route. Regenerated by merge.py — edit the per-service <svc>/openapi.yaml + capabilities.yaml, not this file.

    The version of the OpenAPI document: 8.0.0
    Contact: support@hanzo.ai
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictInt
from typing import Any, ClassVar, Dict, List, Optional
from hanzoai.cloud.models.cloud_day_count import CloudDayCount
from typing import Optional, Set
from typing_extensions import Self

class CloudSearchStats(BaseModel):
    """
    CloudSearchStats
    """ # noqa: E501
    searches_per_day: Optional[List[CloudDayCount]] = Field(default=None, description="SearchesPerDay is always empty, for the same reason as totalSearches.", alias="searchesPerDay")
    total_documents: Optional[StrictInt] = Field(default=None, description="TotalDocuments is the sum of every index's document count.", alias="totalDocuments")
    total_searches: Optional[StrictInt] = Field(default=None, description="TotalSearches is always 0: Meilisearch keeps no query-history counter, so this surface reports the honest zero rather than an estimate.", alias="totalSearches")
    total_sessions: Optional[StrictInt] = Field(default=None, description="TotalSessions is always 0, for the same reason as totalSearches.", alias="totalSessions")
    __properties: ClassVar[List[str]] = ["searchesPerDay", "totalDocuments", "totalSearches", "totalSessions"]

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of CloudSearchStats from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # override the default output from pydantic by calling `to_dict()` of each item in searches_per_day (list)
        _items = []
        if self.searches_per_day:
            for _item_searches_per_day in self.searches_per_day:
                if _item_searches_per_day:
                    _items.append(_item_searches_per_day.to_dict())
            _dict['searchesPerDay'] = _items
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of CloudSearchStats from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "searchesPerDay": [CloudDayCount.from_dict(_item) for _item in obj["searchesPerDay"]] if obj.get("searchesPerDay") is not None else None,
            "totalDocuments": obj.get("totalDocuments"),
            "totalSearches": obj.get("totalSearches"),
            "totalSessions": obj.get("totalSessions")
        })
        return _obj


