# coding: utf-8

"""
    Hanzo Cloud API

    The Hanzo Cloud API as a customer calls it: every operation under /v1/ except the operator's admin product, relay routes, legacy spellings and capabilities still reached by flag. Tagged by product: the first path segment after /v1/.

    The version of the OpenAPI document: v1
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictInt, StrictStr
from typing import Any, ClassVar, Dict, List, Optional
from typing import Optional, Set
from typing_extensions import Self

class ProjectView(BaseModel):
    """
    ProjectView
    """ # noqa: E501
    applications: Optional[StrictInt] = Field(default=None, description="Applications is how many platform apps this org has under the project, counted per request. It is the one fact IAM cannot answer about a project.")
    created_at: Optional[StrictInt] = Field(default=None, description="CreatedAt is IAM's creation time as unix seconds. 0 when IAM's timestamp is absent or unparseable — never a fabricated time.", alias="createdAt")
    description: Optional[StrictStr] = Field(default=None, description="Description is IAM's free text about the project. Nothing derives from it.")
    name: Optional[StrictStr] = Field(default=None, description="Name is IAM's display name, falling back to the slug when the project has none, so this is never empty.")
    org: Optional[StrictStr] = Field(default=None, description="Org is the project's IAM owner, and the tenant every app under it deploys into. It comes from the validated identity, never from the request.")
    slug: Optional[StrictStr] = Field(default=None, description="Slug is the project's IAM name — half of the (org,name) identity, the `:project` path segment, and the scope key an app is filed under. It is the project's address; Name is not.")
    __properties: ClassVar[List[str]] = ["applications", "createdAt", "description", "name", "org", "slug"]

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of ProjectView from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of ProjectView from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "applications": obj.get("applications"),
            "createdAt": obj.get("createdAt"),
            "description": obj.get("description"),
            "name": obj.get("name"),
            "org": obj.get("org"),
            "slug": obj.get("slug")
        })
        return _obj


