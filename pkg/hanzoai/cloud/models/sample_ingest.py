# coding: utf-8

"""
    Hanzo Cloud API

    The Hanzo Cloud API as a customer calls it: every operation under /v1/ except the operator's admin product, relay routes, legacy spellings and capabilities still reached by flag. Tagged by product: the first path segment after /v1/.

    The version of the OpenAPI document: v1
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictFloat, StrictInt, StrictStr
from typing import Any, ClassVar, Dict, List, Optional, Union
from typing import Optional, Set
from typing_extensions import Self

class SampleIngest(BaseModel):
    """
    SampleIngest
    """ # noqa: E501
    gpu_model: Optional[StrictStr] = Field(default=None, description="GPUModel names the representative accelerator (\"GB10\"); GPUs carries how many. A heterogeneous host names its first card rather than inventing a summary.", alias="gpuModel")
    gpu_util: Optional[Union[StrictFloat, StrictInt]] = Field(default=None, description="GPUUtil is accelerator utilization as a fraction 0..1; the warehouse clamps anything outside that.", alias="gpuUtil")
    gpus: Optional[StrictInt] = Field(default=None, description="GPUs is how many accelerators this reading covers.")
    host: Optional[StrictStr] = Field(default=None, description="Host is the node's hostname, for display.")
    mem_free: Optional[StrictInt] = Field(default=None, description="MemFree is host memory still available, in BYTES.", alias="memFree")
    mem_used: Optional[StrictInt] = Field(default=None, description="MemUsed is host memory in use, in BYTES.", alias="memUsed")
    unit: Optional[StrictStr] = Field(default=None, description="Unit is the reporting node's own id — the same id it registered under, and the key the board joins this series onto. Required.")
    __properties: ClassVar[List[str]] = ["gpuModel", "gpuUtil", "gpus", "host", "memFree", "memUsed", "unit"]

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of SampleIngest from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of SampleIngest from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "gpuModel": obj.get("gpuModel"),
            "gpuUtil": obj.get("gpuUtil"),
            "gpus": obj.get("gpus"),
            "host": obj.get("host"),
            "memFree": obj.get("memFree"),
            "memUsed": obj.get("memUsed"),
            "unit": obj.get("unit")
        })
        return _obj


