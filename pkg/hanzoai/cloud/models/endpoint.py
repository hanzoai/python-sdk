# coding: utf-8

"""
    Hanzo Cloud API

    The Hanzo Cloud API as a customer calls it: every operation under /v1/ except the operator's admin product, relay routes, legacy spellings and capabilities still reached by flag. Tagged by product: the first path segment after /v1/.

    The version of the OpenAPI document: v1
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictInt, StrictStr
from typing import Any, ClassVar, Dict, List, Optional
from typing import Optional, Set
from typing_extensions import Self

class Endpoint(BaseModel):
    """
    Endpoint
    """ # noqa: E501
    created: Optional[StrictStr] = Field(default=None, description="CreatedAt is when the endpoint was registered, RFC3339 in UTC — stored in that spelling because it sorts as a string.")
    deliveries7d: Optional[StrictInt] = Field(default=None, description="Deliveries7d is how many deliveries SETTLED in the trailing 7 days — the attempts that ended ok or failed, so a delivery still retrying is in neither counter yet. It is counted from the log at read time rather than stored, and it is filled only on a list or a get; a create answers 0 because there is no history, which is why it is never omitted.")
    description: Optional[StrictStr] = Field(default=None, description="Description is the operator's own label for the endpoint. Never sent anywhere.")
    events: Optional[List[StrictStr]] = Field(default=None, description="Events are the subject patterns this endpoint subscribes to (\"commerce.order.>\"). An EMPTY list means every event, not none.")
    failures7d: Optional[StrictInt] = Field(default=None, description="Failures7d is how many of those settled as failed — the subscriber never accepted it and no further attempt will be made. It is the numerator to Deliveries7d, over the same window.")
    id: Optional[StrictStr] = Field(default=None, description="ID is the endpoint's handle, server-minted and stable for its life. It is what every other route here addresses.")
    org: Optional[StrictStr] = Field(default=None, description="Org is the tenant that owns the endpoint, taken from the validated principal rather than from any request field.")
    secret: Optional[StrictStr] = Field(default=None, description="Secret is the HMAC-SHA256 signing key a subscriber recomputes the signature with. It is returned exactly ONCE, on create: a later read of the endpoint omits it, so a lost secret is replaced rather than recovered.")
    status: Optional[StrictStr] = Field(default=None, description="Status is \"active\" or \"disabled\" — nothing else is accepted. A disabled endpoint keeps its subscription and receives no deliveries, except a manual test send, which goes out anyway.")
    updated: Optional[StrictStr] = Field(default=None, description="UpdatedAt is when its url, events, status or description last changed.")
    url: Optional[StrictStr] = Field(default=None, description="URL is where the POST goes. Changing it is the one edit that redirects an org's events, which is why it is never bindable from a query string.")
    __properties: ClassVar[List[str]] = ["created", "deliveries7d", "description", "events", "failures7d", "id", "org", "secret", "status", "updated", "url"]

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of Endpoint from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of Endpoint from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "created": obj.get("created"),
            "deliveries7d": obj.get("deliveries7d"),
            "description": obj.get("description"),
            "events": obj.get("events"),
            "failures7d": obj.get("failures7d"),
            "id": obj.get("id"),
            "org": obj.get("org"),
            "secret": obj.get("secret"),
            "status": obj.get("status"),
            "updated": obj.get("updated"),
            "url": obj.get("url")
        })
        return _obj


