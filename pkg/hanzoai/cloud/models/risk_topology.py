# coding: utf-8

"""
    Hanzo Cloud API

    The Hanzo Cloud API as a customer calls it: every operation under /v1/ except the operator's admin product, relay routes, legacy spellings and capabilities still reached by flag. Tagged by product: the first path segment after /v1/.

    The version of the OpenAPI document: v1
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictFloat, StrictInt, StrictStr
from typing import Any, ClassVar, Dict, List, Optional, Union
from typing import Optional, Set
from typing_extensions import Self

class RiskTopology(BaseModel):
    """
    RiskTopology
    """ # noqa: E501
    blend: Optional[Union[StrictFloat, StrictInt]] = Field(default=None, description="Blend is how much of a closing window folds into the reference: 1 replaces it outright, less makes the reference expensive to move.")
    depth: Optional[StrictInt] = Field(default=None, description="Depth is how deep each tree is. With Trees it sets how finely the space is partitioned, and therefore how much history it takes to fill.")
    family: Optional[StrictStr] = Field(default=None, description="Family is the KIND of model this candidate is: `halfspace` is an ensemble of half-space trees whose masses are counters, and it is the family this search grid ranks. The parameters below are that family's own — a family that does not partition space with trees has different ones — so read them against this.")
    review: Optional[Union[StrictFloat, StrictInt]] = Field(default=None, description="Review is the appetite this shape was tried at.")
    trees: Optional[StrictInt] = Field(default=None, description="Trees is how many half-space trees the ensemble holds.")
    window: Optional[StrictInt] = Field(default=None, description="Window is how many events make one reference window.")
    __properties: ClassVar[List[str]] = ["blend", "depth", "family", "review", "trees", "window"]

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of RiskTopology from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of RiskTopology from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "blend": obj.get("blend"),
            "depth": obj.get("depth"),
            "family": obj.get("family"),
            "review": obj.get("review"),
            "trees": obj.get("trees"),
            "window": obj.get("window")
        })
        return _obj


