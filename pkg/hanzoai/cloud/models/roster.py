# coding: utf-8

"""
    Hanzo Cloud API

    The Hanzo Cloud API as a customer calls it: every operation under /v1/ except the operator's admin product, relay routes, legacy spellings and capabilities still reached by flag. Tagged by product: the first path segment after /v1/.

    The version of the OpenAPI document: v1
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field
from typing import Any, ClassVar, Dict, List, Optional
from hanzoai.cloud.models.market import Market
from hanzoai.cloud.models.reach import Reach
from typing import Optional, Set
from typing_extensions import Self

class Roster(BaseModel):
    """
    Roster
    """ # noqa: E501
    chains: Optional[List[Market]] = Field(default=None, description="Chains is `[]` where the registry answered and named none, and `null` where it did not answer — never absent, because a missing key and an empty list read alike and only one of them means \"there are none\". Each row carries its OWN reach for its figures, so a chain whose indexer is down is one row saying so.")
    reach: Optional[Reach] = Field(default=None, description="Reach is how far the read of the REGISTRY got. It governs the list: a registry that did not answer yields no rows, and the reason it did not is here rather than in an empty array a caller would read as \"no chains exist\".")
    __properties: ClassVar[List[str]] = ["chains", "reach"]

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of Roster from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # override the default output from pydantic by calling `to_dict()` of each item in chains (list)
        _items = []
        if self.chains:
            for _item_chains in self.chains:
                if _item_chains:
                    _items.append(_item_chains.to_dict())
            _dict['chains'] = _items
        # override the default output from pydantic by calling `to_dict()` of reach
        if self.reach:
            _dict['reach'] = self.reach.to_dict()
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of Roster from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "chains": [Market.from_dict(_item) for _item in obj["chains"]] if obj.get("chains") is not None else None,
            "reach": Reach.from_dict(obj["reach"]) if obj.get("reach") is not None else None
        })
        return _obj


