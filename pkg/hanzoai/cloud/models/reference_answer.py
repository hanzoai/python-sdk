# coding: utf-8

"""
    Hanzo Cloud API

    The Hanzo Cloud API as a customer calls it: every operation under /v1/ except the operator's admin product, relay routes, legacy spellings and capabilities still reached by flag. Tagged by product: the first path segment after /v1/.

    The version of the OpenAPI document: v1
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictBool, StrictFloat, StrictInt, StrictStr
from typing import Any, ClassVar, Dict, List, Optional, Union
from typing import Optional, Set
from typing_extensions import Self

class ReferenceAnswer(BaseModel):
    """
    ReferenceAnswer
    """ # noqa: E501
    age: Optional[StrictStr] = Field(default=None, description="Age is how old that is, as a duration.")
    as_of: Optional[StrictStr] = Field(default=None, description="AsOf is when the oldest contributing publisher was current, RFC 3339.", alias="asOf")
    var_from: Optional[StrictStr] = Field(default=None, description="From is override or baseline — which plane answered.", alias="from")
    hit: Optional[StrictBool] = Field(default=None, description="Hit is whether the key is a member. It is meaningful ONLY when Refusal is empty: false with a refusal means the set could not be consulted, which is not the same as the key being clean.")
    key: Optional[StrictStr] = Field(default=None, description="Key is the key as asked.")
    matched: Optional[StrictStr] = Field(default=None, description="Matched is the member that covered the key, which for a domain or a network is the enclosing entry rather than the key itself.")
    refusal: Optional[StrictStr] = Field(default=None, description="Refusal is why the set could not be consulted, when it could not: never loaded, held elsewhere, or a source we hold no licence for. Non-empty means Hit must not be read as an answer.")
    score: Optional[Union[StrictFloat, StrictInt]] = Field(default=None, description="Score is the published risk weight where the source expresses one.")
    set: Optional[StrictStr] = Field(default=None, description="Set is the set consulted.")
    stale: Optional[StrictBool] = Field(default=None, description="Stale is whether the set is past its freshness bound. A stale set still answers — yesterday's list beats none — and this is how a decision knows it leaned on one.")
    value: Optional[Dict[str, StrictStr]] = Field(default=None, description="Value is what the publisher says about the member — class, operator, scheme, region.")
    verdict: Optional[StrictStr] = Field(default=None, description="Verdict is the tenant's own allow or deny, present only for an override. The baseline never carries one: it states facts and leaves the decision to the caller's policy.")
    version: Optional[StrictStr] = Field(default=None, description="Version is the exact baseline version consulted, composed of each contributing publisher and its content digest. It is what makes a decision reproducible: an auditor takes this string and knows precisely what was consulted.")
    __properties: ClassVar[List[str]] = ["age", "asOf", "from", "hit", "key", "matched", "refusal", "score", "set", "stale", "value", "verdict", "version"]

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of ReferenceAnswer from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of ReferenceAnswer from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "age": obj.get("age"),
            "asOf": obj.get("asOf"),
            "from": obj.get("from"),
            "hit": obj.get("hit"),
            "key": obj.get("key"),
            "matched": obj.get("matched"),
            "refusal": obj.get("refusal"),
            "score": obj.get("score"),
            "set": obj.get("set"),
            "stale": obj.get("stale"),
            "value": obj.get("value"),
            "verdict": obj.get("verdict"),
            "version": obj.get("version")
        })
        return _obj


