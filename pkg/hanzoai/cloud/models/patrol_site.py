# coding: utf-8

"""
    Hanzo Cloud API

    The Hanzo Cloud API as a customer calls it: every operation under /v1/ except the operator's admin product, relay routes, legacy spellings and capabilities still reached by flag. Tagged by product: the first path segment after /v1/.

    The version of the OpenAPI document: v1
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictBool, StrictFloat, StrictInt, StrictStr
from typing import Any, ClassVar, Dict, List, Optional, Union
from hanzoai.cloud.models.patrol_zone import PatrolZone
from typing import Optional, Set
from typing_extensions import Self

class PatrolSite(BaseModel):
    """
    PatrolSite
    """ # noqa: E501
    account: Optional[StrictStr] = Field(default=None, description="Account is the alarm-panel account number the receiving centre reports.")
    cameras: Optional[StrictInt] = Field(default=None, description="Cameras is how many cameras are registered here.")
    category: Optional[StrictStr] = Field(default=None, description="Category is what kind of premises this is.")
    client: Optional[StrictStr] = Field(default=None, description="Client is the customer this site belongs to.")
    contact: Optional[StrictStr] = Field(default=None, description="Contact is the keyholder to reach.")
    eircode: Optional[StrictStr] = Field(default=None, description="Eircode is the postal code.")
    hazards: Optional[List[StrictStr]] = Field(default=None, description="Hazards is the standing warnings, present on a single-site read.")
    keys: Optional[StrictBool] = Field(default=None, description="Keys says whether the operation holds a key set for this site.")
    lat: Optional[Union[StrictFloat, StrictInt]] = Field(default=None, description="Lat is the site's latitude in degrees.")
    lon: Optional[Union[StrictFloat, StrictInt]] = Field(default=None, description="Lon is the site's longitude in degrees.")
    name: Optional[StrictStr] = Field(default=None, description="Name is the site's document name and the segment /v1/patrol/site/{name} addresses it by.")
    notes: Optional[StrictStr] = Field(default=None, description="Notes is the standing brief for the site.")
    ref: Optional[StrictInt] = Field(default=None, description="Ref is the site's number in the register.")
    risk: Optional[StrictInt] = Field(default=None, description="Risk is the assessed risk score.")
    sector: Optional[StrictStr] = Field(default=None, description="Sector is the patrol sector the site sits in.")
    sla: Optional[StrictInt] = Field(default=None, description="SLA is the contracted response time in minutes.")
    status: Optional[StrictStr] = Field(default=None, description="Status is ok, fault, alarm or offline.")
    tier: Optional[StrictStr] = Field(default=None, description="Tier is the contracted service tier.")
    town: Optional[StrictStr] = Field(default=None, description="Town is the town the site is in.")
    unit: Optional[StrictStr] = Field(default=None, description="Unit is the call sign of the unit that covers the site, or empty.")
    visits: Optional[StrictInt] = Field(default=None, description="Visits is how many patrol visits the contract calls for.")
    zones: Optional[List[PatrolZone]] = Field(default=None, description="Zones is the panel's detection zones, present on a single-site read.")
    __properties: ClassVar[List[str]] = ["account", "cameras", "category", "client", "contact", "eircode", "hazards", "keys", "lat", "lon", "name", "notes", "ref", "risk", "sector", "sla", "status", "tier", "town", "unit", "visits", "zones"]

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of PatrolSite from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # override the default output from pydantic by calling `to_dict()` of each item in zones (list)
        _items = []
        if self.zones:
            for _item_zones in self.zones:
                if _item_zones:
                    _items.append(_item_zones.to_dict())
            _dict['zones'] = _items
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of PatrolSite from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "account": obj.get("account"),
            "cameras": obj.get("cameras"),
            "category": obj.get("category"),
            "client": obj.get("client"),
            "contact": obj.get("contact"),
            "eircode": obj.get("eircode"),
            "hazards": obj.get("hazards"),
            "keys": obj.get("keys"),
            "lat": obj.get("lat"),
            "lon": obj.get("lon"),
            "name": obj.get("name"),
            "notes": obj.get("notes"),
            "ref": obj.get("ref"),
            "risk": obj.get("risk"),
            "sector": obj.get("sector"),
            "sla": obj.get("sla"),
            "status": obj.get("status"),
            "tier": obj.get("tier"),
            "town": obj.get("town"),
            "unit": obj.get("unit"),
            "visits": obj.get("visits"),
            "zones": [PatrolZone.from_dict(_item) for _item in obj["zones"]] if obj.get("zones") is not None else None
        })
        return _obj


