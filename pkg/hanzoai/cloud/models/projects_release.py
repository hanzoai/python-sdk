# coding: utf-8

"""
    Hanzo Cloud API

    The Hanzo Cloud API as a customer calls it: every operation under /v1/ except the operator's admin product, relay doors, legacy spellings and capabilities still reached by flag. Tagged by product: the first path segment after /v1/.

    The version of the OpenAPI document: v1
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictBool, StrictInt, StrictStr
from typing import Any, ClassVar, Dict, List, Optional
from typing import Optional, Set
from typing_extensions import Self

class ProjectsRelease(BaseModel):
    """
    ProjectsRelease
    """ # noqa: E501
    active: Optional[StrictBool] = Field(default=None, description="Active is whether this is the release the site is SERVING right now. Exactly one release of a site is active; the others are kept so they can be activated again, until retention reclaims them.")
    bytes: Optional[StrictInt] = Field(default=None, description="Bytes is their total size in bytes.")
    created_at: Optional[StrictInt] = Field(default=None, description="CreatedAt is when the release was cut, as Unix seconds — not when it was last activated.", alias="createdAt")
    objects: Optional[StrictInt] = Field(default=None, description="Objects is how many files the release holds.")
    release_id: Optional[StrictStr] = Field(default=None, description="ReleaseID is derived from a DIGEST of the release's own manifest, so identical content is the same release and a release can never be confused with another one. Activating an older id IS the rollback.", alias="releaseId")
    slug: Optional[StrictStr] = Field(default=None, description="Slug is the site this release belongs to.")
    source: Optional[StrictStr] = Field(default=None, description="Source is what the release was cut from — the build output or upload it was promoted out of.")
    url: Optional[StrictStr] = Field(default=None, description="URL is where the site serves. Present only on the ACTIVE release, since an inactive one is not answering anywhere.")
    __properties: ClassVar[List[str]] = ["active", "bytes", "createdAt", "objects", "releaseId", "slug", "source", "url"]

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of ProjectsRelease from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of ProjectsRelease from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "active": obj.get("active"),
            "bytes": obj.get("bytes"),
            "createdAt": obj.get("createdAt"),
            "objects": obj.get("objects"),
            "releaseId": obj.get("releaseId"),
            "slug": obj.get("slug"),
            "source": obj.get("source"),
            "url": obj.get("url")
        })
        return _obj


