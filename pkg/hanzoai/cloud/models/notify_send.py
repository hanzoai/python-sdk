# coding: utf-8

"""
    Hanzo Cloud API

    Composed from each subsystem's own projection of its router, in the fleet's mount order — every operation below is a route the subsystem that publishes it registered. Tagged by product: the first path segment after /v1/.

    The version of the OpenAPI document: v1
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictStr
from typing import Any, ClassVar, Dict, List, Optional
from typing import Optional, Set
from typing_extensions import Self

class NotifySend(BaseModel):
    """
    NotifySend
    """ # noqa: E501
    body: Optional[StrictStr] = Field(default=None, description="Body is the message text, sent verbatim when present — the no-template path.")
    channel: Optional[StrictStr] = Field(default=None, description="Channel selects the delivery channel, sms or email. The per-channel routes (/send/sms, /send/email) pin it, overriding whatever the body names; on the generic route it is required.")
    event: Optional[StrictStr] = Field(default=None, description="Event is the event name, which doubles as the template id when TemplateID is empty — the IAM OTP path sends event=iam.otp_sent and nothing else.")
    provider: Optional[StrictStr] = Field(default=None, description="Provider pins a provider service name (twilio, plivo, twilio_email, mail). Empty picks the one whose org credentials are actually configured in KMS.")
    subject: Optional[StrictStr] = Field(default=None, description="Subject is the message subject, carried on the email channel only.")
    sync: Optional[StrictStr] = Field(default=None, description="Sync must be exactly \"true\": delivery here is synchronous, and anything else answers 503 because the queue plane that would run an async dispatch is owned elsewhere. Over REST it rides as ?sync=true (the URL binds over the body); a by-name call states it in its arguments.")
    template_id: Optional[StrictStr] = Field(default=None, description="TemplateID selects a built-in template when Body is empty.")
    template_vars: Optional[Any] = None
    to: Optional[List[StrictStr]] = Field(default=None, description="To is the destination address per recipient — a phone number for sms, an email address for email. Several recipients fan out into one provider call each, and the response shape follows the count (see the items field).")
    __properties: ClassVar[List[str]] = ["body", "channel", "event", "provider", "subject", "sync", "template_id", "template_vars", "to"]

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of NotifySend from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # set to None if template_vars (nullable) is None
        # and model_fields_set contains the field
        if self.template_vars is None and "template_vars" in self.model_fields_set:
            _dict['template_vars'] = None

        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of NotifySend from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "body": obj.get("body"),
            "channel": obj.get("channel"),
            "event": obj.get("event"),
            "provider": obj.get("provider"),
            "subject": obj.get("subject"),
            "sync": obj.get("sync"),
            "template_id": obj.get("template_id"),
            "template_vars": obj.get("template_vars"),
            "to": obj.get("to")
        })
        return _obj


