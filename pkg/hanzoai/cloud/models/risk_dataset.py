# coding: utf-8

"""
    Hanzo Cloud API

    The Hanzo Cloud API as a customer calls it: every operation under /v1/ except the operator's admin product, relay doors, legacy spellings and capabilities still reached by flag. Tagged by product: the first path segment after /v1/.

    The version of the OpenAPI document: v1
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictBool, StrictInt, StrictStr
from typing import Any, ClassVar, Dict, List, Optional
from hanzoai.cloud.models.risk_dataset_spec import RiskDatasetSpec
from hanzoai.cloud.models.risk_split_counts import RiskSplitCounts
from typing import Optional, Set
from typing_extensions import Self

class RiskDataset(BaseModel):
    """
    RiskDataset
    """ # noqa: E501
    at: Optional[StrictStr] = Field(default=None, description="At is when this version last changed state, RFC 3339 UTC.")
    by: Optional[StrictStr] = Field(default=None, description="By is who moved it there: the validated user, or the org itself when the caller is a machine with no user behind it.")
    counts: Optional[RiskSplitCounts] = Field(default=None, description="Counts is how the rows fall across the splits.")
    digest: Optional[StrictStr] = Field(default=None, description="Digest fingerprints the SPEC and the ROWS together. Two materialisations of one spec agree on it or the plane says they do not.")
    name: Optional[StrictStr] = Field(default=None, description="Name identifies the dataset across all of its versions.")
    oversize: Optional[StrictInt] = Field(default=None, description="Oversize is how many of the window's subjects this version could NOT carry because their subject identity exceeds the plane's per-subject byte bound.  It is on the wire, not only in a log, because it is the one degradation a caller cannot otherwise detect: the rows that are here look complete, and a dataset silently missing a population is a model silently blind to it. Non-zero does not make a version invalid — it makes it a version whose coverage is STATED. Zero is the normal case and omits.")
    refusal: Optional[StrictStr] = Field(default=None, description="Refusal names why there are no bytes, when there are none.")
    running: Optional[StrictBool] = Field(default=None, description="Running is true while THIS process is materialising the version. A version that is `materializing` and not running was started by a process that is gone — two states the register cannot tell apart, because a register cannot know which processes are alive.")
    share: Optional[StrictInt] = Field(default=None, description="Share is the fraction of the window's subjects admitted, in thousandths. 1000 means the whole window fitted under the cap; anything less means the version is a reproducible sample and says by how much.")
    spec: Optional[RiskDatasetSpec] = Field(default=None, description="Spec is the bound query this version was built from, exactly as recorded.")
    status: Optional[StrictStr] = Field(default=None, description="Status is declared, materializing, ready or refused. Only `ready` has bytes, and `ready` is terminal: a published version is never rewritten.")
    truncated: Optional[StrictBool] = Field(default=None, description="Truncated is true when the row cap bound before the window ran out. The trailing subject is dropped whole when that happens, because half a subject on one side of a split is exactly the leak the grouping prevents.")
    version: Optional[StrictInt] = Field(default=None, description="Version is which version this is, from 1 and monotone within the dataset. A number is never reused — not even after a disposal, where the next declare continues the count — so \"signups v3\" means one thing forever, which is what makes a model's citation of it checkable.")
    __properties: ClassVar[List[str]] = ["at", "by", "counts", "digest", "name", "oversize", "refusal", "running", "share", "spec", "status", "truncated", "version"]

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of RiskDataset from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # override the default output from pydantic by calling `to_dict()` of counts
        if self.counts:
            _dict['counts'] = self.counts.to_dict()
        # override the default output from pydantic by calling `to_dict()` of spec
        if self.spec:
            _dict['spec'] = self.spec.to_dict()
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of RiskDataset from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "at": obj.get("at"),
            "by": obj.get("by"),
            "counts": RiskSplitCounts.from_dict(obj["counts"]) if obj.get("counts") is not None else None,
            "digest": obj.get("digest"),
            "name": obj.get("name"),
            "oversize": obj.get("oversize"),
            "refusal": obj.get("refusal"),
            "running": obj.get("running"),
            "share": obj.get("share"),
            "spec": RiskDatasetSpec.from_dict(obj["spec"]) if obj.get("spec") is not None else None,
            "status": obj.get("status"),
            "truncated": obj.get("truncated"),
            "version": obj.get("version")
        })
        return _obj


