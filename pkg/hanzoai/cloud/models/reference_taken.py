# coding: utf-8

"""
    Hanzo Cloud API

    Composed from each subsystem's own projection of its router, in the fleet's mount order — every operation below is a route the subsystem that publishes it registered. Tagged by product: the first path segment after /v1/.

    The version of the OpenAPI document: v1
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictBool, StrictInt, StrictStr
from typing import Any, ClassVar, Dict, List, Optional
from typing import Optional, Set
from typing_extensions import Self

class ReferenceTaken(BaseModel):
    """
    ReferenceTaken
    """ # noqa: E501
    keys: Optional[StrictInt] = Field(default=None, description="Keys is how many members it carries.")
    refusal: Optional[StrictStr] = Field(default=None, description="Refusal is why this publisher contributed nothing, if it did not. The set keeps its previous version of this source rather than shrinking.")
    resumed: Optional[StrictBool] = Field(default=None, description="Resumed is true when this run continued a version a previous run left half-landed.")
    source: Optional[StrictStr] = Field(default=None, description="Source is the publisher.")
    unchanged: Optional[StrictBool] = Field(default=None, description="Unchanged is true when the publisher's data was byte-for-byte the set we already held.")
    version: Optional[StrictStr] = Field(default=None, description="Version is the content digest that landed.")
    wrote: Optional[StrictInt] = Field(default=None, description="Wrote is how many rows this run actually wrote. Zero with Unchanged means the publisher served the same set again.")
    __properties: ClassVar[List[str]] = ["keys", "refusal", "resumed", "source", "unchanged", "version", "wrote"]

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of ReferenceTaken from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of ReferenceTaken from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "keys": obj.get("keys"),
            "refusal": obj.get("refusal"),
            "resumed": obj.get("resumed"),
            "source": obj.get("source"),
            "unchanged": obj.get("unchanged"),
            "version": obj.get("version"),
            "wrote": obj.get("wrote")
        })
        return _obj


