# coding: utf-8

"""
    Hanzo Cloud — Unified API (V8 · Open Edition)

    The single unified OpenAPI surface for ALL Hanzo services, aggregated from the per-service specs. Every route is https://api.hanzo.ai/v1/<service>/*. Grouped by the canonical domains in capabilities.yaml (the ONE registry). cloud/openapi.yaml is hanzoai/cloud's own woven document, copied verbatim by sync.py, and it wins wherever it and a hand-written spec describe the same route. Regenerated by merge.py — edit the per-service <svc>/openapi.yaml + capabilities.yaml, not this file.

    The version of the OpenAPI document: 8.0.0
    Contact: support@hanzo.ai
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictBool, StrictInt, StrictStr
from typing import Any, ClassVar, Dict, List, Optional
from typing import Optional, Set
from typing_extensions import Self

class CloudRoute(BaseModel):
    """
    CloudRoute
    """ # noqa: E501
    host: Optional[StrictStr] = Field(default=None, description="Host is the exact hostname this route matches, lowercased with any trailing dot stripped. It is a GLOBALLY unique claim — one route across the whole edge may hold a host, so no tenant can hijack another's.")
    id: Optional[StrictStr] = Field(default=None, description="ID identifies the route within the org: [A-Za-z0-9-_.], at most 128 chars. A create that omits it gets a generated one.")
    middlewares: Optional[List[StrictStr]] = Field(default=None, description="Middlewares are the ids of the edge transforms to apply, in this order, before the request reaches the service. At most 16.")
    path_prefix: Optional[StrictStr] = Field(default=None, description="PathPrefix narrows the match to requests under this path; it must start with \"/\". Empty matches every path on the host.", alias="pathPrefix")
    priority: Optional[StrictInt] = Field(default=None, description="Priority orders routes that share a host: higher wins, and equal priorities fall back to the longer PathPrefix.")
    service: Optional[StrictStr] = Field(default=None, description="Service is the id of the backend pool this route dispatches to. A route naming a service that does not exist is skipped at compile, not served.")
    tls: Optional[StrictBool] = Field(default=None, description="TLS asks the edge to terminate TLS for Host with an ACME-managed certificate.")
    __properties: ClassVar[List[str]] = ["host", "id", "middlewares", "pathPrefix", "priority", "service", "tls"]

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of CloudRoute from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of CloudRoute from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "host": obj.get("host"),
            "id": obj.get("id"),
            "middlewares": obj.get("middlewares"),
            "pathPrefix": obj.get("pathPrefix"),
            "priority": obj.get("priority"),
            "service": obj.get("service"),
            "tls": obj.get("tls")
        })
        return _obj


