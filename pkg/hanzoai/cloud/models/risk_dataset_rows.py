# coding: utf-8

"""
    Hanzo Cloud API

    The Hanzo Cloud API as a customer calls it: every operation under /v1/ except the operator's admin product, relay doors, legacy spellings and capabilities still reached by flag. Tagged by product: the first path segment after /v1/.

    The version of the OpenAPI document: v1
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictInt, StrictStr
from typing import Any, ClassVar, Dict, List, Optional
from hanzoai.cloud.models.risk_dataset_row import RiskDatasetRow
from typing import Optional, Set
from typing_extensions import Self

class RiskDatasetRows(BaseModel):
    """
    RiskDatasetRows
    """ # noqa: E501
    dataset: Optional[StrictStr] = Field(default=None, description="Dataset is the dataset the page was read from.")
    digest: Optional[StrictStr] = Field(default=None, description="Digest is the version's fingerprint. An exported page that did not carry it would be bytes with no way to say which dataset they are.")
    dims: Optional[List[StrictStr]] = Field(default=None, description="Dims names what each coordinate of Point means, in Point's own order.")
    limit: Optional[StrictInt] = Field(default=None, description="Limit is the page size actually served: the one asked for, clamped to the plane's own bound of 5000. Fewer rows than Limit means the version ended.")
    offset: Optional[StrictInt] = Field(default=None, description="Offset is where this page starts in the version's own row order, which is by row id and therefore stable forever.")
    rows: Optional[List[RiskDatasetRow]] = Field(default=None, description="Rows is the page. Never null.")
    version: Optional[StrictInt] = Field(default=None, description="Version is which published version it was read from — the one asked for, or the newest published one when the request named none.")
    __properties: ClassVar[List[str]] = ["dataset", "digest", "dims", "limit", "offset", "rows", "version"]

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of RiskDatasetRows from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # override the default output from pydantic by calling `to_dict()` of each item in rows (list)
        _items = []
        if self.rows:
            for _item_rows in self.rows:
                if _item_rows:
                    _items.append(_item_rows.to_dict())
            _dict['rows'] = _items
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of RiskDatasetRows from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "dataset": obj.get("dataset"),
            "digest": obj.get("digest"),
            "dims": obj.get("dims"),
            "limit": obj.get("limit"),
            "offset": obj.get("offset"),
            "rows": [RiskDatasetRow.from_dict(_item) for _item in obj["rows"]] if obj.get("rows") is not None else None,
            "version": obj.get("version")
        })
        return _obj


