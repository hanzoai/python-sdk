# coding: utf-8

"""
    Hanzo Cloud API

    The Hanzo Cloud API as a customer calls it: every operation under /v1/ except the operator's admin product, relay routes, legacy spellings and capabilities still reached by flag. Tagged by product: the first path segment after /v1/.

    The version of the OpenAPI document: v1
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictInt
from typing import Any, ClassVar, Dict, List, Optional
from hanzoai.cloud.models.funnel import Funnel
from hanzoai.cloud.models.progress_view import ProgressView
from typing import Optional, Set
from typing_extensions import Self

class ProfileMetrics(BaseModel):
    """
    ProfileMetrics
    """ # noqa: E501
    funnel: Optional[Funnel] = Field(default=None, description="Funnel is what the org's analytics observed over the trailing window. Read its `available` first: an org with no analytics reports zeros here, and zero traffic and no measurement are different facts.")
    launch_progress: Optional[ProgressView] = Field(default=None, description="LaunchProgress is the org's own position in the launch checklist, folded in so a profile carries both what the org has BUILT and what it has DONE.", alias="launchProgress")
    records: Optional[StrictInt] = Field(default=None, description="Records is how many business records the org holds — the volume that tells a real book of customers from an empty account. It feeds the `customers` signal, which crosses at a threshold rather than at one row.")
    revenue_cents: Optional[StrictInt] = Field(default=None, description="RevenueCents is the org's money OF RECORD — what its books say, in whole cents, never a float and never a display string. This is the number the scaling stage is decided on; funnel.revenue is the beacon's separate, unreconciled view of the same business. Zero when the org has none, and also zero when the books could not be read, which is why the `revenue` signal beside it is the thing to trust.", alias="revenueCents")
    __properties: ClassVar[List[str]] = ["funnel", "launchProgress", "records", "revenueCents"]

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of ProfileMetrics from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # override the default output from pydantic by calling `to_dict()` of funnel
        if self.funnel:
            _dict['funnel'] = self.funnel.to_dict()
        # override the default output from pydantic by calling `to_dict()` of launch_progress
        if self.launch_progress:
            _dict['launchProgress'] = self.launch_progress.to_dict()
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of ProfileMetrics from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "funnel": Funnel.from_dict(obj["funnel"]) if obj.get("funnel") is not None else None,
            "launchProgress": ProgressView.from_dict(obj["launchProgress"]) if obj.get("launchProgress") is not None else None,
            "records": obj.get("records"),
            "revenueCents": obj.get("revenueCents")
        })
        return _obj


