# coding: utf-8

"""
    Hanzo Cloud API

    The Hanzo Cloud API as a customer calls it: every operation under /v1/ except the operator's admin product, relay routes, legacy spellings and capabilities still reached by flag. Tagged by product: the first path segment after /v1/.

    The version of the OpenAPI document: v1
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictStr
from typing import Any, ClassVar, Dict, List, Optional
from hanzoai.cloud.models.argo_destination import ArgoDestination
from hanzoai.cloud.models.argo_group_kind import ArgoGroupKind
from typing import Optional, Set
from typing_extensions import Self

class ArgoProjectSpec(BaseModel):
    """
    ArgoProjectSpec
    """ # noqa: E501
    cluster_resource_whitelist: Optional[List[ArgoGroupKind]] = Field(default=None, description="ClusterResourceWhitelist are the cluster-scoped kinds it may create — [{group:\"*\", kind:\"*\"}] on a synthesized project.", alias="clusterResourceWhitelist")
    description: Optional[StrictStr] = Field(default=None, description="Description is the project's human label: the IAM project's display name, or its description when it has no display name. Absent when IAM carries neither.")
    destinations: Optional[List[ArgoDestination]] = Field(default=None, description="Destinations are the cluster/namespace pairs it may write to — a single {server:\"*\", namespace:\"*\"} on a synthesized project, for the same reason.")
    source_repos: Optional[List[StrictStr]] = Field(default=None, description="SourceRepos are the git repos applications in this project may pull from. [\"*\"] for every project this plane synthesizes or reflects from IAM: the boundary that actually holds on this platform is the IAM org, resolved before a row is ever projected, so the projected fence is deliberately permissive and is NOT an authorization statement.", alias="sourceRepos")
    __properties: ClassVar[List[str]] = ["clusterResourceWhitelist", "description", "destinations", "sourceRepos"]

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of ArgoProjectSpec from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # override the default output from pydantic by calling `to_dict()` of each item in cluster_resource_whitelist (list)
        _items = []
        if self.cluster_resource_whitelist:
            for _item_cluster_resource_whitelist in self.cluster_resource_whitelist:
                if _item_cluster_resource_whitelist:
                    _items.append(_item_cluster_resource_whitelist.to_dict())
            _dict['clusterResourceWhitelist'] = _items
        # override the default output from pydantic by calling `to_dict()` of each item in destinations (list)
        _items = []
        if self.destinations:
            for _item_destinations in self.destinations:
                if _item_destinations:
                    _items.append(_item_destinations.to_dict())
            _dict['destinations'] = _items
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of ArgoProjectSpec from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "clusterResourceWhitelist": [ArgoGroupKind.from_dict(_item) for _item in obj["clusterResourceWhitelist"]] if obj.get("clusterResourceWhitelist") is not None else None,
            "description": obj.get("description"),
            "destinations": [ArgoDestination.from_dict(_item) for _item in obj["destinations"]] if obj.get("destinations") is not None else None,
            "sourceRepos": obj.get("sourceRepos")
        })
        return _obj


