# coding: utf-8

"""
    Hanzo Cloud API

    The Hanzo Cloud API as a customer calls it: every operation under /v1/ except the operator's admin product, relay doors, legacy spellings and capabilities still reached by flag. Tagged by product: the first path segment after /v1/.

    The version of the OpenAPI document: v1
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictStr
from typing import Any, ClassVar, Dict, List, Optional
from hanzoai.cloud.models.projects_domain import ProjectsDomain
from typing import Optional, Set
from typing_extensions import Self

class ProjectsBoundDomains(BaseModel):
    """
    ProjectsBoundDomains
    """ # noqa: E501
    bound: Optional[List[ProjectsDomain]] = Field(default=None, description="Bound is the result of THIS call, one row per host in the request: live for an already-vouched host, pending with the DNS records to publish otherwise.")
    domains: Optional[List[StrictStr]] = Field(default=None, description="Domains are the hostnames that are VERIFIED and routing right now, after this bind.")
    org: Optional[StrictStr] = Field(default=None, description="Org is the organisation that owns the site.")
    slug: Optional[StrictStr] = Field(default=None, description="Slug is the site the hosts were bound to.")
    __properties: ClassVar[List[str]] = ["bound", "domains", "org", "slug"]

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of ProjectsBoundDomains from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # override the default output from pydantic by calling `to_dict()` of each item in bound (list)
        _items = []
        if self.bound:
            for _item_bound in self.bound:
                if _item_bound:
                    _items.append(_item_bound.to_dict())
            _dict['bound'] = _items
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of ProjectsBoundDomains from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "bound": [ProjectsDomain.from_dict(_item) for _item in obj["bound"]] if obj.get("bound") is not None else None,
            "domains": obj.get("domains"),
            "org": obj.get("org"),
            "slug": obj.get("slug")
        })
        return _obj


