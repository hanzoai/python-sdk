# coding: utf-8

"""
    Hanzo Cloud API

    The Hanzo Cloud API as a customer calls it: every operation under /v1/ except the operator's admin product, relay routes, legacy spellings and capabilities still reached by flag. Tagged by product: the first path segment after /v1/.

    The version of the OpenAPI document: v1
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictStr
from typing import Any, ClassVar, Dict, List, Optional
from typing import Optional, Set
from typing_extensions import Self

class NodeView(BaseModel):
    """
    NodeView
    """ # noqa: E501
    caps: Optional[List[StrictStr]] = Field(default=None, description="Caps is the capability list the node reported. It is a self-report, useful to SHOW and never load-bearing: what a node may actually be asked to do is decided at the socket by the deployment's allowlist.")
    commands: Optional[List[StrictStr]] = Field(default=None, description="Commands is the command list the node reported. Same standing as Caps: a self-report, checked again at the socket before anything runs.")
    connected_at: Optional[StrictStr] = Field(default=None, description="ConnectedAt is when this node's socket was established, RFC3339 UTC.", alias="connectedAt")
    display_name: Optional[StrictStr] = Field(default=None, description="DisplayName is the human name the node reported for itself.", alias="displayName")
    id: Optional[StrictStr] = Field(default=None, description="ID is the node's own identifier within the org — the value POST /v1/node/{id}/invoke addresses it by.")
    platform: Optional[StrictStr] = Field(default=None, description="Platform is the operating system and architecture the node reported.")
    version: Optional[StrictStr] = Field(default=None, description="Version is the node agent's own version string.")
    __properties: ClassVar[List[str]] = ["caps", "commands", "connectedAt", "displayName", "id", "platform", "version"]

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of NodeView from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of NodeView from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "caps": obj.get("caps"),
            "commands": obj.get("commands"),
            "connectedAt": obj.get("connectedAt"),
            "displayName": obj.get("displayName"),
            "id": obj.get("id"),
            "platform": obj.get("platform"),
            "version": obj.get("version")
        })
        return _obj


