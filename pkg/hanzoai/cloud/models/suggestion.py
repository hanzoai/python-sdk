# coding: utf-8

"""
    Hanzo Cloud API

    The Hanzo Cloud API as a customer calls it: every operation under /v1/ except the operator's admin product, relay doors, legacy spellings and capabilities still reached by flag. Tagged by product: the first path segment after /v1/.

    The version of the OpenAPI document: v1
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictBool, StrictInt, StrictStr
from typing import Any, ClassVar, Dict, List, Optional
from typing import Optional, Set
from typing_extensions import Self

class Suggestion(BaseModel):
    """
    Suggestion
    """ # noqa: E501
    automatable: Optional[StrictBool] = Field(default=None, description="Automatable is true when the step names a tool, so the Business AI can do it rather than only describe it.")
    detail: Optional[StrictStr] = Field(default=None, description="Detail is the step's own prose — what it asks for.")
    rationale: Optional[StrictStr] = Field(default=None, description="Rationale is why this step is being suggested NOW, written for the person reading it. It explains the ranking, not the step.")
    step_id: Optional[StrictStr] = Field(default=None, description="StepID is the checklist step being recommended — the id every step route takes, so a caller can act on the suggestion directly.", alias="stepId")
    title: Optional[StrictStr] = Field(default=None, description="Title is the step's own one-line quest.")
    unlocks: Optional[StrictInt] = Field(default=None, description="Unlocks is how many downstream steps completing this one immediately makes available (its leverage) — the primary ranking key.")
    __properties: ClassVar[List[str]] = ["automatable", "detail", "rationale", "stepId", "title", "unlocks"]

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of Suggestion from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of Suggestion from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "automatable": obj.get("automatable"),
            "detail": obj.get("detail"),
            "rationale": obj.get("rationale"),
            "stepId": obj.get("stepId"),
            "title": obj.get("title"),
            "unlocks": obj.get("unlocks")
        })
        return _obj


