# coding: utf-8

"""
    Hanzo Cloud API

    The Hanzo Cloud API as a customer calls it: every operation under /v1/ except the operator's admin product, relay routes, legacy spellings and capabilities still reached by flag. Tagged by product: the first path segment after /v1/.

    The version of the OpenAPI document: v1
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictInt, StrictStr
from typing import Any, ClassVar, Dict, List, Optional
from hanzoai.cloud.models.traffic_caller import TrafficCaller
from typing import Optional, Set
from typing_extensions import Self

class TrafficView(BaseModel):
    """
    TrafficView
    """ # noqa: E501
    blind: Optional[StrictInt] = Field(default=None, description="Blind is how many requests in the window carried no identity to attribute them to — no validated credential and no client address. Non-zero on a public plane means the client address is not reaching this process (a TCP load balancer with no PROXY protocol in front of it, typically), so this scope's callers cannot be told apart and nothing can be held against them.")
    callers: Optional[List[TrafficCaller]] = Field(default=None, description="Callers is the scope's busiest callers this window. A credentialed caller appears as a FINGERPRINT — a per-process one-way digest: enough to recognise the same caller across requests, never enough to reconstruct the credential.")
    ceiling: Optional[StrictInt] = Field(default=None, description="Ceiling is the most callers this scope may hold at once.")
    denied: Optional[StrictInt] = Field(default=None, description="Denied is how many of them the gate refused.")
    lanes: Optional[Dict[str, StrictInt]] = Field(default=None, description="Lanes is the request count per lane — agent, human, bot, unknown. This is the split that separates a customer's automation from a scraper.")
    mode: Optional[StrictStr] = Field(default=None, description="Mode is the abuse gate's posture for this scope: \"shadow\" records the scorer's action without enforcing it, \"live\" enforces it.")
    org: Optional[StrictStr] = Field(default=None, description="Org is the scope this view was taken for — the validated principal's own, never a value the caller supplied. Empty names the anonymous lane, the one scope that has no tenant.")
    refused: Optional[StrictInt] = Field(default=None, description="Refused is how many callers this scope's ceilings turned away in the window.")
    requests: Optional[StrictInt] = Field(default=None, description="Requests is how many requests this scope made in the window.")
    screens: Optional[StrictInt] = Field(default=None, description="Screens is how many of them were put to the scorer — the billable unit of the risk product. Counted from the first request, whatever the SKU costs.")
    strain: Optional[StrictStr] = Field(default=None, description="Strain is what this scope's ceilings are doing: \"clear\" below them, \"full\" at them, \"refuse\" once a caller has been turned away inside this window — which means that caller is UNMEASURED and the numbers here are a sample rather than a census. It is reported rather than logged because the alternative — a bound that degrades a scope silently — is the failure this design exists to rule out. No other scope can move it.")
    tracked: Optional[StrictInt] = Field(default=None, description="Tracked is how many callers this scope holds state for right now, and Ceiling is the most it may hold. Tracked == Ceiling is the fact a bound that binds cannot hide.")
    unscored: Optional[StrictInt] = Field(default=None, description="Unscored is how many of those screens got NO answer — the scorer was absent, stuck, slow, erroring or silent. An unanswered screen allows ordinary traffic, so this is the number that separates \"a quiet day\" from \"the judge stopped answering and nothing said so\".")
    window_sec: Optional[StrictInt] = Field(default=None, description="WindowSec is the span the counts cover, in seconds.")
    __properties: ClassVar[List[str]] = ["blind", "callers", "ceiling", "denied", "lanes", "mode", "org", "refused", "requests", "screens", "strain", "tracked", "unscored", "window_sec"]

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of TrafficView from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # override the default output from pydantic by calling `to_dict()` of each item in callers (list)
        _items = []
        if self.callers:
            for _item_callers in self.callers:
                if _item_callers:
                    _items.append(_item_callers.to_dict())
            _dict['callers'] = _items
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of TrafficView from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "blind": obj.get("blind"),
            "callers": [TrafficCaller.from_dict(_item) for _item in obj["callers"]] if obj.get("callers") is not None else None,
            "ceiling": obj.get("ceiling"),
            "denied": obj.get("denied"),
            "lanes": obj.get("lanes"),
            "mode": obj.get("mode"),
            "org": obj.get("org"),
            "refused": obj.get("refused"),
            "requests": obj.get("requests"),
            "screens": obj.get("screens"),
            "strain": obj.get("strain"),
            "tracked": obj.get("tracked"),
            "unscored": obj.get("unscored"),
            "window_sec": obj.get("window_sec")
        })
        return _obj


