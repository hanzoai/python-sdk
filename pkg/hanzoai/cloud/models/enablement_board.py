# coding: utf-8

"""
    Hanzo Cloud API

    The Hanzo Cloud API as a customer calls it: every operation under /v1/ except the operator's admin product, relay routes, legacy spellings and capabilities still reached by flag. Tagged by product: the first path segment after /v1/.

    The version of the OpenAPI document: v1
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictStr
from typing import Any, ClassVar, Dict, List, Optional
from hanzoai.cloud.models.user_enablement_item import UserEnablementItem
from typing import Optional, Set
from typing_extensions import Self

class EnablementBoard(BaseModel):
    """
    EnablementBoard
    """ # noqa: E501
    betas: Optional[List[UserEnablementItem]] = Field(default=None, description="Betas are the subset of Items the caller's org may still opt into.")
    items: Optional[List[UserEnablementItem]] = Field(default=None, description="Items is every managed item, each resolved for the caller's org.")
    org: Optional[StrictStr] = Field(default=None, description="Org is the org this view was resolved for; empty for a caller with no validated principal, who sees only the generally-available items.")
    __properties: ClassVar[List[str]] = ["betas", "items", "org"]

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of EnablementBoard from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # override the default output from pydantic by calling `to_dict()` of each item in betas (list)
        _items = []
        if self.betas:
            for _item_betas in self.betas:
                if _item_betas:
                    _items.append(_item_betas.to_dict())
            _dict['betas'] = _items
        # override the default output from pydantic by calling `to_dict()` of each item in items (list)
        _items = []
        if self.items:
            for _item_items in self.items:
                if _item_items:
                    _items.append(_item_items.to_dict())
            _dict['items'] = _items
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of EnablementBoard from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "betas": [UserEnablementItem.from_dict(_item) for _item in obj["betas"]] if obj.get("betas") is not None else None,
            "items": [UserEnablementItem.from_dict(_item) for _item in obj["items"]] if obj.get("items") is not None else None,
            "org": obj.get("org")
        })
        return _obj


