# coding: utf-8

"""
    Hanzo Cloud API

    Composed from each subsystem's own projection of its router, in the fleet's mount order — every operation below is a route the subsystem that publishes it registered. Tagged by product: the first path segment after /v1/.

    The version of the OpenAPI document: v1
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictFloat, StrictInt, StrictStr
from typing import Any, ClassVar, Dict, List, Optional, Union
from hanzoai.cloud.models.risk_source_coverage import RiskSourceCoverage
from typing import Optional, Set
from typing_extensions import Self

class RiskLabelCoverage(BaseModel):
    """
    RiskLabelCoverage
    """ # noqa: E501
    contested: Optional[StrictInt] = Field(default=None, description="Contested is how many matured events have two visible assertions that disagree. It is the number that says whether the precedence rule is load-bearing or decorative, and it is the one to watch after wiring a new source.")
    events: Optional[StrictInt] = None
    explore: Optional[Union[StrictFloat, StrictInt]] = Field(default=None, description="Explore is the share of judged events whose winning assertion came from the below-the-line sample. A blocked transaction never produces a chargeback, so a training set with no exploration in it is a description of the incumbent block list rather than of the world — and a champion measured on it is measured on whether it agrees with the incumbent.")
    facts: Optional[StrictInt] = Field(default=None, description="Facts is how many assertions the window holds; Events is how many distinct judged events they cover. The two differ by exactly the corroboration and the conflict in the plane.")
    var_from: Optional[StrictStr] = Field(default=None, alias="from")
    horizon: Optional[StrictInt] = None
    judged: Optional[StrictInt] = Field(default=None, description="Judged is how many MATURED events resolve, at their own as-of, to something other than unjudged.")
    matured: Optional[StrictInt] = Field(default=None, description="Matured is how many of those events have aged past the horizon and may therefore be admitted to a supervised set at all. It counts every matured event, judged or not — it is the DENOMINATOR an operator divides Judged by, and a denominator that excluded the unjudged would read 1.0 on a plane with one label in it.")
    pending: Optional[StrictInt] = Field(default=None, description="Pending is how many of this tenant's assertions the DERIVED columnar copy is not known to hold yet. Every count above is folded from the record, so they are right regardless — but a materialiser that joins in the warehouse while this is non-zero is joining against an incomplete answer key, and a missing fraud label is indistinguishable from an honest customer. It is reported at the training gate because that is where somebody is deciding whether the ground truth is good enough to fit on. Counted under a cap, so it saturates rather than costing a full scan on every read.")
    productive: Optional[StrictInt] = None
    sources: Optional[List[RiskSourceCoverage]] = Field(default=None, description="Sources breaks the judged events down by the source that WON, so a plane that looks labelled because one noisy source dominates is visible as such.")
    to: Optional[StrictStr] = None
    unlabelled: Optional[StrictInt] = Field(default=None, description="Unlabelled is how many MATURED events had no assertion knowable by their own as-of — including every assertion that arrived after that instant. It is the field that says WHY judged is low: a tenant whose ground truth was filed long after the events it judges reads matured=n, judged=0, unlabelled=n, which is diagnosable, rather than a bare zero, which is not.")
    unmatured: Optional[StrictInt] = Field(default=None, description="Unmatured is how many events in the window have NOT aged past the horizon. They are not unlabelled — they are not yet askable, and a supervised set must exclude them rather than treat them as negatives. Matured + Unmatured is Events.")
    unproductive: Optional[StrictInt] = None
    __properties: ClassVar[List[str]] = ["contested", "events", "explore", "facts", "from", "horizon", "judged", "matured", "pending", "productive", "sources", "to", "unlabelled", "unmatured", "unproductive"]

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of RiskLabelCoverage from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # override the default output from pydantic by calling `to_dict()` of each item in sources (list)
        _items = []
        if self.sources:
            for _item_sources in self.sources:
                if _item_sources:
                    _items.append(_item_sources.to_dict())
            _dict['sources'] = _items
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of RiskLabelCoverage from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "contested": obj.get("contested"),
            "events": obj.get("events"),
            "explore": obj.get("explore"),
            "facts": obj.get("facts"),
            "from": obj.get("from"),
            "horizon": obj.get("horizon"),
            "judged": obj.get("judged"),
            "matured": obj.get("matured"),
            "pending": obj.get("pending"),
            "productive": obj.get("productive"),
            "sources": [RiskSourceCoverage.from_dict(_item) for _item in obj["sources"]] if obj.get("sources") is not None else None,
            "to": obj.get("to"),
            "unlabelled": obj.get("unlabelled"),
            "unmatured": obj.get("unmatured"),
            "unproductive": obj.get("unproductive")
        })
        return _obj


