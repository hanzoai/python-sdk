# coding: utf-8

"""
    Hanzo Cloud API

    Composed from each subsystem's own projection of its router, in the fleet's mount order — every operation below is a route the subsystem that publishes it registered. Tagged by product: the first path segment after /v1/.

    The version of the OpenAPI document: v1
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictInt, StrictStr
from typing import Any, ClassVar, Dict, List, Optional
from typing import Optional, Set
from typing_extensions import Self

class VectorCollection(BaseModel):
    """
    VectorCollection
    """ # noqa: E501
    created_at: Optional[StrictStr] = Field(default=None, description="CreatedAt is the collection's creation time (RFC 3339); Qdrant does not report one, so it is empty today.", alias="createdAt")
    dimension: Optional[StrictInt] = Field(default=None, description="Dimension is the size of one vector in the collection.")
    distance_metric: Optional[StrictStr] = Field(default=None, description="DistanceMetric is the collection's distance function; \"cosine\" when the collection's detail could not be read.", alias="distanceMetric")
    name: Optional[StrictStr] = Field(default=None, description="Name is the collection name.")
    storage_bytes: Optional[StrictInt] = Field(default=None, description="StorageBytes is the collection's on-disk size, omitted when unknown.", alias="storageBytes")
    vector_count: Optional[StrictInt] = Field(default=None, description="VectorCount is the collection's point count.", alias="vectorCount")
    __properties: ClassVar[List[str]] = ["createdAt", "dimension", "distanceMetric", "name", "storageBytes", "vectorCount"]

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of VectorCollection from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of VectorCollection from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "createdAt": obj.get("createdAt"),
            "dimension": obj.get("dimension"),
            "distanceMetric": obj.get("distanceMetric"),
            "name": obj.get("name"),
            "storageBytes": obj.get("storageBytes"),
            "vectorCount": obj.get("vectorCount")
        })
        return _obj


