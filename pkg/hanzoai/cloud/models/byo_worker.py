# coding: utf-8

"""
    Hanzo Cloud API

    The Hanzo Cloud API as a customer calls it: every operation under /v1/ except the operator's admin product, relay routes, legacy spellings and capabilities still reached by flag. Tagged by product: the first path segment after /v1/.

    The version of the OpenAPI document: v1
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictInt, StrictStr
from typing import Any, ClassVar, Dict, List, Optional
from hanzoai.cloud.models.byo_gpu import ByoGPU
from hanzoai.cloud.models.engine_advertisement import EngineAdvertisement
from typing import Optional, Set
from typing_extensions import Self

class ByoWorker(BaseModel):
    """
    ByoWorker
    """ # noqa: E501
    arch: Optional[StrictStr] = Field(default=None, description="Arch/CPUs/Memory are the connecting host's static CPU spec, mirrored from the registration: Arch is runtime.GOARCH (amd64 | arm64), Memory is total RAM in BYTES — the same fields a code-linked run-target carries, so the /v1/visor/fleet board renders a linked node's arch + cores + RAM like any other unit.")
    capabilities: Optional[List[StrictStr]] = Field(default=None, description="Capabilities is what this worker offers the org: \"studio.render\" when the node can render, \"engine.serve\" when it serves a model endpoint. A node advertises one only once it can honour it, so an absent list means a node that has dialed in but is not ready to serve any of them yet.")
    cpu_model: Optional[StrictStr] = Field(default=None, description="CPUModel is the processor as the host names it (\"Apple M3 Max\"), for display.", alias="cpuModel")
    cpus: Optional[StrictInt] = Field(default=None, description="CPUs is the host's logical core count.")
    cuda: Optional[StrictStr] = Field(default=None, description="Cuda is the host's CUDA toolkit version. NVIDIA hosts report it.")
    driver: Optional[StrictStr] = Field(default=None, description="Driver is the host's NVIDIA kernel driver version — distinct from Cuda, and the one that bounds which CUDA versions can run on this box.")
    engine: Optional[EngineAdvertisement] = Field(default=None, description="Engine is the hanzo-engine model server this node runs, when it runs one (`hanzo link --serve-engine`). Absent means the node takes jobs but serves no model endpoint.")
    first_seen: Optional[StrictStr] = Field(default=None, description="FirstSeen is when this node first dialed in, RFC 3339 — the start of its presence record, which `hanzo unlink` ends.", alias="firstSeen")
    gpus: Optional[List[ByoGPU]] = Field(default=None, description="GPUs are the accelerators the host found on itself. Empty is a real answer: a CPU-only machine can dial in and take non-GPU work.")
    hip: Optional[StrictStr] = Field(default=None, description="Hip is the host's HIP runtime version, the AMD counterpart to Cuda.")
    hostname: Optional[StrictStr] = Field(default=None, description="Hostname is what the host calls itself. It equals ID for any hostname already in the [a-z0-9-] alphabet, and differs when sanitizing had to change it.")
    id: Optional[StrictStr] = Field(default=None, description="ID is the node's id in the fleet — the sanitized hostname it registered under, which is also the `unit` its samples and its gpu-jobs lane key on. This is the id to use everywhere else on the compute surface.")
    job_queue: Optional[StrictStr] = Field(default=None, description="JobQueue is the tasks NAMESPACE this worker claims render jobs out of — \"gpu-jobs\" unless `hanzo link` was pointed at another. Within it, a job aimed at this node alone rides the task-queue value \"gpu:<id>\".", alias="jobQueue")
    last_heartbeat: Optional[StrictStr] = Field(default=None, description="LastHeartbeat is the most recent beat this node sent, RFC 3339. It is what Status is computed from, so a reader can check the judgement.", alias="lastHeartbeat")
    location: Optional[StrictStr] = Field(default=None, description="Location is always \"on-prem\" — a machine that dialed in has no cloud region, and inventing one would put it somewhere it is not.")
    memory: Optional[StrictInt] = Field(default=None, description="Memory is the host's total RAM in BYTES.")
    os: Optional[StrictStr] = Field(default=None, description="Os is the host's operating system: linux, darwin or windows.")
    provider: Optional[StrictStr] = Field(default=None, description="Provider is always \"byo\": this machine is the operator's, not one Hanzo provisioned. It exists so a fold into the machines/GPUs pages says which rows are rented and which are the customer's own.")
    rocm: Optional[StrictStr] = Field(default=None, description="Rocm is the host's ROCm version. AMD hosts report it; empty otherwise.")
    status: Optional[StrictStr] = Field(default=None, description="Status is \"online\" when the last heartbeat landed within 90s, else \"offline\" — so it is a fact about heartbeat freshness, not about the box being powered on. A worker that has never beaten reads offline.")
    version: Optional[StrictStr] = Field(default=None, description="Version is the `hanzo` CLI version running on the node. It is what to check when a worker is missing a field a newer registration reports.")
    __properties: ClassVar[List[str]] = ["arch", "capabilities", "cpuModel", "cpus", "cuda", "driver", "engine", "firstSeen", "gpus", "hip", "hostname", "id", "jobQueue", "lastHeartbeat", "location", "memory", "os", "provider", "rocm", "status", "version"]

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of ByoWorker from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # override the default output from pydantic by calling `to_dict()` of engine
        if self.engine:
            _dict['engine'] = self.engine.to_dict()
        # override the default output from pydantic by calling `to_dict()` of each item in gpus (list)
        _items = []
        if self.gpus:
            for _item_gpus in self.gpus:
                if _item_gpus:
                    _items.append(_item_gpus.to_dict())
            _dict['gpus'] = _items
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of ByoWorker from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "arch": obj.get("arch"),
            "capabilities": obj.get("capabilities"),
            "cpuModel": obj.get("cpuModel"),
            "cpus": obj.get("cpus"),
            "cuda": obj.get("cuda"),
            "driver": obj.get("driver"),
            "engine": EngineAdvertisement.from_dict(obj["engine"]) if obj.get("engine") is not None else None,
            "firstSeen": obj.get("firstSeen"),
            "gpus": [ByoGPU.from_dict(_item) for _item in obj["gpus"]] if obj.get("gpus") is not None else None,
            "hip": obj.get("hip"),
            "hostname": obj.get("hostname"),
            "id": obj.get("id"),
            "jobQueue": obj.get("jobQueue"),
            "lastHeartbeat": obj.get("lastHeartbeat"),
            "location": obj.get("location"),
            "memory": obj.get("memory"),
            "os": obj.get("os"),
            "provider": obj.get("provider"),
            "rocm": obj.get("rocm"),
            "status": obj.get("status"),
            "version": obj.get("version")
        })
        return _obj


