# coding: utf-8

"""
    Hanzo Cloud API

    The Hanzo Cloud API as a customer calls it: every operation under /v1/ except the operator's admin product, relay doors, legacy spellings and capabilities still reached by flag. Tagged by product: the first path segment after /v1/.

    The version of the OpenAPI document: v1
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictFloat, StrictInt, StrictStr
from typing import Any, ClassVar, Dict, List, Optional, Union
from typing import Optional, Set
from typing_extensions import Self

class RiskCause(BaseModel):
    """
    RiskCause
    """ # noqa: E501
    baseline: Optional[Union[StrictFloat, StrictInt]] = Field(default=None, description="Baseline is the number it was measured against — always this organisation's own history, never a fixed limit and never another organisation's.")
    citation: Optional[StrictStr] = Field(default=None, description="Citation is where those words come from, so the claim is checkable rather than asserted — which is what a chargeback network or a regulator asks for.")
    feature: Optional[StrictStr] = Field(default=None, description="Feature is the dimension that contributed.")
    indicator: Optional[StrictStr] = Field(default=None, description="Indicator is the supervisor's own words for the thing being looked for.")
    observed: Optional[Union[StrictFloat, StrictInt]] = Field(default=None, description="Observed is the raw number the coordinate was computed from.")
    severity: Optional[StrictStr] = Field(default=None, description="Severity is how much weight this dimension carries.")
    share: Optional[Union[StrictFloat, StrictInt]] = Field(default=None, description="Share is this feature's part of the score, in [0,1]. Zero across every cause means no single feature accounts for the alert and the combination does; the causes are then ordered by how far each sits from unremarkable.")
    typology: Optional[StrictStr] = Field(default=None, description="Typology is the laundering or abuse pattern this dimension detects.")
    unit: Optional[StrictStr] = Field(default=None, description="Unit is how to read Observed, which is what turns a coordinate into a sentence.")
    without: Optional[Union[StrictFloat, StrictInt]] = Field(default=None, description="Without is the score the same event would have received with this coordinate at its neutral value — the counterfactual itself.")
    __properties: ClassVar[List[str]] = ["baseline", "citation", "feature", "indicator", "observed", "severity", "share", "typology", "unit", "without"]

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of RiskCause from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of RiskCause from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "baseline": obj.get("baseline"),
            "citation": obj.get("citation"),
            "feature": obj.get("feature"),
            "indicator": obj.get("indicator"),
            "observed": obj.get("observed"),
            "severity": obj.get("severity"),
            "share": obj.get("share"),
            "typology": obj.get("typology"),
            "unit": obj.get("unit"),
            "without": obj.get("without")
        })
        return _obj


