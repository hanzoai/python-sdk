# coding: utf-8

"""
    Hanzo Cloud — Unified API (V8 · Open Edition)

    The single unified OpenAPI surface for ALL Hanzo services, aggregated from the per-service specs. Every route is https://api.hanzo.ai/v1/<service>/*. Grouped by the canonical domains in capabilities.yaml (the ONE registry). Regenerated by merge.py — edit the per-service <svc>/openapi.yaml + capabilities.yaml, not this file.

    The version of the OpenAPI document: 8.0.0
    Contact: support@hanzo.ai
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictBool, StrictInt
from typing import Any, ClassVar, Dict, List, Optional
from hanzoai.cloud.models.billing_cap_reason import BillingCapReason
from typing import Optional, Set
from typing_extensions import Self

class BillingCapVerdict(BaseModel):
    """
    The per-request spend-cap verdict for a (project, service) scope, evaluated over the org's spend-alert rows (most-restrictive-wins). Fails CLOSED: if a hard-enforceable row's spend cannot be computed the verdict denies. 
    """ # noqa: E501
    allow: StrictBool = Field(description="Whether the request is permitted.")
    reason: BillingCapReason
    cap_cents: Optional[StrictInt] = Field(default=None, description="The tightest violated cap's ceiling in USD cents (0 when allowed).", alias="capCents")
    spent_cents: Optional[StrictInt] = Field(default=None, description="Period spend for the scope in USD cents at decision time.", alias="spentCents")
    warn_pct: Optional[StrictInt] = Field(default=None, description="Utilization percent of the most-utilized covering cap when at/over its soft threshold (0 otherwise, and 0 on a deny).", alias="warnPct")
    additional_properties: Dict[str, Any] = {}
    __properties: ClassVar[List[str]] = ["allow", "reason", "capCents", "spentCents", "warnPct"]

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of BillingCapVerdict from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        * Fields in `self.additional_properties` are added to the output dict.
        """
        excluded_fields: Set[str] = set([
            "additional_properties",
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # puts key-value pairs in additional_properties in the top level
        if self.additional_properties is not None:
            for _key, _value in self.additional_properties.items():
                _dict[_key] = _value

        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of BillingCapVerdict from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "allow": obj.get("allow"),
            "reason": obj.get("reason"),
            "capCents": obj.get("capCents"),
            "spentCents": obj.get("spentCents"),
            "warnPct": obj.get("warnPct")
        })
        # store additional fields in additional_properties
        for _key in obj.keys():
            if _key not in cls.__properties:
                _obj.additional_properties[_key] = obj.get(_key)

        return _obj


