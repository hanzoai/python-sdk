# coding: utf-8

"""
    Hanzo Cloud API

    The Hanzo Cloud API as a customer calls it: every operation under /v1/ except the operator's admin product, relay routes, legacy spellings and capabilities still reached by flag. Tagged by product: the first path segment after /v1/.

    The version of the OpenAPI document: v1
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictInt, StrictStr
from typing import Any, ClassVar, Dict, List, Optional
from typing import Optional, Set
from typing_extensions import Self

class DeploymentView(BaseModel):
    """
    DeploymentView
    """ # noqa: E501
    application_id: Optional[StrictStr] = Field(default=None, description="ApplicationID is the app this deployed — the app's `id`, not its slug.", alias="applicationId")
    build_id: Optional[StrictStr] = Field(default=None, description="BuildID is the build record behind a git deploy, whose logs and status live at /v1/platform/builds. Empty for an image deploy.", alias="buildId")
    commit: Optional[StrictStr] = Field(default=None, description="Commit is the git ref this built — the commit a deploy or a push named, else the app's branch. Empty for an image deploy, which builds nothing.")
    created_at: Optional[StrictInt] = Field(default=None, description="CreatedAt is when the attempt was recorded, unix seconds.", alias="createdAt")
    id: Optional[StrictStr] = Field(default=None, description="ID is the deployment's id (`dep_…`), minted when the attempt is recorded. The app's currentDeploymentId points at one of these.")
    image: Optional[StrictStr] = Field(default=None, description="Image is the full `repo:tag` this deployment put in the CR. For a git deploy it is the ref the in-cluster build pushes to, known before the build runs.")
    message: Optional[StrictStr] = Field(default=None, description="Message is why this attempt is not live: the failure, or the note that a newer deployment went live before this build finished. Empty while it is fine.")
    org: Optional[StrictStr] = Field(default=None, description="Org is the tenant the deployment belongs to, from the validated identity.")
    source: Optional[StrictStr] = Field(default=None, description="Source is which lane produced it: `git` (built from the repo) or `image` (an already-built ref deployed as-is, including promote and rollback).")
    status: Optional[StrictStr] = Field(default=None, description="Status is where the attempt got to: `building` while its image is being built, `deploying` once its CR reached the cluster — which is the terminal success state, the app's own status is what turns `live` — `error` with the reason in Message, or `superseded` when a newer version went live first.")
    updated_at: Optional[StrictInt] = Field(default=None, description="UpdatedAt is its last transition, unix seconds — so for a terminal deployment it is when it reached that state.", alias="updatedAt")
    version: Optional[StrictInt] = Field(default=None, description="Version counts this app's deployments, from 1 and monotonically. It is what ORDERS them: a deploy only goes live if no higher version already is, so a build that finishes late is superseded instead of overwriting a newer one.")
    __properties: ClassVar[List[str]] = ["applicationId", "buildId", "commit", "createdAt", "id", "image", "message", "org", "source", "status", "updatedAt", "version"]

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of DeploymentView from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of DeploymentView from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "applicationId": obj.get("applicationId"),
            "buildId": obj.get("buildId"),
            "commit": obj.get("commit"),
            "createdAt": obj.get("createdAt"),
            "id": obj.get("id"),
            "image": obj.get("image"),
            "message": obj.get("message"),
            "org": obj.get("org"),
            "source": obj.get("source"),
            "status": obj.get("status"),
            "updatedAt": obj.get("updatedAt"),
            "version": obj.get("version")
        })
        return _obj


