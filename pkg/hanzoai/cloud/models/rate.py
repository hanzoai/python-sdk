# coding: utf-8

"""
    Hanzo Cloud API

    The Hanzo Cloud API as a customer calls it: every operation under /v1/ except the operator's admin product, relay routes, legacy spellings and capabilities still reached by flag. Tagged by product: the first path segment after /v1/.

    The version of the OpenAPI document: v1
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictInt, StrictStr
from typing import Any, ClassVar, Dict, List, Optional
from typing import Optional, Set
from typing_extensions import Self

class Rate(BaseModel):
    """
    Rate
    """ # noqa: E501
    component: Optional[StrictStr] = Field(default=None, description="Component is which of the four this line bills under.")
    name: Optional[StrictStr] = Field(default=None, description="Name addresses the rate.")
    note: Optional[StrictStr] = Field(default=None, description="Note is what the line covers.")
    per: Optional[StrictStr] = Field(default=None, description="Per names one unit, so a reader never has to guess what the number is per.")
    rate_micro_usd: Optional[StrictInt] = Field(default=None, description="Micros is the price of ONE unit, in micro-USD. It is the number the ledger books, resolved through the same authority the metering path reads.")
    title: Optional[StrictStr] = Field(default=None, description="Title is how the line reads on a price list.")
    __properties: ClassVar[List[str]] = ["component", "name", "note", "per", "rate_micro_usd", "title"]

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of Rate from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of Rate from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "component": obj.get("component"),
            "name": obj.get("name"),
            "note": obj.get("note"),
            "per": obj.get("per"),
            "rate_micro_usd": obj.get("rate_micro_usd"),
            "title": obj.get("title")
        })
        return _obj


