# coding: utf-8

"""
    Hanzo Cloud API

    The Hanzo Cloud API as a customer calls it: every operation under /v1/ except the operator's admin product, relay routes, legacy spellings and capabilities still reached by flag. Tagged by product: the first path segment after /v1/.

    The version of the OpenAPI document: v1
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictBool, StrictStr
from typing import Any, ClassVar, Dict, List, Optional
from typing import Optional, Set
from typing_extensions import Self

class JourneyStep(BaseModel):
    """
    JourneyStep
    """ # noqa: E501
    args: Optional[Dict[str, Dict[str, Any]]] = Field(default=None, description="Args are the tool's default arguments, merged under whatever the caller passes at run time, so a step ships with the arguments that make it work.")
    deps: Optional[List[StrictStr]] = Field(default=None, description="Dependencies are step ids that must be done/skipped before this step is available. The wire key is `deps` (the blueprint contract); the Go field keeps its descriptive name.")
    detail: Optional[StrictStr] = Field(default=None, description="Detail is the juncture — what the Guide explains, or asks for, at this step.")
    draft: Optional[StrictStr] = Field(default=None, description="Draft, when set, is the prompt the embedded AI answers first; its output is folded into one of Args before the tool runs, so the model writes the content and the tool only delivers it.")
    draft_into: Optional[StrictStr] = Field(default=None, description="DraftInto names the argument the drafted text lands in. Empty means \"brief\".", alias="draftInto")
    enabled: Optional[StrictBool] = Field(default=None, description="Enabled is the admin on/off lever. A NIL pointer reads as ENABLED (absence == on): a legacy/org curriculum that omits the field keeps every step, and only an explicit `enabled: false` (an admin disable) drops a step from the journey. See on() in blueprint.go and the Blueprint.Curriculum() projection.")
    id: Optional[StrictStr] = Field(default=None, description="ID is the stable slug the whole plane addresses this step by — the value in `deps`, in `next`, in the progress rows, and in the URL of every step route. Renaming it orphans an org's recorded progress for this step.")
    section: Optional[StrictStr] = Field(default=None, description="Section is the id of the phase this step groups under. A disabled section takes its steps out of the journey with it.")
    signal: Optional[StrictStr] = Field(default=None, description="Signal, when set, names a machine detector (detect.go). When the detector reports the org's real state present, the step auto-marks done.")
    title: Optional[StrictStr] = Field(default=None, description="Title is the one-line quest as a person reads it in the checklist.")
    tool: Optional[StrictStr] = Field(default=None, description="Tool, when set, names the MCP tool the Business AI runs for \"do it for me\". A step with no tool can only be completed by a person; it is the field the `automatable` flag on every projection of this step is derived from.")
    __properties: ClassVar[List[str]] = ["args", "deps", "detail", "draft", "draftInto", "enabled", "id", "section", "signal", "title", "tool"]

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of JourneyStep from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of JourneyStep from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "args": obj.get("args"),
            "deps": obj.get("deps"),
            "detail": obj.get("detail"),
            "draft": obj.get("draft"),
            "draftInto": obj.get("draftInto"),
            "enabled": obj.get("enabled"),
            "id": obj.get("id"),
            "section": obj.get("section"),
            "signal": obj.get("signal"),
            "title": obj.get("title"),
            "tool": obj.get("tool")
        })
        return _obj


