# coding: utf-8

"""
    Hanzo Cloud API

    The Hanzo Cloud API as a customer calls it: every operation under /v1/ except the operator's admin product, relay doors, legacy spellings and capabilities still reached by flag. Tagged by product: the first path segment after /v1/.

    The version of the OpenAPI document: v1
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictStr
from typing import Any, ClassVar, Dict, List, Optional
from hanzoai.cloud.models.endpoint_view import EndpointView
from typing import Optional, Set
from typing_extensions import Self

class SyncView(BaseModel):
    """
    SyncView
    """ # noqa: E501
    actor: Optional[StrictStr] = Field(default=None, description="Actor is the identity a reconcile writes AS. It is the loop guard: a change this identity made is one we already have, so it is not synced back.")
    created_at: Optional[StrictStr] = Field(default=None, description="CreatedAt is when the link was first declared, RFC3339 in UTC.", alias="createdAt")
    direction: Optional[StrictStr] = Field(default=None, description="Direction is which way work flows: \"both\", \"pull\" (target ← source), \"push\" (source → target), or \"off\" — which keeps the link declared and moves nothing.")
    id: Optional[StrictStr] = Field(default=None, description="ID is the link's handle, derived from its source and target — which is what makes re-declaring the same pair an update rather than a duplicate.")
    kind: Optional[StrictStr] = Field(default=None, description="Kind is what is being synced. \"git\" today; the field exists so a storage or database link is a value here rather than a second route family.")
    source: Optional[EndpointView] = Field(default=None, description="Source is the side read FROM on a pull.")
    target: Optional[EndpointView] = Field(default=None, description="Target is the side written TO on a push.")
    trigger: Optional[StrictStr] = Field(default=None, description="Trigger is what starts a reconcile: \"webhook\" (the provider tells us), \"poll\" (we ask on a schedule), or \"manual\" (only an explicit call).")
    updated_at: Optional[StrictStr] = Field(default=None, description="UpdatedAt is bumped by every reconcile, so it reads as the LAST-SYNCED time rather than the last edit. Absent until the first one runs.", alias="updatedAt")
    __properties: ClassVar[List[str]] = ["actor", "createdAt", "direction", "id", "kind", "source", "target", "trigger", "updatedAt"]

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of SyncView from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # override the default output from pydantic by calling `to_dict()` of source
        if self.source:
            _dict['source'] = self.source.to_dict()
        # override the default output from pydantic by calling `to_dict()` of target
        if self.target:
            _dict['target'] = self.target.to_dict()
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of SyncView from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "actor": obj.get("actor"),
            "createdAt": obj.get("createdAt"),
            "direction": obj.get("direction"),
            "id": obj.get("id"),
            "kind": obj.get("kind"),
            "source": EndpointView.from_dict(obj["source"]) if obj.get("source") is not None else None,
            "target": EndpointView.from_dict(obj["target"]) if obj.get("target") is not None else None,
            "trigger": obj.get("trigger"),
            "updatedAt": obj.get("updatedAt")
        })
        return _obj


