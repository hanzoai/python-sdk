# coding: utf-8

"""
    Hanzo Cloud API

    The Hanzo Cloud API as a customer calls it: every operation under /v1/ except the operator's admin product, relay routes, legacy spellings and capabilities still reached by flag. Tagged by product: the first path segment after /v1/.

    The version of the OpenAPI document: v1
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictBool, StrictInt, StrictStr
from typing import Any, ClassVar, Dict, List, Optional
from hanzoai.cloud.models.day import Day
from hanzoai.cloud.models.figures import Figures
from hanzoai.cloud.models.reach import Reach
from typing import Optional, Set
from typing_extensions import Self

class Market(BaseModel):
    """
    Market
    """ # noqa: E501
    amm: Optional[StrictBool] = Field(default=None, description="Amm reports whether an automated market maker is deployed on this chain, which is the registry's factory addresses being present and not the indexer having rows. The two disagree in exactly the interesting case: a chain with a factory and nothing traded yet is a live venue with no history, and a chain with neither has no venue at all.")
    coin: Optional[StrictStr] = None
    day: Optional[Day] = None
    factory: Optional[Dict[str, StrictStr]] = Field(default=None, description="Factory is the AMM's factory contracts, by generation, omitted where none is deployed. Addresses come from the registry because that is what the indexer itself ingested from; anything else is a second copy free to drift.")
    figures: Optional[Figures] = Field(default=None, description="Figures is the chain's whole market maker, and Day its most recent active one. Both are absent unless Reach says Read, so a caller cannot mistake a zero this process never received for one the indexer computed. Day is also absent on a chain that has never traded — which reach reports as Read, so the two absences are told apart by the state beside them and never by the gap itself.")
    graph: Optional[StrictStr] = Field(default=None, description="Graph is where this chain's indexer answers, empty where it has none.")
    id: Optional[StrictInt] = Field(default=None, description="ID is the EVM chain id, which is what a wallet must agree with.")
    name: Optional[StrictStr] = None
    reach: Optional[Reach] = Field(default=None, description="Reach is how far the read of this chain's FIGURES got — its own, so one indexer being down describes one row and leaves the others to answer.")
    rpc: Optional[StrictStr] = Field(default=None, description="RPC is the chain's PUBLIC JSON-RPC, empty where the registry names only a route this process happens to have. The registry's own `rpc` field is the INDEXER's route to the node and is sometimes inside its cluster — plain HTTP on a `.svc.cluster.local` name — which is reachable from the indexer, from nothing else, and from no browser. Publishing that as the chain's endpoint hands every caller an address that cannot answer them. See [endpoint].")
    slug: Optional[StrictStr] = Field(default=None, description="Slug is the chain's word in every indexer path — `cchain`, `zoo`. It is the value a caller passes back as `chain`, and it is NOT the chain id: `96369`, `C` and `c-chain` all answer 404 in that position.")
    __properties: ClassVar[List[str]] = ["amm", "coin", "day", "factory", "figures", "graph", "id", "name", "reach", "rpc", "slug"]

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of Market from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # override the default output from pydantic by calling `to_dict()` of day
        if self.day:
            _dict['day'] = self.day.to_dict()
        # override the default output from pydantic by calling `to_dict()` of figures
        if self.figures:
            _dict['figures'] = self.figures.to_dict()
        # override the default output from pydantic by calling `to_dict()` of reach
        if self.reach:
            _dict['reach'] = self.reach.to_dict()
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of Market from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "amm": obj.get("amm"),
            "coin": obj.get("coin"),
            "day": Day.from_dict(obj["day"]) if obj.get("day") is not None else None,
            "factory": obj.get("factory"),
            "figures": Figures.from_dict(obj["figures"]) if obj.get("figures") is not None else None,
            "graph": obj.get("graph"),
            "id": obj.get("id"),
            "name": obj.get("name"),
            "reach": Reach.from_dict(obj["reach"]) if obj.get("reach") is not None else None,
            "rpc": obj.get("rpc"),
            "slug": obj.get("slug")
        })
        return _obj


