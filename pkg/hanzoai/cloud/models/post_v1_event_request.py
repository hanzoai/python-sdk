# coding: utf-8

"""
    Hanzo Cloud API

    Composed from each subsystem's own projection of its router, in the fleet's mount order — every operation below is a route the subsystem that publishes it registered. Tagged by product: the first path segment after /v1/.

    The version of the OpenAPI document: v1
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import json
import pprint
from pydantic import BaseModel, ConfigDict, Field, StrictStr, ValidationError, field_validator
from typing import Any, List, Optional
from hanzoai.cloud.models.capture_batch import CaptureBatch
from hanzoai.cloud.models.event import Event
from hanzoai.cloud.models.insights_body import InsightsBody
from pydantic import StrictStr, Field
from typing import Union, List, Set, Optional, Dict
from typing_extensions import Literal, Self

POSTV1EVENTREQUEST_ONE_OF_SCHEMAS = ["CaptureBatch", "Event", "InsightsBody", "List[Event]"]

class PostV1EventRequest(BaseModel):
    """
    PostV1EventRequest
    """
    # data type: Event
    oneof_schema_1_validator: Optional[Event] = None
    # data type: List[Event]
    oneof_schema_2_validator: Optional[List[Event]] = None
    # data type: CaptureBatch
    oneof_schema_3_validator: Optional[CaptureBatch] = None
    # data type: InsightsBody
    oneof_schema_4_validator: Optional[InsightsBody] = None
    actual_instance: Optional[Union[CaptureBatch, Event, InsightsBody, List[Event]]] = None
    one_of_schemas: Set[str] = { "CaptureBatch", "Event", "InsightsBody", "List[Event]" }

    model_config = ConfigDict(
        validate_assignment=True,
        protected_namespaces=(),
    )


    def __init__(self, *args, **kwargs) -> None:
        if args:
            if len(args) > 1:
                raise ValueError("If a position argument is used, only 1 is allowed to set `actual_instance`")
            if kwargs:
                raise ValueError("If a position argument is used, keyword arguments cannot be used.")
            super().__init__(actual_instance=args[0])
        else:
            super().__init__(**kwargs)

    @field_validator('actual_instance')
    def actual_instance_must_validate_oneof(cls, v):
        instance = PostV1EventRequest.model_construct()
        error_messages = []
        match = 0
        # validate data type: Event
        if not isinstance(v, Event):
            error_messages.append(f"Error! Input type `{type(v)}` is not `Event`")
        else:
            match += 1
        # validate data type: List[Event]
        try:
            instance.oneof_schema_2_validator = v
            match += 1
        except (ValidationError, ValueError) as e:
            error_messages.append(str(e))
        # validate data type: CaptureBatch
        if not isinstance(v, CaptureBatch):
            error_messages.append(f"Error! Input type `{type(v)}` is not `CaptureBatch`")
        else:
            match += 1
        # validate data type: InsightsBody
        if not isinstance(v, InsightsBody):
            error_messages.append(f"Error! Input type `{type(v)}` is not `InsightsBody`")
        else:
            match += 1
        if match > 1:
            # more than 1 match
            raise ValueError("Multiple matches found when setting `actual_instance` in PostV1EventRequest with oneOf schemas: CaptureBatch, Event, InsightsBody, List[Event]. Details: " + ", ".join(error_messages))
        elif match == 0:
            # no match
            raise ValueError("No match found when setting `actual_instance` in PostV1EventRequest with oneOf schemas: CaptureBatch, Event, InsightsBody, List[Event]. Details: " + ", ".join(error_messages))
        else:
            return v

    @classmethod
    def from_dict(cls, obj: Union[str, Dict[str, Any]]) -> Self:
        return cls.from_json(json.dumps(obj))

    @classmethod
    def from_json(cls, json_str: str) -> Self:
        """Returns the object represented by the json string"""
        instance = cls.model_construct()
        error_messages = []
        match = 0

        # deserialize data into Event
        try:
            instance.actual_instance = Event.from_json(json_str)
            match += 1
        except (ValidationError, ValueError) as e:
            error_messages.append(str(e))
        # deserialize data into List[Event]
        try:
            # validation
            instance.oneof_schema_2_validator = json.loads(json_str)
            # assign value to actual_instance
            instance.actual_instance = instance.oneof_schema_2_validator
            match += 1
        except (ValidationError, ValueError) as e:
            error_messages.append(str(e))
        # deserialize data into CaptureBatch
        try:
            instance.actual_instance = CaptureBatch.from_json(json_str)
            match += 1
        except (ValidationError, ValueError) as e:
            error_messages.append(str(e))
        # deserialize data into InsightsBody
        try:
            instance.actual_instance = InsightsBody.from_json(json_str)
            match += 1
        except (ValidationError, ValueError) as e:
            error_messages.append(str(e))

        if match > 1:
            # more than 1 match
            raise ValueError("Multiple matches found when deserializing the JSON string into PostV1EventRequest with oneOf schemas: CaptureBatch, Event, InsightsBody, List[Event]. Details: " + ", ".join(error_messages))
        elif match == 0:
            # no match
            raise ValueError("No match found when deserializing the JSON string into PostV1EventRequest with oneOf schemas: CaptureBatch, Event, InsightsBody, List[Event]. Details: " + ", ".join(error_messages))
        else:
            return instance

    def to_json(self) -> str:
        """Returns the JSON representation of the actual instance"""
        if self.actual_instance is None:
            return "null"

        if hasattr(self.actual_instance, "to_json") and callable(self.actual_instance.to_json):
            return self.actual_instance.to_json()
        else:
            return json.dumps(self.actual_instance)

    def to_dict(self) -> Optional[Union[Dict[str, Any], CaptureBatch, Event, InsightsBody, List[Event]]]:
        """Returns the dict representation of the actual instance"""
        if self.actual_instance is None:
            return None

        if hasattr(self.actual_instance, "to_dict") and callable(self.actual_instance.to_dict):
            return self.actual_instance.to_dict()
        else:
            # primitive type
            return self.actual_instance

    def to_str(self) -> str:
        """Returns the string representation of the actual instance"""
        return pprint.pformat(self.model_dump())


