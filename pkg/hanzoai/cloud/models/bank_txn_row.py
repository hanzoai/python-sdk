# coding: utf-8

"""
    Hanzo Cloud API

    The Hanzo Cloud API as a customer calls it: every operation under /v1/ except the operator's admin product, relay routes, legacy spellings and capabilities still reached by flag. Tagged by product: the first path segment after /v1/.

    The version of the OpenAPI document: v1
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictInt, StrictStr
from typing import Any, ClassVar, Dict, List, Optional
from typing import Optional, Set
from typing_extensions import Self

class BankTxnRow(BaseModel):
    """
    BankTxnRow
    """ # noqa: E501
    amount_cents: Optional[StrictInt] = Field(default=None, description="AmountCents is the size of the movement in whole cents, always POSITIVE — direction carries the sign, so a caller must read both to know which way money went.", alias="amountCents")
    connector: Optional[StrictStr] = Field(default=None, description="Connector names the feed this row arrived on — which bank or processor connection it was synced from. With externalId it is the row's identity, so re-syncing the same statement never books a second copy.")
    currency: Optional[StrictStr] = Field(default=None, description="Currency is the ISO code the bank reported the line in.")
    description: Optional[StrictStr] = Field(default=None, description="Description is the statement memo as the bank wrote it.")
    direction: Optional[StrictStr] = Field(default=None, description="Direction is which way the money moved: an inflow into the account or an outflow from it, from the org's point of view.")
    external_id: Optional[StrictStr] = Field(default=None, description="ExternalID is the bank's OWN id for the line, carried verbatim. It is unique only within its connector.", alias="externalId")
    matched_voucher: Optional[StrictStr] = Field(default=None, description="MatchedVoucher names the ledger voucher this line was reconciled against — the bill it paid, or the settlement it cleared. Absent when nothing matched, which for an inflow is what raises a question.", alias="matchedVoucher")
    merchant: Optional[StrictStr] = Field(default=None, description="Merchant is the counterparty the feed identified, where it did.")
    posted_at: Optional[StrictStr] = Field(default=None, description="PostedAt is the bank's posting date for the line, not when we synced it.", alias="postedAt")
    status: Optional[StrictStr] = Field(default=None, description="Status is where the line got to: posted (an outflow booked straight to an expense), settled (an outflow that paid down a scanned bill), reconciled (an inflow that cleared a pending settlement), transfer (a move between the org's own accounts, recorded but with no effect on the books), or unmatched (an inflow nobody could place, which is waiting on a human answer).")
    __properties: ClassVar[List[str]] = ["amountCents", "connector", "currency", "description", "direction", "externalId", "matchedVoucher", "merchant", "postedAt", "status"]

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of BankTxnRow from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of BankTxnRow from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "amountCents": obj.get("amountCents"),
            "connector": obj.get("connector"),
            "currency": obj.get("currency"),
            "description": obj.get("description"),
            "direction": obj.get("direction"),
            "externalId": obj.get("externalId"),
            "matchedVoucher": obj.get("matchedVoucher"),
            "merchant": obj.get("merchant"),
            "postedAt": obj.get("postedAt"),
            "status": obj.get("status")
        })
        return _obj


