# coding: utf-8

"""
    Hanzo Cloud API

    The Hanzo Cloud API as a customer calls it: every operation under /v1/ except the operator's admin product, relay doors, legacy spellings and capabilities still reached by flag. Tagged by product: the first path segment after /v1/.

    The version of the OpenAPI document: v1
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictInt, StrictStr
from typing import Any, ClassVar, Dict, List, Optional
from typing import Optional, Set
from typing_extensions import Self

class CalendarPost(BaseModel):
    """
    CalendarPost
    """ # noqa: E501
    body: Optional[StrictStr] = Field(default=None, description="Body is the post text. Required.")
    channel: Optional[StrictStr] = Field(default=None, description="Channel is the target network: x, facebook, instagram, linkedin, tiktok, youtube or threads. Required — a post must name where it goes.")
    created_at: Optional[StrictInt] = Field(default=None, description="CreatedAt is unix seconds when the post was added, server-assigned and never rewritten.", alias="createdAt")
    error: Optional[StrictStr] = Field(default=None, description="Error is the exact reason the last publish attempt failed — the honest record behind a \"failed\" status, never a faked success.")
    id: Optional[StrictStr] = Field(default=None, description="ID is the server-assigned post id (\"cal_\" + 128 random bits).")
    published_at: Optional[StrictInt] = Field(default=None, description="PublishedAt is when the publish succeeded; 0 until it does.", alias="publishedAt")
    scheduled_at: Optional[StrictInt] = Field(default=None, description="ScheduledAt is the unix publish time; 0 leaves the post a draft, and any value makes it \"scheduled\" for the durable sweep to pick up.", alias="scheduledAt")
    status: Optional[StrictStr] = Field(default=None, description="Status is draft, scheduled, published, failed or canceled. Server-owned.")
    title: Optional[StrictStr] = Field(default=None, description="Title is the post's internal label, capped at 1024 bytes.")
    updated_at: Optional[StrictInt] = Field(default=None, description="UpdatedAt is unix seconds of the last write, server-assigned. The durable sweep writes too — claiming a due post, publishing it and recording a failure each bump it — so this moves without anyone editing the post.", alias="updatedAt")
    __properties: ClassVar[List[str]] = ["body", "channel", "createdAt", "error", "id", "publishedAt", "scheduledAt", "status", "title", "updatedAt"]

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of CalendarPost from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of CalendarPost from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "body": obj.get("body"),
            "channel": obj.get("channel"),
            "createdAt": obj.get("createdAt"),
            "error": obj.get("error"),
            "id": obj.get("id"),
            "publishedAt": obj.get("publishedAt"),
            "scheduledAt": obj.get("scheduledAt"),
            "status": obj.get("status"),
            "title": obj.get("title"),
            "updatedAt": obj.get("updatedAt")
        })
        return _obj


