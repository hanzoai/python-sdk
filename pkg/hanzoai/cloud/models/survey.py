# coding: utf-8

"""
    Hanzo Cloud API

    The Hanzo Cloud API as a customer calls it: every operation under /v1/ except the operator's admin product, relay routes, legacy spellings and capabilities still reached by flag. Tagged by product: the first path segment after /v1/.

    The version of the OpenAPI document: v1
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictStr
from typing import Any, ClassVar, Dict, List, Optional
from hanzoai.cloud.models.precompile import Precompile
from hanzoai.cloud.models.reach import Reach
from typing import Optional, Set
from typing_extensions import Self

class Survey(BaseModel):
    """
    Survey
    """ # noqa: E501
    carries: Optional[List[Precompile]] = Field(default=None, description="Carries is all four where the node answered and `null` where it did not. It is never a short list: a partial read reporting three would let a reader count them and conclude the chain is missing one.")
    chain: Optional[StrictStr] = None
    reach: Optional[Reach] = None
    rpc: Optional[StrictStr] = Field(default=None, description="RPC is the endpoint that was asked, so an answer names where it came from.")
    __properties: ClassVar[List[str]] = ["carries", "chain", "reach", "rpc"]

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of Survey from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # override the default output from pydantic by calling `to_dict()` of each item in carries (list)
        _items = []
        if self.carries:
            for _item_carries in self.carries:
                if _item_carries:
                    _items.append(_item_carries.to_dict())
            _dict['carries'] = _items
        # override the default output from pydantic by calling `to_dict()` of reach
        if self.reach:
            _dict['reach'] = self.reach.to_dict()
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of Survey from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "carries": [Precompile.from_dict(_item) for _item in obj["carries"]] if obj.get("carries") is not None else None,
            "chain": obj.get("chain"),
            "reach": Reach.from_dict(obj["reach"]) if obj.get("reach") is not None else None,
            "rpc": obj.get("rpc")
        })
        return _obj


