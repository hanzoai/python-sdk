# coding: utf-8

"""
    Hanzo Cloud API

    The Hanzo Cloud API as a customer calls it: every operation under /v1/ except the operator's admin product, relay routes, legacy spellings and capabilities still reached by flag. Tagged by product: the first path segment after /v1/.

    The version of the OpenAPI document: v1
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictBool, StrictInt, StrictStr
from typing import Any, ClassVar, Dict, List, Optional
from typing import Optional, Set
from typing_extensions import Self

class Charge(BaseModel):
    """
    Charge
    """ # noqa: E501
    amount_cents: Optional[StrictInt] = Field(default=None, description="AmountCents is what this line costs.", alias="amountCents")
    as_of: Optional[StrictStr] = Field(default=None, description="AsOf is when a pass-through amount was last checked against its source.", alias="asOf")
    code: Optional[StrictStr] = Field(default=None, description="Code names the line so a caller can branch on it without reading prose.")
    label: Optional[StrictStr] = Field(default=None, description="Label is what the payer sees on the invoice.")
    pass_through: Optional[StrictBool] = Field(default=None, description="PassThrough marks money we collect and remit rather than keep — the state's fee is not our revenue, and a quote that hides that is a quote that reads as a bigger margin than it is.", alias="passThrough")
    recurring: Optional[StrictStr] = Field(default=None, description="Recurring marks a line that repeats. An agent of record is billed every year for as long as the entity stands, and a payer agreeing to a one-time total is not agreeing to that.")
    source: Optional[StrictStr] = Field(default=None, description="Source names who publishes this amount, for a line we merely pass through. Empty for a price of ours, which needs no external authority.")
    stale: Optional[StrictBool] = Field(default=None, description="Stale reports that AsOf is older than the review window — the figure may have moved and nobody has looked. It does not block; it tells.")
    __properties: ClassVar[List[str]] = ["amountCents", "asOf", "code", "label", "passThrough", "recurring", "source", "stale"]

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of Charge from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of Charge from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "amountCents": obj.get("amountCents"),
            "asOf": obj.get("asOf"),
            "code": obj.get("code"),
            "label": obj.get("label"),
            "passThrough": obj.get("passThrough"),
            "recurring": obj.get("recurring"),
            "source": obj.get("source"),
            "stale": obj.get("stale")
        })
        return _obj


