# coding: utf-8

"""
    Hanzo Cloud API

    The Hanzo Cloud API as a customer calls it: every operation under /v1/ except the operator's admin product, relay doors, legacy spellings and capabilities still reached by flag. Tagged by product: the first path segment after /v1/.

    The version of the OpenAPI document: v1
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictStr
from typing import Any, ClassVar, Dict, List, Optional
from hanzoai.cloud.models.build_turn import BuildTurn
from typing import Optional, Set
from typing_extensions import Self

class BuildView(BaseModel):
    """
    BuildView
    """ # noqa: E501
    agent: Optional[StrictStr] = Field(default=None, description="Agent is the label the surface that did the work calls itself by.")
    ended_at: Optional[StrictStr] = Field(default=None, description="EndedAt is when it finished, same format. Empty means it has not — the build is still going.", alias="endedAt")
    model: Optional[StrictStr] = Field(default=None, description="Model is the model that did the work, taken from the FIRST turn whose body names one — a transcript states it, this route does not resolve it. Empty when no turn said.")
    org: Optional[StrictStr] = Field(default=None, description="Org is the org that published this build, echoed from the URL. It is part of the build's public ADDRESS and not a tenant key — this route is anonymous, and the only rows it can reach are ones an author explicitly published.")
    project: Optional[StrictStr] = Field(default=None, description="Project is the product's slug, the other half of that address.")
    repo: Optional[StrictStr] = Field(default=None, description="Repo is the repository the work was done in, as the session reported it.")
    session: Optional[StrictStr] = Field(default=None, description="Session is the id of the agent session this story IS — the same value a produced commit carries in its `Hanzo-Session:` trailer, which is what ties the repository's history to this page.")
    started_at: Optional[StrictStr] = Field(default=None, description="StartedAt is when the session opened, RFC 3339 in UTC.", alias="startedAt")
    status: Optional[StrictStr] = Field(default=None, description="Status is the session's own: running, paused, done or error. A build can be read while it is still being written, so this is not always terminal — and an `error` build is still a readable story, not a missing page.")
    title: Optional[StrictStr] = Field(default=None, description="Title is the human line the session was opened or renamed with. Empty when nobody gave it one.")
    turns: Optional[List[BuildTurn]] = Field(default=None, description="Turns is the whole transcript, oldest first, capped at 1000: a published build is a story to read down, not an archive to page.")
    verify: Optional[StrictStr] = Field(default=None, description="Verify is the exact command that re-derives every commit binding below straight from git, so nothing here has to be taken on trust.")
    __properties: ClassVar[List[str]] = ["agent", "endedAt", "model", "org", "project", "repo", "session", "startedAt", "status", "title", "turns", "verify"]

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of BuildView from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # override the default output from pydantic by calling `to_dict()` of each item in turns (list)
        _items = []
        if self.turns:
            for _item_turns in self.turns:
                if _item_turns:
                    _items.append(_item_turns.to_dict())
            _dict['turns'] = _items
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of BuildView from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "agent": obj.get("agent"),
            "endedAt": obj.get("endedAt"),
            "model": obj.get("model"),
            "org": obj.get("org"),
            "project": obj.get("project"),
            "repo": obj.get("repo"),
            "session": obj.get("session"),
            "startedAt": obj.get("startedAt"),
            "status": obj.get("status"),
            "title": obj.get("title"),
            "turns": [BuildTurn.from_dict(_item) for _item in obj["turns"]] if obj.get("turns") is not None else None,
            "verify": obj.get("verify")
        })
        return _obj


