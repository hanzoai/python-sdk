# coding: utf-8

"""
    Hanzo Cloud — Unified API (V8 · Open Edition)

    The single unified OpenAPI surface for ALL Hanzo services, aggregated from the per-service specs. Every route is https://api.hanzo.ai/v1/<service>/*. Grouped by the canonical domains in capabilities.yaml (the ONE registry). cloud/openapi.yaml is hanzoai/cloud's own woven document, copied verbatim by sync.py, and it wins wherever it and a hand-written spec describe the same route. Regenerated by merge.py — edit the per-service <svc>/openapi.yaml + capabilities.yaml, not this file.

    The version of the OpenAPI document: 8.0.0
    Contact: support@hanzo.ai
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from datetime import datetime
from pydantic import BaseModel, ConfigDict, Field, StrictBool, StrictInt, StrictStr
from typing import Any, ClassVar, Dict, List, Optional
from hanzoai.cloud.models.cloud_usage import CloudUsage
from typing import Optional, Set
from typing_extensions import Self

class CloudStatus(BaseModel):
    """
    CloudStatus
    """ # noqa: E501
    addr: Optional[StrictStr] = Field(default=None, description="Addr is the socket or address serving it.")
    disabled: Optional[StrictBool] = Field(default=None, description="Disabled is true when Unload stopped it deliberately, as opposed to it having crashed. Both answer 503, so without this an operator cannot tell a maintenance window from an outage — and would page for the former.")
    name: Optional[StrictStr] = None
    pid: Optional[StrictInt] = Field(default=None, description="PID is the child process, or 0 when this host did not start it.")
    prefix: Optional[StrictStr] = Field(default=None, description="Prefix is the FIRST subtree this plugin answers — the one a log line names it by. Prefixes is every subtree, and a plugin may own several. Reporting only the first would understate the blast radius of taking this plugin down, which is the question a fleet view exists to answer.")
    prefixes: Optional[List[StrictStr]] = None
    reloads: Optional[StrictInt] = Field(default=None, description="Reloads counts successful swaps since Load. A climbing number on one host and not its peers is the signal that a rollout is uneven.")
    restarts: Optional[StrictInt] = Field(default=None, description="Restarts counts times the supervisor brought this plugin back after it died on its own. Distinct from Reloads, which are deliberate: a nonzero Restarts is a plugin crashing, and a climbing one is a crash loop.")
    running: Optional[StrictBool] = Field(default=None, description="Running is false after Unload, or after a child exited and no Reload has replaced it. Its routes stay registered and answer 503, so a false here is the difference between \"not deployed\" and \"deployed but down\".")
    since: Optional[datetime] = Field(default=None, description="Since is when the CURRENT instance started — it resets on Reload, so it reports the age of what is running, not of the mount.")
    source: Optional[StrictStr] = Field(default=None, description="Source is where the binary came from: \"embedded\", \"path\", \"url\", or \"remote\" for an instance this host did not start.")
    usage: Optional[CloudUsage] = Field(default=None, description="Usage is what this plugin costs right now, read from the kernel.")
    version: Optional[StrictStr] = Field(default=None, description="Version is the artifact's SHA-256 when it was installed from a URL — the only version identifier that cannot drift from the bits actually running, since it IS the bits. Empty for the other sources.")
    __properties: ClassVar[List[str]] = ["addr", "disabled", "name", "pid", "prefix", "prefixes", "reloads", "restarts", "running", "since", "source", "usage", "version"]

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of CloudStatus from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # override the default output from pydantic by calling `to_dict()` of usage
        if self.usage:
            _dict['usage'] = self.usage.to_dict()
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of CloudStatus from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "addr": obj.get("addr"),
            "disabled": obj.get("disabled"),
            "name": obj.get("name"),
            "pid": obj.get("pid"),
            "prefix": obj.get("prefix"),
            "prefixes": obj.get("prefixes"),
            "reloads": obj.get("reloads"),
            "restarts": obj.get("restarts"),
            "running": obj.get("running"),
            "since": obj.get("since"),
            "source": obj.get("source"),
            "usage": CloudUsage.from_dict(obj["usage"]) if obj.get("usage") is not None else None,
            "version": obj.get("version")
        })
        return _obj


