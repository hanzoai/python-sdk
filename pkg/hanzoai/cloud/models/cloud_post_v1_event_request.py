# coding: utf-8

"""
    Hanzo Cloud — Unified API (V8 · Open Edition)

    The single unified OpenAPI surface for ALL Hanzo services, aggregated from the per-service specs. Every route is https://api.hanzo.ai/v1/<service>/*. Grouped by the canonical domains in capabilities.yaml (the ONE registry). cloud/openapi.yaml is hanzoai/cloud's own woven document, copied verbatim by sync.py, and it wins wherever it and a hand-written spec describe the same route. Regenerated by merge.py — edit the per-service <svc>/openapi.yaml + capabilities.yaml, not this file.

    The version of the OpenAPI document: 8.0.0
    Contact: support@hanzo.ai
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import json
import pprint
from pydantic import BaseModel, ConfigDict, Field, StrictStr, ValidationError, field_validator
from typing import Any, List, Optional
from hanzoai.cloud.models.cloud_capture_batch import CloudCaptureBatch
from hanzoai.cloud.models.cloud_event import CloudEvent
from hanzoai.cloud.models.cloud_insights_body import CloudInsightsBody
from pydantic import StrictStr, Field
from typing import Union, List, Set, Optional, Dict
from typing_extensions import Literal, Self

CLOUDPOSTV1EVENTREQUEST_ONE_OF_SCHEMAS = ["CloudCaptureBatch", "CloudEvent", "CloudInsightsBody", "List[CloudEvent]"]

class CloudPostV1EventRequest(BaseModel):
    """
    CloudPostV1EventRequest
    """
    # data type: CloudEvent
    oneof_schema_1_validator: Optional[CloudEvent] = None
    # data type: List[CloudEvent]
    oneof_schema_2_validator: Optional[List[CloudEvent]] = None
    # data type: CloudCaptureBatch
    oneof_schema_3_validator: Optional[CloudCaptureBatch] = None
    # data type: CloudInsightsBody
    oneof_schema_4_validator: Optional[CloudInsightsBody] = None
    actual_instance: Optional[Union[CloudCaptureBatch, CloudEvent, CloudInsightsBody, List[CloudEvent]]] = None
    one_of_schemas: Set[str] = { "CloudCaptureBatch", "CloudEvent", "CloudInsightsBody", "List[CloudEvent]" }

    model_config = ConfigDict(
        validate_assignment=True,
        protected_namespaces=(),
    )


    def __init__(self, *args, **kwargs) -> None:
        if args:
            if len(args) > 1:
                raise ValueError("If a position argument is used, only 1 is allowed to set `actual_instance`")
            if kwargs:
                raise ValueError("If a position argument is used, keyword arguments cannot be used.")
            super().__init__(actual_instance=args[0])
        else:
            super().__init__(**kwargs)

    @field_validator('actual_instance')
    def actual_instance_must_validate_oneof(cls, v):
        instance = CloudPostV1EventRequest.model_construct()
        error_messages = []
        match = 0
        # validate data type: CloudEvent
        if not isinstance(v, CloudEvent):
            error_messages.append(f"Error! Input type `{type(v)}` is not `CloudEvent`")
        else:
            match += 1
        # validate data type: List[CloudEvent]
        try:
            instance.oneof_schema_2_validator = v
            match += 1
        except (ValidationError, ValueError) as e:
            error_messages.append(str(e))
        # validate data type: CloudCaptureBatch
        if not isinstance(v, CloudCaptureBatch):
            error_messages.append(f"Error! Input type `{type(v)}` is not `CloudCaptureBatch`")
        else:
            match += 1
        # validate data type: CloudInsightsBody
        if not isinstance(v, CloudInsightsBody):
            error_messages.append(f"Error! Input type `{type(v)}` is not `CloudInsightsBody`")
        else:
            match += 1
        if match > 1:
            # more than 1 match
            raise ValueError("Multiple matches found when setting `actual_instance` in CloudPostV1EventRequest with oneOf schemas: CloudCaptureBatch, CloudEvent, CloudInsightsBody, List[CloudEvent]. Details: " + ", ".join(error_messages))
        elif match == 0:
            # no match
            raise ValueError("No match found when setting `actual_instance` in CloudPostV1EventRequest with oneOf schemas: CloudCaptureBatch, CloudEvent, CloudInsightsBody, List[CloudEvent]. Details: " + ", ".join(error_messages))
        else:
            return v

    @classmethod
    def from_dict(cls, obj: Union[str, Dict[str, Any]]) -> Self:
        return cls.from_json(json.dumps(obj))

    @classmethod
    def from_json(cls, json_str: str) -> Self:
        """Returns the object represented by the json string"""
        instance = cls.model_construct()
        error_messages = []
        match = 0

        # deserialize data into CloudEvent
        try:
            instance.actual_instance = CloudEvent.from_json(json_str)
            match += 1
        except (ValidationError, ValueError) as e:
            error_messages.append(str(e))
        # deserialize data into List[CloudEvent]
        try:
            # validation
            instance.oneof_schema_2_validator = json.loads(json_str)
            # assign value to actual_instance
            instance.actual_instance = instance.oneof_schema_2_validator
            match += 1
        except (ValidationError, ValueError) as e:
            error_messages.append(str(e))
        # deserialize data into CloudCaptureBatch
        try:
            instance.actual_instance = CloudCaptureBatch.from_json(json_str)
            match += 1
        except (ValidationError, ValueError) as e:
            error_messages.append(str(e))
        # deserialize data into CloudInsightsBody
        try:
            instance.actual_instance = CloudInsightsBody.from_json(json_str)
            match += 1
        except (ValidationError, ValueError) as e:
            error_messages.append(str(e))

        if match > 1:
            # more than 1 match
            raise ValueError("Multiple matches found when deserializing the JSON string into CloudPostV1EventRequest with oneOf schemas: CloudCaptureBatch, CloudEvent, CloudInsightsBody, List[CloudEvent]. Details: " + ", ".join(error_messages))
        elif match == 0:
            # no match
            raise ValueError("No match found when deserializing the JSON string into CloudPostV1EventRequest with oneOf schemas: CloudCaptureBatch, CloudEvent, CloudInsightsBody, List[CloudEvent]. Details: " + ", ".join(error_messages))
        else:
            return instance

    def to_json(self) -> str:
        """Returns the JSON representation of the actual instance"""
        if self.actual_instance is None:
            return "null"

        if hasattr(self.actual_instance, "to_json") and callable(self.actual_instance.to_json):
            return self.actual_instance.to_json()
        else:
            return json.dumps(self.actual_instance)

    def to_dict(self) -> Optional[Union[Dict[str, Any], CloudCaptureBatch, CloudEvent, CloudInsightsBody, List[CloudEvent]]]:
        """Returns the dict representation of the actual instance"""
        if self.actual_instance is None:
            return None

        if hasattr(self.actual_instance, "to_dict") and callable(self.actual_instance.to_dict):
            return self.actual_instance.to_dict()
        else:
            # primitive type
            return self.actual_instance

    def to_str(self) -> str:
        """Returns the string representation of the actual instance"""
        return pprint.pformat(self.model_dump())


