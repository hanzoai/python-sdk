# coding: utf-8

"""
    Hanzo Cloud API

    The Hanzo Cloud API as a customer calls it: every operation under /v1/ except the operator's admin product, relay routes, legacy spellings and capabilities still reached by flag. Tagged by product: the first path segment after /v1/.

    The version of the OpenAPI document: v1
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictInt, StrictStr
from typing import Any, ClassVar, Dict, List, Optional
from typing import Optional, Set
from typing_extensions import Self

class Number(BaseModel):
    """
    Number
    """ # noqa: E501
    capable: Optional[List[StrictStr]] = Field(default=None, description="Capable is what the number can carry: any of \"voice\", \"sms\", \"mms\", \"fax\". A number missing \"sms\" cannot send one no matter what this platform does.")
    country: Optional[StrictStr] = Field(default=None, description="Country is the ISO 3166-1 alpha-2 code the number is issued under. Numbering is national, so this is what makes a search answerable at all.")
    currency: Optional[StrictStr] = Field(default=None, description="Currency is the ISO 4217 code Monthly is denominated in. Without it the number beside it means nothing, so the two are always read together.")
    e164: Optional[StrictStr] = Field(default=None, description="E164 is the number in E.164: a leading + and digits only, no spaces or dashes. That is what a carrier accepts and what a search result must be bought by.")
    id: Optional[StrictStr] = Field(default=None, description="ID is the carrier's handle for the number, and the id every route here addresses it by. It is not the number itself — see E164.")
    monthly: Optional[StrictInt] = Field(default=None, description="Monthly is the recurring rental in the MINOR unit of Currency (cents for USD), exactly as the carrier quoted it. It is a price, not a charge: nothing is billed by this field.")
    org: Optional[StrictStr] = Field(default=None, description="Org is the tenant holding the number. A search result carries none — nobody holds it yet — which is how an available number is told from a held one.")
    type: Optional[StrictStr] = Field(default=None, description="Type is what kind of number it is: \"local\", \"national\", \"tollfree\" or \"mobile\". It decides both price and what a carrier will let it originate.")
    __properties: ClassVar[List[str]] = ["capable", "country", "currency", "e164", "id", "monthly", "org", "type"]

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of Number from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of Number from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "capable": obj.get("capable"),
            "country": obj.get("country"),
            "currency": obj.get("currency"),
            "e164": obj.get("e164"),
            "id": obj.get("id"),
            "monthly": obj.get("monthly"),
            "org": obj.get("org"),
            "type": obj.get("type")
        })
        return _obj


