# coding: utf-8

"""
    Hanzo Cloud API

    Composed from each subsystem's own projection of its router, in the fleet's mount order — every operation below is a route the subsystem that publishes it registered. Tagged by product: the first path segment after /v1/.

    The version of the OpenAPI document: v1
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictInt, StrictStr
from typing import Any, ClassVar, Dict, List, Optional
from typing import Optional, Set
from typing_extensions import Self

class GrantIn(BaseModel):
    """
    GrantIn
    """ # noqa: E501
    amount_cents: Optional[StrictInt] = Field(default=None, description="AmountCents is the credit, in whole cents. Must be positive and within the per-grant cap.", alias="amountCents")
    currency: Optional[StrictStr] = Field(default=None, description="Currency is the ISO code, lower-cased. Empty means usd.")
    org: Optional[StrictStr] = Field(default=None, description="Org is the tenant to credit. Required.")
    reason: Optional[StrictStr] = Field(default=None, description="Reason is the operator's justification, recorded on the audit row.")
    source: Optional[StrictStr] = Field(default=None, description="Source is the money bucket: \"trial\" (default) for a non-cash comp that is never refundable, or \"prepaid\" for real money. Anything unknown falls back to trial.")
    user: Optional[StrictStr] = Field(default=None, description="User optionally names a MEMBER to credit, by bare IAM username. Empty credits the org. Which of the two the money actually lands on is decided by account.Payer, not here: a pooled org keeps one balance whatever is named.")
    __properties: ClassVar[List[str]] = ["amountCents", "currency", "org", "reason", "source", "user"]

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of GrantIn from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of GrantIn from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "amountCents": obj.get("amountCents"),
            "currency": obj.get("currency"),
            "org": obj.get("org"),
            "reason": obj.get("reason"),
            "source": obj.get("source"),
            "user": obj.get("user")
        })
        return _obj


