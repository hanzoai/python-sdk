# coding: utf-8

"""
    Hanzo Cloud API

    The Hanzo Cloud API as a customer calls it: every operation under /v1/ except the operator's admin product, relay routes, legacy spellings and capabilities still reached by flag. Tagged by product: the first path segment after /v1/.

    The version of the OpenAPI document: v1
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictInt, StrictStr
from typing import Any, ClassVar, Dict, List, Optional
from typing import Optional, Set
from typing_extensions import Self

class TodoProject(BaseModel):
    """
    TodoProject
    """ # noqa: E501
    created_at: Optional[StrictInt] = Field(default=None, description="CreatedAt is when the board was created, in unix seconds. 0 on a forge board for the same reason Description is absent.", alias="createdAt")
    description: Optional[StrictStr] = Field(default=None, description="Description is whatever an index board was created with. Absent on a forge board: this projection takes the repository's name and nothing else about the repository.")
    id: Optional[StrictStr] = Field(default=None, description="ID is the board's opaque handle, and it is NOT how you address it — Key is. Its shape says which source answered: a forge board's is the repository's full name (\"hanzoai/cloud\"), an index board's a minted \"prj_\" id.")
    key: Optional[StrictStr] = Field(default=None, description="Key addresses the board everywhere else — /v1/todo/projects/<key>/issues — and prefixes every issue identifier on it. An index board's key is 2-8 uppercase alphanumerics starting with a letter (\"ENG\", \"OPS2\") and is matched case-insensitively; a forge board's is the repository name as the forge spells it.")
    name: Optional[StrictStr] = Field(default=None, description="Name is the board's display name. For a forge board it is the repository name, so it equals Key; an index board carries its own.")
    org: Optional[StrictStr] = Field(default=None, description="Org is the IAM org the board belongs to, taken from the validated principal and never from the request. Every board a caller can see is in it.")
    updated_at: Optional[StrictInt] = Field(default=None, description="UpdatedAt is when the board record last changed, in unix seconds — the BOARD, not the work on it, so filing an issue does not move it. 0 on a forge board.", alias="updatedAt")
    __properties: ClassVar[List[str]] = ["createdAt", "description", "id", "key", "name", "org", "updatedAt"]

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of TodoProject from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of TodoProject from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "createdAt": obj.get("createdAt"),
            "description": obj.get("description"),
            "id": obj.get("id"),
            "key": obj.get("key"),
            "name": obj.get("name"),
            "org": obj.get("org"),
            "updatedAt": obj.get("updatedAt")
        })
        return _obj


