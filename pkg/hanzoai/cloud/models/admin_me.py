# coding: utf-8

"""
    Hanzo Cloud API

    Composed from each subsystem's own projection of its router, in the fleet's mount order — every operation below is a route the subsystem that publishes it registered. Tagged by product: the first path segment after /v1/.

    The version of the OpenAPI document: v1
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictBool, StrictStr
from typing import Any, ClassVar, Dict, List, Optional
from typing import Optional, Set
from typing_extensions import Self

class AdminMe(BaseModel):
    """
    AdminMe
    """ # noqa: E501
    display_name: Optional[StrictStr] = Field(default=None, alias="displayName")
    email: Optional[StrictStr] = None
    is_super_admin: Optional[StrictBool] = Field(default=None, alias="isSuperAdmin")
    is_white_label: Optional[StrictBool] = Field(default=None, description="IsWhiteLabel marks the admitted NON-super tier: an admin of an enabled white-label tenant org. Mutually exclusive with IsSuperAdmin (the gate lets exactly one tier through). The operator SPA reads it to render the SUBTREE cockpit — the fleet god-view nav (finance/revenue/metrics/o11y/providers) is hidden — while a super sees the whole fleet.", alias="isWhiteLabel")
    name: Optional[StrictStr] = None
    owner: Optional[StrictStr] = None
    scope_orgs: Optional[List[StrictStr]] = Field(default=None, description="ScopeOrgs is the caller's visible tenant window: empty for a SuperAdmin (means ALL orgs), or the WL tenant's own subtree (today the singleton {org}). The SPA threads it through the faceting/drill-down layer so a WL tenant can never widen a filter past their subtree.", alias="scopeOrgs")
    __properties: ClassVar[List[str]] = ["displayName", "email", "isSuperAdmin", "isWhiteLabel", "name", "owner", "scopeOrgs"]

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of AdminMe from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of AdminMe from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "displayName": obj.get("displayName"),
            "email": obj.get("email"),
            "isSuperAdmin": obj.get("isSuperAdmin"),
            "isWhiteLabel": obj.get("isWhiteLabel"),
            "name": obj.get("name"),
            "owner": obj.get("owner"),
            "scopeOrgs": obj.get("scopeOrgs")
        })
        return _obj


