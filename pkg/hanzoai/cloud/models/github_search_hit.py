# coding: utf-8

"""
    Hanzo Cloud API

    The Hanzo Cloud API as a customer calls it: every operation under /v1/ except the operator's admin product, relay routes, legacy spellings and capabilities still reached by flag. Tagged by product: the first path segment after /v1/.

    The version of the OpenAPI document: v1
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictBool, StrictInt, StrictStr
from typing import Any, ClassVar, Dict, List, Optional
from typing import Optional, Set
from typing_extensions import Self

class GithubSearchHit(BaseModel):
    """
    GithubSearchHit
    """ # noqa: E501
    clone_url: Optional[StrictStr] = Field(default=None, description="CloneURL is the repository's https git remote.")
    default_branch: Optional[StrictStr] = Field(default=None, description="DefaultBranch is the branch a clone checks out.")
    description: Optional[StrictStr] = Field(default=None, description="Description is the blurb the repository's owner wrote. Empty when it has none.")
    full_name: Optional[StrictStr] = Field(default=None, description="FullName is the repository's \"owner/repo\" on GitHub. Finding it here does NOT make it forkable: githubFork takes a repo the org's installation was granted, and a hit from the public index usually is not one.")
    html_url: Optional[StrictStr] = Field(default=None, description="HTMLURL is the repository's page on github.com.")
    language: Optional[StrictStr] = Field(default=None, description="Language is the primary language GitHub detected from the file mix (\"Go\", \"TypeScript\"). Empty when GitHub attributes none.")
    private: Optional[StrictBool] = Field(default=None, description="Private is GitHub's visibility flag, passed through. This op reads the public index — the org's token only charges the rate limit to the installation — so it is false for everything a search can reach.")
    stars: Optional[StrictInt] = Field(default=None, description="Stars is GitHub's stargazers_count as the SEARCH INDEX held it when the query ran — a snapshot, not a live count off the repository.")
    __properties: ClassVar[List[str]] = ["clone_url", "default_branch", "description", "full_name", "html_url", "language", "private", "stars"]

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of GithubSearchHit from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of GithubSearchHit from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "clone_url": obj.get("clone_url"),
            "default_branch": obj.get("default_branch"),
            "description": obj.get("description"),
            "full_name": obj.get("full_name"),
            "html_url": obj.get("html_url"),
            "language": obj.get("language"),
            "private": obj.get("private"),
            "stars": obj.get("stars")
        })
        return _obj


