# coding: utf-8

"""
    Hanzo Cloud API

    The Hanzo Cloud API as a customer calls it: every operation under /v1/ except the operator's admin product, relay doors, legacy spellings and capabilities still reached by flag. Tagged by product: the first path segment after /v1/.

    The version of the OpenAPI document: v1
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictInt, StrictStr
from typing import Any, ClassVar, Dict, List, Optional
from typing import Optional, Set
from typing_extensions import Self

class ExecRequest(BaseModel):
    """
    ExecRequest
    """ # noqa: E501
    argv: Optional[List[StrictStr]] = Field(default=None, description="Argv is the command as an argument vector, which is the honest form: it cannot be word-split by accident. Send this OR Command, not both.")
    command: Optional[StrictStr] = Field(default=None, description="Command is a shell line, for a caller that holds one. It is a convenience over Argv and is the only input that ever reaches a shell.")
    dir: Optional[StrictStr] = Field(default=None, description="Dir is the working directory to run in. Empty runs in the class's own workdir — /mnt/data for exec, /work for dev.")
    id: Optional[StrictStr] = Field(default=None, description="ID is the sandbox to run in, from the path.")
    stdin: Optional[StrictStr] = Field(default=None, description="Stdin is fed to the command on its standard input.")
    timeout_sec: Optional[StrictInt] = Field(default=None, description="TimeoutSec bounds the run in seconds. Zero takes the default.", alias="timeoutSec")
    __properties: ClassVar[List[str]] = ["argv", "command", "dir", "id", "stdin", "timeoutSec"]

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of ExecRequest from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of ExecRequest from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "argv": obj.get("argv"),
            "command": obj.get("command"),
            "dir": obj.get("dir"),
            "id": obj.get("id"),
            "stdin": obj.get("stdin"),
            "timeoutSec": obj.get("timeoutSec")
        })
        return _obj


