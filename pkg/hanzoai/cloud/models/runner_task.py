# coding: utf-8

"""
    Hanzo Cloud API

    The Hanzo Cloud API as a customer calls it: every operation under /v1/ except the operator's admin product, relay routes, legacy spellings and capabilities still reached by flag. Tagged by product: the first path segment after /v1/.

    The version of the OpenAPI document: v1
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictInt, StrictStr
from typing import Any, ClassVar, Dict, List, Optional
from hanzoai.cloud.models.runner_context import RunnerContext
from hanzoai.cloud.models.runner_need import RunnerNeed
from hanzoai.cloud.models.runner_pair import RunnerPair
from typing import Optional, Set
from typing_extensions import Self

class RunnerTask(BaseModel):
    """
    RunnerTask
    """ # noqa: E501
    context: Optional[RunnerContext] = Field(default=None, description="Context is what the job evaluates github.* against.")
    id: Optional[StrictInt] = Field(default=None, description="ID identifies this task on every later state and log report.")
    needs: Optional[List[RunnerNeed]] = Field(default=None, description="Needs is what the jobs this one depends on produced.")
    secrets: Optional[List[RunnerPair]] = Field(default=None, description="Secrets are the values masked out of the log and exposed as secrets.*.")
    vars: Optional[List[RunnerPair]] = Field(default=None, description="Vars are the values exposed as vars.*.")
    workflow: Optional[StrictStr] = Field(default=None, description="Workflow is the workflow document verbatim. The forge decides THAT a job runs; act, on the runner, decides what the document means.")
    __properties: ClassVar[List[str]] = ["context", "id", "needs", "secrets", "vars", "workflow"]

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of RunnerTask from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # override the default output from pydantic by calling `to_dict()` of context
        if self.context:
            _dict['context'] = self.context.to_dict()
        # override the default output from pydantic by calling `to_dict()` of each item in needs (list)
        _items = []
        if self.needs:
            for _item_needs in self.needs:
                if _item_needs:
                    _items.append(_item_needs.to_dict())
            _dict['needs'] = _items
        # override the default output from pydantic by calling `to_dict()` of each item in secrets (list)
        _items = []
        if self.secrets:
            for _item_secrets in self.secrets:
                if _item_secrets:
                    _items.append(_item_secrets.to_dict())
            _dict['secrets'] = _items
        # override the default output from pydantic by calling `to_dict()` of each item in vars (list)
        _items = []
        if self.vars:
            for _item_vars in self.vars:
                if _item_vars:
                    _items.append(_item_vars.to_dict())
            _dict['vars'] = _items
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of RunnerTask from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "context": RunnerContext.from_dict(obj["context"]) if obj.get("context") is not None else None,
            "id": obj.get("id"),
            "needs": [RunnerNeed.from_dict(_item) for _item in obj["needs"]] if obj.get("needs") is not None else None,
            "secrets": [RunnerPair.from_dict(_item) for _item in obj["secrets"]] if obj.get("secrets") is not None else None,
            "vars": [RunnerPair.from_dict(_item) for _item in obj["vars"]] if obj.get("vars") is not None else None,
            "workflow": obj.get("workflow")
        })
        return _obj


