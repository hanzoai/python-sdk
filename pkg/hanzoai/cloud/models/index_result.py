# coding: utf-8

"""
    Hanzo Cloud API

    Composed from each subsystem's own projection of its router, in the fleet's mount order — every operation below is a route the subsystem that publishes it registered. Tagged by product: the first path segment after /v1/.

    The version of the OpenAPI document: v1
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictBool, StrictInt, StrictStr
from typing import Any, ClassVar, Dict, List, Optional
from typing import Optional, Set
from typing_extensions import Self

class IndexResult(BaseModel):
    """
    IndexResult
    """ # noqa: E501
    chunks: Optional[StrictInt] = Field(default=None, description="Chunks is how many AST-boundary chunks the repo holds after this pass.")
    files: Optional[StrictInt] = Field(default=None, description="Files is how many files the repo holds after this pass.")
    indexed: Optional[StrictInt] = Field(default=None, description="Indexed is how many files were parsed and written on this pass.")
    pruned: Optional[StrictInt] = Field(default=None, description="Pruned is how many stored files were deleted because prune was set and they were absent from the request.")
    repo: Optional[StrictStr] = Field(default=None, description="Repo is the repository that was indexed.")
    semantic: Optional[StrictBool] = Field(default=None, description="Semantic reports whether the semantic tier was available for this pass. When false the index is lexical + symbolic only and hybrid search still works.")
    skipped: Optional[StrictInt] = Field(default=None, description="Skipped is how many files were unchanged by content hash and left alone.")
    symbols: Optional[StrictInt] = Field(default=None, description="Symbols is how many symbol definitions the repo holds after this pass.")
    vectors: Optional[StrictInt] = Field(default=None, description="Vectors is how many of those chunks carry an embedding.")
    __properties: ClassVar[List[str]] = ["chunks", "files", "indexed", "pruned", "repo", "semantic", "skipped", "symbols", "vectors"]

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of IndexResult from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of IndexResult from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "chunks": obj.get("chunks"),
            "files": obj.get("files"),
            "indexed": obj.get("indexed"),
            "pruned": obj.get("pruned"),
            "repo": obj.get("repo"),
            "semantic": obj.get("semantic"),
            "skipped": obj.get("skipped"),
            "symbols": obj.get("symbols"),
            "vectors": obj.get("vectors")
        })
        return _obj


