# coding: utf-8

"""
    Hanzo Cloud API

    The Hanzo Cloud API as a customer calls it: every operation under /v1/ except the operator's admin product, relay doors, legacy spellings and capabilities still reached by flag. Tagged by product: the first path segment after /v1/.

    The version of the OpenAPI document: v1
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictInt, StrictStr
from typing import Any, ClassVar, Dict, List, Optional
from typing import Optional, Set
from typing_extensions import Self

class TrustItem(BaseModel):
    """
    TrustItem
    """ # noqa: E501
    available: Optional[StrictStr] = Field(default=None, description="Available is \"now\" when the item can be read immediately, or \"on request\" when it is released only to a party who asks and is answered. It is the one field a page renders the difference from.")
    body: Optional[StrictStr] = Field(default=None, description="Body is the item's content, for the kinds that are text rather than a file — an article, a subprocessor entry, a note. Empty for anything released on request: a summary of a document is still the document.")
    framework: Optional[StrictStr] = Field(default=None, description="Framework is the standard the item speaks to, when it speaks to one.")
    id: Optional[StrictStr] = Field(default=None, description="ID addresses the item — for reading it if it is available now, or for naming it in a request if it is not.")
    kind: Optional[StrictStr] = Field(default=None, description="Kind is what the item is: report, letter, policy, questionnaire, subprocessor, article or update.")
    name: Optional[StrictStr] = Field(default=None, description="Name is the item's title.")
    signed: Optional[StrictStr] = Field(default=None, description="Signed is \"self\" when the org states it itself and \"auditor\" when an independent auditor put their name to it. It is the reason an item is available now or on request, so a reader can see the rule rather than infer it.")
    summary: Optional[StrictStr] = Field(default=None, description="Summary is a line about the item.")
    updated_at: Optional[StrictInt] = Field(default=None, description="UpdatedAt is when the item last changed, in unix milliseconds.", alias="updatedAt")
    __properties: ClassVar[List[str]] = ["available", "body", "framework", "id", "kind", "name", "signed", "summary", "updatedAt"]

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of TrustItem from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of TrustItem from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "available": obj.get("available"),
            "body": obj.get("body"),
            "framework": obj.get("framework"),
            "id": obj.get("id"),
            "kind": obj.get("kind"),
            "name": obj.get("name"),
            "signed": obj.get("signed"),
            "summary": obj.get("summary"),
            "updatedAt": obj.get("updatedAt")
        })
        return _obj


