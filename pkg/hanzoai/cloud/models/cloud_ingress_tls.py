# coding: utf-8

"""
    Hanzo Cloud — Unified API (V8 · Open Edition)

    The single unified OpenAPI surface for ALL Hanzo services, aggregated from the per-service specs. Every route is https://api.hanzo.ai/v1/<service>/*. Grouped by the canonical domains in capabilities.yaml (the ONE registry). cloud/openapi.yaml is hanzoai/cloud's own woven document, copied verbatim by sync.py, and it wins wherever it and a hand-written spec describe the same route. Regenerated by merge.py — edit the per-service <svc>/openapi.yaml + capabilities.yaml, not this file.

    The version of the OpenAPI document: 8.0.0
    Contact: support@hanzo.ai
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictBool, StrictStr
from typing import Any, ClassVar, Dict, List, Optional
from hanzoai.cloud.models.cloud_tls_config import CloudTLSConfig
from typing import Optional, Set
from typing_extensions import Self

class CloudIngressTLS(BaseModel):
    """
    CloudIngressTLS
    """ # noqa: E501
    acme_directory: Optional[StrictStr] = Field(default=None, description="ACMEDirectory is the ACME endpoint in use: the staging URL, or \"letsencrypt-production\".", alias="acmeDirectory")
    acme_email: Optional[StrictStr] = Field(default=None, description="ACMEEmail is the account email the PROCESS was started with (CLOUD_INGRESS_ACME_EMAIL), not the stored config's.", alias="acmeEmail")
    config: Optional[CloudTLSConfig] = Field(default=None, description="Config is the caller org's stored ACME intent.")
    edge_enabled: Optional[StrictBool] = Field(default=None, description="EdgeEnabled is true when the edge listeners are actually bound.", alias="edgeEnabled")
    managed_hosts: Optional[List[StrictStr]] = Field(default=None, description="ManagedHosts is every host the ACME HostPolicy will issue a certificate for — the union across ALL orgs of TLS-marked routes and configured extraHosts, because one process holds one certificate cache.", alias="managedHosts")
    note: Optional[StrictStr] = Field(default=None, description="Note states which fields hot-apply and which need an edge restart.")
    role: Optional[StrictStr] = Field(default=None, description="Role is \"edge\" when this instance binds the listeners, else \"app\".")
    __properties: ClassVar[List[str]] = ["acmeDirectory", "acmeEmail", "config", "edgeEnabled", "managedHosts", "note", "role"]

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of CloudIngressTLS from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # override the default output from pydantic by calling `to_dict()` of config
        if self.config:
            _dict['config'] = self.config.to_dict()
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of CloudIngressTLS from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "acmeDirectory": obj.get("acmeDirectory"),
            "acmeEmail": obj.get("acmeEmail"),
            "config": CloudTLSConfig.from_dict(obj["config"]) if obj.get("config") is not None else None,
            "edgeEnabled": obj.get("edgeEnabled"),
            "managedHosts": obj.get("managedHosts"),
            "note": obj.get("note"),
            "role": obj.get("role")
        })
        return _obj


