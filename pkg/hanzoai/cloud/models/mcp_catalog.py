# coding: utf-8

"""
    Hanzo Cloud API

    Composed from each subsystem's own projection of its router, in the fleet's mount order — every operation below is a route the subsystem that publishes it registered. Tagged by product: the first path segment after /v1/.

    The version of the OpenAPI document: v1
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictInt
from typing import Any, ClassVar, Dict, List, Optional
from hanzoai.cloud.models.mcp_listing import MCPListing
from typing import Optional, Set
from typing_extensions import Self

class McpCatalog(BaseModel):
    """
    McpCatalog
    """ # noqa: E501
    catalog: Optional[List[MCPListing]] = Field(default=None, description="Catalog is this page of listings, featured first, then by name.")
    limit: Optional[StrictInt] = Field(default=None, description="Limit is the page size that was actually applied — the default or the clamp, when the request asked for neither or for too much.")
    offset: Optional[StrictInt] = Field(default=None, description="Offset is where this page started, so a caller pages from what the server did rather than from what it asked for.")
    total: Optional[StrictInt] = Field(default=None, description="Total is how many listings the filter matched, which is more than this page holds whenever there is a next one.")
    __properties: ClassVar[List[str]] = ["catalog", "limit", "offset", "total"]

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of McpCatalog from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # override the default output from pydantic by calling `to_dict()` of each item in catalog (list)
        _items = []
        if self.catalog:
            for _item_catalog in self.catalog:
                if _item_catalog:
                    _items.append(_item_catalog.to_dict())
            _dict['catalog'] = _items
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of McpCatalog from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "catalog": [MCPListing.from_dict(_item) for _item in obj["catalog"]] if obj.get("catalog") is not None else None,
            "limit": obj.get("limit"),
            "offset": obj.get("offset"),
            "total": obj.get("total")
        })
        return _obj


