# coding: utf-8

"""
    Hanzo Cloud API

    The Hanzo Cloud API as a customer calls it: every operation under /v1/ except the operator's admin product, relay routes, legacy spellings and capabilities still reached by flag. Tagged by product: the first path segment after /v1/.

    The version of the OpenAPI document: v1
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictBool, StrictInt, StrictStr
from typing import Any, ClassVar, Dict, List, Optional
from typing import Optional, Set
from typing_extensions import Self

class GraphTriple(BaseModel):
    """
    GraphTriple
    """ # noqa: E501
    names: Optional[StrictBool] = Field(default=None, description="Names is the author's declaration that Object is an entity and the assertion is an EDGE, written `[[key]]`. Absent, the relation is a property of Subject. It is read from the notation and never guessed from the value's shape.")
    object: Optional[StrictStr] = Field(default=None, description="Object is what the relation points at. When Names is true it has been unwrapped from its `[[…]]` and is another entity's key.")
    predicate: Optional[StrictStr] = Field(default=None, description="Predicate is the relation, exactly as the line spells it before the `::`. This surface holds no vocabulary, so it renames nothing.")
    section: Optional[StrictInt] = Field(default=None, description="Section is which section of the source stated it, counting from zero. It is the second half of every resulting assertion's evidence, `<source>#<section>`.")
    subject: Optional[StrictStr] = Field(default=None, description="Subject is the entity the statement is about: the nearest heading above the line, or the request's own subject where no heading has appeared yet.")
    __properties: ClassVar[List[str]] = ["names", "object", "predicate", "section", "subject"]

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of GraphTriple from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of GraphTriple from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "names": obj.get("names"),
            "object": obj.get("object"),
            "predicate": obj.get("predicate"),
            "section": obj.get("section"),
            "subject": obj.get("subject")
        })
        return _obj


