# coding: utf-8

"""
    Hanzo Cloud API

    The Hanzo Cloud API as a customer calls it: every operation under /v1/ except the operator's admin product, relay doors, legacy spellings and capabilities still reached by flag. Tagged by product: the first path segment after /v1/.

    The version of the OpenAPI document: v1
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictBool, StrictInt, StrictStr
from typing import Any, ClassVar, Dict, List, Optional
from hanzoai.cloud.models.git_ops_deploy import GitOpsDeploy
from hanzoai.cloud.models.git_ops_operation import GitOpsOperation
from typing import Optional, Set
from typing_extensions import Self

class GitOpsApp(BaseModel):
    """
    GitOpsApp
    """ # noqa: E501
    automated: Optional[StrictBool] = Field(default=None, description="Automated is whether CD applies new commits without being asked. It reads the PRESENCE of spec.syncPolicy.automated, which is a block rather than a boolean; false means drift is reported and nothing moves.")
    health: Optional[StrictStr] = Field(default=None, description="Health is CD's verdict on the objects it manages, verbatim: Healthy, Progressing, Degraded, Suspended, Missing or Unknown.")
    history: Optional[List[GitOpsDeploy]] = Field(default=None, description="History is the recent deploy log, NEWEST FIRST and capped at ten. CD appends oldest-first and bounds the list itself; the reversal happens here so a caller never has to know the storage order to show what shipped last. Empty (never null) for an Application that has deployed nothing.")
    name: Optional[StrictStr] = Field(default=None, description="Name is what CD calls this tracked source, not the workload it deploys — the Application CR's own metadata.name. The fleet ApplicationSet mints these as <namespace>-<app>.")
    namespace: Optional[StrictStr] = Field(default=None, description="Namespace is where the Application OBJECT lives: CD's own controller namespace, which is the same one for every row here. It is NOT the destination the workloads land in — this endpoint lists cluster-wide and never reads spec.destination.")
    operation: Optional[GitOpsOperation] = Field(default=None, description="Operation is the last sync attempt and how it ended. Absent when CD has run none, which is the honest gap between \"never tried\" and \"tried and failed\".")
    path: Optional[StrictStr] = Field(default=None, description="Path is the directory inside that repository CD renders, relative to its root.")
    project: Optional[StrictStr] = Field(default=None, description="Project is the AppProject fence the sync is admitted under: which repos this Application may pull from and which destinations it may write to. Empty when the CR declares none.")
    reconciled_at: Optional[StrictStr] = Field(default=None, description="ReconciledAt is when CD last COMPARED this Application against git, RFC 3339. It moves on every comparison, including ones that applied nothing.", alias="reconciledAt")
    repo_url: Optional[StrictStr] = Field(default=None, description="RepoURL is the git repository CD polls for this Application's desired state.", alias="repoURL")
    resources: Optional[StrictInt] = Field(default=None, description="Resources is how MANY objects CD manages for this Application (len(status.resources)) — a count, not the objects. Zero for an Application CD has not reconciled.")
    revision: Optional[StrictStr] = Field(default=None, description="Revision is the commit CD last APPLIED (status.sync.revision). Empty means it has applied none — never read that as the head of TargetRevision.")
    self_heal: Optional[StrictBool] = Field(default=None, description="SelfHeal is whether CD also reverts changes made directly in the cluster (syncPolicy.automated.selfHeal). Meaningless unless Automated.", alias="selfHeal")
    sync: Optional[StrictStr] = Field(default=None, description="Sync is CD's verdict on git versus cluster, verbatim: Synced, OutOfSync or Unknown. It is about the applied REVISION, so an Application can be Synced to a commit that is several behind the branch it tracks.")
    target_revision: Optional[StrictStr] = Field(default=None, description="TargetRevision is the git ref CD TRACKS — usually a branch such as \"main\". It is what CD aims at; Revision is what it has reached.", alias="targetRevision")
    __properties: ClassVar[List[str]] = ["automated", "health", "history", "name", "namespace", "operation", "path", "project", "reconciledAt", "repoURL", "resources", "revision", "selfHeal", "sync", "targetRevision"]

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of GitOpsApp from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # override the default output from pydantic by calling `to_dict()` of each item in history (list)
        _items = []
        if self.history:
            for _item_history in self.history:
                if _item_history:
                    _items.append(_item_history.to_dict())
            _dict['history'] = _items
        # override the default output from pydantic by calling `to_dict()` of operation
        if self.operation:
            _dict['operation'] = self.operation.to_dict()
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of GitOpsApp from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "automated": obj.get("automated"),
            "health": obj.get("health"),
            "history": [GitOpsDeploy.from_dict(_item) for _item in obj["history"]] if obj.get("history") is not None else None,
            "name": obj.get("name"),
            "namespace": obj.get("namespace"),
            "operation": GitOpsOperation.from_dict(obj["operation"]) if obj.get("operation") is not None else None,
            "path": obj.get("path"),
            "project": obj.get("project"),
            "reconciledAt": obj.get("reconciledAt"),
            "repoURL": obj.get("repoURL"),
            "resources": obj.get("resources"),
            "revision": obj.get("revision"),
            "selfHeal": obj.get("selfHeal"),
            "sync": obj.get("sync"),
            "targetRevision": obj.get("targetRevision")
        })
        return _obj


