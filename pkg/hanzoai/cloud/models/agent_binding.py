# coding: utf-8

"""
    Hanzo Cloud API

    The Hanzo Cloud API as a customer calls it: every operation under /v1/ except the operator's admin product, relay doors, legacy spellings and capabilities still reached by flag. Tagged by product: the first path segment after /v1/.

    The version of the OpenAPI document: v1
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictStr
from typing import Any, ClassVar, Dict, List, Optional
from typing import Optional, Set
from typing_extensions import Self

class AgentBinding(BaseModel):
    """
    AgentBinding
    """ # noqa: E501
    agent_name: Optional[StrictStr] = Field(default=None, description="AgentName is the cloud Agent (/v1/agents) this machine runs — the agent a message to the bot is actually run against. It is the one field that decides what the bot DOES.", alias="agentName")
    bot_version: Optional[StrictStr] = Field(default=None, description="BotVersion pins the @hanzo/bot runtime version the machine runs. Empty means the machine took the default in force when it was bound.", alias="botVersion")
    created_time: Optional[StrictStr] = Field(default=None, description="CreatedTime is when the binding was first made.", alias="createdTime")
    machine_id: Optional[StrictStr] = Field(default=None, description="MachineId is the bound machine as vm addresses it, owner-qualified (\"<org>/<machine>\"). The unqualified half is what this surface's :id routes take.", alias="machineId")
    message: Optional[StrictStr] = Field(default=None, description="Message is vm's human-readable detail on Status (\"machine provisioning; @hanzo/bot runtime not yet confirmed\") — the reason behind the state, not a second state.")
    name: Optional[StrictStr] = Field(default=None, description="Name is the binding's own key, which is the machine's id: a machine hosts at most one agent, so the binding is named for it. This is the key a bots list joins bindings onto machines by.")
    org: Optional[StrictStr] = Field(default=None, description="Org is the Hanzo tenant the binding belongs to.")
    owner: Optional[StrictStr] = Field(default=None, description="Owner is the tenant vm filed the binding under, resolved from the ?owner it was called with — which is the caller's validated org and never a body field.")
    provider: Optional[StrictStr] = Field(default=None, description="Provider is the cloud the bound machine runs on, carried here so a bindings list says where each bot lives without a second read per machine.")
    public_ip: Optional[StrictStr] = Field(default=None, description="PublicIp is the bound machine's public address as vm recorded it on the binding. Empty while the machine has none yet.", alias="publicIp")
    status: Optional[StrictStr] = Field(default=None, description="Status is the binding's lifecycle in VM's OWN words — \"Pending\" while the machine provisions and the runtime is unconfirmed, \"running\" once vm has confirmed it. The vocabulary is vm's and passes through unmapped, which is why its capitalization does not match the machine states beside it, and it is vm's reconciled reading rather than anything asserted here.")
    updated_time: Optional[StrictStr] = Field(default=None, description="UpdatedTime is when vm last reconciled it — the age of Status.", alias="updatedTime")
    __properties: ClassVar[List[str]] = ["agentName", "botVersion", "createdTime", "machineId", "message", "name", "org", "owner", "provider", "publicIp", "status", "updatedTime"]

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of AgentBinding from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of AgentBinding from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "agentName": obj.get("agentName"),
            "botVersion": obj.get("botVersion"),
            "createdTime": obj.get("createdTime"),
            "machineId": obj.get("machineId"),
            "message": obj.get("message"),
            "name": obj.get("name"),
            "org": obj.get("org"),
            "owner": obj.get("owner"),
            "provider": obj.get("provider"),
            "publicIp": obj.get("publicIp"),
            "status": obj.get("status"),
            "updatedTime": obj.get("updatedTime")
        })
        return _obj


