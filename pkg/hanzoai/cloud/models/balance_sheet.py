# coding: utf-8

"""
    Hanzo Cloud API

    The Hanzo Cloud API as a customer calls it: every operation under /v1/ except the operator's admin product, relay doors, legacy spellings and capabilities still reached by flag. Tagged by product: the first path segment after /v1/.

    The version of the OpenAPI document: v1
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictBool, StrictInt, StrictStr
from typing import Any, ClassVar, Dict, List, Optional
from hanzoai.cloud.models.balance_line import BalanceLine
from typing import Optional, Set
from typing_extensions import Self

class BalanceSheet(BaseModel):
    """
    BalanceSheet
    """ # noqa: E501
    as_of: Optional[StrictStr] = Field(default=None, description="AsOf is the posting time the statement is taken at, inclusive. A balance sheet is a snapshot, not a window, so there is no From. Absent means as of now.", alias="asOf")
    assets: Optional[List[BalanceLine]] = Field(default=None, description="Assets are what the org OWNS at that instant, one line per account that has a balance. Cash, receivables, funds captured but not yet settled.")
    balanced: Optional[StrictBool] = Field(default=None, description="Balanced is whether assets equal liabilities plus equity — the accounting equation, computed from the totals above rather than assumed. False means the ledger is broken, not that the statement is.")
    equity: Optional[List[BalanceLine]] = Field(default=None, description="Equity is what is left over for the owners. It carries a DERIVED retained earnings line holding cumulative income minus expense, because this ledger has no period close that sweeps the P&L into equity — without that line the equation would not close.")
    liabilities: Optional[List[BalanceLine]] = Field(default=None, description="Liabilities are what the org OWES — including customers' unspent prepaid credit, which is their money until it is consumed and so is carried here rather than counted as revenue.")
    total_assets: Optional[StrictInt] = Field(default=None, description="TotalAssets is the sum of the asset lines, in cents.", alias="totalAssets")
    total_equity: Optional[StrictInt] = Field(default=None, description="TotalEquity is the sum of the equity lines including retained earnings, in cents.", alias="totalEquity")
    total_liabilities: Optional[StrictInt] = Field(default=None, description="TotalLiabilities is the sum of the liability lines, in cents.", alias="totalLiabilities")
    __properties: ClassVar[List[str]] = ["asOf", "assets", "balanced", "equity", "liabilities", "totalAssets", "totalEquity", "totalLiabilities"]

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of BalanceSheet from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # override the default output from pydantic by calling `to_dict()` of each item in assets (list)
        _items = []
        if self.assets:
            for _item_assets in self.assets:
                if _item_assets:
                    _items.append(_item_assets.to_dict())
            _dict['assets'] = _items
        # override the default output from pydantic by calling `to_dict()` of each item in equity (list)
        _items = []
        if self.equity:
            for _item_equity in self.equity:
                if _item_equity:
                    _items.append(_item_equity.to_dict())
            _dict['equity'] = _items
        # override the default output from pydantic by calling `to_dict()` of each item in liabilities (list)
        _items = []
        if self.liabilities:
            for _item_liabilities in self.liabilities:
                if _item_liabilities:
                    _items.append(_item_liabilities.to_dict())
            _dict['liabilities'] = _items
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of BalanceSheet from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "asOf": obj.get("asOf"),
            "assets": [BalanceLine.from_dict(_item) for _item in obj["assets"]] if obj.get("assets") is not None else None,
            "balanced": obj.get("balanced"),
            "equity": [BalanceLine.from_dict(_item) for _item in obj["equity"]] if obj.get("equity") is not None else None,
            "liabilities": [BalanceLine.from_dict(_item) for _item in obj["liabilities"]] if obj.get("liabilities") is not None else None,
            "totalAssets": obj.get("totalAssets"),
            "totalEquity": obj.get("totalEquity"),
            "totalLiabilities": obj.get("totalLiabilities")
        })
        return _obj


