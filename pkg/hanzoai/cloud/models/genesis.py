# coding: utf-8

"""
    Hanzo Cloud API

    The Hanzo Cloud API as a customer calls it: every operation under /v1/ except the operator's admin product, relay routes, legacy spellings and capabilities still reached by flag. Tagged by product: the first path segment after /v1/.

    The version of the OpenAPI document: v1
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictInt, StrictStr
from typing import Any, ClassVar, Dict, List, Optional
from typing import Optional, Set
from typing_extensions import Self

class Genesis(BaseModel):
    """
    Genesis
    """ # noqa: E501
    at: Optional[StrictInt] = Field(default=None, description="At is the unix second the genesis root was computed.")
    block: Optional[StrictInt] = Field(default=None, description="Block is the L1 block the anchoring transaction landed in. Set only once the receipt has been read; absent otherwise.")
    chain_id: Optional[StrictInt] = Field(default=None, description="ChainID is the EVM chain the root is committed to — the Hanzo L1 by default.", alias="chainId")
    note: Optional[StrictStr] = Field(default=None, description="Note explains an unanchored genesis honestly — anchor wiring absent, or the submit error — rather than reporting a commit that did not happen.")
    root: Optional[StrictStr] = Field(default=None, description="Root is the 0x-prefixed keccak256 root of the founding allocation. It is ALWAYS computed, whether or not the on-chain anchor is wired, because the root is the tamper-evident witness.")
    status: Optional[StrictStr] = Field(default=None, description="Status is pending (root computed, not yet on-chain) or anchored (committed).")
    tx_hash: Optional[StrictStr] = Field(default=None, description="TxHash is the L1 transaction hash of the anchoring commit. Empty until anchored.", alias="txHash")
    __properties: ClassVar[List[str]] = ["at", "block", "chainId", "note", "root", "status", "txHash"]

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of Genesis from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of Genesis from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "at": obj.get("at"),
            "block": obj.get("block"),
            "chainId": obj.get("chainId"),
            "note": obj.get("note"),
            "root": obj.get("root"),
            "status": obj.get("status"),
            "txHash": obj.get("txHash")
        })
        return _obj


