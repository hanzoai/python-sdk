# coding: utf-8

"""
    Hanzo Cloud — Unified API (V8 · Open Edition)

    The single unified OpenAPI surface for ALL Hanzo services, aggregated from the per-service specs. Every route is https://api.hanzo.ai/v1/<service>/*. Grouped by the canonical domains in capabilities.yaml (the ONE registry). Regenerated by merge.py — edit the per-service <svc>/openapi.yaml + capabilities.yaml, not this file.

    The version of the OpenAPI document: 8.0.0
    Contact: support@hanzo.ai
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictBool, StrictBytes, StrictInt, StrictStr, field_validator
from typing import Any, ClassVar, Dict, List, Optional, Union
from typing import Optional, Set
from typing_extensions import Self

class ResearchArtifact(BaseModel):
    """
    A research-diary record — a raw-artifact-class record tied to a run. CONTENT- ADDRESSED inside the trust boundary: submit the bytes (base64 content) and the SERVER hashes them; that sha256 is the identity + ref (sha256:<hash>). A client sha256, if sent, must MATCH the bytes. Bytes are retrievable by hash at GET /v1/research/artifacts/{sha256}. 
    """ # noqa: E501
    content: Union[StrictBytes, StrictStr] = Field(description="base64 bytes on WRITE — the server hashes + stores them (never returned on read)")
    sha256: Optional[StrictStr] = Field(default=None, description="SERVER-derived identity (read); on write it is optional and must match the content bytes")
    kind: StrictStr
    ref: Optional[StrictStr] = Field(default=None, description="server-derived content address (sha256:<hash>); a client ref is ignored")
    run_id: Optional[StrictStr] = Field(default=None, description="the run/snapshot id it belongs to")
    project: Optional[StrictStr] = None
    visibility: Optional[StrictStr] = Field(default='private', description="Read-only on ingest; set via a grant.")
    retention_class: Optional[StrictStr] = 'raw-artifact'
    git_sha: Optional[StrictStr] = None
    git_branch: Optional[StrictStr] = None
    git_dirty: Optional[StrictBool] = None
    lib_versions: Optional[Dict[str, StrictStr]] = None
    ts: Optional[StrictInt] = None
    __properties: ClassVar[List[str]] = ["content", "sha256", "kind", "ref", "run_id", "project", "visibility", "retention_class", "git_sha", "git_branch", "git_dirty", "lib_versions", "ts"]

    @field_validator('kind')
    def kind_validate_enum(cls, value):
        """Validates the enum"""
        if value not in set(['snapshot', 'report']):
            raise ValueError("must be one of enum values ('snapshot', 'report')")
        return value

    @field_validator('visibility')
    def visibility_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['private', 'org', 'public']):
            raise ValueError("must be one of enum values ('private', 'org', 'public')")
        return value

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of ResearchArtifact from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        * OpenAPI `readOnly` fields are excluded.
        """
        excluded_fields: Set[str] = set([
            "retention_class",
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of ResearchArtifact from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "content": obj.get("content"),
            "sha256": obj.get("sha256"),
            "kind": obj.get("kind"),
            "ref": obj.get("ref"),
            "run_id": obj.get("run_id"),
            "project": obj.get("project"),
            "visibility": obj.get("visibility") if obj.get("visibility") is not None else 'private',
            "retention_class": obj.get("retention_class") if obj.get("retention_class") is not None else 'raw-artifact',
            "git_sha": obj.get("git_sha"),
            "git_branch": obj.get("git_branch"),
            "git_dirty": obj.get("git_dirty"),
            "lib_versions": obj.get("lib_versions"),
            "ts": obj.get("ts")
        })
        return _obj


