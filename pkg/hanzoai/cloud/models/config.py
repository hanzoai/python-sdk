# coding: utf-8

"""
    Hanzo Cloud API

    Composed from each subsystem's own projection of its router, in the fleet's mount order — every operation below is a route the subsystem that publishes it registered. Tagged by product: the first path segment after /v1/.

    The version of the OpenAPI document: v1
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictInt, StrictStr
from typing import Any, ClassVar, Dict, List, Optional
from typing import Optional, Set
from typing_extensions import Self

class Config(BaseModel):
    """
    Config
    """ # noqa: E501
    max_age: Optional[StrictStr] = Field(default=None, description="MaxAge caps message age, e.g. \"24h\" or \"7d\"; \"0\" (default) is unlimited.")
    max_bytes: Optional[StrictInt] = Field(default=None, description="MaxBytes caps the stream's total stored bytes; -1 (default) is unlimited.")
    max_msg_size: Optional[StrictInt] = Field(default=None, description="MaxMsgSize caps one message's size in bytes; -1 (default) is the broker's limit.")
    max_msgs: Optional[StrictInt] = Field(default=None, description="MaxMsgs caps the number of stored messages; -1 (default) is unlimited.")
    name: Optional[StrictStr] = Field(default=None, description="Name is the stream name, unique within the org (alphanumeric, hyphens, underscores).")
    num_replicas: Optional[StrictInt] = Field(default=None, description="Replicas is the number of stream replicas (1–5); this plane runs 1.")
    retention: Optional[StrictStr] = Field(default=None, description="Retention is the retention policy: limits (default), interest, or workqueue.")
    storage: Optional[StrictStr] = Field(default=None, description="Storage is the storage backend: file (default) or memory.")
    subjects: Optional[List[StrictStr]] = Field(default=None, description="Subjects are the org-relative subjects bound to this stream (wildcards supported). Default: the stream name.")
    __properties: ClassVar[List[str]] = ["max_age", "max_bytes", "max_msg_size", "max_msgs", "name", "num_replicas", "retention", "storage", "subjects"]

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of Config from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of Config from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "max_age": obj.get("max_age"),
            "max_bytes": obj.get("max_bytes"),
            "max_msg_size": obj.get("max_msg_size"),
            "max_msgs": obj.get("max_msgs"),
            "name": obj.get("name"),
            "num_replicas": obj.get("num_replicas"),
            "retention": obj.get("retention"),
            "storage": obj.get("storage"),
            "subjects": obj.get("subjects")
        })
        return _obj


