# coding: utf-8

"""
    Hanzo Cloud API

    The Hanzo Cloud API as a customer calls it: every operation under /v1/ except the operator's admin product, relay routes, legacy spellings and capabilities still reached by flag. Tagged by product: the first path segment after /v1/.

    The version of the OpenAPI document: v1
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictFloat, StrictInt, StrictStr
from typing import Any, ClassVar, Dict, List, Optional, Union
from typing import Optional, Set
from typing_extensions import Self

class RiskLabelFact(BaseModel):
    """
    RiskLabelFact
    """ # noqa: E501
    at: Optional[StrictStr] = Field(default=None, description="At is when the judged event happened, RFC 3339.")
    confidence: Optional[Union[StrictFloat, StrictInt]] = Field(default=None, description="Confidence in [0,1]. A processor chargeback is 1; an analyst's hunch is not. It breaks a tie WITHIN a precedence rank and can never lift a weak source above a strong one — otherwise every caller would send 1.  A litigation hold is NOT a field here. It is a fact about the record and not about the world, so it is not part of what was asserted, it is not in the content digest, and it has its own op — which is also the only way one can be released. Carried here it was silently a no-op on any record that already existed: the digest was the same, the insert was ignored, and the caller was told `duplicate` while the hold it asked for was never placed.")
    disposition: Optional[StrictStr] = Field(default=None, description="Disposition is productive, unproductive, or empty for an explicit unjudged — the AML engine's own vocabulary, verbatim.")
    evidence: Optional[StrictStr] = Field(default=None, description="Evidence points at the record this conclusion came from: a dispute id, a case id, a decision id. Required, because a label with no evidence cannot be defended when the adverse action it fed is challenged.")
    kind: Optional[StrictStr] = Field(default=None, description="Kind is what the subject is: account, agent, merchant, payout, person, session or transaction. Closed, because a typo in an open field would shard a tenant's labels into a partition nothing reads and nothing would say so.")
    seen: Optional[StrictStr] = Field(default=None, description="Seen is when this assertion became KNOWABLE, RFC 3339. It is required and it is not At: a chargeback lands 30 to 120 days after the transaction it judges, and a training set joined on At alone knows the future. Everything this plane does to prevent leakage is computed from Seen.")
    source: Optional[StrictStr] = Field(default=None, description="Source is who asserted: chargeoff, dispute, case, refund, review or sample. It is the primary term of the precedence rule, so it is closed — an unknown source has no rank and a conflict with it could not be resolved.")
    subject: Optional[StrictStr] = Field(default=None, description="Subject identifies the thing being judged, in the tenant's own namespace.")
    __properties: ClassVar[List[str]] = ["at", "confidence", "disposition", "evidence", "kind", "seen", "source", "subject"]

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of RiskLabelFact from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of RiskLabelFact from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "at": obj.get("at"),
            "confidence": obj.get("confidence"),
            "disposition": obj.get("disposition"),
            "evidence": obj.get("evidence"),
            "kind": obj.get("kind"),
            "seen": obj.get("seen"),
            "source": obj.get("source"),
            "subject": obj.get("subject")
        })
        return _obj


