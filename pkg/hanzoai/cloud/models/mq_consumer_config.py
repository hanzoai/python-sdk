# coding: utf-8

"""
    Hanzo Cloud — Unified API (V8 · Open Edition)

    The single unified OpenAPI surface for ALL Hanzo services, aggregated from the per-service specs. Every route is https://api.hanzo.ai/v1/<service>/*. Grouped by the canonical domains in capabilities.yaml (the ONE registry). Regenerated by merge.py — edit the per-service <svc>/openapi.yaml + capabilities.yaml, not this file.

    The version of the OpenAPI document: 8.0.0
    Contact: support@hanzo.ai
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from datetime import datetime
from pydantic import BaseModel, ConfigDict, Field, StrictInt, StrictStr, field_validator
from typing import Any, ClassVar, Dict, List, Optional
from typing_extensions import Annotated
from typing import Optional, Set
from typing_extensions import Self

class MqConsumerConfig(BaseModel):
    """
    JetStream consumer configuration.
    """ # noqa: E501
    durable_name: Optional[Annotated[str, Field(strict=True, max_length=256)]] = Field(default=None, description="Durable consumer name. If set, the consumer survives client disconnections. ")
    filter_subject: Optional[StrictStr] = Field(default=None, description="Subject filter. Only messages matching this subject are delivered. Supports wildcards. ")
    ack_policy: Optional[StrictStr] = Field(default='explicit', description="Acknowledgment policy. `explicit` requires per-message ack. `all` acks all messages up to the acked sequence. `none` disables acks. ")
    deliver_policy: Optional[StrictStr] = Field(default='all', description="Where to start delivery. `all` delivers from the beginning. `last` delivers the last message. `new` delivers only new messages. `by_start_sequence` and `by_start_time` start from a specific point. ")
    opt_start_seq: Optional[StrictInt] = Field(default=None, description="Starting sequence number (used with deliver_policy `by_start_sequence`). ")
    opt_start_time: Optional[datetime] = Field(default=None, description="Starting timestamp (used with deliver_policy `by_start_time`). ")
    max_deliver: Optional[StrictInt] = Field(default=-1, description="Maximum delivery attempts before the message is dropped or sent to a dead letter subject (-1 for unlimited). ")
    ack_wait: Optional[StrictStr] = Field(default='30s', description="Time to wait for acknowledgment before redelivery (e.g., \"30s\", \"5m\"). Defaults to \"30s\". ")
    replay_policy: Optional[StrictStr] = Field(default='instant', description="Replay policy for historical messages. `instant` delivers as fast as possible. `original` preserves original timing gaps. ")
    max_ack_pending: Optional[StrictInt] = Field(default=1000, description="Maximum number of unacknowledged messages before delivery pauses. ")
    description: Optional[StrictStr] = Field(default=None, description="Optional human-readable description.")
    __properties: ClassVar[List[str]] = ["durable_name", "filter_subject", "ack_policy", "deliver_policy", "opt_start_seq", "opt_start_time", "max_deliver", "ack_wait", "replay_policy", "max_ack_pending", "description"]

    @field_validator('durable_name')
    def durable_name_validate_regular_expression(cls, value):
        """Validates the regular expression"""
        if value is None:
            return value

        if not re.match(r"^[a-zA-Z0-9_-]+$", value):
            raise ValueError(r"must validate the regular expression /^[a-zA-Z0-9_-]+$/")
        return value

    @field_validator('ack_policy')
    def ack_policy_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['none', 'all', 'explicit']):
            raise ValueError("must be one of enum values ('none', 'all', 'explicit')")
        return value

    @field_validator('deliver_policy')
    def deliver_policy_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['all', 'last', 'new', 'by_start_sequence', 'by_start_time', 'last_per_subject']):
            raise ValueError("must be one of enum values ('all', 'last', 'new', 'by_start_sequence', 'by_start_time', 'last_per_subject')")
        return value

    @field_validator('replay_policy')
    def replay_policy_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['instant', 'original']):
            raise ValueError("must be one of enum values ('instant', 'original')")
        return value

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of MqConsumerConfig from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of MqConsumerConfig from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "durable_name": obj.get("durable_name"),
            "filter_subject": obj.get("filter_subject"),
            "ack_policy": obj.get("ack_policy") if obj.get("ack_policy") is not None else 'explicit',
            "deliver_policy": obj.get("deliver_policy") if obj.get("deliver_policy") is not None else 'all',
            "opt_start_seq": obj.get("opt_start_seq"),
            "opt_start_time": obj.get("opt_start_time"),
            "max_deliver": obj.get("max_deliver") if obj.get("max_deliver") is not None else -1,
            "ack_wait": obj.get("ack_wait") if obj.get("ack_wait") is not None else '30s',
            "replay_policy": obj.get("replay_policy") if obj.get("replay_policy") is not None else 'instant',
            "max_ack_pending": obj.get("max_ack_pending") if obj.get("max_ack_pending") is not None else 1000,
            "description": obj.get("description")
        })
        return _obj


