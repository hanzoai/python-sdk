# coding: utf-8

"""
    Hanzo Cloud — Unified API (V8 · Open Edition)

    The single unified OpenAPI surface for ALL Hanzo services, aggregated from the per-service specs. Every route is https://api.hanzo.ai/v1/<service>/*. Grouped by the canonical domains in capabilities.yaml (the ONE registry). cloud/openapi.yaml is hanzoai/cloud's own woven document, copied verbatim by sync.py, and it wins wherever it and a hand-written spec describe the same route. Regenerated by merge.py — edit the per-service <svc>/openapi.yaml + capabilities.yaml, not this file.

    The version of the OpenAPI document: 8.0.0
    Contact: support@hanzo.ai
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictBool, StrictStr
from typing import Any, ClassVar, Dict, List, Optional
from typing import Optional, Set
from typing_extensions import Self

class CloudJourneyStep(BaseModel):
    """
    CloudJourneyStep
    """ # noqa: E501
    args: Optional[Dict[str, Dict[str, Any]]] = None
    deps: Optional[List[StrictStr]] = Field(default=None, description="Dependencies are step ids that must be done/skipped before this step is available. The wire key is `deps` (the blueprint contract); the Go field keeps its descriptive name.")
    detail: Optional[StrictStr] = Field(default=None, description="the prose/juncture — what the Guide asks/explains here")
    draft: Optional[StrictStr] = None
    draft_into: Optional[StrictStr] = Field(default=None, alias="draftInto")
    enabled: Optional[StrictBool] = Field(default=None, description="Enabled is the admin on/off lever. A NIL pointer reads as ENABLED (absence == on): a legacy/org curriculum that omits the field keeps every step, and only an explicit `enabled: false` (an admin disable) drops a step from the journey. See on() in blueprint.go and the Blueprint.Curriculum() projection.")
    id: Optional[StrictStr] = None
    section: Optional[StrictStr] = Field(default=None, description="the phase (section id) this step groups under")
    signal: Optional[StrictStr] = Field(default=None, description="Signal, when set, names a machine detector (detect.go). When the detector reports the org's real state present, the step auto-marks done.")
    title: Optional[StrictStr] = None
    tool: Optional[StrictStr] = Field(default=None, description="Tool, when set, is the MCP tool the Business AI runs for \"do it for me\". Args are its default arguments; Draft is an optional AI prompt whose output fills the DraftInto arg (default \"brief\").")
    __properties: ClassVar[List[str]] = ["args", "deps", "detail", "draft", "draftInto", "enabled", "id", "section", "signal", "title", "tool"]

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of CloudJourneyStep from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of CloudJourneyStep from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "args": obj.get("args"),
            "deps": obj.get("deps"),
            "detail": obj.get("detail"),
            "draft": obj.get("draft"),
            "draftInto": obj.get("draftInto"),
            "enabled": obj.get("enabled"),
            "id": obj.get("id"),
            "section": obj.get("section"),
            "signal": obj.get("signal"),
            "title": obj.get("title"),
            "tool": obj.get("tool")
        })
        return _obj


