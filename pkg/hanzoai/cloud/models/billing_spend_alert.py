# coding: utf-8

"""
    Hanzo Cloud — Unified API (V8 · Open Edition)

    The single unified OpenAPI surface for ALL Hanzo services, aggregated from the per-service specs. Every route is https://api.hanzo.ai/v1/<service>/*. Grouped by the canonical domains in capabilities.yaml (the ONE registry). Regenerated by merge.py — edit the per-service <svc>/openapi.yaml + capabilities.yaml, not this file.

    The version of the OpenAPI document: 8.0.0
    Contact: support@hanzo.ai
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from datetime import datetime
from pydantic import BaseModel, ConfigDict, Field, StrictBool, StrictInt, StrictStr, field_validator
from typing import Any, ClassVar, Dict, List, Optional
from typing_extensions import Annotated
from typing import Optional, Set
from typing_extensions import Self

class BillingSpendAlert(BaseModel):
    """
    A tenant-configured budget on cumulative spend for a SCOPE (project, service). It is a soft alert by default; with `enforce: true` it is a HARD per-scope spend cap — once `period` spend for the scope would exceed `threshold`, the metering gate denies with reason `spend_cap` (402). Period spend is DERIVED on demand from the append-only usage ledger for the current UTC month; the row carries NO running total, so a cap change takes effect on the next request and can never drift from the debited spend. Scope axes use \"\" as a wildcard: (\"\",\"\") is the org-wide default row, (P,\"\") every service in project P, (\"\",S) service S in any project. 
    """ # noqa: E501
    id: StrictStr
    user_id: StrictStr = Field(description="The org the cap is scoped to — server-pinned to the validated X-Org-Id, never a client field.", alias="userId")
    title: Optional[StrictStr] = None
    threshold: StrictInt = Field(description="The scope's spend cap / alert ceiling in USD cents (Money). 0 = no cap (a rate-limit-only row).")
    currency: StrictStr
    project: StrictStr = Field(description="Scope project axis; \"\" = wildcard (the org-wide default row).")
    service: StrictStr = Field(description="Scope service axis (server-derived route/provider); \"\" = wildcard (all services).")
    enforce: StrictBool = Field(description="false (default) = SOFT (warn only, never blocks); true = HARD cap (402 on breach).")
    soft_pct: Annotated[int, Field(le=100, strict=True, ge=1)] = Field(description="Soft-warn threshold as a percent of `threshold` (unset/0 defaults to 80).", alias="softPct")
    rate_limit_rpm: StrictInt = Field(description="Requests-per-minute ceiling for the scope (429 when exceeded). 0 = none.", alias="rateLimitRpm")
    period: Optional[Annotated[str, Field(strict=True)]] = Field(default=None, description="The UTC calendar month the cap is measured over, e.g. \"2026-07\" (Schedule window; resets on the 1st).")
    resets_at: Optional[datetime] = Field(default=None, description="Midnight UTC on the first of the next month — when period spend resets to 0.", alias="resetsAt")
    period_spent_cents: Optional[StrictInt] = Field(default=None, description="DERIVED spend for the scope in `period`, in USD cents, summed from the deduped usage ledger. Absent if the aggregation errored.", alias="periodSpentCents")
    over: Optional[StrictBool] = Field(default=None, description="DERIVED — true iff a further billable request would breach the cap (the SAME boundary `enforce` uses, so it never drifts from the verdict).")
    warn: Optional[StrictBool] = Field(default=None, description="DERIVED — true iff period spend has reached the soft-warn threshold (`softPct` of `threshold`).")
    triggered_at: Optional[StrictStr] = Field(default=None, description="ISO timestamp the alert last fired; empty if never.", alias="triggeredAt")
    created_at: Optional[datetime] = Field(default=None, alias="createdAt")
    updated_at: Optional[datetime] = Field(default=None, alias="updatedAt")
    additional_properties: Dict[str, Any] = {}
    __properties: ClassVar[List[str]] = ["id", "userId", "title", "threshold", "currency", "project", "service", "enforce", "softPct", "rateLimitRpm", "period", "resetsAt", "periodSpentCents", "over", "warn", "triggeredAt", "createdAt", "updatedAt"]

    @field_validator('period')
    def period_validate_regular_expression(cls, value):
        """Validates the regular expression"""
        if value is None:
            return value

        if not re.match(r"^\d{4}-\d{2}$", value):
            raise ValueError(r"must validate the regular expression /^\d{4}-\d{2}$/")
        return value

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of BillingSpendAlert from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        * Fields in `self.additional_properties` are added to the output dict.
        """
        excluded_fields: Set[str] = set([
            "additional_properties",
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # puts key-value pairs in additional_properties in the top level
        if self.additional_properties is not None:
            for _key, _value in self.additional_properties.items():
                _dict[_key] = _value

        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of BillingSpendAlert from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "id": obj.get("id"),
            "userId": obj.get("userId"),
            "title": obj.get("title"),
            "threshold": obj.get("threshold"),
            "currency": obj.get("currency") if obj.get("currency") is not None else 'usd',
            "project": obj.get("project"),
            "service": obj.get("service"),
            "enforce": obj.get("enforce"),
            "softPct": obj.get("softPct"),
            "rateLimitRpm": obj.get("rateLimitRpm"),
            "period": obj.get("period"),
            "resetsAt": obj.get("resetsAt"),
            "periodSpentCents": obj.get("periodSpentCents"),
            "over": obj.get("over"),
            "warn": obj.get("warn"),
            "triggeredAt": obj.get("triggeredAt"),
            "createdAt": obj.get("createdAt"),
            "updatedAt": obj.get("updatedAt")
        })
        # store additional fields in additional_properties
        for _key in obj.keys():
            if _key not in cls.__properties:
                _obj.additional_properties[_key] = obj.get(_key)

        return _obj


