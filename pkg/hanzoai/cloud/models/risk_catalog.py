# coding: utf-8

"""
    Hanzo Cloud API

    Composed from each subsystem's own projection of its router, in the fleet's mount order — every operation below is a route the subsystem that publishes it registered. Tagged by product: the first path segment after /v1/.

    The version of the OpenAPI document: v1
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictStr
from typing import Any, ClassVar, Dict, List, Optional
from hanzoai.cloud.models.risk_band import RiskBand
from hanzoai.cloud.models.risk_model_feature import RiskModelFeature
from hanzoai.cloud.models.risk_org_feature import RiskOrgFeature
from typing import Optional, Set
from typing_extensions import Self

class RiskCatalog(BaseModel):
    """
    RiskCatalog
    """ # noqa: E501
    gap: Optional[StrictStr] = Field(default=None, description="Gap says why a lens could not be measured, when that is the case. Each reason names its own lens, because \"the surface is unreadable\" and \"the network baseline is unreadable\" are different facts.")
    model: Optional[List[RiskModelFeature]] = Field(default=None, description="Model is the governed inventory: one entry per dimension of the model space, each carrying the typology it serves and the published standard that asks for it. It is the same for every organisation, because it is the model's shape.")
    network: Optional[List[RiskBand]] = Field(default=None, description="Network is the published cross-organisation baseline over the same window, so the surface above has something to be read AGAINST. It is the same for every caller and it names nobody.  It carries no tenant and cannot be made to: the table it reads has no org column, every figure is a quantile over at least kAnonOrgs organisations weighted one vote each, and a band that does not meet that floor is dropped on the way out.")
    surface: Optional[List[RiskOrgFeature]] = Field(default=None, description="Surface is what this organisation's own event surface carries, per dimension, measured over the window. A dimension present in no bucket is blind here — the model reads its neutral value and a reviewer has to be able to see that.")
    tenant: Optional[StrictStr] = Field(default=None, description="Tenant is whose surface was measured.")
    __properties: ClassVar[List[str]] = ["gap", "model", "network", "surface", "tenant"]

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of RiskCatalog from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # override the default output from pydantic by calling `to_dict()` of each item in model (list)
        _items = []
        if self.model:
            for _item_model in self.model:
                if _item_model:
                    _items.append(_item_model.to_dict())
            _dict['model'] = _items
        # override the default output from pydantic by calling `to_dict()` of each item in network (list)
        _items = []
        if self.network:
            for _item_network in self.network:
                if _item_network:
                    _items.append(_item_network.to_dict())
            _dict['network'] = _items
        # override the default output from pydantic by calling `to_dict()` of each item in surface (list)
        _items = []
        if self.surface:
            for _item_surface in self.surface:
                if _item_surface:
                    _items.append(_item_surface.to_dict())
            _dict['surface'] = _items
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of RiskCatalog from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "gap": obj.get("gap"),
            "model": [RiskModelFeature.from_dict(_item) for _item in obj["model"]] if obj.get("model") is not None else None,
            "network": [RiskBand.from_dict(_item) for _item in obj["network"]] if obj.get("network") is not None else None,
            "surface": [RiskOrgFeature.from_dict(_item) for _item in obj["surface"]] if obj.get("surface") is not None else None,
            "tenant": obj.get("tenant")
        })
        return _obj


