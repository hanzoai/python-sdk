# coding: utf-8

"""
    Hanzo Cloud API

    The Hanzo Cloud API as a customer calls it: every operation under /v1/ except the operator's admin product, relay routes, legacy spellings and capabilities still reached by flag. Tagged by product: the first path segment after /v1/.

    The version of the OpenAPI document: v1
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictStr
from typing import Any, ClassVar, Dict, List, Optional
from typing import Optional, Set
from typing_extensions import Self

class RunnerContext(BaseModel):
    """
    RunnerContext
    """ # noqa: E501
    actions_url: Optional[StrictStr] = Field(default=None, description="ActionsURL is where act fetches an action repository from when a step names one. A runner that receives nothing here composes an unusable URL and every job dies before its first step, so the runner supplies its own fallback rather than trusting this to be set.")
    actor: Optional[StrictStr] = Field(default=None, description="Actor is who caused the run.")
    api_url: Optional[StrictStr] = Field(default=None, description="APIURL is where the job reaches the forge's API.")
    base_ref: Optional[StrictStr] = Field(default=None, description="BaseRef is the target branch of a pull request, empty otherwise.")
    event: Optional[StrictStr] = Field(default=None, description="Event is the webhook payload that triggered the run, as opaque JSON. It stays bytes because its shape belongs to the event and not to us: the runner hands it to act, which evaluates github.event.* against it.")
    event_name: Optional[StrictStr] = Field(default=None, description="EventName is what triggered the run, e.g. \"push\".")
    head_ref: Optional[StrictStr] = Field(default=None, description="HeadRef is the source branch of a pull request, empty otherwise.")
    job: Optional[StrictStr] = Field(default=None, description="Job is which job of the workflow document this task is.")
    ref: Optional[StrictStr] = Field(default=None, description="Ref is the full ref the run is for, e.g. \"refs/heads/main\".")
    ref_name: Optional[StrictStr] = Field(default=None, description="RefName is that ref without its refs/heads/ or refs/tags/ prefix.")
    ref_type: Optional[StrictStr] = Field(default=None, description="RefType is \"branch\" or \"tag\".")
    repository: Optional[StrictStr] = Field(default=None, description="Repository is \"<owner>/<name>\".")
    repository_owner: Optional[StrictStr] = Field(default=None, description="RepositoryOwner is the owner half of it.")
    retention_days: Optional[StrictStr] = Field(default=None, description="RetentionDays is how long the run's artifacts are kept, as a number in a string because that is what an expression reads.")
    run_attempt: Optional[StrictStr] = Field(default=None, description="RunAttempt is which attempt of that run this is, counting from 1.")
    run_id: Optional[StrictStr] = Field(default=None, description="RunID identifies the run this task belongs to.")
    run_number: Optional[StrictStr] = Field(default=None, description="RunNumber is the run's position in its repository, counting from 1.")
    runtime_token: Optional[StrictStr] = Field(default=None, description="RuntimeToken authorizes the job against the actions runtime: artifacts, the cache. The runner passes it as ACTIONS_RUNTIME_TOKEN and masks it out of the log.")
    server_url: Optional[StrictStr] = Field(default=None, description="ServerURL is the forge's own origin, as a link in a log points at it.")
    sha: Optional[StrictStr] = Field(default=None, description="Sha is the commit being run, in full.")
    token: Optional[StrictStr] = Field(default=None, description="Token is the job's credential against the forge's own API — github.token in an expression.")
    __properties: ClassVar[List[str]] = ["actions_url", "actor", "api_url", "base_ref", "event", "event_name", "head_ref", "job", "ref", "ref_name", "ref_type", "repository", "repository_owner", "retention_days", "run_attempt", "run_id", "run_number", "runtime_token", "server_url", "sha", "token"]

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of RunnerContext from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of RunnerContext from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "actions_url": obj.get("actions_url"),
            "actor": obj.get("actor"),
            "api_url": obj.get("api_url"),
            "base_ref": obj.get("base_ref"),
            "event": obj.get("event"),
            "event_name": obj.get("event_name"),
            "head_ref": obj.get("head_ref"),
            "job": obj.get("job"),
            "ref": obj.get("ref"),
            "ref_name": obj.get("ref_name"),
            "ref_type": obj.get("ref_type"),
            "repository": obj.get("repository"),
            "repository_owner": obj.get("repository_owner"),
            "retention_days": obj.get("retention_days"),
            "run_attempt": obj.get("run_attempt"),
            "run_id": obj.get("run_id"),
            "run_number": obj.get("run_number"),
            "runtime_token": obj.get("runtime_token"),
            "server_url": obj.get("server_url"),
            "sha": obj.get("sha"),
            "token": obj.get("token")
        })
        return _obj


