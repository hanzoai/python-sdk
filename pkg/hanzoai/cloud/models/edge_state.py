# coding: utf-8

"""
    Hanzo Cloud API

    The Hanzo Cloud API as a customer calls it: every operation under /v1/ except the operator's admin product, relay routes, legacy spellings and capabilities still reached by flag. Tagged by product: the first path segment after /v1/.

    The version of the OpenAPI document: v1
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictBool, StrictStr
from typing import Any, ClassVar, Dict, List, Optional
from typing import Optional, Set
from typing_extensions import Self

class EdgeState(BaseModel):
    """
    EdgeState
    """ # noqa: E501
    configured: Optional[StrictBool] = Field(default=None, description="Configured is whether the edge holds credentials to act at all. False means every purge is a no-op.")
    error: Optional[StrictStr] = Field(default=None, description="Error is the blocker, so an operator reads it instead of guessing at it.")
    freshness: Optional[StrictStr] = Field(default=None, description="Freshness says, in one phrase, how long after a publish a reader sees it. It is the sentence an operator actually wants; the booleans above are how a machine reads the same fact.")
    policy: Optional[Dict[str, StrictStr]] = Field(default=None, description="Policy is the Cache-Control this edge serves each class of object with. It is DERIVED from the one canonical function, never a second copy: half the confusion when a publish looks stale is not knowing what the TTLs are, and reading them out of the source is not something an operator should have to do to answer \"how long until this is live\".")
    provider: Optional[StrictStr] = Field(default=None, description="Provider is the CDN behind this edge, or \"none\". It is the first thing an operator wants and the only vendor name this API returns.")
    reach: Optional[List[StrictStr]] = Field(default=None, description="Reach is the apexes a publish is invalidated on. A site is served on more than one — the site plane's own and the first-party apex — and a purge that covers one of them looks identical from here to a purge that covers both.")
    status: Optional[StrictStr] = Field(default=None, description="Status is \"ok\" when a publish reaches readers immediately, else \"degraded\".")
    __properties: ClassVar[List[str]] = ["configured", "error", "freshness", "policy", "provider", "reach", "status"]

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of EdgeState from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of EdgeState from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "configured": obj.get("configured"),
            "error": obj.get("error"),
            "freshness": obj.get("freshness"),
            "policy": obj.get("policy"),
            "provider": obj.get("provider"),
            "reach": obj.get("reach"),
            "status": obj.get("status")
        })
        return _obj


