# coding: utf-8

"""
    Hanzo Cloud API

    The Hanzo Cloud API as a customer calls it: every operation under /v1/ except the operator's admin product, relay routes, legacy spellings and capabilities still reached by flag. Tagged by product: the first path segment after /v1/.

    The version of the OpenAPI document: v1
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictInt, StrictStr
from typing import Any, ClassVar, Dict, List, Optional
from typing import Optional, Set
from typing_extensions import Self

class SlackFile(BaseModel):
    """
    SlackFile
    """ # noqa: E501
    id: Optional[StrictStr] = Field(default=None, description="ID is the file's Slack ID and the handle the file read takes.")
    mimetype: Optional[StrictStr] = Field(default=None, description="Mimetype is the type Slack recorded at upload. It is the uploader's word, so it describes the file rather than deciding how the bytes are served.")
    name: Optional[StrictStr] = Field(default=None, description="Name is the filename as uploaded.")
    permalink: Optional[StrictStr] = Field(default=None, description="Permalink opens the file in Slack, for a person. It needs a Slack session and is never the bytes.")
    size: Optional[StrictInt] = Field(default=None, description="Size is the file's length in bytes.")
    title: Optional[StrictStr] = Field(default=None, description="Title is the title Slack displays, which may differ from the filename.")
    __properties: ClassVar[List[str]] = ["id", "mimetype", "name", "permalink", "size", "title"]

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of SlackFile from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of SlackFile from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "id": obj.get("id"),
            "mimetype": obj.get("mimetype"),
            "name": obj.get("name"),
            "permalink": obj.get("permalink"),
            "size": obj.get("size"),
            "title": obj.get("title")
        })
        return _obj


