# coding: utf-8

"""
    Hanzo Cloud API

    Composed from each subsystem's own projection of its router, in the fleet's mount order — every operation below is a route the subsystem that publishes it registered. Tagged by product: the first path segment after /v1/.

    The version of the OpenAPI document: v1
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictInt, StrictStr
from typing import Any, ClassVar, Dict, List, Optional
from hanzoai.cloud.models.env_var_json import EnvVarJSON
from hanzoai.cloud.models.git_origin import GitOrigin
from hanzoai.cloud.models.image_origin import ImageOrigin
from typing import Optional, Set
from typing_extensions import Self

class CreateAppReq(BaseModel):
    """
    CreateAppReq
    """ # noqa: E501
    build_type: Optional[StrictStr] = Field(default=None, description="BuildType is `pack` — the zero-config default that detects any project — or `dockerfile`, the explicit escape hatch. An image app never builds.", alias="buildType")
    description: Optional[StrictStr] = Field(default=None, description="Description is free text about what the application is.")
    dockerfile: Optional[StrictStr] = Field(default=None, description="Dockerfile is the path to build from, for buildType `dockerfile`.")
    domains: Optional[List[StrictStr]] = Field(default=None, description="Domains are extra ingress hosts. The canonical default host is always attached; a bare custom host is refused here and must go through add-domain → verify first.")
    env: Optional[List[EnvVarJSON]] = Field(default=None, description="Env is the application's environment. Keys must match `^[A-Za-z_][A-Za-z0-9_]*$`; a variable marked `secret: true` is sealed into KMS and its plaintext is never written to the database.")
    environment: Optional[StrictStr] = Field(default=None, description="Environment is the deploy target this app names (\"production\" by default).")
    image: Optional[ImageOrigin] = Field(default=None, description="Image is the container image to run, for source `image`.")
    name: Optional[StrictStr] = Field(default=None, description="Name is the application's display name. Required; the slug is derived from it when none is given.")
    port: Optional[StrictInt] = Field(default=None, description="Port is the container port the app listens on.")
    project: Optional[StrictStr] = Field(default=None, description="Project is the project to create the application under, from the path.")
    replicas: Optional[StrictInt] = Field(default=None, description="Replicas is how many copies to run; clamped to the deployment's limit rather than refused.")
    repo: Optional[GitOrigin] = Field(default=None, description="Repo is the git source to build from, for source `git`.")
    slug: Optional[StrictStr] = Field(default=None, description="Slug is the app's identity in the cluster — its CR name and part of its host. Given or derived from Name, it must match `^[a-z0-9]([a-z0-9-]{0,38}[a-z0-9])?$`, and one already used in this project is 409.")
    source: Optional[StrictStr] = Field(default=None, description="Source is `git`, which requires repo.url, or `image`, which requires image.repository. Anything else is 400.")
    storage_gb: Optional[StrictInt] = Field(default=None, description="StorageGB is the persistent volume size in GiB; absent means stateless. Clamped to the deployment's limit rather than refused.", alias="storageGb")
    __properties: ClassVar[List[str]] = ["buildType", "description", "dockerfile", "domains", "env", "environment", "image", "name", "port", "project", "replicas", "repo", "slug", "source", "storageGb"]

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of CreateAppReq from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # override the default output from pydantic by calling `to_dict()` of each item in env (list)
        _items = []
        if self.env:
            for _item_env in self.env:
                if _item_env:
                    _items.append(_item_env.to_dict())
            _dict['env'] = _items
        # override the default output from pydantic by calling `to_dict()` of image
        if self.image:
            _dict['image'] = self.image.to_dict()
        # override the default output from pydantic by calling `to_dict()` of repo
        if self.repo:
            _dict['repo'] = self.repo.to_dict()
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of CreateAppReq from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "buildType": obj.get("buildType"),
            "description": obj.get("description"),
            "dockerfile": obj.get("dockerfile"),
            "domains": obj.get("domains"),
            "env": [EnvVarJSON.from_dict(_item) for _item in obj["env"]] if obj.get("env") is not None else None,
            "environment": obj.get("environment"),
            "image": ImageOrigin.from_dict(obj["image"]) if obj.get("image") is not None else None,
            "name": obj.get("name"),
            "port": obj.get("port"),
            "project": obj.get("project"),
            "replicas": obj.get("replicas"),
            "repo": GitOrigin.from_dict(obj["repo"]) if obj.get("repo") is not None else None,
            "slug": obj.get("slug"),
            "source": obj.get("source"),
            "storageGb": obj.get("storageGb")
        })
        return _obj


