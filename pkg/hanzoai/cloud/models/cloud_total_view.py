# coding: utf-8

"""
    Hanzo Cloud — Unified API (V8 · Open Edition)

    The single unified OpenAPI surface for ALL Hanzo services, aggregated from the per-service specs. Every route is https://api.hanzo.ai/v1/<service>/*. Grouped by the canonical domains in capabilities.yaml (the ONE registry). cloud/openapi.yaml is hanzoai/cloud's own woven document, copied verbatim by sync.py, and it wins wherever it and a hand-written spec describe the same route. Regenerated by merge.py — edit the per-service <svc>/openapi.yaml + capabilities.yaml, not this file.

    The version of the OpenAPI document: 8.0.0
    Contact: support@hanzo.ai
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictFloat, StrictInt, StrictStr
from typing import Any, ClassVar, Dict, List, Optional, Union
from typing import Optional, Set
from typing_extensions import Self

class CloudTotalView(BaseModel):
    """
    CloudTotalView
    """ # noqa: E501
    confidence: Optional[StrictStr] = Field(default=None, description="Confidence says how much the counters mean; a percentage-only meter leaves them at zero.")
    cost_cents: Optional[StrictInt] = Field(default=None, description="CostCents is the row's cost in US cents. For an \"account\" row this is the PROVIDER's own charge, not a Hanzo one.", alias="costCents")
    provider: Optional[StrictStr] = Field(default=None, description="Provider is the upstream the usage was measured against.")
    requests: Optional[StrictInt] = Field(default=None, description="Requests is how many requests the row covers.")
    scope: Optional[StrictStr] = Field(default=None, description="Scope is whose row it is: \"user\" for the caller's own linked accounts, \"org\" for the whole tenant's Hanzo-routed usage.")
    source: Optional[StrictStr] = Field(default=None, description="Source is where the row came from: \"account\" is the provider's own meter on the caller's linked account, \"hanzo\" is Hanzo-routed inference. The two are never summed.")
    tokens: Optional[StrictInt] = Field(default=None, description="Tokens is the total tokens the row covers.")
    used_pct: Optional[Union[StrictFloat, StrictInt]] = Field(default=None, description="UsedPct is how much of a plan window the row consumed, 0–100. It is a share, never money.", alias="usedPct")
    window: Optional[StrictStr] = Field(default=None, description="Window is the meter window class the row rolls up, when it has one.")
    windows: Optional[StrictInt] = Field(default=None, description="Windows is how many window instances rolled up into the row.")
    __properties: ClassVar[List[str]] = ["confidence", "costCents", "provider", "requests", "scope", "source", "tokens", "usedPct", "window", "windows"]

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of CloudTotalView from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of CloudTotalView from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "confidence": obj.get("confidence"),
            "costCents": obj.get("costCents"),
            "provider": obj.get("provider"),
            "requests": obj.get("requests"),
            "scope": obj.get("scope"),
            "source": obj.get("source"),
            "tokens": obj.get("tokens"),
            "usedPct": obj.get("usedPct"),
            "window": obj.get("window"),
            "windows": obj.get("windows")
        })
        return _obj


