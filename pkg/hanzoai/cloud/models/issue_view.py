# coding: utf-8

"""
    Hanzo Cloud API

    The Hanzo Cloud API as a customer calls it: every operation under /v1/ except the operator's admin product, relay routes, legacy spellings and capabilities still reached by flag. Tagged by product: the first path segment after /v1/.

    The version of the OpenAPI document: v1
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictInt, StrictStr
from typing import Any, ClassVar, Dict, List, Optional
from typing import Optional, Set
from typing_extensions import Self

class IssueView(BaseModel):
    """
    IssueView
    """ # noqa: E501
    assignee: Optional[StrictStr] = Field(default=None, description="Assignee is who holds the work — an IAM username, or the login of the FIRST assignee when a forge issue has several. Absent when nobody holds it, which is exactly the state a claim needs.")
    created_at: Optional[StrictInt] = Field(default=None, description="CreatedAt is when the item was opened, in unix seconds. 0 when the source gave no parseable timestamp.", alias="createdAt")
    description: Optional[StrictStr] = Field(default=None, description="Description is the body, markdown as its author wrote it. Absent when empty.")
    due_at: Optional[StrictInt] = Field(default=None, description="DueAt is when the work is due, in unix seconds; absent means no due date. A forge row takes it from its MILESTONE's due date, since a forge issue has no deadline of its own. Never before StartAt, and never past 2200-01-01.", alias="dueAt")
    ext_ref: Optional[StrictStr] = Field(default=None, description="ExtRef anchors the item to something outside the todo — a mirrored issue (\"github:owner/repo#123\"), a pushed PR branch, or a record on another plane. It is the idempotency key the mirror upsert matches on. Absent when the item has no external origin.", alias="extRef")
    id: Optional[StrictStr] = Field(default=None, description="ID is the work item's opaque handle, and it is NOT how you address it — ProjectKey plus Number is. Its shape says which source answered: a forge issue's is the forge's own numeric id in decimal, an index row's a minted \"issue_\" id.")
    identifier: Optional[StrictStr] = Field(default=None, description="Identifier is the human handle, \"<key>#<number>\" — the board and the number on it, joined. ONE spelling whichever source answered, because a list where forge rows read cli#1 and index rows read OPS-3 is two products in one list.")
    kind: Optional[StrictStr] = Field(default=None, description="Kind is what the item IS: issue, pr or epic. Set once at create and never changed, so a row does not migrate between surfaces. Deliberately not \"task\" — that word is the async plane (contract.go).")
    labels: Optional[List[StrictStr]] = Field(default=None, description="Labels are the item's remaining tags, with the status and priority labels lifted OUT — a column that stayed here would render twice, once as the card's column and once as a chip on the card. Always present; empty is [].")
    number: Optional[StrictInt] = Field(default=None, description="Number is the item's number ON ITS BOARD, from 1 and monotonic there — the forge's own issue number for a forge row, allocated inside the create transaction for an index row so it cannot race. Unique per board, never across the org.")
    priority: Optional[StrictStr] = Field(default=None, description="Priority is urgent, high, medium, low or none. Also a label on a forge row. Never empty: \"none\" when nothing names one, so callers compare a value rather than test for absence.")
    project_key: Optional[StrictStr] = Field(default=None, description="ProjectKey is the board this item is on: the repository name for a forge issue, the index board's key otherwise. With Number it is the item's address in every other route.", alias="projectKey")
    repo: Optional[StrictStr] = Field(default=None, description="Repo is the git repository the item is bound to, so a repository's Issues and PRs tabs are filters over this one table. Absent when the item is not repo-bound.")
    source: Optional[StrictStr] = Field(default=None, description="Source is which surface OPENED it: team, git, crm, helpdesk, cms or agent. Also set once. It is the ORIGIN, not the subject — source=helpdesk is an engineering issue opened from a support escalation, not a support ticket.")
    start_at: Optional[StrictInt] = Field(default=None, description="StartAt is when the work starts, in unix seconds; absent means unscheduled. A forge row takes it from when the issue was opened, but only once the issue has a due date — an interval needs both ends.", alias="startAt")
    status: Optional[StrictStr] = Field(default=None, description="Status is the board column: backlog, todo, in_progress, done or canceled, and nothing else. On a forge row it is read off a LABEL, so relabelling in the forge web UI moves the card here and vice versa — and a CLOSED forge issue reads done whatever its labels say. Never empty: \"backlog\" when nothing names a column.")
    title: Optional[StrictStr] = Field(default=None, description="Title is the item's one-line summary.")
    updated_at: Optional[StrictInt] = Field(default=None, description="UpdatedAt is when it last changed, in unix seconds.", alias="updatedAt")
    __properties: ClassVar[List[str]] = ["assignee", "createdAt", "description", "dueAt", "extRef", "id", "identifier", "kind", "labels", "number", "priority", "projectKey", "repo", "source", "startAt", "status", "title", "updatedAt"]

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of IssueView from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of IssueView from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "assignee": obj.get("assignee"),
            "createdAt": obj.get("createdAt"),
            "description": obj.get("description"),
            "dueAt": obj.get("dueAt"),
            "extRef": obj.get("extRef"),
            "id": obj.get("id"),
            "identifier": obj.get("identifier"),
            "kind": obj.get("kind"),
            "labels": obj.get("labels"),
            "number": obj.get("number"),
            "priority": obj.get("priority"),
            "projectKey": obj.get("projectKey"),
            "repo": obj.get("repo"),
            "source": obj.get("source"),
            "startAt": obj.get("startAt"),
            "status": obj.get("status"),
            "title": obj.get("title"),
            "updatedAt": obj.get("updatedAt")
        })
        return _obj


