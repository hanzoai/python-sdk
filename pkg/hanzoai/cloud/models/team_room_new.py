# coding: utf-8

"""
    Hanzo Cloud API

    The Hanzo Cloud API as a customer calls it: every operation under /v1/ except the operator's admin product, relay routes, legacy spellings and capabilities still reached by flag. Tagged by product: the first path segment after /v1/.

    The version of the OpenAPI document: v1
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictBool, StrictStr
from typing import Any, ClassVar, Dict, List, Optional
from typing import Optional, Set
from typing_extensions import Self

class TeamRoomNew(BaseModel):
    """
    TeamRoomNew
    """ # noqa: E501
    bindings: Optional[List[StrictStr]] = Field(default=None, description="Bindings are what the room is about, each \"<kind>:<ref>\".")
    life: Optional[StrictStr] = Field(default=None, description="Life is the lifecycle intent, \"standing\" or \"bound\"; empty reads standing.")
    members: Optional[List[StrictStr]] = Field(default=None, description="Members are the account uuids in the room. A public room may open empty — anyone in the org can find it — and a private one that names nobody is refused rather than created unreachable.")
    name: Optional[StrictStr] = Field(default=None, description="Name is what a person sees in a sidebar — \"bugfix-1010\", not \"#bugfix-1010\". The sigil is how a client DRAWS a room, and storing it would put it in the name twice the first time a client added its own.")
    private: Optional[StrictBool] = Field(default=None, description="Private restricts the room to its members. Public is the default because a room nobody can find is the more surprising of the two.")
    space: Optional[StrictStr] = Field(default=None, description="Space is where the room is opened. Optional: an org with one space has no choice to make, so it does not have to state one. An org with several must, because picking for it would make the room's home depend on iteration order.")
    topic: Optional[StrictStr] = Field(default=None, description="Topic is the room's one-line subject.")
    __properties: ClassVar[List[str]] = ["bindings", "life", "members", "name", "private", "space", "topic"]

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of TeamRoomNew from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of TeamRoomNew from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "bindings": obj.get("bindings"),
            "life": obj.get("life"),
            "members": obj.get("members"),
            "name": obj.get("name"),
            "private": obj.get("private"),
            "space": obj.get("space"),
            "topic": obj.get("topic")
        })
        return _obj


