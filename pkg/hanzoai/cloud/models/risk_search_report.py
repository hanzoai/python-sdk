# coding: utf-8

"""
    Hanzo Cloud API

    The Hanzo Cloud API as a customer calls it: every operation under /v1/ except the operator's admin product, relay routes, legacy spellings and capabilities still reached by flag. Tagged by product: the first path segment after /v1/.

    The version of the OpenAPI document: v1
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictBool, StrictInt, StrictStr
from typing import Any, ClassVar, Dict, List, Optional
from hanzoai.cloud.models.risk_model_value import RiskModelValue
from hanzoai.cloud.models.risk_trial import RiskTrial
from typing import Optional, Set
from typing_extensions import Self

class RiskSearchReport(BaseModel):
    """
    RiskSearchReport
    """ # noqa: E501
    done: Optional[StrictBool] = Field(default=None, description="Done is false while the run is still going; the trials below are then the ones finished so far.")
    ended: Optional[StrictStr] = Field(default=None, description="Ended is when it finished, RFC 3339. Absent while it is still going.")
    events: Optional[StrictInt] = Field(default=None, description="Events is how much of this organisation's history was replayed.")
    fitted: Optional[RiskModelValue] = Field(default=None, description="Fitted is the winning shape FITTED over your own history and published as one of your organisation's own model values. Name its address on PUT /v1/risk/state/model and the winning shape becomes the model you are running.  It is why this op answers something you can act on. A trial keeps counts and not the model that produced them, so a report without this named a shape nobody could install — and the adoption path refused a shape change besides. Fitting the winner once is a sixty-fifth pass over the same history; keeping all sixty-four fitted models resident instead would cost a measured 21 MiB per run for sixty-three shapes nobody adopts.  Two things about it are worth knowing before you adopt it. Its realised rate can differ from the winner's above, because the ranking measures every candidate under one fixed reference geometry so the comparison is a comparison, while this is fitted under YOUR geometry — the one an outsider cannot predict. And it has learned the window this search replayed and nothing older, so adopting it trades history for fit.")
    gap: Optional[StrictStr] = Field(default=None, description="Gap says why the winning shape could not be fitted into an adoptable value, when it could not. It is separate from Refusal because they are different facts: a refusal means the ranking below proves nothing, a gap means the ranking stands and only the value is missing.")
    id: Optional[StrictStr] = Field(default=None, description="ID is the run.")
    refusal: Optional[StrictStr] = Field(default=None, description="Refusal says why the run proves nothing, when it does. An empty history is REFUSED rather than reported as zero alerts: \"no alerts\" is exactly what a quiet model looks like, and choosing a shape on the strength of an empty replay is the failure a sandbox exists to prevent.")
    started: Optional[StrictStr] = Field(default=None, description="Started is when the run was accepted, RFC 3339.")
    trials: Optional[List[RiskTrial]] = Field(default=None, description="Trials is every shape tried, best first.")
    winner: Optional[RiskTrial] = Field(default=None, description="Winner is the best-fitting shape, absent when nothing fit.")
    __properties: ClassVar[List[str]] = ["done", "ended", "events", "fitted", "gap", "id", "refusal", "started", "trials", "winner"]

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of RiskSearchReport from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # override the default output from pydantic by calling `to_dict()` of fitted
        if self.fitted:
            _dict['fitted'] = self.fitted.to_dict()
        # override the default output from pydantic by calling `to_dict()` of each item in trials (list)
        _items = []
        if self.trials:
            for _item_trials in self.trials:
                if _item_trials:
                    _items.append(_item_trials.to_dict())
            _dict['trials'] = _items
        # override the default output from pydantic by calling `to_dict()` of winner
        if self.winner:
            _dict['winner'] = self.winner.to_dict()
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of RiskSearchReport from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "done": obj.get("done"),
            "ended": obj.get("ended"),
            "events": obj.get("events"),
            "fitted": RiskModelValue.from_dict(obj["fitted"]) if obj.get("fitted") is not None else None,
            "gap": obj.get("gap"),
            "id": obj.get("id"),
            "refusal": obj.get("refusal"),
            "started": obj.get("started"),
            "trials": [RiskTrial.from_dict(_item) for _item in obj["trials"]] if obj.get("trials") is not None else None,
            "winner": RiskTrial.from_dict(obj["winner"]) if obj.get("winner") is not None else None
        })
        return _obj


