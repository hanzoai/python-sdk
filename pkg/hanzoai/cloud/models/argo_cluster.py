# coding: utf-8

"""
    Hanzo Cloud API

    The Hanzo Cloud API as a customer calls it: every operation under /v1/ except the operator's admin product, relay doors, legacy spellings and capabilities still reached by flag. Tagged by product: the first path segment after /v1/.

    The version of the OpenAPI document: v1
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictStr
from typing import Any, ClassVar, Dict, List, Optional
from hanzoai.cloud.models.argo_cluster_info import ArgoClusterInfo
from hanzoai.cloud.models.argo_connection_state import ArgoConnectionState
from typing import Optional, Set
from typing_extensions import Self

class ArgoCluster(BaseModel):
    """
    ArgoCluster
    """ # noqa: E501
    connection_state: Optional[ArgoConnectionState] = Field(default=None, description="ConnectionState is whether the destination is reachable.", alias="connectionState")
    info: Optional[ArgoClusterInfo] = Field(default=None, description="Info is the connection state again plus the count of applications targeting this destination.")
    name: Optional[StrictStr] = Field(default=None, description="Name is what the Destination column shows: \"in-cluster\" for this cluster, otherwise whatever spec.destination.name declares, falling back to the server URL when it declares none.")
    server: Optional[StrictStr] = Field(default=None, description="Server is the destination's API URL, and the key the list is deduplicated by. https://kubernetes.default.svc is this cluster.")
    __properties: ClassVar[List[str]] = ["connectionState", "info", "name", "server"]

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of ArgoCluster from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # override the default output from pydantic by calling `to_dict()` of connection_state
        if self.connection_state:
            _dict['connectionState'] = self.connection_state.to_dict()
        # override the default output from pydantic by calling `to_dict()` of info
        if self.info:
            _dict['info'] = self.info.to_dict()
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of ArgoCluster from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "connectionState": ArgoConnectionState.from_dict(obj["connectionState"]) if obj.get("connectionState") is not None else None,
            "info": ArgoClusterInfo.from_dict(obj["info"]) if obj.get("info") is not None else None,
            "name": obj.get("name"),
            "server": obj.get("server")
        })
        return _obj


