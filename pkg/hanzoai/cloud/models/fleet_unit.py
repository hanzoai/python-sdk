# coding: utf-8

"""
    Hanzo Cloud API

    The Hanzo Cloud API as a customer calls it: every operation under /v1/ except the operator's admin product, relay routes, legacy spellings and capabilities still reached by flag. Tagged by product: the first path segment after /v1/.

    The version of the OpenAPI document: v1
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictInt, StrictStr
from typing import Any, ClassVar, Dict, List, Optional
from hanzoai.cloud.models.fleet_metrics import FleetMetrics
from hanzoai.cloud.models.fleet_spec import FleetSpec
from typing import Optional, Set
from typing_extensions import Self

class FleetUnit(BaseModel):
    """
    FleetUnit
    """ # noqa: E501
    host: Optional[StrictStr] = Field(default=None, description="Host is the unit's hostname. Empty for a unit that is not one host: a cluster row has no hostname to report.")
    kind: Optional[StrictStr] = Field(default=None, description="Kind is what the unit IS: laptop, cloud, gpu, cluster, machine or worker.")
    label: Optional[StrictStr] = Field(default=None, description="Label is the name to show a human — a target's label, a worker's hostname, a machine's display name. Empty when the source has none to give.")
    metrics: Optional[FleetMetrics] = Field(default=None, description="Metrics is the unit's latest utilization: its own live snapshot when it keeps one (a run-target's heartbeat wins), else the newest sample from the series for the SAME source. Absent means nothing is known about this unit's load — which is deliberately not the same as a reading of zero.")
    queued: Optional[StrictInt] = Field(default=None, description="Queued is how many renders are waiting on THIS GPU's own lane in the org's gpu-jobs queue. BYO units only — an agent run-target dispatches, it does not queue — and omitted when nothing is waiting.")
    running: Optional[StrictInt] = Field(default=None, description="Running is what the unit is executing right now: agent sessions in flight for a run-target, claimed renders for a BYO GPU.")
    sessions: Optional[StrictInt] = Field(default=None, description="Sessions is how many agent sessions are open on this unit. Always present, and 0 for a source that cannot host agent sessions at all — a fact about that plane, not a gap in the reading.")
    source: Optional[StrictStr] = Field(default=None, description="Source is the plane this row came from: \"agent\" (a linked run-target), \"byo\" (a worker or cluster the org dialed in) or \"visor\" (a machine Hanzo provisioned). It is half the row's identity, and it says which face owns the unit — /v1/agents/targets, /v1/visor/fleet/workers, /v1/visor/machines.")
    spec: Optional[FleetSpec] = Field(default=None, description="Spec is the unit's static capability. Absent when the source reported none — unknown capability, never a zeroed one.")
    status: Optional[StrictStr] = Field(default=None, description="Status is liveness in the SOURCE's own vocabulary, because each plane decides it differently: a run-target's is derived from its heartbeat, a BYO worker's is online/offline on the 90s window, a BYO cluster's is \"attached\", and a Visor machine's is the provider's word for its lifecycle state.")
    unit: Optional[StrictStr] = Field(default=None, description="Unit is the SOURCE's own id for this unit — a run-target id, a BYO worker id, a Visor machine name — so a row links straight back to the face that owns it. It is unique within a source, not across them: two planes may mint the same id, which is why (source, unit) together is the identity.")
    __properties: ClassVar[List[str]] = ["host", "kind", "label", "metrics", "queued", "running", "sessions", "source", "spec", "status", "unit"]

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of FleetUnit from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # override the default output from pydantic by calling `to_dict()` of metrics
        if self.metrics:
            _dict['metrics'] = self.metrics.to_dict()
        # override the default output from pydantic by calling `to_dict()` of spec
        if self.spec:
            _dict['spec'] = self.spec.to_dict()
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of FleetUnit from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "host": obj.get("host"),
            "kind": obj.get("kind"),
            "label": obj.get("label"),
            "metrics": FleetMetrics.from_dict(obj["metrics"]) if obj.get("metrics") is not None else None,
            "queued": obj.get("queued"),
            "running": obj.get("running"),
            "sessions": obj.get("sessions"),
            "source": obj.get("source"),
            "spec": FleetSpec.from_dict(obj["spec"]) if obj.get("spec") is not None else None,
            "status": obj.get("status"),
            "unit": obj.get("unit")
        })
        return _obj


