# coding: utf-8

"""
    Hanzo Cloud API

    The Hanzo Cloud API as a customer calls it: every operation under /v1/ except the operator's admin product, relay routes, legacy spellings and capabilities still reached by flag. Tagged by product: the first path segment after /v1/.

    The version of the OpenAPI document: v1
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictFloat, StrictInt, StrictStr
from typing import Any, ClassVar, Dict, List, Optional, Union
from typing import Optional, Set
from typing_extensions import Self

class EsignRecipient(BaseModel):
    """
    EsignRecipient
    """ # noqa: E501
    email: Optional[StrictStr] = Field(default=None, description="Email is where this recipient's signing link is meant to go, lower-cased.")
    id: Optional[StrictStr] = Field(default=None, description="ID is the recipient id, which is what a field is placed against.")
    name: Optional[StrictStr] = Field(default=None, description="Name is the recipient's display name, empty when none was given.")
    read_status: Optional[StrictStr] = Field(default=None, description="ReadStatus is NOT_OPENED until they first open their link, then OPENED.", alias="readStatus")
    rejection_reason: Optional[StrictStr] = Field(default=None, description="RejectionReason is why they declined, null unless they did.", alias="rejectionReason")
    role: Optional[StrictStr] = Field(default=None, description="Role is SIGNER, CC, VIEWER, APPROVER or ASSISTANT. A document waits only for its SIGNERs and APPROVERs before it can seal.")
    send_status: Optional[StrictStr] = Field(default=None, description="SendStatus is NOT_SENT until the document goes out, then SENT. A CC recipient is SENT from the moment they are added.", alias="sendStatus")
    signed_at: Optional[StrictInt] = Field(default=None, description="SignedAt is when they finished or declined, in unix milliseconds; null while neither has happened.", alias="signedAt")
    signing_order: Optional[Union[StrictFloat, StrictInt]] = Field(default=None, description="SigningOrder is their position in a SEQUENTIAL document, null when they were added without one. A PARALLEL document ignores it.", alias="signingOrder")
    signing_status: Optional[StrictStr] = Field(default=None, description="SigningStatus is NOT_SIGNED, SIGNED or REJECTED. A CC recipient is SIGNED from the moment they are added, because they are never asked.", alias="signingStatus")
    __properties: ClassVar[List[str]] = ["email", "id", "name", "readStatus", "rejectionReason", "role", "sendStatus", "signedAt", "signingOrder", "signingStatus"]

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of EsignRecipient from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of EsignRecipient from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "email": obj.get("email"),
            "id": obj.get("id"),
            "name": obj.get("name"),
            "readStatus": obj.get("readStatus"),
            "rejectionReason": obj.get("rejectionReason"),
            "role": obj.get("role"),
            "sendStatus": obj.get("sendStatus"),
            "signedAt": obj.get("signedAt"),
            "signingOrder": obj.get("signingOrder"),
            "signingStatus": obj.get("signingStatus")
        })
        return _obj


