# coding: utf-8

"""
    Hanzo Cloud — Unified API (V8 · Open Edition)

    The single unified OpenAPI surface for ALL Hanzo services, aggregated from the per-service specs. Every route is https://api.hanzo.ai/v1/<service>/*. Grouped by the canonical domains in capabilities.yaml (the ONE registry). Regenerated by merge.py — edit the per-service <svc>/openapi.yaml + capabilities.yaml, not this file.

    The version of the OpenAPI document: 8.0.0
    Contact: support@hanzo.ai
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictStr
from typing import Any, ClassVar, Dict, List, Optional
from hanzoai.cloud.models.notify_channel import NotifyChannel
from typing import Optional, Set
from typing_extensions import Self

class NotifySendRequest(BaseModel):
    """
    Notification send request (`notify/pkg/types.SendRequest`). Delivery content is either a raw `subject`/`body`, or a template resolved from `template_id`/`event` rendered against `template_vars`. 
    """ # noqa: E501
    to: List[StrictStr] = Field(description="Destination address(es). Phone number for SMS, email for email. Multiple recipients fan out into one provider call each and produce one response entry per recipient. ")
    channel: Optional[NotifyChannel] = None
    provider: Optional[StrictStr] = Field(default=None, description="Pins a specific provider service name (e.g. `twilio`, `plivo`, `twilio_email`, `mail`). When omitted the fold picks the org's default for the channel from configured KMS credentials. ")
    subject: Optional[StrictStr] = Field(default=None, description="Raw subject; used verbatim when `body` is set (no-template path).")
    body: Optional[StrictStr] = Field(default=None, description="Raw message body. When non-empty it wins verbatim and no template is rendered. ")
    template_id: Optional[StrictStr] = Field(default=None, description="Built-in template identifier. When `body` is empty, the template is resolved from `template_id` or, failing that, `event`. ")
    template_vars: Optional[Dict[str, Any]] = Field(default=None, description="Free-form variables the template is rendered against (e.g. `otp`, `app`, `recipient`). A missing `app` defaults to `Hanzo`. ")
    event: Optional[StrictStr] = Field(default=None, description="Event-catalog identifier. Also used as a fallback template id when `template_id` is unset (the IAM OTP path sends `event: iam.otp_sent` with no `template_id`). ")
    idempotency_key: Optional[StrictStr] = Field(default=None, description="Per-tenant deduplication key. Accepted on the wire; the sync fold does not persist a Message row. ")
    send_at: Optional[StrictStr] = Field(default=None, description="RFC3339 schedule time for async dispatch. Async is not available in the fold, so a set value has no effect on the sync path. ")
    options: Optional[Dict[str, Any]] = Field(default=None, description="Free-form per-provider knob bag.")
    __properties: ClassVar[List[str]] = ["to", "channel", "provider", "subject", "body", "template_id", "template_vars", "event", "idempotency_key", "send_at", "options"]

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of NotifySendRequest from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of NotifySendRequest from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "to": obj.get("to"),
            "channel": obj.get("channel"),
            "provider": obj.get("provider"),
            "subject": obj.get("subject"),
            "body": obj.get("body"),
            "template_id": obj.get("template_id"),
            "template_vars": obj.get("template_vars"),
            "event": obj.get("event"),
            "idempotency_key": obj.get("idempotency_key"),
            "send_at": obj.get("send_at"),
            "options": obj.get("options")
        })
        return _obj


