# coding: utf-8

"""
    Hanzo Cloud — Unified API (V8 · Open Edition)

    The single unified OpenAPI surface for ALL Hanzo services, aggregated from the per-service specs. Every route is https://api.hanzo.ai/v1/<service>/*. Grouped by the canonical domains in capabilities.yaml (the ONE registry). Regenerated by merge.py — edit the per-service <svc>/openapi.yaml + capabilities.yaml, not this file.

    The version of the OpenAPI document: 8.0.0
    Contact: support@hanzo.ai
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictInt, StrictStr, field_validator
from typing import Any, ClassVar, Dict, List, Optional
from typing_extensions import Annotated
from typing import Optional, Set
from typing_extensions import Self

class MqStreamConfig(BaseModel):
    """
    JetStream stream configuration.
    """ # noqa: E501
    name: Annotated[str, Field(strict=True, max_length=256)] = Field(description="Stream name (alphanumeric, hyphens, underscores).")
    subjects: List[StrictStr] = Field(description="Subjects bound to this stream. Supports wildcards. ")
    retention: Optional[StrictStr] = Field(default='limits', description="Message retention policy. `limits` keeps messages until limits are hit. `interest` keeps messages while consumers exist. `workqueue` deletes messages after acknowledgment. ")
    max_msgs: Optional[StrictInt] = Field(default=-1, description="Maximum number of messages (-1 for unlimited).")
    max_bytes: Optional[StrictInt] = Field(default=-1, description="Maximum total bytes (-1 for unlimited).")
    max_age: Optional[StrictStr] = Field(default='0', description="Maximum message age (e.g., \"24h\", \"7d\", \"0\" for unlimited). ")
    max_msg_size: Optional[StrictInt] = Field(default=-1, description="Maximum single message size in bytes (-1 for default).")
    storage: Optional[StrictStr] = Field(default='file', description="Storage backend for stream data.")
    num_replicas: Optional[Annotated[int, Field(le=5, strict=True, ge=1)]] = Field(default=1, description="Number of replicas in the cluster.")
    discard: Optional[StrictStr] = Field(default='old', description="Discard policy when limits are reached. `old` discards the oldest messages. `new` rejects new messages. ")
    duplicate_window: Optional[StrictStr] = Field(default='2m', description="Window for message deduplication based on Nats-Msg-Id header (e.g., \"2m\"). Defaults to \"2m\". ")
    description: Optional[StrictStr] = Field(default=None, description="Optional human-readable description.")
    __properties: ClassVar[List[str]] = ["name", "subjects", "retention", "max_msgs", "max_bytes", "max_age", "max_msg_size", "storage", "num_replicas", "discard", "duplicate_window", "description"]

    @field_validator('name')
    def name_validate_regular_expression(cls, value):
        """Validates the regular expression"""
        if not re.match(r"^[a-zA-Z0-9_-]+$", value):
            raise ValueError(r"must validate the regular expression /^[a-zA-Z0-9_-]+$/")
        return value

    @field_validator('retention')
    def retention_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['limits', 'interest', 'workqueue']):
            raise ValueError("must be one of enum values ('limits', 'interest', 'workqueue')")
        return value

    @field_validator('storage')
    def storage_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['file', 'memory']):
            raise ValueError("must be one of enum values ('file', 'memory')")
        return value

    @field_validator('discard')
    def discard_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['old', 'new']):
            raise ValueError("must be one of enum values ('old', 'new')")
        return value

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of MqStreamConfig from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of MqStreamConfig from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "name": obj.get("name"),
            "subjects": obj.get("subjects"),
            "retention": obj.get("retention") if obj.get("retention") is not None else 'limits',
            "max_msgs": obj.get("max_msgs") if obj.get("max_msgs") is not None else -1,
            "max_bytes": obj.get("max_bytes") if obj.get("max_bytes") is not None else -1,
            "max_age": obj.get("max_age") if obj.get("max_age") is not None else '0',
            "max_msg_size": obj.get("max_msg_size") if obj.get("max_msg_size") is not None else -1,
            "storage": obj.get("storage") if obj.get("storage") is not None else 'file',
            "num_replicas": obj.get("num_replicas") if obj.get("num_replicas") is not None else 1,
            "discard": obj.get("discard") if obj.get("discard") is not None else 'old',
            "duplicate_window": obj.get("duplicate_window") if obj.get("duplicate_window") is not None else '2m',
            "description": obj.get("description")
        })
        return _obj


