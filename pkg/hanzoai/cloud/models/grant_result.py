# coding: utf-8

"""
    Hanzo Cloud API

    Composed from each subsystem's own projection of its router, in the fleet's mount order — every operation below is a route the subsystem that publishes it registered. Tagged by product: the first path segment after /v1/.

    The version of the OpenAPI document: v1
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictInt, StrictStr
from typing import Any, ClassVar, Dict, List, Optional
from typing import Optional, Set
from typing_extensions import Self

class GrantResult(BaseModel):
    """
    GrantResult
    """ # noqa: E501
    balance_cents: Optional[StrictInt] = Field(default=None, description="BalanceCents is the account balance AFTER the grant, in whole cents.", alias="balanceCents")
    balance_exact: Optional[StrictStr] = Field(default=None, description="BalanceExact is that same balance at full 18-decimal precision, so a sub-cent debit is visible rather than rounded away.", alias="balanceExact")
    currency: Optional[StrictStr] = Field(default=None, description="Currency is the lower-cased ISO code the grant was denominated in.")
    granted_cents: Optional[StrictInt] = Field(default=None, description="GrantedCents is the amount actually credited.", alias="grantedCents")
    org: Optional[StrictStr] = Field(default=None, description="Org is the tenant whose ledger was credited.")
    source: Optional[StrictStr] = Field(default=None, description="Source is the money bucket: \"trial\" (non-cash comp) or \"prepaid\" (real money).")
    subject: Optional[StrictStr] = Field(default=None, description="Subject is the ACCOUNT the credit landed on inside that ledger: the org slug for a pooled org, \"<org>/<name>\" for a member of a per-member one. It is echoed because the operator does not choose it — account.Payer does — so naming a member of a pooled org credits the pool and the receipt has to say so.")
    transaction_id: Optional[StrictStr] = Field(default=None, description="TransactionID is the ledger entry id, for reconciliation against commerce.", alias="transactionId")
    __properties: ClassVar[List[str]] = ["balanceCents", "balanceExact", "currency", "grantedCents", "org", "source", "subject", "transactionId"]

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of GrantResult from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of GrantResult from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "balanceCents": obj.get("balanceCents"),
            "balanceExact": obj.get("balanceExact"),
            "currency": obj.get("currency"),
            "grantedCents": obj.get("grantedCents"),
            "org": obj.get("org"),
            "source": obj.get("source"),
            "subject": obj.get("subject"),
            "transactionId": obj.get("transactionId")
        })
        return _obj


