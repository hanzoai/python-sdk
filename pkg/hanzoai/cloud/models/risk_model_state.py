# coding: utf-8

"""
    Hanzo Cloud API

    The Hanzo Cloud API as a customer calls it: every operation under /v1/ except the operator's admin product, relay doors, legacy spellings and capabilities still reached by flag. Tagged by product: the first path segment after /v1/.

    The version of the OpenAPI document: v1
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictBool, StrictFloat, StrictInt, StrictStr
from typing import Any, ClassVar, Dict, List, Optional, Union
from hanzoai.cloud.models.risk_aggregates import RiskAggregates
from hanzoai.cloud.models.risk_model_value import RiskModelValue
from hanzoai.cloud.models.risk_surface import RiskSurface
from typing import Optional, Set
from typing_extensions import Self

class RiskModelState(BaseModel):
    """
    RiskModelState
    """ # noqa: E501
    aggregates: Optional[RiskAggregates] = Field(default=None, description="Aggregates reports the pressure on this organisation's own sliding aggregates, and whether they have started forgetting subjects to stay inside their bound.")
    blind: Optional[Dict[str, StrictInt]] = Field(default=None, description="Blind counts, per feature, how often it took its neutral value for want of data. A feature blind on most traffic is not contributing whatever the inventory claims for it.")
    cut: Optional[Union[StrictFloat, StrictInt]] = Field(default=None, description="Cut is the threshold in force, derived from Stated as a quantile of the scores actually observed.")
    descends: Optional[StrictStr] = Field(default=None, description="Descends is the published value the working model grew out of: the newest one whose mass count it has reached or passed. Empty when nothing has been published yet.  It is DERIVED from the count and never stored, so an instant rollback is right for free — adopting an older value moves the count backward and this answers with that older value, where a stored pointer would be a second fact to keep in step. Read with Learned it is also the DRIFT: this model is Descends plus however many events the two counts differ by.")
    disposed: Optional[StrictInt] = Field(default=None, description="Disposed is how many published values retention has taken. It is DERIVED from the lowest surviving sequence, so it cannot drift from what it describes, and it is reported because a retention that binds is a fact an operator must be able to read rather than a silence.")
    learned: Optional[StrictInt] = Field(default=None, description="Learned is how many events the model has learned from.")
    live: Optional[StrictBool] = Field(default=None, description="Live is false while the model is in shadow — scoring, learning and recording what it WOULD have alerted on, and changing no outcome. Shadow is the default for a new tenant.")
    policy: Optional[StrictInt] = Field(default=None, description="Policy is the version of the decision regime this model is deciding under, from your organisation's own policy history (GET /v1/risk/policy). Every score cites it, so it is the join between a past decision and the appetite that produced its threshold. Zero means no regime has ever been stated and the default posture — shadow — is in force.")
    realised: Optional[Union[StrictFloat, StrictInt]] = Field(default=None, description="Realised is the share that actually was. Reading it beside Stated is what makes the appetite a measured commitment rather than an intention.")
    refused: Optional[Dict[str, StrictInt]] = Field(default=None, description="Refused counts events the model would not score, by reason. None of them was examined; a refusal is counted, never silent.")
    sample: Optional[Union[StrictFloat, StrictInt]] = Field(default=None, description="Sample is the share of below-the-line events retained for review, which is how the miss rate is measured rather than assumed.")
    saturated: Optional[StrictBool] = Field(default=None, description="Saturated means no threshold can honour the stated appetite because too much of the stream scores in the top bucket, so the model is alerting on nothing — the one state that must never be mistaken for quiet.")
    shape: Optional[StrictStr] = Field(default=None, description="Shape is the model's identity, as `<family>:<digest>`: the KIND of model, and that family's own digest over the inventory in order and the detector's geometry parameters. It is what an auditor pins an alert to, because learned state is only meaningful against the space that produced it — and the family leads it because two families' masses are not fitted differently, they are different kinds of number.")
    stated: Optional[Union[StrictFloat, StrictInt]] = Field(default=None, description="Stated is the share of the stream this organisation said may be examined.")
    surface: Optional[RiskSurface] = Field(default=None, description="Surface reports what of the tenant's OWN event surface has been folded in.")
    tenant: Optional[StrictStr] = Field(default=None, description="Tenant is the qualified key the model is held under — the brand whose issuer vouched for the caller and the organisation it acts for. It is echoed so a reader can see the answer is its own and not a parameter it passed.")
    values: Optional[List[RiskModelValue]] = Field(default=None, description="Values is your organisation's own published model values, newest first — every state it deliberately named, each addressed by its own content and immutable. This is what PUT /v1/risk/state/model names, so it is reported HERE rather than behind an address of its own: they are part of what a review of one model reads, and a list of names is a few hundred bytes.  Compare each one's `shape` with the `shape` above: equal means adopting it restores masses into the space this model already runs, and different means adopting it REPLANTS the model into the space that value describes — which is how the shape a search found becomes the shape you are running.  The working model is NOT in it. Publication is a boundary somebody marked; the state between two boundaries is in-process counters, and calling those a value would be a claim about reproducibility that nothing could honour.")
    warm: Optional[StrictBool] = Field(default=None, description="Warm is whether that is enough for the model to have an opinion at all. Below it the model declines to score, which is an ordinary state and is not a clean bill of health.")
    __properties: ClassVar[List[str]] = ["aggregates", "blind", "cut", "descends", "disposed", "learned", "live", "policy", "realised", "refused", "sample", "saturated", "shape", "stated", "surface", "tenant", "values", "warm"]

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of RiskModelState from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # override the default output from pydantic by calling `to_dict()` of aggregates
        if self.aggregates:
            _dict['aggregates'] = self.aggregates.to_dict()
        # override the default output from pydantic by calling `to_dict()` of surface
        if self.surface:
            _dict['surface'] = self.surface.to_dict()
        # override the default output from pydantic by calling `to_dict()` of each item in values (list)
        _items = []
        if self.values:
            for _item_values in self.values:
                if _item_values:
                    _items.append(_item_values.to_dict())
            _dict['values'] = _items
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of RiskModelState from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "aggregates": RiskAggregates.from_dict(obj["aggregates"]) if obj.get("aggregates") is not None else None,
            "blind": obj.get("blind"),
            "cut": obj.get("cut"),
            "descends": obj.get("descends"),
            "disposed": obj.get("disposed"),
            "learned": obj.get("learned"),
            "live": obj.get("live"),
            "policy": obj.get("policy"),
            "realised": obj.get("realised"),
            "refused": obj.get("refused"),
            "sample": obj.get("sample"),
            "saturated": obj.get("saturated"),
            "shape": obj.get("shape"),
            "stated": obj.get("stated"),
            "surface": RiskSurface.from_dict(obj["surface"]) if obj.get("surface") is not None else None,
            "tenant": obj.get("tenant"),
            "values": [RiskModelValue.from_dict(_item) for _item in obj["values"]] if obj.get("values") is not None else None,
            "warm": obj.get("warm")
        })
        return _obj


