# coding: utf-8

"""
    Hanzo Cloud API

    The Hanzo Cloud API as a customer calls it: every operation under /v1/ except the operator's admin product, relay doors, legacy spellings and capabilities still reached by flag. Tagged by product: the first path segment after /v1/.

    The version of the OpenAPI document: v1
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictInt, StrictStr
from typing import Any, ClassVar, Dict, List, Optional
from typing import Optional, Set
from typing_extensions import Self

class ActivityRow(BaseModel):
    """
    ActivityRow
    """ # noqa: E501
    action: Optional[StrictStr] = Field(default=None, description="Action is one of created, updated, deleted.")
    actor: Optional[StrictStr] = Field(default=None, description="Actor is the email of the principal who made the change. Empty for a write by an in-process composer; a project key can never appear here, because evaluating flags is all a key may do.")
    at: Optional[StrictStr] = Field(default=None, description="At is when the change was made, RFC 3339 UTC.")
    detail: Optional[StrictStr] = Field(default=None, description="Detail is free-form context about the change. Nothing writes it today, so it is absent from every row the store serves.")
    id: Optional[StrictInt] = Field(default=None, description="ID is the log's own sequence number, rising with each entry. The log is served newest-first, which is this descending.")
    key: Optional[StrictStr] = Field(default=None, description="Key is the flag that changed. It survives a delete, so the log still names flags the definition store no longer holds.")
    __properties: ClassVar[List[str]] = ["action", "actor", "at", "detail", "id", "key"]

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of ActivityRow from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of ActivityRow from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "action": obj.get("action"),
            "actor": obj.get("actor"),
            "at": obj.get("at"),
            "detail": obj.get("detail"),
            "id": obj.get("id"),
            "key": obj.get("key")
        })
        return _obj


