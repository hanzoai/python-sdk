# coding: utf-8

"""
    Hanzo Cloud API

    The Hanzo Cloud API as a customer calls it: every operation under /v1/ except the operator's admin product, relay routes, legacy spellings and capabilities still reached by flag. Tagged by product: the first path segment after /v1/.

    The version of the OpenAPI document: v1
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictBool, StrictInt, StrictStr
from typing import Any, ClassVar, Dict, List, Optional
from typing import Optional, Set
from typing_extensions import Self

class RiskLineage(BaseModel):
    """
    RiskLineage
    """ # noqa: E501
    dataset: Optional[StrictStr] = Field(default=None, description="Dataset is the dataset traced.")
    digest: Optional[StrictStr] = Field(default=None, description="Digest is the version's fingerprint, repeated here so a lineage answer is self-contained.")
    var_from: Optional[StrictStr] = Field(default=None, description="From is where the window actually read opens, RFC 3339. Same as the spec's.", alias="from")
    holds: Optional[StrictInt] = Field(default=None, description="Holds is what the source holds for the same window NOW. The difference between it and Rows is the whole of the reproducibility claim.")
    oversize: Optional[StrictInt] = Field(default=None, description="Oversize is how many subjects the window held that were too large to represent when this version was built. It is part of the fingerprint, so it is part of what \"reproducible\" is measured over.")
    refusal: Optional[StrictStr] = Field(default=None, description="Refusal says which way it failed — the window expired, or the source now holds a different count. Absent when Reproducible is true.")
    reproducible: Optional[StrictBool] = Field(default=None, description="Reproducible is true when the source still holds what this version was built from — measured by asking it again, not recalled. False is ordinary: the source is fed by a rollup that runs behind the events, so \"it holds more now\" is the common case and it means re-running the spec would not produce this version.")
    retention: Optional[StrictStr] = Field(default=None, description="Retention is the source's own expiry rule as the store reports it, read at materialisation time rather than assumed. A source whose retention is shorter than this window cannot re-derive it.")
    rows: Optional[StrictInt] = Field(default=None, description="Rows is how many rows the source held for that window at materialisation time. Holds is the same question asked now, and the difference between them is the whole of the reproducibility claim.")
    share: Optional[StrictInt] = Field(default=None, description="Share is the fraction of subjects admitted, in thousandths.")
    source: Optional[StrictStr] = Field(default=None, description="Source is the plane the rows were derived from.")
    subjects: Optional[StrictInt] = Field(default=None, description="Subjects is how many distinct subjects those rows belonged to. It is the real sample size — the row count flatters it whenever a subject is active.")
    to: Optional[StrictStr] = Field(default=None, description="To is where it ends: the spec's own end pulled BACK by the maturity horizon, so it is usually earlier than the spec says. This is the window a reproduction has to ask for — asking the spec's would not return these rows.")
    version: Optional[StrictInt] = Field(default=None, description="Version is the version traced — the one asked for, or the newest published one when the request named none.")
    __properties: ClassVar[List[str]] = ["dataset", "digest", "from", "holds", "oversize", "refusal", "reproducible", "retention", "rows", "share", "source", "subjects", "to", "version"]

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of RiskLineage from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of RiskLineage from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "dataset": obj.get("dataset"),
            "digest": obj.get("digest"),
            "from": obj.get("from"),
            "holds": obj.get("holds"),
            "oversize": obj.get("oversize"),
            "refusal": obj.get("refusal"),
            "reproducible": obj.get("reproducible"),
            "retention": obj.get("retention"),
            "rows": obj.get("rows"),
            "share": obj.get("share"),
            "source": obj.get("source"),
            "subjects": obj.get("subjects"),
            "to": obj.get("to"),
            "version": obj.get("version")
        })
        return _obj


