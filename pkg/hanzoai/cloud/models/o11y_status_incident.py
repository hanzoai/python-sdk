# coding: utf-8

"""
    Hanzo Cloud API

    The Hanzo Cloud API as a customer calls it: every operation under /v1/ except the operator's admin product, relay doors, legacy spellings and capabilities still reached by flag. Tagged by product: the first path segment after /v1/.

    The version of the OpenAPI document: v1
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictStr
from typing import Any, ClassVar, Dict, List, Optional
from hanzoai.cloud.models.o11y_status_component import O11yStatusComponent
from typing import Optional, Set
from typing_extensions import Self

class O11yStatusIncident(BaseModel):
    """
    O11yStatusIncident
    """ # noqa: E501
    affected_components: Optional[List[O11yStatusComponent]] = Field(default=None, description="AffectedComponents is what this incident covers. It is COUNTED rather than classified: some services down is a partial outage and every probed service down is a full one, because deciding that one service is critical and another is not would need a judgement nobody has measured.")
    current_worst_impact: Optional[StrictStr] = Field(default=None, description="CurrentWorstImpact is the incident's impact on the PLATFORM, which is not the same question as the component's own condition above.")
    id: Optional[StrictStr] = Field(default=None, description="ID is derived from the service, so the same outage keeps one id across reads rather than being reported as a new incident every 15 seconds.")
    last_update_at: Optional[StrictStr] = Field(default=None, description="LastUpdateAt is when the failing measurement this incident reports was read, RFC3339 UTC.")
    last_update_message: Optional[StrictStr] = Field(default=None, description="LastUpdateMessage says what was observed, not what is being done about it — there is no operator writing updates here, only the probe that failed.")
    name: Optional[StrictStr] = Field(default=None, description="Name is the one-line headline, built from the service that stopped answering.")
    status: Optional[StrictStr] = Field(default=None, description="Status is always \"investigating\" — the member of the client's closed set that means detected, cause not yet established, which is exactly what an automated prober knows. Nothing here ever claims \"identified\": that would assert a diagnosis no measurement made.")
    url: Optional[StrictStr] = Field(default=None, description="URL points at the HUMAN status page, not back at this JSON. Every link in this document goes to the same place.")
    __properties: ClassVar[List[str]] = ["affected_components", "current_worst_impact", "id", "last_update_at", "last_update_message", "name", "status", "url"]

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of O11yStatusIncident from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # override the default output from pydantic by calling `to_dict()` of each item in affected_components (list)
        _items = []
        if self.affected_components:
            for _item_affected_components in self.affected_components:
                if _item_affected_components:
                    _items.append(_item_affected_components.to_dict())
            _dict['affected_components'] = _items
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of O11yStatusIncident from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "affected_components": [O11yStatusComponent.from_dict(_item) for _item in obj["affected_components"]] if obj.get("affected_components") is not None else None,
            "current_worst_impact": obj.get("current_worst_impact"),
            "id": obj.get("id"),
            "last_update_at": obj.get("last_update_at"),
            "last_update_message": obj.get("last_update_message"),
            "name": obj.get("name"),
            "status": obj.get("status"),
            "url": obj.get("url")
        })
        return _obj


