# coding: utf-8

"""
    Hanzo Cloud API

    The Hanzo Cloud API as a customer calls it: every operation under /v1/ except the operator's admin product, relay doors, legacy spellings and capabilities still reached by flag. Tagged by product: the first path segment after /v1/.

    The version of the OpenAPI document: v1
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictInt, StrictStr
from typing import Any, ClassVar, Dict, List, Optional
from hanzoai.cloud.models.taxon import Taxon
from typing import Optional, Set
from typing_extensions import Self

class Category(BaseModel):
    """
    Category
    """ # noqa: E501
    brands: Optional[List[StrictStr]] = Field(default=None, description="Brands are the brands whose console shows this category. Absent means every brand.")
    id: Optional[StrictStr] = Field(default=None, description="ID is the stable slug this category is addressed by, e.g. \"observe\".")
    label: Optional[StrictStr] = Field(default=None, description="Label is the display name, e.g. \"Observe\".")
    order: Optional[StrictInt] = Field(default=None, description="Order is where the category sits among its siblings, ascending.")
    owner: Optional[StrictStr] = Field(default=None, description="Owner is the org this category belongs to: the platform's own org for a category every tenant sees, or your org for one you added. It tells a console which rows it may offer to edit.")
    summary: Optional[StrictStr] = Field(default=None, description="Summary is the one line describing what the category groups, shown as the header copy on its landing page.")
    taxa: Optional[List[Taxon]] = Field(default=None, description="Taxa are the products filed under this category, in display order.")
    __properties: ClassVar[List[str]] = ["brands", "id", "label", "order", "owner", "summary", "taxa"]

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of Category from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # override the default output from pydantic by calling `to_dict()` of each item in taxa (list)
        _items = []
        if self.taxa:
            for _item_taxa in self.taxa:
                if _item_taxa:
                    _items.append(_item_taxa.to_dict())
            _dict['taxa'] = _items
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of Category from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "brands": obj.get("brands"),
            "id": obj.get("id"),
            "label": obj.get("label"),
            "order": obj.get("order"),
            "owner": obj.get("owner"),
            "summary": obj.get("summary"),
            "taxa": [Taxon.from_dict(_item) for _item in obj["taxa"]] if obj.get("taxa") is not None else None
        })
        return _obj


