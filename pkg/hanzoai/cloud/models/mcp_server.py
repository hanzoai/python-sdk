# coding: utf-8

"""
    Hanzo Cloud API

    The Hanzo Cloud API as a customer calls it: every operation under /v1/ except the operator's admin product, relay doors, legacy spellings and capabilities still reached by flag. Tagged by product: the first path segment after /v1/.

    The version of the OpenAPI document: v1
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictBool, StrictInt, StrictStr
from typing import Any, ClassVar, Dict, List, Optional
from typing import Optional, Set
from typing_extensions import Self

class MCPServer(BaseModel):
    """
    MCPServer
    """ # noqa: E501
    auth_header: Optional[StrictStr] = Field(default=None, description="AuthHeader is the request header the KMS-held credential is injected into, e.g. \"Authorization\". Absent when the server needs no credential.", alias="authHeader")
    created_at: Optional[StrictInt] = Field(default=None, description="CreatedAt is when the server was registered, Unix seconds.", alias="createdAt")
    has_secret: Optional[StrictBool] = Field(default=None, description="HasSecret is whether a credential is sealed in KMS for this server. The VALUE is never returned by any route.", alias="hasSecret")
    id: Optional[StrictStr] = Field(default=None, description="ID is the server's id within the org. It also PREFIXES every tool name the server contributes, which is what keeps two servers' \"search\" apart.")
    listing: Optional[StrictStr] = Field(default=None, description="Listing is the catalog entry this server was enabled from, when it was. Empty means the org typed the URL in itself.")
    name: Optional[StrictStr] = Field(default=None, description="Name is the org's label for the server.")
    org: Optional[StrictStr] = Field(default=None, description="Org is the org that registered the server — the validated caller's.")
    source: Optional[StrictStr] = Field(default=None, description="Source is where the registration came from: \"catalog\" when it was enabled off the shelf, \"org\" when the org registered the URL itself. It is DERIVED from Listing rather than stored, because two columns for one fact is two chances to disagree.")
    url: Optional[StrictStr] = Field(default=None, description="URL is the server's JSON-RPC endpoint. Always a public http(s) host: the registration boundary and the dialer both refuse anything else.")
    __properties: ClassVar[List[str]] = ["authHeader", "createdAt", "hasSecret", "id", "listing", "name", "org", "source", "url"]

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of MCPServer from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of MCPServer from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "authHeader": obj.get("authHeader"),
            "createdAt": obj.get("createdAt"),
            "hasSecret": obj.get("hasSecret"),
            "id": obj.get("id"),
            "listing": obj.get("listing"),
            "name": obj.get("name"),
            "org": obj.get("org"),
            "source": obj.get("source"),
            "url": obj.get("url")
        })
        return _obj


