# coding: utf-8

"""
    Hanzo Cloud API

    The Hanzo Cloud API as a customer calls it: every operation under /v1/ except the operator's admin product, relay routes, legacy spellings and capabilities still reached by flag. Tagged by product: the first path segment after /v1/.

    The version of the OpenAPI document: v1
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictInt, StrictStr
from typing import Any, ClassVar, Dict, List, Optional
from typing import Optional, Set
from typing_extensions import Self

class BuildSummary(BaseModel):
    """
    BuildSummary
    """ # noqa: E501
    agent: Optional[StrictStr] = Field(default=None, description="Agent is the label the surface that did the work calls itself by.")
    ended_at: Optional[StrictStr] = Field(default=None, description="EndedAt is when it finished, same format. Empty means it is still going.", alias="endedAt")
    org: Optional[StrictStr] = Field(default=None, description="Org and Project are the build's public ADDRESS — the pair the full story is read at, and the pair a visitor sees in the URL bar. Not a tenant key: this index is anonymous and lists only what authors published.")
    project: Optional[StrictStr] = Field(default=None, description="Project is the product's slug, the second half of that address.")
    repo: Optional[StrictStr] = Field(default=None, description="Repo is the repository the work was done in, as the session reported it.")
    session: Optional[StrictStr] = Field(default=None, description="Session is the agent session behind the build, and the value its commits name in their `Hanzo-Session:` trailer.")
    started_at: Optional[StrictStr] = Field(default=None, description="StartedAt is when the session opened, RFC 3339 in UTC.", alias="startedAt")
    status: Optional[StrictStr] = Field(default=None, description="Status is the session's own: running, paused, done or error — so a card can show a build still being written.")
    title: Optional[StrictStr] = Field(default=None, description="Title is the human line for the card. Sent even when empty, like every field here, because that is what this route has always sent.")
    turns: Optional[StrictInt] = Field(default=None, description="Turns is HOW MANY turns the transcript holds — a COUNT, unlike the `turn` on each turn of the full story, which is that turn's position. The full read returns at most 1000 of them; this number is not capped.")
    __properties: ClassVar[List[str]] = ["agent", "endedAt", "org", "project", "repo", "session", "startedAt", "status", "title", "turns"]

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of BuildSummary from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of BuildSummary from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "agent": obj.get("agent"),
            "endedAt": obj.get("endedAt"),
            "org": obj.get("org"),
            "project": obj.get("project"),
            "repo": obj.get("repo"),
            "session": obj.get("session"),
            "startedAt": obj.get("startedAt"),
            "status": obj.get("status"),
            "title": obj.get("title"),
            "turns": obj.get("turns")
        })
        return _obj


