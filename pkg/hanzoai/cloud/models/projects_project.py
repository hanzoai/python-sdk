# coding: utf-8

"""
    Hanzo Cloud API

    The Hanzo Cloud API as a customer calls it: every operation under /v1/ except the operator's admin product, relay routes, legacy spellings and capabilities still reached by flag. Tagged by product: the first path segment after /v1/.

    The version of the OpenAPI document: v1
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictBool, StrictInt, StrictStr
from typing import Any, ClassVar, Dict, List, Optional
from hanzoai.cloud.models.projects_repo import ProjectsRepo
from typing import Optional, Set
from typing_extensions import Self

class ProjectsProject(BaseModel):
    """
    ProjectsProject
    """ # noqa: E501
    analytics: Optional[StrictBool] = Field(default=None, description="Analytics is whether the web-analytics beacon is injected into this site's pages. It is ON by default — a project has to opt out — and it is what the static builder reads to decide whether to inject at all.")
    bucket: Optional[StrictStr] = Field(default=None, description="Bucket is the object-store bucket the site's files are served out of.")
    cache_control: Optional[StrictStr] = Field(default=None, description="CacheControl is the Cache-Control policy the edge serves this site's HTML under — how long a reader may hold a stale page before asking again. Assets are content-addressed and are not governed by it.", alias="cacheControl")
    created_at: Optional[StrictInt] = Field(default=None, description="CreatedAt is when the project was created, as Unix seconds.", alias="createdAt")
    current_deployment_id: Optional[StrictStr] = Field(default=None, description="CurrentDeploymentID names the deployment currently serving, so a caller can ask what is live without scanning the history.", alias="currentDeploymentId")
    description: Optional[StrictStr] = Field(default=None, description="Description is the one-line summary, which is copied onto forks of this project and shown on a gallery card.")
    forked_from: Optional[StrictStr] = Field(default=None, description="ForkedFrom is the parent this project was forked from (\"<org>/<slug>\" of a published project, or a catalog template slug) — the attribution edge a gallery credits.", alias="forkedFrom")
    framework: Optional[StrictStr] = Field(default=None, description="Framework is a BUILD HINT from a closed set, defaulting to static. It tells CI how to build a linked repo and never gates a deploy, so a wrong value costs a build rather than access.")
    hidden: Optional[StrictBool] = Field(default=None, description="Hidden is PLATFORM MODERATION, and it is a different axis from visibility: it pulls a public project out of the catalogue without editing the publisher's own choice, so un-hiding restores exactly what they asked for. A project is listed only when it is public AND not hidden. Always present, never omitted, for the same reason as visibility.")
    hidden_reason: Optional[StrictStr] = Field(default=None, description="HiddenReason is why moderation hid it. Absent when it is not hidden.", alias="hiddenReason")
    id: Optional[StrictStr] = Field(default=None, description="ID is the project's internal identifier. It is stable across a rename, but it is not what the API addresses this project by — `slug` is.")
    key: Optional[StrictStr] = Field(default=None, description="Key is the project's publishable ingest key, minted at create. It is the value the injected beacon carries and the ONE thing that attributes this site's events; the static-builder reads it beside analytics.  Publishable means it belongs in a page's source: it names a write scope and mints no principal, so it is returned in full rather than masked. Masking it would only mean every caller needed a second endpoint to get the thing the page already ships.")
    last_purge_at: Optional[StrictInt] = Field(default=None, description="LastPurgeAt is when the edge cache was last cleared, as Unix seconds, so a console can say how fresh what readers see actually is. Absent means never.", alias="lastPurgeAt")
    license: Optional[StrictStr] = Field(default=None, description="License is the terms that upstream work carries. Absent has the same reading: undeclared, not unencumbered.")
    live_url: Optional[StrictStr] = Field(default=None, description="LiveURL is where the site answers today. Absent until something has been deployed.", alias="liveUrl")
    name: Optional[StrictStr] = Field(default=None, description="Name is the project's display name, free text a person chose.")
    org: Optional[StrictStr] = Field(default=None, description="Org is the organisation that owns the project, and therefore who pays for it and who may change it. It is also the AUTHORSHIP line a gallery credits; there is no separate author field.")
    repo: Optional[ProjectsRepo] = Field(default=None, description="Repo is the git source this project builds from, empty when it is deployed by uploading an artifact instead.")
    slug: Optional[StrictStr] = Field(default=None, description="Slug is the identifier that MATTERS: the handle every later call addresses, the S3 key segment the site's objects live under, and the label of the public host `<slug>.hanzo.app`. Because it is a hostname it is constrained and reserved labels such as `api` are refused.")
    space: Optional[StrictStr] = Field(default=None, description="Space is the project's Base data space, which is where a deployed site's form, forum and data submissions land. Absent means the site stores nothing.")
    starred: Optional[StrictBool] = Field(default=None, description="Starred is THIS CALLER's star, not a property of the project — two people in the same org see different values for the same row, which is the whole point of it. Always present so a client can tell \"not starred\" from \"this API is too old to say\", the same reason visibility and hidden are.")
    status: Optional[StrictStr] = Field(default=None, description="Status is where the project stands — whether a build has ever succeeded and whether anything is serving right now.")
    tags: Optional[Dict[str, StrictStr]] = Field(default=None, description="Tags is the site's browser tag config: platform slug → non-secret pixel id (GA measurement, Meta pixel, …) — what track.js injects and the server CAPI reads, per site. Omitted when none are set. The API SECRET is never here (KMS).")
    updated_at: Optional[StrictInt] = Field(default=None, description="UpdatedAt is when the project's own record last changed, as Unix seconds. A deploy is not an edit of the project, so this does not move on every publish.", alias="updatedAt")
    upstream: Optional[StrictStr] = Field(default=None, description="Upstream credits the third-party work this project was published from — a free-text line, because the honest answer is a name and a title that no enum could hold. Absent means NOBODY HAS SAID, not that there is nothing to say.")
    visibility: Optional[StrictStr] = Field(default=None, description="Visibility is \"public\" or \"private\", and Hidden reports platform moderation. Both are always present (never omitempty) so a consumer can tell a real answer from \"this API is too old to say\" — and so a console never renders a project as public because a field was missing.  Authorship is deliberately absent: it is Org, above.")
    __properties: ClassVar[List[str]] = ["analytics", "bucket", "cacheControl", "createdAt", "currentDeploymentId", "description", "forkedFrom", "framework", "hidden", "hiddenReason", "id", "key", "lastPurgeAt", "license", "liveUrl", "name", "org", "repo", "slug", "space", "starred", "status", "tags", "updatedAt", "upstream", "visibility"]

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of ProjectsProject from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # override the default output from pydantic by calling `to_dict()` of repo
        if self.repo:
            _dict['repo'] = self.repo.to_dict()
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of ProjectsProject from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "analytics": obj.get("analytics"),
            "bucket": obj.get("bucket"),
            "cacheControl": obj.get("cacheControl"),
            "createdAt": obj.get("createdAt"),
            "currentDeploymentId": obj.get("currentDeploymentId"),
            "description": obj.get("description"),
            "forkedFrom": obj.get("forkedFrom"),
            "framework": obj.get("framework"),
            "hidden": obj.get("hidden"),
            "hiddenReason": obj.get("hiddenReason"),
            "id": obj.get("id"),
            "key": obj.get("key"),
            "lastPurgeAt": obj.get("lastPurgeAt"),
            "license": obj.get("license"),
            "liveUrl": obj.get("liveUrl"),
            "name": obj.get("name"),
            "org": obj.get("org"),
            "repo": ProjectsRepo.from_dict(obj["repo"]) if obj.get("repo") is not None else None,
            "slug": obj.get("slug"),
            "space": obj.get("space"),
            "starred": obj.get("starred"),
            "status": obj.get("status"),
            "tags": obj.get("tags"),
            "updatedAt": obj.get("updatedAt"),
            "upstream": obj.get("upstream"),
            "visibility": obj.get("visibility")
        })
        return _obj


