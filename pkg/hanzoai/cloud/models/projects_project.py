# coding: utf-8

"""
    Hanzo Cloud — Unified API (V8 · Open Edition)

    The single unified OpenAPI surface for ALL Hanzo services, aggregated from the per-service specs. Every route is https://api.hanzo.ai/v1/<service>/*. Grouped by the canonical domains in capabilities.yaml (the ONE registry). Regenerated by merge.py — edit the per-service <svc>/openapi.yaml + capabilities.yaml, not this file.

    The version of the OpenAPI document: 8.0.0
    Contact: support@hanzo.ai
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictInt, StrictStr, field_validator
from typing import Any, ClassVar, Dict, List, Optional
from hanzoai.cloud.models.projects_repo_ref import ProjectsRepoRef
from typing import Optional, Set
from typing_extensions import Self

class ProjectsProject(BaseModel):
    """
    The org-scoped project — the slug-keyed record of a deployable site (the json shape console consumes).
    """ # noqa: E501
    id: StrictStr
    org: StrictStr
    slug: StrictStr
    name: StrictStr
    description: Optional[StrictStr] = None
    repo: ProjectsRepoRef
    framework: StrictStr = Field(description="Build hint.")
    status: StrictStr = Field(description="Project lifecycle status.")
    live_url: Optional[StrictStr] = Field(default=None, description="Canonical live URL, https://<slug>.<apex>. Set once deployed.", alias="liveUrl")
    bucket: Optional[StrictStr] = Field(default=None, description="S3-origin bucket holding the site.")
    current_deployment_id: Optional[StrictStr] = Field(default=None, alias="currentDeploymentId")
    cache_control: Optional[StrictStr] = Field(default=None, description="Per-project HTML/document Cache-Control policy applied at the S3 origin.", alias="cacheControl")
    last_purge_at: Optional[StrictInt] = Field(default=None, description="Unix time (seconds) of the last edge cache-tag purge.", alias="lastPurgeAt")
    created_at: StrictInt = Field(alias="createdAt")
    updated_at: StrictInt = Field(alias="updatedAt")
    __properties: ClassVar[List[str]] = ["id", "org", "slug", "name", "description", "repo", "framework", "status", "liveUrl", "bucket", "currentDeploymentId", "cacheControl", "lastPurgeAt", "createdAt", "updatedAt"]

    @field_validator('framework')
    def framework_validate_enum(cls, value):
        """Validates the enum"""
        if value not in set(['static', 'vite', 'next', 'react', 'astro', 'svelte', 'vue', 'remix', 'nuxt', 'unity', 'unreal', 'godot']):
            raise ValueError("must be one of enum values ('static', 'vite', 'next', 'react', 'astro', 'svelte', 'vue', 'remix', 'nuxt', 'unity', 'unreal', 'godot')")
        return value

    @field_validator('status')
    def status_validate_enum(cls, value):
        """Validates the enum"""
        if value not in set(['draft', 'building', 'live', 'error']):
            raise ValueError("must be one of enum values ('draft', 'building', 'live', 'error')")
        return value

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of ProjectsProject from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # override the default output from pydantic by calling `to_dict()` of repo
        if self.repo:
            _dict['repo'] = self.repo.to_dict()
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of ProjectsProject from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "id": obj.get("id"),
            "org": obj.get("org"),
            "slug": obj.get("slug"),
            "name": obj.get("name"),
            "description": obj.get("description"),
            "repo": ProjectsRepoRef.from_dict(obj["repo"]) if obj.get("repo") is not None else None,
            "framework": obj.get("framework"),
            "status": obj.get("status"),
            "liveUrl": obj.get("liveUrl"),
            "bucket": obj.get("bucket"),
            "currentDeploymentId": obj.get("currentDeploymentId"),
            "cacheControl": obj.get("cacheControl"),
            "lastPurgeAt": obj.get("lastPurgeAt"),
            "createdAt": obj.get("createdAt"),
            "updatedAt": obj.get("updatedAt")
        })
        return _obj


