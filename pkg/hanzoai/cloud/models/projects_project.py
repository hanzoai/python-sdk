# coding: utf-8

"""
    Hanzo Cloud API

    Composed from each subsystem's own projection of its router, in the fleet's mount order — every operation below is a route the subsystem that publishes it registered. Tagged by product: the first path segment after /v1/.

    The version of the OpenAPI document: v1
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictBool, StrictInt, StrictStr
from typing import Any, ClassVar, Dict, List, Optional
from hanzoai.cloud.models.projects_repo import ProjectsRepo
from typing import Optional, Set
from typing_extensions import Self

class ProjectsProject(BaseModel):
    """
    ProjectsProject
    """ # noqa: E501
    analytics: Optional[StrictBool] = Field(default=None, description="Analytics is the wired-by-default web-analytics flag (default true). It is the value the app's static-builder reads as deployment.analytics to inject the beacon. Space is the project's Base data space (\"<org>/<slug>\") a deployed site posts form/forum/data submissions to under /v1/base.")
    bucket: Optional[StrictStr] = None
    cache_control: Optional[StrictStr] = Field(default=None, description="Cache is the site's edge-cache state: the HTML/document Cache-Control policy in effect (TTL) and the last edge-purge time, so a console can show freshness.", alias="cacheControl")
    created_at: Optional[StrictInt] = Field(default=None, alias="createdAt")
    current_deployment_id: Optional[StrictStr] = Field(default=None, alias="currentDeploymentId")
    description: Optional[StrictStr] = None
    forked_from: Optional[StrictStr] = Field(default=None, description="ForkedFrom is the parent this project was forked from (\"<org>/<slug>\" of a published project, or a catalog template slug) — the attribution edge a gallery credits.", alias="forkedFrom")
    framework: Optional[StrictStr] = None
    hidden: Optional[StrictBool] = None
    hidden_reason: Optional[StrictStr] = Field(default=None, alias="hiddenReason")
    id: Optional[StrictStr] = None
    key: Optional[StrictStr] = Field(default=None, description="Key is the project's publishable ingest key, minted at create. It is the value the injected beacon carries and the ONE thing that attributes this site's events; the static-builder reads it beside analytics.  Publishable means it belongs in a page's source: it names a write scope and mints no principal, so it is returned in full rather than masked. Masking it would only mean every caller needed a second endpoint to get the thing the page already ships.")
    last_purge_at: Optional[StrictInt] = Field(default=None, alias="lastPurgeAt")
    license: Optional[StrictStr] = None
    live_url: Optional[StrictStr] = Field(default=None, alias="liveUrl")
    name: Optional[StrictStr] = None
    org: Optional[StrictStr] = None
    repo: Optional[ProjectsRepo] = None
    slug: Optional[StrictStr] = None
    space: Optional[StrictStr] = None
    status: Optional[StrictStr] = None
    tags: Optional[Dict[str, StrictStr]] = Field(default=None, description="Tags is the site's browser tag config: platform slug → non-secret pixel id (GA measurement, Meta pixel, …) — what track.js injects and the server CAPI reads, per site. Omitted when none are set. The API SECRET is never here (KMS).")
    updated_at: Optional[StrictInt] = Field(default=None, alias="updatedAt")
    upstream: Optional[StrictStr] = Field(default=None, description="Upstream/License credit the third-party work this project was published from, and the terms it carries. Omitted when nothing is declared: an absent credit means \"nobody has said\", not \"there is nothing to say\".")
    visibility: Optional[StrictStr] = Field(default=None, description="Visibility is \"public\" or \"private\", and Hidden reports platform moderation. Both are always present (never omitempty) so a consumer can tell a real answer from \"this API is too old to say\" — and so a console never renders a project as public because a field was missing.  Authorship is deliberately absent: it is Org, above.")
    __properties: ClassVar[List[str]] = ["analytics", "bucket", "cacheControl", "createdAt", "currentDeploymentId", "description", "forkedFrom", "framework", "hidden", "hiddenReason", "id", "key", "lastPurgeAt", "license", "liveUrl", "name", "org", "repo", "slug", "space", "status", "tags", "updatedAt", "upstream", "visibility"]

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of ProjectsProject from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # override the default output from pydantic by calling `to_dict()` of repo
        if self.repo:
            _dict['repo'] = self.repo.to_dict()
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of ProjectsProject from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "analytics": obj.get("analytics"),
            "bucket": obj.get("bucket"),
            "cacheControl": obj.get("cacheControl"),
            "createdAt": obj.get("createdAt"),
            "currentDeploymentId": obj.get("currentDeploymentId"),
            "description": obj.get("description"),
            "forkedFrom": obj.get("forkedFrom"),
            "framework": obj.get("framework"),
            "hidden": obj.get("hidden"),
            "hiddenReason": obj.get("hiddenReason"),
            "id": obj.get("id"),
            "key": obj.get("key"),
            "lastPurgeAt": obj.get("lastPurgeAt"),
            "license": obj.get("license"),
            "liveUrl": obj.get("liveUrl"),
            "name": obj.get("name"),
            "org": obj.get("org"),
            "repo": ProjectsRepo.from_dict(obj["repo"]) if obj.get("repo") is not None else None,
            "slug": obj.get("slug"),
            "space": obj.get("space"),
            "status": obj.get("status"),
            "tags": obj.get("tags"),
            "updatedAt": obj.get("updatedAt"),
            "upstream": obj.get("upstream"),
            "visibility": obj.get("visibility")
        })
        return _obj


