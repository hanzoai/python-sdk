# coding: utf-8

"""
    Hanzo Cloud API

    The Hanzo Cloud API as a customer calls it: every operation under /v1/ except the operator's admin product, relay routes, legacy spellings and capabilities still reached by flag. Tagged by product: the first path segment after /v1/.

    The version of the OpenAPI document: v1
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictFloat, StrictInt, StrictStr
from typing import Any, ClassVar, Dict, List, Optional, Union
from typing import Optional, Set
from typing_extensions import Self

class SampleView(BaseModel):
    """
    SampleView
    """ # noqa: E501
    at: Optional[StrictStr] = Field(default=None, description="At is when the reading was MEASURED, RFC 3339 in UTC — the x-axis a chart plots against. The series is returned oldest first, so it only increases.")
    cost_cents: Optional[StrictInt] = Field(default=None, description="CostCents is what this unit resold for over the hour the reading falls in, in whole US cents. 0 means UNPRICED, not free: the operator's own machines — a linked run-target, a dialed-in BYO worker — are metered for utilization and never resold, so only a priced source ever fills it.", alias="costCents")
    cpus: Optional[StrictInt] = Field(default=None, description="CPUs is logical cores. The static capability rides every row on purpose: a chart can size load against cores without joining a registry whose row may since have been rewritten or the unit deregistered.")
    gpu_model: Optional[StrictStr] = Field(default=None, description="GPUModel names the representative accelerator (\"GB10\"); GPUs carries how many.", alias="gpuModel")
    gpu_util: Optional[Union[StrictFloat, StrictInt]] = Field(default=None, description="GPUUtil is aggregate accelerator utilization as a FRACTION of 1 — 0.42 is 42% busy. Anything a reporter sends outside 0..1 is clamped into it on write.", alias="gpuUtil")
    gpus: Optional[StrictInt] = Field(default=None, description="GPUs is how many accelerators the reading covers.")
    host: Optional[StrictStr] = Field(default=None, description="Host is the hostname the unit reported at the time of the reading.")
    kind: Optional[StrictStr] = Field(default=None, description="Kind is what the measured unit is: laptop, cloud, gpu, cluster, machine or worker.")
    load1: Optional[Union[StrictFloat, StrictInt]] = Field(default=None, description="Load1 is the 1-minute load average — runnable processes, not a percentage.")
    load5: Optional[Union[StrictFloat, StrictInt]] = Field(default=None, description="Load5 is the 5-minute load average, the same units as Load1.")
    load15: Optional[Union[StrictFloat, StrictInt]] = Field(default=None, description="Load15 is the 15-minute load average, the same units as Load1.")
    mem_free: Optional[StrictInt] = Field(default=None, description="MemFree is host memory available, in BYTES, as reported rather than derived.", alias="memFree")
    mem_used: Optional[StrictInt] = Field(default=None, description="MemUsed is host memory in use, in BYTES.", alias="memUsed")
    memory: Optional[StrictInt] = Field(default=None, description="Memory is total system RAM in BYTES at the time of the reading.")
    source: Optional[StrictStr] = Field(default=None, description="Source is the plane that reported the reading: \"agent\", \"byo\" or \"visor\" — the same vocabulary the board's rows carry, and what ?source= narrows on.")
    unit: Optional[StrictStr] = Field(default=None, description="Unit is the source's own id for the measured unit. With Source it is the key the chart groups by, and the key the board joins a unit's latest reading on.")
    __properties: ClassVar[List[str]] = ["at", "costCents", "cpus", "gpuModel", "gpuUtil", "gpus", "host", "kind", "load1", "load5", "load15", "memFree", "memUsed", "memory", "source", "unit"]

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of SampleView from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of SampleView from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "at": obj.get("at"),
            "costCents": obj.get("costCents"),
            "cpus": obj.get("cpus"),
            "gpuModel": obj.get("gpuModel"),
            "gpuUtil": obj.get("gpuUtil"),
            "gpus": obj.get("gpus"),
            "host": obj.get("host"),
            "kind": obj.get("kind"),
            "load1": obj.get("load1"),
            "load5": obj.get("load5"),
            "load15": obj.get("load15"),
            "memFree": obj.get("memFree"),
            "memUsed": obj.get("memUsed"),
            "memory": obj.get("memory"),
            "source": obj.get("source"),
            "unit": obj.get("unit")
        })
        return _obj


