# coding: utf-8

"""
    Hanzo Cloud API

    Composed from each subsystem's own projection of its router, in the fleet's mount order — every operation below is a route the subsystem that publishes it registered. Tagged by product: the first path segment after /v1/.

    The version of the OpenAPI document: v1
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictBool, StrictFloat, StrictInt, StrictStr
from typing import Any, ClassVar, Dict, List, Optional, Union
from typing import Optional, Set
from typing_extensions import Self

class RiskValue(BaseModel):
    """
    RiskValue
    """ # noqa: E501
    baseline: Optional[Union[StrictFloat, StrictInt]] = Field(default=None, description="Baseline is what Observed was measured against: this organisation's own history for this subject.")
    blind: Optional[StrictBool] = Field(default=None, description="Blind marks a coordinate that could not be computed and took its neutral value. A model silently reading neutral for a dimension it never has data for is indistinguishable from one reading a genuine absence of risk.")
    feature: Optional[StrictStr] = Field(default=None, description="Feature is the dimension.")
    observed: Optional[Union[StrictFloat, StrictInt]] = Field(default=None, description="Observed is the raw number X was computed from, quoted so the coordinate reads back as a sentence rather than a bare ratio.")
    unit: Optional[StrictStr] = Field(default=None, description="Unit is how to read Observed.")
    x: Optional[Union[StrictFloat, StrictInt]] = Field(default=None, description="X is the coordinate in the model space, always dimensionless.")
    __properties: ClassVar[List[str]] = ["baseline", "blind", "feature", "observed", "unit", "x"]

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of RiskValue from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of RiskValue from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "baseline": obj.get("baseline"),
            "blind": obj.get("blind"),
            "feature": obj.get("feature"),
            "observed": obj.get("observed"),
            "unit": obj.get("unit"),
            "x": obj.get("x")
        })
        return _obj


