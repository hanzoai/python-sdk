# coding: utf-8

"""
    Hanzo Cloud — Unified API (V8 · Open Edition)

    The single unified OpenAPI surface for ALL Hanzo services, aggregated from the per-service specs. Every route is https://api.hanzo.ai/v1/<service>/*. Grouped by the canonical domains in capabilities.yaml (the ONE registry). Regenerated by merge.py — edit the per-service <svc>/openapi.yaml + capabilities.yaml, not this file.

    The version of the OpenAPI document: 8.0.0
    Contact: support@hanzo.ai
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from datetime import datetime
from pydantic import BaseModel, ConfigDict, Field, StrictInt, StrictStr, field_validator
from typing import Any, ClassVar, Dict, List, Optional
from typing import Optional, Set
from typing_extensions import Self

class EngineServingEndpoint(BaseModel):
    """
    EngineServingEndpoint
    """ # noqa: E501
    name: Optional[StrictStr] = None
    model: Optional[StrictStr] = Field(default=None, description="Model identifier or path")
    status: Optional[StrictStr] = None
    replicas: Optional[StrictInt] = None
    min_replicas: Optional[StrictInt] = None
    max_replicas: Optional[StrictInt] = None
    gpu_type: Optional[StrictStr] = None
    gpu_per_replica: Optional[StrictInt] = None
    framework: Optional[StrictStr] = None
    url: Optional[StrictStr] = Field(default=None, description="Inference endpoint URL")
    created_at: Optional[datetime] = None
    updated_at: Optional[datetime] = None
    __properties: ClassVar[List[str]] = ["name", "model", "status", "replicas", "min_replicas", "max_replicas", "gpu_type", "gpu_per_replica", "framework", "url", "created_at", "updated_at"]

    @field_validator('status')
    def status_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['provisioning', 'running', 'scaling', 'failed', 'stopped']):
            raise ValueError("must be one of enum values ('provisioning', 'running', 'scaling', 'failed', 'stopped')")
        return value

    @field_validator('framework')
    def framework_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['vllm', 'tgi', 'triton', 'custom']):
            raise ValueError("must be one of enum values ('vllm', 'tgi', 'triton', 'custom')")
        return value

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of EngineServingEndpoint from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of EngineServingEndpoint from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "name": obj.get("name"),
            "model": obj.get("model"),
            "status": obj.get("status"),
            "replicas": obj.get("replicas"),
            "min_replicas": obj.get("min_replicas"),
            "max_replicas": obj.get("max_replicas"),
            "gpu_type": obj.get("gpu_type"),
            "gpu_per_replica": obj.get("gpu_per_replica"),
            "framework": obj.get("framework"),
            "url": obj.get("url"),
            "created_at": obj.get("created_at"),
            "updated_at": obj.get("updated_at")
        })
        return _obj


