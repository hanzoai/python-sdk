# coding: utf-8

"""
    Hanzo Cloud API

    The Hanzo Cloud API as a customer calls it: every operation under /v1/ except the operator's admin product, relay routes, legacy spellings and capabilities still reached by flag. Tagged by product: the first path segment after /v1/.

    The version of the OpenAPI document: v1
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictInt, StrictStr
from typing import Any, ClassVar, Dict, List, Optional
from typing import Optional, Set
from typing_extensions import Self

class Definition(BaseModel):
    """
    Definition
    """ # noqa: E501
    code: Optional[StrictStr] = Field(default=None, description="Code is the source to run, capped so one function cannot amplify the store.")
    env_names: Optional[List[StrictStr]] = Field(default=None, description="EnvNames are the secret NAMES to mount. Values live in the secret store and are never carried here.", alias="envNames")
    environment: Optional[StrictStr] = Field(default=None, description="Environment is a second spelling of runtime, accepted so a console that says \"environment\" needs no translation.")
    handler: Optional[StrictStr] = Field(default=None, description="Handler is the entry point within the code.")
    image: Optional[StrictStr] = Field(default=None, description="Image names a prebuilt image to run instead of source.")
    memory_limit: Optional[StrictStr] = Field(default=None, description="MemoryLimit is the memory the function runs with, defaulting to 256Mi. It is also the multiplier on the GB-seconds compute charge.", alias="memoryLimit")
    name: StrictStr = Field(description="Name is the function's org-unique handle and the segment that addresses it, matching ^[A-Za-z0-9][A-Za-z0-9._-]{0,63}$. The names that would shadow a collection route are reserved.")
    namespace: Optional[StrictStr] = Field(default=None, description="Namespace groups functions for display. It is cosmetic — the org is the isolation key — and is normalised to a DNS-safe label.")
    runtime: Optional[StrictStr] = Field(default=None, description="Runtime is the language the code runs under: node, python or deno.")
    target: Optional[StrictStr] = Field(default=None, description="Target is where the function runs: sandbox (the default) or fleet, the org's own GPU fleet. fleet supports runtime=python only.")
    timeout_sec: Optional[StrictInt] = Field(default=None, description="TimeoutSec is the per-invocation deadline, defaulting to 30 and clamped at 900 — a larger value is capped rather than reset to the default.", alias="timeoutSec")
    __properties: ClassVar[List[str]] = ["code", "envNames", "environment", "handler", "image", "memoryLimit", "name", "namespace", "runtime", "target", "timeoutSec"]

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of Definition from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of Definition from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "code": obj.get("code"),
            "envNames": obj.get("envNames"),
            "environment": obj.get("environment"),
            "handler": obj.get("handler"),
            "image": obj.get("image"),
            "memoryLimit": obj.get("memoryLimit"),
            "name": obj.get("name"),
            "namespace": obj.get("namespace"),
            "runtime": obj.get("runtime"),
            "target": obj.get("target"),
            "timeoutSec": obj.get("timeoutSec")
        })
        return _obj


