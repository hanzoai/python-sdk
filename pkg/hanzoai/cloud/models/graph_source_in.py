# coding: utf-8

"""
    Hanzo Cloud API

    The Hanzo Cloud API as a customer calls it: every operation under /v1/ except the operator's admin product, relay routes, legacy spellings and capabilities still reached by flag. Tagged by product: the first path segment after /v1/.

    The version of the OpenAPI document: v1
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictStr
from typing import Any, ClassVar, Dict, List, Optional
from typing import Optional, Set
from typing_extensions import Self

class GraphSourceIn(BaseModel):
    """
    GraphSourceIn
    """ # noqa: E501
    at: Optional[StrictStr] = Field(default=None, description="At is when what the source says was so, RFC 3339. Required by ingest — which records — and read by nothing in extract, which records nothing. Required for the same reason /v1/graph requires it: it is part of the assertion's content address, so re-reading one source at one instant records one set of rows however many times it is delivered. A clock read here instead would append the whole document again on every re-read.")
    source: Optional[StrictStr] = Field(default=None, description="Source names where the text came from — a URL, a document id, a page title. It is stamped on every assertion as its source, and with the section number as its evidence, so a claim can be traced back to the passage that made it. Required.")
    subject: Optional[StrictStr] = Field(default=None, description="Subject is the entity the text is about before any heading names one. A document that states relations above its first heading needs it; one whose every section is headed does not.")
    text: Optional[StrictStr] = Field(default=None, description="Text is the document. Relations are read from it and from nothing else: a line written `relation:: value` states one, and prose states none. Required.")
    __properties: ClassVar[List[str]] = ["at", "source", "subject", "text"]

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of GraphSourceIn from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of GraphSourceIn from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "at": obj.get("at"),
            "source": obj.get("source"),
            "subject": obj.get("subject"),
            "text": obj.get("text")
        })
        return _obj


