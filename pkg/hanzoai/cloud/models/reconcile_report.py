# coding: utf-8

"""
    Hanzo Cloud API

    The Hanzo Cloud API as a customer calls it: every operation under /v1/ except the operator's admin product, relay routes, legacy spellings and capabilities still reached by flag. Tagged by product: the first path segment after /v1/.

    The version of the OpenAPI document: v1
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictBool, StrictInt, StrictStr
from typing import Any, ClassVar, Dict, List, Optional
from hanzoai.cloud.models.applied_resource import AppliedResource
from hanzoai.cloud.models.reconcile_source import ReconcileSource
from typing import Optional, Set
from typing_extensions import Self

class ReconcileReport(BaseModel):
    """
    ReconcileReport
    """ # noqa: E501
    declared: Optional[StrictInt] = Field(default=None, description="Declared is how many objects the rendered source declares — the denominator the three outcome counts below are read against. Zero means the render produced nothing, which trips the prune fuse rather than sweeping the fleet.")
    failed: Optional[StrictInt] = Field(default=None, description="Failed is how many objects the apply could not reconcile. Non-zero is a PARTIAL run reported at 200: the engine applied what it could and each failure names itself in Results, so a caller reads this number rather than the status code to learn whether the fleet matches the source.")
    instance: Optional[StrictStr] = Field(default=None, description="Instance is the tracking id this run stamps on everything it manages, so a later run can tell the objects it owns from objects another instance declares. DEPLOY_ENGINE_INSTANCE names it; the default is `universe`.")
    prune: Optional[StrictBool] = Field(default=None, description="Prune reports whether DELETION was enabled for this run. False means an object the source no longer declares was left alone rather than removed, so a zero Pruned below means \"nothing to delete\" only when this is true.")
    pruned: Optional[StrictInt] = Field(default=None, description="Pruned is how many live objects this run DELETED because the source no longer declares them. Always 0 when Prune is false.")
    results: Optional[List[AppliedResource]] = Field(default=None, description="Results is one entry per object the run acted on, in the order the engine applied them. Empty (never null) when the run reconciled nothing.")
    revision: Optional[StrictStr] = Field(default=None, description="Revision is the source commit this run applied, as the source resolved it — a git commit SHA, not an image tag. It is what an operator cites when asking what the cluster was last made to match.")
    source: Optional[ReconcileSource] = Field(default=None, description="Source is the git coordinate the run rendered. It is this deployment's own configuration echoed back, never a request parameter, and it is reported so a reader of the answer knows WHICH tree the revision names.")
    synced: Optional[StrictInt] = Field(default=None, description="Synced is how many objects the run applied successfully.")
    __properties: ClassVar[List[str]] = ["declared", "failed", "instance", "prune", "pruned", "results", "revision", "source", "synced"]

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of ReconcileReport from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # override the default output from pydantic by calling `to_dict()` of each item in results (list)
        _items = []
        if self.results:
            for _item_results in self.results:
                if _item_results:
                    _items.append(_item_results.to_dict())
            _dict['results'] = _items
        # override the default output from pydantic by calling `to_dict()` of source
        if self.source:
            _dict['source'] = self.source.to_dict()
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of ReconcileReport from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "declared": obj.get("declared"),
            "failed": obj.get("failed"),
            "instance": obj.get("instance"),
            "prune": obj.get("prune"),
            "pruned": obj.get("pruned"),
            "results": [AppliedResource.from_dict(_item) for _item in obj["results"]] if obj.get("results") is not None else None,
            "revision": obj.get("revision"),
            "source": ReconcileSource.from_dict(obj["source"]) if obj.get("source") is not None else None,
            "synced": obj.get("synced")
        })
        return _obj


