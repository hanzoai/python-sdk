# coding: utf-8

"""
    Hanzo Cloud API

    The Hanzo Cloud API as a customer calls it: every operation under /v1/ except the operator's admin product, relay routes, legacy spellings and capabilities still reached by flag. Tagged by product: the first path segment after /v1/.

    The version of the OpenAPI document: v1
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictBool, StrictInt, StrictStr
from typing import Any, ClassVar, Dict, List, Optional
from hanzoai.cloud.models.trial_balance_row import TrialBalanceRow
from typing import Optional, Set
from typing_extensions import Self

class TrialBalance(BaseModel):
    """
    TrialBalance
    """ # noqa: E501
    balanced: Optional[StrictBool] = Field(default=None, description="Balanced is the proof this report exists to give: whether total debits equal total credits. It is computed from the rows above, never assumed, and false means the ledger itself is broken rather than that the report is wrong.")
    var_from: Optional[StrictStr] = Field(default=None, description="From is the posting time the window opens at, as it was asked for. Absent means the report runs from the beginning of the ledger.", alias="from")
    rows: Optional[List[TrialBalanceRow]] = Field(default=None, description="Rows are the accounts that MOVED in one of the windows. An account that never moved is omitted rather than listed at zero, so this is shorter than the chart.")
    to: Optional[StrictStr] = Field(default=None, description="To is the posting time the window closes at, inclusive. Absent means \"up to now\" — every posting the ledger holds.")
    total_credit: Optional[StrictInt] = Field(default=None, description="TotalCredit is the sum of every row's closing credit column, in cents.", alias="totalCredit")
    total_debit: Optional[StrictInt] = Field(default=None, description="TotalDebit is the sum of every row's CLOSING debit column, in cents.", alias="totalDebit")
    __properties: ClassVar[List[str]] = ["balanced", "from", "rows", "to", "totalCredit", "totalDebit"]

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of TrialBalance from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # override the default output from pydantic by calling `to_dict()` of each item in rows (list)
        _items = []
        if self.rows:
            for _item_rows in self.rows:
                if _item_rows:
                    _items.append(_item_rows.to_dict())
            _dict['rows'] = _items
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of TrialBalance from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "balanced": obj.get("balanced"),
            "from": obj.get("from"),
            "rows": [TrialBalanceRow.from_dict(_item) for _item in obj["rows"]] if obj.get("rows") is not None else None,
            "to": obj.get("to"),
            "totalCredit": obj.get("totalCredit"),
            "totalDebit": obj.get("totalDebit")
        })
        return _obj


