# coding: utf-8

"""
    Hanzo Cloud API

    The Hanzo Cloud API as a customer calls it: every operation under /v1/ except the operator's admin product, relay doors, legacy spellings and capabilities still reached by flag. Tagged by product: the first path segment after /v1/.

    The version of the OpenAPI document: v1
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictBool, StrictStr
from typing import Any, ClassVar, Dict, List, Optional
from hanzoai.cloud.models.completion import Completion
from hanzoai.cloud.models.diagnostic import Diagnostic
from hanzoai.cloud.models.location import Location
from hanzoai.cloud.models.symbol import Symbol
from typing import Optional, Set
from typing_extensions import Self

class Answer(BaseModel):
    """
    Answer
    """ # noqa: E501
    cold: Optional[StrictBool] = Field(default=None, description="Cold reports that this request paid to PREPARE the revision — the tree write, the dependency fetch and the language server's first index. It is the billed event, surfaced so a caller can see what it was charged for.")
    completions: Optional[List[Completion]] = Field(default=None, description="Completions is complete's answer: the candidates at the position, typed and resolved through the repository's dependencies rather than guessed from text.")
    diagnostics: Optional[List[Diagnostic]] = Field(default=None, description="Diagnostics is diagnostics' answer: every problem the server finds in the whole file, position ignored. Empty means it found none.")
    hover: Optional[StrictStr] = Field(default=None, description="Hover is hover's answer: the type and documentation as the language server itself renders them, so it is prose meant to be shown, not parsed.")
    lang: Optional[StrictStr] = Field(default=None, description="Lang is the language the server that answered speaks (\"go\"), as the daemon reports it. Empty when the daemon named none.")
    locations: Optional[List[Location]] = Field(default=None, description="Locations is locate's answer: where the symbol is defined, referenced, typed or implemented, per the relation asked for. Empty means the server resolved nothing there, which is an answer.")
    op: Optional[StrictStr] = Field(default=None, description="Op is the question that was asked: hover, locate, symbols, diagnostics or complete. It names which result field below is the populated one.")
    path: Optional[StrictStr] = Field(default=None, description="Path is the repo-relative file the question was about, echoed back.")
    repo: Optional[StrictStr] = Field(default=None, description="Repo is the repository the question was about, echoed back.")
    rev: Optional[StrictStr] = Field(default=None, description="Rev is the RESOLVED commit sha, never the branch or tag that was asked for. It is what makes an answer re-askable: a branch moves, this does not.")
    symbols: Optional[List[Symbol]] = Field(default=None, description="Symbols is symbols' answer: the file's whole outline, position ignored.")
    __properties: ClassVar[List[str]] = ["cold", "completions", "diagnostics", "hover", "lang", "locations", "op", "path", "repo", "rev", "symbols"]

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of Answer from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # override the default output from pydantic by calling `to_dict()` of each item in completions (list)
        _items = []
        if self.completions:
            for _item_completions in self.completions:
                if _item_completions:
                    _items.append(_item_completions.to_dict())
            _dict['completions'] = _items
        # override the default output from pydantic by calling `to_dict()` of each item in diagnostics (list)
        _items = []
        if self.diagnostics:
            for _item_diagnostics in self.diagnostics:
                if _item_diagnostics:
                    _items.append(_item_diagnostics.to_dict())
            _dict['diagnostics'] = _items
        # override the default output from pydantic by calling `to_dict()` of each item in locations (list)
        _items = []
        if self.locations:
            for _item_locations in self.locations:
                if _item_locations:
                    _items.append(_item_locations.to_dict())
            _dict['locations'] = _items
        # override the default output from pydantic by calling `to_dict()` of each item in symbols (list)
        _items = []
        if self.symbols:
            for _item_symbols in self.symbols:
                if _item_symbols:
                    _items.append(_item_symbols.to_dict())
            _dict['symbols'] = _items
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of Answer from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "cold": obj.get("cold"),
            "completions": [Completion.from_dict(_item) for _item in obj["completions"]] if obj.get("completions") is not None else None,
            "diagnostics": [Diagnostic.from_dict(_item) for _item in obj["diagnostics"]] if obj.get("diagnostics") is not None else None,
            "hover": obj.get("hover"),
            "lang": obj.get("lang"),
            "locations": [Location.from_dict(_item) for _item in obj["locations"]] if obj.get("locations") is not None else None,
            "op": obj.get("op"),
            "path": obj.get("path"),
            "repo": obj.get("repo"),
            "rev": obj.get("rev"),
            "symbols": [Symbol.from_dict(_item) for _item in obj["symbols"]] if obj.get("symbols") is not None else None
        })
        return _obj


