# coding: utf-8

"""
    Hanzo Cloud API

    The Hanzo Cloud API as a customer calls it: every operation under /v1/ except the operator's admin product, relay routes, legacy spellings and capabilities still reached by flag. Tagged by product: the first path segment after /v1/.

    The version of the OpenAPI document: v1
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictStr
from typing import Any, ClassVar, Dict, List, Optional
from typing import Optional, Set
from typing_extensions import Self

class TrustPublish(BaseModel):
    """
    TrustPublish
    """ # noqa: E501
    attester: Optional[StrictStr] = Field(default=None, description="Attester is who vouched for it: \"self\" for anything the org states itself, or \"auditor\" for anything an independent auditor put their name to. REQUIRED, and anything other than \"self\" is read as \"auditor\" — the safe direction, since an auditor-signed item can only ever be released on request.")
    body: Optional[StrictStr] = Field(default=None, description="Body is the item's content for the kinds that are text rather than a file: an article, a subprocessor entry, a dated note.")
    document: Optional[StrictStr] = Field(default=None, description="Document is a data-room document holding the item's bytes, uploaded first through POST /v1/dataroom/documents. Optional: an item can be content with no file. The document must already exist in the caller org's own store.")
    framework: Optional[StrictStr] = Field(default=None, description="Framework is the standard it speaks to. Optional and free text — the value is the org's own, not a list this API keeps.")
    kind: Optional[StrictStr] = Field(default=None, description="Kind is what the item is: report, letter, policy, questionnaire, subprocessor, article or update. Required.")
    name: Optional[StrictStr] = Field(default=None, description="Name is the item's title. Required.")
    summary: Optional[StrictStr] = Field(default=None, description="Summary is a line about it. Optional.")
    tier: Optional[StrictStr] = Field(default=None, description="Tier is who may read it: \"public\" or \"gated\". It DEFAULTS TO GATED and anything that is not exactly \"public\" is gated, so an item published by a caller that says nothing is private and someone has to release it on purpose. \"public\" is refused for an auditor-signed item.")
    __properties: ClassVar[List[str]] = ["attester", "body", "document", "framework", "kind", "name", "summary", "tier"]

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of TrustPublish from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of TrustPublish from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "attester": obj.get("attester"),
            "body": obj.get("body"),
            "document": obj.get("document"),
            "framework": obj.get("framework"),
            "kind": obj.get("kind"),
            "name": obj.get("name"),
            "summary": obj.get("summary"),
            "tier": obj.get("tier")
        })
        return _obj


