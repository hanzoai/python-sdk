# coding: utf-8

"""
    Hanzo Cloud API

    The Hanzo Cloud API as a customer calls it: every operation under /v1/ except the operator's admin product, relay doors, legacy spellings and capabilities still reached by flag. Tagged by product: the first path segment after /v1/.

    The version of the OpenAPI document: v1
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictInt, StrictStr
from typing import Any, ClassVar, Dict, List, Optional
from typing import Optional, Set
from typing_extensions import Self

class Query(BaseModel):
    """
    Query
    """ # noqa: E501
    character: Optional[StrictInt] = Field(default=None, description="Character is a 0-based UTF-16 code-unit offset within Line, per the LSP specification — not a byte offset and not a rune index.")
    line: Optional[StrictInt] = Field(default=None, description="Line is 0-based, per the LSP specification.")
    path: Optional[StrictStr] = Field(default=None, description="Path is the repo-relative file, e.g. \"apps/lsp/lsp.go\".")
    relation: Optional[StrictStr] = Field(default=None, description="Relation refines locate: definition, reference, type or implementation. Empty means definition. Every other op ignores it.")
    repo: Optional[StrictStr] = Field(default=None, description="Repo is the repository NAME within the caller's own org, e.g. \"cloud\". Not a URL and not an owner/name pair: the owner is the validated principal's org, so this names a repository the caller already owns.")
    rev: Optional[StrictStr] = Field(default=None, description="Rev is a branch, tag or commit sha. Empty means the default branch. It is resolved to a commit before anything else happens, so an answer is always about one immutable tree.")
    __properties: ClassVar[List[str]] = ["character", "line", "path", "relation", "repo", "rev"]

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of Query from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of Query from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "character": obj.get("character"),
            "line": obj.get("line"),
            "path": obj.get("path"),
            "relation": obj.get("relation"),
            "repo": obj.get("repo"),
            "rev": obj.get("rev")
        })
        return _obj


