# coding: utf-8

"""
    Hanzo Cloud API

    The Hanzo Cloud API as a customer calls it: every operation under /v1/ except the operator's admin product, relay routes, legacy spellings and capabilities still reached by flag. Tagged by product: the first path segment after /v1/.

    The version of the OpenAPI document: v1
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictInt, StrictStr
from typing import Any, ClassVar, Dict, List, Optional
from hanzoai.cloud.models.charge import Charge
from typing import Optional, Set
from typing_extensions import Self

class Tariff(BaseModel):
    """
    Tariff
    """ # noqa: E501
    currency: Optional[StrictStr] = Field(default=None, description="Currency is the ISO code every amount on this quote is denominated in.")
    due_now_cents: Optional[StrictInt] = Field(default=None, description="DueNowCents is what is charged to begin: every non-recurring line.", alias="dueNowCents")
    jurisdiction: Optional[StrictStr] = Field(default=None, description="Jurisdiction is the state of formation the filing fee belongs to.")
    lines: Optional[List[Charge]] = Field(default=None, description="Lines are the charges, in the order a reader should see them.")
    recurring: Optional[StrictStr] = Field(default=None, description="Recurring is how often RecurringCents repeats — \"yearly\" for an agent of record. Empty when nothing on this quote recurs.")
    recurring_cents: Optional[StrictInt] = Field(default=None, description="RecurringCents is what repeats, and Recurring says how often.", alias="recurringCents")
    structure: Optional[StrictStr] = Field(default=None, description="Structure is the entity this prices: c-corp, llc or dao-llc.")
    __properties: ClassVar[List[str]] = ["currency", "dueNowCents", "jurisdiction", "lines", "recurring", "recurringCents", "structure"]

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of Tariff from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # override the default output from pydantic by calling `to_dict()` of each item in lines (list)
        _items = []
        if self.lines:
            for _item_lines in self.lines:
                if _item_lines:
                    _items.append(_item_lines.to_dict())
            _dict['lines'] = _items
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of Tariff from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "currency": obj.get("currency"),
            "dueNowCents": obj.get("dueNowCents"),
            "jurisdiction": obj.get("jurisdiction"),
            "lines": [Charge.from_dict(_item) for _item in obj["lines"]] if obj.get("lines") is not None else None,
            "recurring": obj.get("recurring"),
            "recurringCents": obj.get("recurringCents"),
            "structure": obj.get("structure")
        })
        return _obj


