# coding: utf-8

"""
    Hanzo Cloud — Unified API (V8 · Open Edition)

    The single unified OpenAPI surface for ALL Hanzo services, aggregated from the per-service specs. Every route is https://api.hanzo.ai/v1/<service>/*. Grouped by the canonical domains in capabilities.yaml (the ONE registry). cloud/openapi.yaml is hanzoai/cloud's own woven document, copied verbatim by sync.py, and it wins wherever it and a hand-written spec describe the same route. Regenerated by merge.py — edit the per-service <svc>/openapi.yaml + capabilities.yaml, not this file.

    The version of the OpenAPI document: 8.0.0
    Contact: support@hanzo.ai
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, StrictInt, StrictStr, field_validator
from typing import Any, ClassVar, Dict, List, Optional
from typing import Optional, Set
from typing_extensions import Self

class MlDeployModelRequest(BaseModel):
    """
    MlDeployModelRequest
    """ # noqa: E501
    model_id: StrictStr
    model_version: Optional[StrictStr] = None
    runtime: Optional[StrictStr] = 'vllm'
    gpu: Optional[StrictStr] = 'a100'
    replicas: Optional[StrictInt] = 1
    environment: Optional[StrictStr] = 'dev'
    __properties: ClassVar[List[str]] = ["model_id", "model_version", "runtime", "gpu", "replicas", "environment"]

    @field_validator('runtime')
    def runtime_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['vllm', 'triton', 'onnx', 'custom']):
            raise ValueError("must be one of enum values ('vllm', 'triton', 'onnx', 'custom')")
        return value

    @field_validator('gpu')
    def gpu_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['a100', 'h100', 'l40s', 't4']):
            raise ValueError("must be one of enum values ('a100', 'h100', 'l40s', 't4')")
        return value

    @field_validator('environment')
    def environment_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['dev', 'staging', 'production']):
            raise ValueError("must be one of enum values ('dev', 'staging', 'production')")
        return value

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of MlDeployModelRequest from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of MlDeployModelRequest from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "model_id": obj.get("model_id"),
            "model_version": obj.get("model_version"),
            "runtime": obj.get("runtime") if obj.get("runtime") is not None else 'vllm',
            "gpu": obj.get("gpu") if obj.get("gpu") is not None else 'a100',
            "replicas": obj.get("replicas") if obj.get("replicas") is not None else 1,
            "environment": obj.get("environment") if obj.get("environment") is not None else 'dev'
        })
        return _obj


