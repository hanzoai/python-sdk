# coding: utf-8

"""
    Hanzo Cloud API

    The Hanzo Cloud API as a customer calls it: every operation under /v1/ except the operator's admin product, relay routes, legacy spellings and capabilities still reached by flag. Tagged by product: the first path segment after /v1/.

    The version of the OpenAPI document: v1
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictBool, StrictInt, StrictStr
from typing import Any, ClassVar, Dict, List, Optional
from hanzoai.cloud.models.record import Record
from typing import Optional, Set
from typing_extensions import Self

class ProjectsDomain(BaseModel):
    """
    ProjectsDomain
    """ # noqa: E501
    created_at: Optional[StrictInt] = Field(default=None, description="CreatedAt is when the host was claimed, as Unix seconds — not when it went live.", alias="createdAt")
    detail: Optional[StrictStr] = Field(default=None, description="Detail is what is holding the claim up, in words a person can act on.")
    host: Optional[StrictStr] = Field(default=None, description="Host is the custom hostname claimed for this site.")
    records: Optional[List[Record]] = Field(default=None, description="Records are EXACTLY the DNS records to publish to prove ownership and route the host. Present only while pending, because a live host has already proved it; absent is therefore \"nothing left to do\", not \"we cannot say what to do\".")
    status: Optional[StrictStr] = Field(default=None, description="Status is `live` when the edge answers for this host now, `pending` while the claim is waiting on DNS proof of ownership. A pending host is claimed but serves nothing.")
    url: Optional[StrictStr] = Field(default=None, description="URL is where the host will serve once it is live — present on a pending claim too, so a console can show the destination before it works.")
    verified: Optional[StrictBool] = Field(default=None, description="Verified is the same fact as a boolean, for a caller that only needs the yes or no. It cannot disagree with status.")
    __properties: ClassVar[List[str]] = ["createdAt", "detail", "host", "records", "status", "url", "verified"]

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of ProjectsDomain from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # override the default output from pydantic by calling `to_dict()` of each item in records (list)
        _items = []
        if self.records:
            for _item_records in self.records:
                if _item_records:
                    _items.append(_item_records.to_dict())
            _dict['records'] = _items
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of ProjectsDomain from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "createdAt": obj.get("createdAt"),
            "detail": obj.get("detail"),
            "host": obj.get("host"),
            "records": [Record.from_dict(_item) for _item in obj["records"]] if obj.get("records") is not None else None,
            "status": obj.get("status"),
            "url": obj.get("url"),
            "verified": obj.get("verified")
        })
        return _obj


