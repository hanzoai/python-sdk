# coding: utf-8

"""
    Hanzo Cloud API

    The Hanzo Cloud API as a customer calls it: every operation under /v1/ except the operator's admin product, relay routes, legacy spellings and capabilities still reached by flag. Tagged by product: the first path segment after /v1/.

    The version of the OpenAPI document: v1
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictInt, StrictStr
from typing import Any, ClassVar, Dict, List, Optional
from typing import Optional, Set
from typing_extensions import Self

class Opportunity(BaseModel):
    """
    Opportunity
    """ # noqa: E501
    amount: Optional[StrictInt] = Field(default=None, description="Amount is the deal value in minor units (cents) of Currency.")
    close_date: Optional[StrictInt] = Field(default=None, description="CloseDate is the expected close as a unix second (0 = unset).", alias="closeDate")
    company_id: Optional[StrictStr] = Field(default=None, description="CompanyID links the deal to one of the org's companies; empty when unlinked, and cleared when that company is deleted. A write naming a company the org does not own is refused with 422.", alias="companyId")
    created_at: Optional[StrictInt] = Field(default=None, description="CreatedAt is the unix second the opportunity was created. Server-owned.", alias="createdAt")
    currency: Optional[StrictStr] = Field(default=None, description="Currency is the ISO code Amount is denominated in; a write that names none stores USD.")
    id: Optional[StrictStr] = Field(default=None, description="ID is the server-minted opportunity id (\"oppo_\" + 128 random bits).")
    name: Optional[StrictStr] = Field(default=None, description="Name is the deal name.")
    point_of_contact_id: Optional[StrictStr] = Field(default=None, description="PointOfContact links the deal to one of the org's contacts; empty when unlinked, and cleared when that contact is deleted. A write naming a contact the org does not own is refused with 422.", alias="pointOfContactId")
    stage: Optional[StrictStr] = Field(default=None, description="Stage is the pipeline stage, always one of NEW, SCREENING, MEETING, PROPOSAL or CUSTOMER — stored upper-case whatever case the write used.")
    updated_at: Optional[StrictInt] = Field(default=None, description="UpdatedAt is the unix second of the last write. Server-owned.", alias="updatedAt")
    __properties: ClassVar[List[str]] = ["amount", "closeDate", "companyId", "createdAt", "currency", "id", "name", "pointOfContactId", "stage", "updatedAt"]

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of Opportunity from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of Opportunity from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "amount": obj.get("amount"),
            "closeDate": obj.get("closeDate"),
            "companyId": obj.get("companyId"),
            "createdAt": obj.get("createdAt"),
            "currency": obj.get("currency"),
            "id": obj.get("id"),
            "name": obj.get("name"),
            "pointOfContactId": obj.get("pointOfContactId"),
            "stage": obj.get("stage"),
            "updatedAt": obj.get("updatedAt")
        })
        return _obj


