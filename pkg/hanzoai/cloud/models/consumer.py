# coding: utf-8

"""
    Hanzo Cloud API

    The Hanzo Cloud API as a customer calls it: every operation under /v1/ except the operator's admin product, relay routes, legacy spellings and capabilities still reached by flag. Tagged by product: the first path segment after /v1/.

    The version of the OpenAPI document: v1
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from datetime import datetime
from pydantic import BaseModel, ConfigDict, Field, StrictInt, StrictStr
from typing import Any, ClassVar, Dict, List, Optional
from hanzoai.cloud.models.durable import Durable
from hanzoai.cloud.models.sequences import Sequences
from typing import Optional, Set
from typing_extensions import Self

class Consumer(BaseModel):
    """
    Consumer
    """ # noqa: E501
    ack_floor: Optional[Sequences] = Field(default=None, description="AckFloor is the highest contiguously acknowledged sequence pair.")
    config: Optional[Durable] = Field(default=None, description="Config is the consumer's configuration.")
    created: Optional[datetime] = Field(default=None, description="Created is when the consumer was created.")
    delivered: Optional[Sequences] = Field(default=None, description="Delivered is the highest delivered sequence pair.")
    name: Optional[StrictStr] = Field(default=None, description="Name is the consumer name.")
    num_ack_pending: Optional[StrictInt] = Field(default=None, description="AckPending is the number of delivered, not yet acknowledged messages.")
    num_pending: Optional[StrictInt] = Field(default=None, description="Pending is the number of messages yet to be delivered.")
    num_redelivered: Optional[StrictInt] = Field(default=None, description="Redelivered is the number of messages currently being redelivered.")
    num_waiting: Optional[StrictInt] = Field(default=None, description="Waiting is the number of pull requests waiting for messages.")
    stream_name: Optional[StrictStr] = Field(default=None, description="Stream is the stream this consumer reads.")
    __properties: ClassVar[List[str]] = ["ack_floor", "config", "created", "delivered", "name", "num_ack_pending", "num_pending", "num_redelivered", "num_waiting", "stream_name"]

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of Consumer from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # override the default output from pydantic by calling `to_dict()` of ack_floor
        if self.ack_floor:
            _dict['ack_floor'] = self.ack_floor.to_dict()
        # override the default output from pydantic by calling `to_dict()` of config
        if self.config:
            _dict['config'] = self.config.to_dict()
        # override the default output from pydantic by calling `to_dict()` of delivered
        if self.delivered:
            _dict['delivered'] = self.delivered.to_dict()
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of Consumer from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "ack_floor": Sequences.from_dict(obj["ack_floor"]) if obj.get("ack_floor") is not None else None,
            "config": Durable.from_dict(obj["config"]) if obj.get("config") is not None else None,
            "created": obj.get("created"),
            "delivered": Sequences.from_dict(obj["delivered"]) if obj.get("delivered") is not None else None,
            "name": obj.get("name"),
            "num_ack_pending": obj.get("num_ack_pending"),
            "num_pending": obj.get("num_pending"),
            "num_redelivered": obj.get("num_redelivered"),
            "num_waiting": obj.get("num_waiting"),
            "stream_name": obj.get("stream_name")
        })
        return _obj


