# coding: utf-8

"""
    Hanzo Cloud API

    The Hanzo Cloud API as a customer calls it: every operation under /v1/ except the operator's admin product, relay doors, legacy spellings and capabilities still reached by flag. Tagged by product: the first path segment after /v1/.

    The version of the OpenAPI document: v1
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictBool, StrictInt, StrictStr
from typing import Any, ClassVar, Dict, List, Optional
from hanzoai.cloud.models.mcp_package import MCPPackage
from hanzoai.cloud.models.mcp_remote import MCPRemote
from typing import Optional, Set
from typing_extensions import Self

class MCPListing(BaseModel):
    """
    MCPListing
    """ # noqa: E501
    description: Optional[StrictStr] = Field(default=None, description="Description is the publisher's one-line summary.")
    featured: Optional[StrictBool] = Field(default=None, description="Featured puts the listing on the front of the shelf. Curation.")
    hidden: Optional[StrictBool] = Field(default=None, description="Hidden keeps the listing out of the org-visible catalog. Curation: a sync never changes it. Only a SuperAdmin sets it, and only a SuperAdmin sees a hidden entry listed.")
    id: Optional[StrictStr] = Field(default=None, description="ID addresses the listing in a URL. It is the reverse-DNS NAME with its one slash written as an underscore — reversible, because a namespace never contains an underscore — so the id is readable and stable rather than a hash that means nothing to whoever reads a link.")
    logo: Optional[StrictStr] = Field(default=None, description="Logo is the brand mark to render for the listing — the publisher's icon when the entry carries one, or the one an admin set. Curation.")
    name: Optional[StrictStr] = Field(default=None, description="Name is the publisher's reverse-DNS name, e.g. \"com.stripe/mcp\".")
    official: Optional[StrictBool] = Field(default=None, description="Official is whether this is the vendor's OWN server rather than someone else's copy of it. Derived on every sync (see isOfficial) until a SuperAdmin sets it explicitly, after which the admin's answer stands.")
    packages: Optional[List[MCPPackage]] = Field(default=None, description="Packages are the runnable package forms — npm, pypi, oci — each with the runtime that launches it and the transport it then speaks.")
    registry: Optional[StrictStr] = Field(default=None, description="Registry is the upstream this row was synced from.")
    remotes: Optional[List[MCPRemote]] = Field(default=None, description="Remotes are the hosted endpoints the publisher serves the server at.")
    repo: Optional[StrictStr] = Field(default=None, description="Repo is the source repository URL, when the entry names one.")
    site: Optional[StrictStr] = Field(default=None, description="Site is the project's homepage, when the entry names one.")
    synced: Optional[StrictInt] = Field(default=None, description="Synced is when this row was last confirmed against upstream, Unix seconds.")
    title: Optional[StrictStr] = Field(default=None, description="Title is the human-readable display name, when the entry carries one.")
    transports: Optional[List[StrictStr]] = Field(default=None, description="Transports are the distinct transports this server can be reached over, sorted: some of \"stdio\", \"streamable-http\", \"sse\". A listing with \"streamable-http\" is one an org can enable here and now; a listing that is only \"stdio\" needs a process to run it.")
    vendor: Optional[StrictStr] = Field(default=None, description="Vendor is the namespace half of Name — the publisher, e.g. \"com.stripe\".")
    version: Optional[StrictStr] = Field(default=None, description="Version is the published version of this listing.")
    __properties: ClassVar[List[str]] = ["description", "featured", "hidden", "id", "logo", "name", "official", "packages", "registry", "remotes", "repo", "site", "synced", "title", "transports", "vendor", "version"]

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of MCPListing from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # override the default output from pydantic by calling `to_dict()` of each item in packages (list)
        _items = []
        if self.packages:
            for _item_packages in self.packages:
                if _item_packages:
                    _items.append(_item_packages.to_dict())
            _dict['packages'] = _items
        # override the default output from pydantic by calling `to_dict()` of each item in remotes (list)
        _items = []
        if self.remotes:
            for _item_remotes in self.remotes:
                if _item_remotes:
                    _items.append(_item_remotes.to_dict())
            _dict['remotes'] = _items
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of MCPListing from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "description": obj.get("description"),
            "featured": obj.get("featured"),
            "hidden": obj.get("hidden"),
            "id": obj.get("id"),
            "logo": obj.get("logo"),
            "name": obj.get("name"),
            "official": obj.get("official"),
            "packages": [MCPPackage.from_dict(_item) for _item in obj["packages"]] if obj.get("packages") is not None else None,
            "registry": obj.get("registry"),
            "remotes": [MCPRemote.from_dict(_item) for _item in obj["remotes"]] if obj.get("remotes") is not None else None,
            "repo": obj.get("repo"),
            "site": obj.get("site"),
            "synced": obj.get("synced"),
            "title": obj.get("title"),
            "transports": obj.get("transports"),
            "vendor": obj.get("vendor"),
            "version": obj.get("version")
        })
        return _obj


