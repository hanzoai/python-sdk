# coding: utf-8

"""
    Hanzo Cloud — Unified API (V8 · Open Edition)

    The single unified OpenAPI surface for ALL Hanzo services, aggregated from the per-service specs. Every route is https://api.hanzo.ai/v1/<service>/*. Grouped by the canonical domains in capabilities.yaml (the ONE registry). Regenerated by merge.py — edit the per-service <svc>/openapi.yaml + capabilities.yaml, not this file.

    The version of the OpenAPI document: 8.0.0
    Contact: support@hanzo.ai
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictInt, StrictStr, field_validator
from typing import Any, ClassVar, Dict, List, Optional
from typing_extensions import Annotated
from typing import Optional, Set
from typing_extensions import Self

class MqKVBucketConfig(BaseModel):
    """
    KV bucket creation configuration.
    """ # noqa: E501
    name: Annotated[str, Field(strict=True, max_length=256)] = Field(description="Bucket name.")
    history: Optional[Annotated[int, Field(le=64, strict=True, ge=1)]] = Field(default=1, description="Maximum number of revisions per key.")
    ttl: Optional[StrictStr] = Field(default='0', description="Default TTL for keys (e.g., \"1h\", \"7d\", \"0\" for no expiry). ")
    max_value_size: Optional[StrictInt] = Field(default=-1, description="Maximum value size in bytes (-1 for default).")
    max_bytes: Optional[StrictInt] = Field(default=-1, description="Maximum total bucket size (-1 for unlimited).")
    storage: Optional[StrictStr] = Field(default='file', description="Storage backend.")
    num_replicas: Optional[Annotated[int, Field(le=5, strict=True, ge=1)]] = Field(default=1, description="Number of replicas.")
    description: Optional[StrictStr] = Field(default=None, description="Optional human-readable description.")
    __properties: ClassVar[List[str]] = ["name", "history", "ttl", "max_value_size", "max_bytes", "storage", "num_replicas", "description"]

    @field_validator('name')
    def name_validate_regular_expression(cls, value):
        """Validates the regular expression"""
        if not re.match(r"^[a-zA-Z0-9_-]+$", value):
            raise ValueError(r"must validate the regular expression /^[a-zA-Z0-9_-]+$/")
        return value

    @field_validator('storage')
    def storage_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['file', 'memory']):
            raise ValueError("must be one of enum values ('file', 'memory')")
        return value

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of MqKVBucketConfig from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of MqKVBucketConfig from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "name": obj.get("name"),
            "history": obj.get("history") if obj.get("history") is not None else 1,
            "ttl": obj.get("ttl") if obj.get("ttl") is not None else '0',
            "max_value_size": obj.get("max_value_size") if obj.get("max_value_size") is not None else -1,
            "max_bytes": obj.get("max_bytes") if obj.get("max_bytes") is not None else -1,
            "storage": obj.get("storage") if obj.get("storage") is not None else 'file',
            "num_replicas": obj.get("num_replicas") if obj.get("num_replicas") is not None else 1,
            "description": obj.get("description")
        })
        return _obj


