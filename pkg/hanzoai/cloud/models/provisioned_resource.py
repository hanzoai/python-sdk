# coding: utf-8

"""
    Hanzo Cloud API

    Composed from each subsystem's own projection of its router, in the fleet's mount order — every operation below is a route the subsystem that publishes it registered. Tagged by product: the first path segment after /v1/.

    The version of the OpenAPI document: v1
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictInt, StrictStr
from typing import Any, ClassVar, Dict, List, Optional
from typing import Optional, Set
from typing_extensions import Self

class ProvisionedResource(BaseModel):
    """
    ProvisionedResource
    """ # noqa: E501
    database: Optional[StrictStr] = Field(default=None, description="Database is the logical database, collection, index or bucket this resource resolves to on its backend.")
    host: Optional[StrictStr] = Field(default=None, description="Host is the address that actually routes to this resource — a dedicated instance's own in-cluster Service, or the public gateway for a shared one.")
    id: Optional[StrictStr] = Field(default=None, description="ID is the resource's server-minted handle, \"rs_\"-prefixed.")
    kind: Optional[StrictStr] = Field(default=None, description="Kind is the product: sql, vector, datastore, kv, search, s3 or docdb.")
    name: Optional[StrictStr] = Field(default=None, description="Name is the org-unique slug the caller provisioned the resource under.")
    port: Optional[StrictInt] = Field(default=None, description="Port is the port a client connects to on Host.")
    status: Optional[StrictStr] = Field(default=None, description="Status is \"ready\", or \"provisioning\" while a dedicated instance is still being materialized. A dedicated resource's status is reconciled from the operator's live CR before this is answered, so it is never a stale ready.")
    username: Optional[StrictStr] = Field(default=None, description="Username is the credential's user, for the kinds that mint one per resource. Absent for a kind whose backend authenticates with a shared, out-of-band key.")
    __properties: ClassVar[List[str]] = ["database", "host", "id", "kind", "name", "port", "status", "username"]

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of ProvisionedResource from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of ProvisionedResource from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "database": obj.get("database"),
            "host": obj.get("host"),
            "id": obj.get("id"),
            "kind": obj.get("kind"),
            "name": obj.get("name"),
            "port": obj.get("port"),
            "status": obj.get("status"),
            "username": obj.get("username")
        })
        return _obj


