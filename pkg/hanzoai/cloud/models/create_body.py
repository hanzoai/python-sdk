# coding: utf-8

"""
    Hanzo Cloud API

    Composed from each subsystem's own projection of its router, in the fleet's mount order — every operation below is a route the subsystem that publishes it registered. Tagged by product: the first path segment after /v1/.

    The version of the OpenAPI document: v1
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictStr
from typing import Any, ClassVar, Dict, List, Optional
from hanzoai.cloud.models.arm import Arm
from typing import Optional, Set
from typing_extensions import Self

class CreateBody(BaseModel):
    """
    CreateBody
    """ # noqa: E501
    exposure_event: Optional[StrictStr] = Field(default=None, description="ExposureEvent is the event that marks a subject as enrolled — the analysis denominator — defaulting to the SDK's $feature_flag_called marker.", alias="exposureEvent")
    flag_key: Optional[StrictStr] = Field(default=None, description="FlagKey names the assignment flag this experiment writes, defaulting to exp_<id>. It must be a slug.", alias="flagKey")
    id: StrictStr = Field(description="ID is the experiment's slug, claimed once per project. It must match [A-Za-z0-9][A-Za-z0-9._-]{0,127}.")
    metric_event: StrictStr = Field(description="MetricEvent is the event that counts as a conversion — the analysis numerator.", alias="metricEvent")
    name: Optional[StrictStr] = Field(default=None, description="Name is free text for a reader; the id is what addresses the experiment.")
    subject_kind: Optional[StrictStr] = Field(default=None, description="SubjectKind is the unit assigned and measured: user (the default), org, session or audience.", alias="subjectKind")
    variants: List[Arm] = Field(description="Arms are the arms, at least two. Weights that are all zero become an even split; otherwise they must sum to 100, and at most one arm may be flagged control.")
    __properties: ClassVar[List[str]] = ["exposureEvent", "flagKey", "id", "metricEvent", "name", "subjectKind", "variants"]

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of CreateBody from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # override the default output from pydantic by calling `to_dict()` of each item in variants (list)
        _items = []
        if self.variants:
            for _item_variants in self.variants:
                if _item_variants:
                    _items.append(_item_variants.to_dict())
            _dict['variants'] = _items
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of CreateBody from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "exposureEvent": obj.get("exposureEvent"),
            "flagKey": obj.get("flagKey"),
            "id": obj.get("id"),
            "metricEvent": obj.get("metricEvent"),
            "name": obj.get("name"),
            "subjectKind": obj.get("subjectKind"),
            "variants": [Arm.from_dict(_item) for _item in obj["variants"]] if obj.get("variants") is not None else None
        })
        return _obj


