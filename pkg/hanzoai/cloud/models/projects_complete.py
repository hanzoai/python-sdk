# coding: utf-8

"""
    Hanzo Cloud API

    The Hanzo Cloud API as a customer calls it: every operation under /v1/ except the operator's admin product, relay doors, legacy spellings and capabilities still reached by flag. Tagged by product: the first path segment after /v1/.

    The version of the OpenAPI document: v1
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictInt, StrictStr
from typing import Any, ClassVar, Dict, List, Optional
from typing import Optional, Set
from typing_extensions import Self

class ProjectsComplete(BaseModel):
    """
    ProjectsComplete
    """ # noqa: E501
    bytes: Optional[StrictInt] = Field(default=None, description="Bytes is their total size in bytes.")
    commit: Optional[StrictStr] = Field(default=None, description="Commit is the revision that was built, recorded on the deployment.")
    files: Optional[StrictInt] = Field(default=None, description="Files is how many objects CI published.")
    id: Optional[StrictStr] = Field(default=None, description="ID is the queued deployment to complete, from the path.")
    keys: Optional[List[StrictStr]] = Field(default=None, description="Keys is the manifest CI just uploaded, RELATIVE to the deployment prefix. It is what replaces `aws s3 sync --delete`: an upload grant authorizes writes only, so CI cannot remove a file, and cloud reconciles the prefix against this list instead (grant.go). Omit it and nothing is deleted — the prefix only grows, which is the old pre-grant behaviour and a safe default.")
    live_url: Optional[StrictStr] = Field(default=None, description="LiveURL is a HINT at the address the site should serve at. The public host is claimed by cloud first, so this can refine the URL a deployment reports but can never assert a subdomain another tenant holds.", alias="liveUrl")
    message: Optional[StrictStr] = Field(default=None, description="Message is what happened, in words — on an error completion, why it failed.")
    slug: Optional[StrictStr] = Field(default=None, description="Slug is the project the deployment belongs to, from the path.")
    status: Optional[StrictStr] = Field(default=None, description="Status is how the build ended: `live` if it succeeded, `error` if it did not. Nothing else is accepted.")
    __properties: ClassVar[List[str]] = ["bytes", "commit", "files", "id", "keys", "liveUrl", "message", "slug", "status"]

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of ProjectsComplete from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of ProjectsComplete from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "bytes": obj.get("bytes"),
            "commit": obj.get("commit"),
            "files": obj.get("files"),
            "id": obj.get("id"),
            "keys": obj.get("keys"),
            "liveUrl": obj.get("liveUrl"),
            "message": obj.get("message"),
            "slug": obj.get("slug"),
            "status": obj.get("status")
        })
        return _obj


