# coding: utf-8

"""
    Hanzo Cloud — Unified API

    The single unified OpenAPI surface for ALL Hanzo services, aggregated from the per-service specs. Every route is https://api.hanzo.ai/v1/<service>/*. Regenerated by merge.py — edit the per-service <svc>/openapi.yaml, not this file.

    The version of the OpenAPI document: 1.0.0
    Contact: support@hanzo.ai
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from datetime import datetime
from pydantic import BaseModel, ConfigDict, StrictInt, StrictStr, field_validator
from typing import Any, ClassVar, Dict, List, Optional
from typing import Optional, Set
from typing_extensions import Self

class MlDeployment(BaseModel):
    """
    MlDeployment
    """ # noqa: E501
    id: Optional[StrictStr] = None
    model_id: Optional[StrictStr] = None
    model_version: Optional[StrictStr] = None
    runtime: Optional[StrictStr] = None
    gpu: Optional[StrictStr] = None
    replicas: Optional[StrictInt] = None
    environment: Optional[StrictStr] = None
    endpoint: Optional[StrictStr] = None
    status: Optional[StrictStr] = None
    created_at: Optional[datetime] = None
    __properties: ClassVar[List[str]] = ["id", "model_id", "model_version", "runtime", "gpu", "replicas", "environment", "endpoint", "status", "created_at"]

    @field_validator('runtime')
    def runtime_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['vllm', 'triton', 'onnx', 'custom']):
            raise ValueError("must be one of enum values ('vllm', 'triton', 'onnx', 'custom')")
        return value

    @field_validator('gpu')
    def gpu_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['a100', 'h100', 'l40s', 't4']):
            raise ValueError("must be one of enum values ('a100', 'h100', 'l40s', 't4')")
        return value

    @field_validator('environment')
    def environment_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['dev', 'staging', 'production']):
            raise ValueError("must be one of enum values ('dev', 'staging', 'production')")
        return value

    @field_validator('status')
    def status_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['deploying', 'active', 'failed', 'stopped']):
            raise ValueError("must be one of enum values ('deploying', 'active', 'failed', 'stopped')")
        return value

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of MlDeployment from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of MlDeployment from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "id": obj.get("id"),
            "model_id": obj.get("model_id"),
            "model_version": obj.get("model_version"),
            "runtime": obj.get("runtime"),
            "gpu": obj.get("gpu"),
            "replicas": obj.get("replicas"),
            "environment": obj.get("environment"),
            "endpoint": obj.get("endpoint"),
            "status": obj.get("status"),
            "created_at": obj.get("created_at")
        })
        return _obj


