# coding: utf-8

"""
    Hanzo Cloud — Unified API

    The single unified OpenAPI surface for ALL Hanzo services, aggregated from the per-service specs. Every route is https://api.hanzo.ai/v1/<service>/*. Regenerated by merge.py — edit the per-service <svc>/openapi.yaml, not this file.

    The version of the OpenAPI document: 1.0.0
    Contact: support@hanzo.ai
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictInt, StrictStr, field_validator
from typing import Any, ClassVar, Dict, List, Optional
from typing import Optional, Set
from typing_extensions import Self

class PubsubStreamConfig(BaseModel):
    """
    PubsubStreamConfig
    """ # noqa: E501
    name: StrictStr = Field(description="Unique stream name")
    subjects: List[StrictStr] = Field(description="Subjects captured by this stream")
    storage: Optional[StrictStr] = Field(default='file', description="Storage backend")
    replicas: Optional[StrictInt] = Field(default=1, description="Replication factor")
    retention: Optional[StrictStr] = 'limits'
    max_msgs: Optional[StrictInt] = Field(default=-1, description="Maximum messages to retain")
    max_bytes: Optional[StrictInt] = Field(default=-1, description="Maximum bytes to retain")
    max_age: Optional[StrictInt] = Field(default=0, description="Maximum age in nanoseconds (0 = unlimited)")
    discard: Optional[StrictStr] = Field(default='old', description="Discard policy when limits are reached")
    __properties: ClassVar[List[str]] = ["name", "subjects", "storage", "replicas", "retention", "max_msgs", "max_bytes", "max_age", "discard"]

    @field_validator('storage')
    def storage_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['file', 'memory']):
            raise ValueError("must be one of enum values ('file', 'memory')")
        return value

    @field_validator('replicas')
    def replicas_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set([1, 3, 5]):
            raise ValueError("must be one of enum values (1, 3, 5)")
        return value

    @field_validator('retention')
    def retention_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['limits', 'interest', 'workqueue']):
            raise ValueError("must be one of enum values ('limits', 'interest', 'workqueue')")
        return value

    @field_validator('discard')
    def discard_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['old', 'new']):
            raise ValueError("must be one of enum values ('old', 'new')")
        return value

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of PubsubStreamConfig from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of PubsubStreamConfig from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "name": obj.get("name"),
            "subjects": obj.get("subjects"),
            "storage": obj.get("storage") if obj.get("storage") is not None else 'file',
            "replicas": obj.get("replicas") if obj.get("replicas") is not None else 1,
            "retention": obj.get("retention") if obj.get("retention") is not None else 'limits',
            "max_msgs": obj.get("max_msgs") if obj.get("max_msgs") is not None else -1,
            "max_bytes": obj.get("max_bytes") if obj.get("max_bytes") is not None else -1,
            "max_age": obj.get("max_age") if obj.get("max_age") is not None else 0,
            "discard": obj.get("discard") if obj.get("discard") is not None else 'old'
        })
        return _obj


