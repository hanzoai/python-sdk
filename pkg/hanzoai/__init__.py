# coding: utf-8

# flake8: noqa

"""
    Hanzo Cloud — Unified API

    The single unified OpenAPI surface for ALL Hanzo services, aggregated from the per-service specs. Every route is https://api.hanzo.ai/v1/<service>/*. Regenerated by merge.py — edit the per-service <svc>/openapi.yaml, not this file.

    The version of the OpenAPI document: 1.0.0
    Contact: support@hanzo.ai
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from importlib.metadata import PackageNotFoundError, version as _version

try:
    # Single-sourced from the installed distribution, never a literal. This said
    # "1.0.0" — the openapi-generator default — while pyproject said 3.1.x, so
    # `hanzoai.__version__` reported a release that never existed.
    __version__ = _version("hanzoai")
except PackageNotFoundError:  # source tree, not installed
    __version__ = "0.0.0+dev"


# Optional ZAP-native transport (opt-in via the `zap` extra). The transport is an
# httpx.BaseTransport, so `zap` requires httpx + hanzo-zap (neither is a base dep —
# the client core is urllib3). Guard the import so `import hanzoai` still works when
# the extra is absent; the names appear only when `pip install hanzoai[zap]` is used.
# Additive and intentionally NOT part of the locked `__all__`.
try:
    from . import zap as zap
    from .zap import (
        ZapTransport as ZapTransport,
        AsyncZapTransport as AsyncZapTransport,
        zap_http_client as zap_http_client,
        async_zap_http_client as async_zap_http_client,
    )
except ImportError:  # pragma: no cover - hanzoai[zap] not installed
    pass

# THE CLIENT IS `hanzoai.cloud`. It is generated from hanzoai/cloud's openapi.yaml
# at the ref this tree's .spec-lock names, and it is the only generated client in
# this distribution.
#
# There used to be a second one at `hanzoai.api` / `hanzoai.models`, re-exported
# here as 2,202 flat names — a whole parallel way to call the same service,
# shipped inside the same wheel. It was not an older copy of this one; it was a
# projection of a document that stopped existing. Of the 1,695 routes it bound,
# 243 are still in the document and 1,452 are not, so the great majority of those
# names built a request the server answers with a 404. Two ways to call one API is
# already a defect; one of them being 86% wrong is how a caller reaches for the
# obvious flat name and gets nothing.
#
# Nothing is re-exported flat here on purpose. A name that appears in two places
# is a name that can disagree with itself, and the namespace already says which
# service and which document a symbol came from.
from . import cloud as cloud

# The tenant fabric — hand-written, and the only names this package adds beside
# `cloud`. They are here because they belong to no one operation: `Client` is
# how an operator credential is scoped to one customer, and `Result` is what an
# approval-gated call answers with. Nothing generated is re-exported alongside
# them; the namespace still says which document a symbol came from.
from .client import Client as Client
from .grant import Grant as Grant
from .result import (
    Done as Done,
    Held as Held,
    Result as Result,
    Approval as Approval,
    is_done as is_done,
    is_held as is_held,
    result as result,
    unwrap as unwrap,
)

__all__ = [
    "cloud",
    "__version__",
    "Client",
    "Grant",
    "Approval",
    "Done",
    "Held",
    "Result",
    "is_done",
    "is_held",
    "result",
    "unwrap",
]
