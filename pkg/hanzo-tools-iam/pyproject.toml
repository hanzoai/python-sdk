[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "hanzo-tools-iam"
version = "0.1.1"
description = "Hanzo MCP tool for IAM — identity, access management, users, orgs, roles, and permissions"
readme = "README.md"
requires-python = ">=3.12"
license = { text = "MIT" }
authors = [{ name = "Hanzo AI", email = "dev@hanzo.ai" }]
keywords = ["hanzo", "mcp", "iam", "identity", "access", "tools"]
classifiers = [
    "Development Status :: 4 - Beta",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
]

dependencies = [
    "hanzo-tools-core>=0.1.0",
    "hanzo-tools-auth>=0.1.0",
    # The endpoint seam (IAM_ROUTE_PREFIX, IAM_WHOAMI_PATH, OIDC_DISCOVERY_PATH)
    # is spelled once, in hanzo-iam, and imported -- never re-spelled here.
    # 1.30.4 is the floor because IAM_WHOAMI_PATH lands there.
    "hanzo-iam>=1.30.4",
    "httpx>=0.27.0",
]

[project.entry-points."hanzo.tools"]
iam = "hanzo_tools.iam:TOOLS"

[tool.hatch.build.targets.wheel]
packages = ["hanzo_tools"]
